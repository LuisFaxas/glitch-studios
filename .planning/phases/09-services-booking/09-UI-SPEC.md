---
phase: 9
slug: services-booking
status: draft
shadcn_initialized: true
preset: base-nova / neutral / cssVariables
created: 2026-04-20
---

# Phase 9 — UI Design Contract: Services & Booking

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

Carries forward all tokens locked in Phase 01.2 (Design Language), Phase 03 (Booking System), and Phase 08 (Auth & Navigation). This phase introduces no new palette, no new fonts, and no new component library. Scope: enrich service detail panel, polish booking wizard, add coming-soon manifesto, add admin booking-live toggle.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn/ui |
| Preset | base-nova, baseColor: neutral, cssVariables: true (detected from `components.json`) |
| Component library | Radix UI (via shadcn) |
| Icon library | Lucide React |
| Font — headings / nav / mono elements | JetBrains Mono (`--font-mono`) |
| Font — body / form input | Inter (`--font-sans`) |
| Site theme | dark monochrome (`.dark` class on `<html>`) — no light mode in scope |

Pre-existing CSS tokens relied on (from `src/styles/globals.css`):
- `--color-bg-dominant: #000000`
- `--color-bg-secondary: #111111`
- `--color-border: #222222`
- `--color-text-fg: #f5f5f0`
- `--color-text-muted: #888888`
- `--color-text-subdued: #555555`
- `--color-tile-hover: #1a1a1a`
- `--color-tile-hover-border: #444444`
- `--gap-tile: 4px`
- `--pad-tile: 16px`
- `--pad-page: 24px`

---

## Spacing Scale

Declared values (multiples of 4 — align with existing `--gap-tile`, `--pad-tile`, `--pad-page`):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Tile gutters (`--gap-tile`), inline icon-to-text gaps |
| sm | 8px | Compact stack spacing (stepper dots, policy row gaps) |
| md | 16px | Default element padding (`--pad-tile`, service tile interior, form field vertical rhythm) |
| lg | 24px | Section padding (`--pad-page`), service detail-panel section separation, wizard step vertical rhythm |
| xl | 32px | Layout gaps — between detail panel sections, between wizard body and summary sidebar |
| 2xl | 48px | Manifesto hero vertical rhythm, major section breaks on `/services` |
| 3xl | 64px | Page-level top/bottom spacing on manifesto + service detail hero |

Exceptions:
- Minimum tap target: 48px height — applies to all tiles, wizard CTAs, service tile, calendar day tile, time-slot tile, admin toggle switch. Pre-existing constraint from `bottom-tab-bar.tsx` and `calendar-day-tile.tsx`; do not shrink.
- Persistent booking summary sidebar (desktop): fixed width 320px — sits alongside the 5-step wizard body, `xl` gap (32px) between body and summary. Rationale: wide enough to show service name + date + time + deposit line without wrapping at common brand lengths.
- Mobile collapsible summary: pinned top of wizard body, 56px collapsed height (header only), expands inline with `lg` (24px) internal padding. No overlay — pushes content down.
- Stepper dot diameter: 12px (exception to 4-mult scale only because pre-existing in `booking-flow-stepper.tsx` — do not redesign).

---

## Typography

| Role | Font | Size | Weight | Line Height | Tracking |
|------|------|------|--------|-------------|----------|
| Body | Inter (`--font-sans`) | 14px | 400 regular | 1.5 | normal |
| Form label | Inter (`--font-sans`) | 14px | 400 regular | 1.4 | normal |
| Nav item / step label / small uppercase label | JetBrains Mono (`--font-mono`) | 11px uppercase | 700 bold | 1 | 0.05em |
| Section heading (detail panel section titles, step titles, wizard step heading) | JetBrains Mono (`--font-mono`) | 20px uppercase | 700 bold | 1.2 | 0.05em |
| Page heading (service detail name, `/services` page h1) | JetBrains Mono (`--font-mono`) | 28px uppercase | 700 bold | 1.2 | 0.05em |
| Manifesto display h1 ("WE'RE BUILDING GLITCH STUDIOS.") | JetBrains Mono (`--font-mono`) | 28px mobile / 48px desktop, uppercase | 700 bold | 1.1 | 0.05em |

Notes:
- Hover-only RGB-split glitch rule applies site-wide (from `feedback_glitch_headers`). ALL headings in this phase — manifesto h1, service detail h1, detail-panel section titles, step headings — must be rendered via `GlitchHeading` component (or an equivalent that only triggers glitch on hover). No auto-running glitch animations on any heading.
- Stepper subtitles (new in Phase 9, D-11) use Inter 14px regular `--color-text-muted` (#888888), line-height 1.5. They are NOT headings and do NOT get the glitch treatment.
- Body paragraphs on the manifesto: Inter 16px, line-height 1.6, color `--color-text-fg` (#f5f5f0). Max width 640px for readability.
- All mono text uses `tracking-[0.05em]` (established pattern).

---

## Color

60 / 30 / 10 split:

| Role | Value | % | Usage |
|------|-------|---|-------|
| Dominant (60%) | `#000000` | 60 | Page background, manifesto background, wizard page background, admin settings page background |
| Secondary (30%) | `#111111` | 30 | Service detail panel surface, service tile surface, wizard step card, summary sidebar card, admin settings card, stepper inactive dot fill, calendar tile default |
| Tile borders | `#222222` | — | All tile and card borders (pre-existing `--border` dark token) |
| Text (fg) | `#f5f5f0` | — | All primary text |
| Text (muted) | `#888888` | — | Stepper subtitles, helper copy ("Choose a date — greyed out means unavailable"), deposit/policy inline notes, "Contact for quote" secondary label, example-work captions |
| Text (subdued) | `#555555` | — | Disabled/unavailable date tiles, unavailable time-slot labels |
| Accent (10%) | `#f5f5f0` (inverted) | 10 | Reserved list below |
| Destructive | `#dc2626` | — | Payment error inline text only. Never as a background or border. Confirmation modal CTA uses the same destructive token for the confirm button in the admin toggle flow. |

Accent reserved for (explicit list — do NOT extend):
1. Active stepper dot (fill `#f5f5f0`, text `#000000`) — current step indicator only.
2. Active service tile in Step 1 of wizard (bg `#f5f5f0`, text `#000000`).
3. Selected date tile in calendar (existing `calendar-day-tile.tsx` selected state).
4. Selected time-slot tile (existing `time-slot-tile.tsx` selected state).
5. Primary CTA in wizard / manifesto ("CONTINUE", "COMPLETE BOOKING", "NOTIFY ME") — filled button bg `#f5f5f0`, text `#000000`.
6. Admin booking-live toggle ON state — switch track `#f5f5f0`.

Do NOT introduce any color outside the four base tokens (`#000000`, `#111111`, `#222222`, `#f5f5f0`) plus the existing `--color-text-muted` / `--color-text-subdued` and the single `destructive` token. No neon. No colored glow. No per-service category color coding.

---

## Layout Contracts

### `/services` page (toggle ON)

- Master-detail layout preserved from pre-existing `ServiceGrid` component.
- Desktop: left tile grid (2-column at ≥lg), right detail panel sticky-positioned on scroll, 32px gap.
- Mobile (<lg): tile grid becomes single column; tapping a tile expands an inline accordion panel below it (existing pattern — do not rewrite).
- Detail panel is scoped to `#111111` surface, `#222222` border, 24px internal padding. Vertical rhythm: 24px between sections, 16px between a section title and its body content.

### `/services` page (toggle OFF) and `/book` page (toggle OFF or zero bookable services)

- Both routes render the same `<ComingSoonManifesto>` component. No master-detail, no tile grid, no wizard.
- Full-width hero on dominant `#000000` background.
- Content sections (what's coming, notify-me form) stacked vertically, centered with max-width 960px.

### `/book` page (toggle ON, services exist)

- Desktop layout: two-column flex. Left = wizard body (flex-grow), Right = persistent summary sidebar (320px fixed width). 32px gap.
- Mobile layout: single column. Collapsible summary pinned to top of wizard body, wizard body below.
- Stepper (`booking-flow-stepper.tsx`): horizontal bar at top of wizard body on desktop, compact dot-only version on mobile (pre-existing behavior).
- Wizard body card: `#111111` bg, `#222222` border, 24px internal padding on desktop, 16px on mobile.

### Admin toggle surfaces (D-04)

- Both `/admin/settings` and `/admin/services` (or `/admin/bookings`) render a `<BookingLiveToggle>` component in a dedicated card.
- Card: `#111111` bg, `#222222` border, 24px padding. Contains: toggle switch (shadcn `Switch`), status label ("BOOKING: LIVE" / "BOOKING: COMING SOON"), helper text (14px muted Inter) explaining what toggling does, and an explicit "Save" button that opens the confirmation modal.
- Confirmation modal: shadcn `AlertDialog`. Title mono 20px uppercase. Body Inter 14px. Cancel button secondary style, Confirm button destructive style (bg `#dc2626`, text `#f5f5f0`) regardless of toggle direction — reinforces that this is a site-wide change.

---

## Booking Wizard — Per-Step Contract

Each of the 5 steps gets: step heading (mono 20px uppercase), one-line subtitle (Inter 14px muted), body content. Step subtitles are NEW in Phase 9 (D-11).

| # | Step | Heading | Subtitle (copy) |
|---|------|---------|-----------------|
| 1 | Service | "SELECT SERVICE" | "Pick what you're booking. Price and duration below each option." |
| 2 | Date | "SELECT DATE" | "Greyed-out dates are closed. Tap an open day to see times." |
| 3 | Time | "SELECT TIME" | "All times shown are the studio's local time zone." |
| 4 | Details | "YOUR DETAILS" | "We'll use these to confirm your session and send your receipt." |
| 5 | Payment | "CONFIRM & PAY" | "Deposit secures your slot. Balance is due at the session." |

### Persistent Booking Summary Sidebar (D-10)

| Row | Label (mono 11px uppercase muted) | Value (Inter 14px fg) | Visible from step |
|-----|-----------------------------------|----------------------|-------------------|
| 1 | SERVICE | service name or em-dash placeholder `—` | 1+ |
| 2 | DATE | "Thu, Apr 23 2026" or `—` | 2+ |
| 3 | TIME | "2:00 PM – 4:00 PM" or `—` | 3+ |
| 4 | DURATION | "2 hours" (from `durationMinutes`) or `—` | 1+ (once service chosen) |
| 5 | DEPOSIT | "$75 (50% of $150)" or `—` | 1+ (derived from `service_booking_config`) |

- Summary card: `#111111` bg, `#222222` border, 24px padding.
- Each row: 8px vertical gap between rows, label/value stacked with 4px between label and value.
- Empty / not-yet-selected rows render `—` in `#555555`. No hiding rows — the full shape of the booking is always visible so the user sees what's still required.
- On mobile: collapsed state shows "BOOKING SUMMARY ▾" header + a single-line status like "Mixing · Apr 23 · 2pm" (muted). Tap expands to full 5-row table.

### Step 4 (Details) — Terms Surface Block (D-12)

A `Terms` block appears at the bottom of Step 4 body, above the "Continue to Payment" CTA:

- Container: `#111111` bg on a slightly darker inset region — actually render as `bg-transparent border-t border-[#222222] pt-4 mt-6` so it reads as a footer, not a new card.
- Heading (mono 11px uppercase muted): "DEPOSIT & CANCELLATION".
- Three lines of 14px Inter body, 8px vertical gap:
  - "Deposit: ${depositAmount} ({depositPercent}% of ${totalPrice})."
  - "Cancel up to {cancellationWindowHours}h before your session for a full refund."
  - "{refundPolicy}" — pulled verbatim from `service_booking_config.refund_policy`.
- No checkbox required — surfacing the terms is the contract, not explicit consent. User consent is implied by proceeding to pay (matches existing Phase 3 decision).

### Step 1 Rich Service Tiles (D-13)

Each service tile renders four pieces of info, stacked:

1. **Name** — mono 16px uppercase bold `#f5f5f0`, tracking-[0.05em], hover-only glitch.
2. **One-line description** — Inter 14px `#888888`, line-height 1.5, single line truncated with ellipsis at narrow widths.
3. **Price label** — mono 14px `#f5f5f0` (e.g., "FROM $150").
4. **Duration** — Inter 12px `#888888` with clock icon (Lucide `Clock`, 14px, `#888888`), e.g., "2 HOURS". Rendered inline: `[icon] 2 HOURS`.

Tile container: 100% width in Step 1 grid, 16px padding, 48px min-height per info group. Hover: `#1a1a1a` bg + `#444444` border. Selected: `#f5f5f0` bg + `#000000` text inversion on all four pieces of info.

---

## Service Detail Panel — Section Order & Contract (D-08)

The existing panel (name, description, priceLabel, features) is extended with 4 new sections. Final order top-to-bottom:

| # | Section | Heading (mono 11px uppercase) | Body |
|---|---------|-------------------------------|------|
| 1 | Name | — (h1 mono 28px) | Service name |
| 2 | Short description | — (body 14px) | Existing description field |
| 3 | Price | "PRICING" | `priceLabel` (mono 16px fg) + deposit hint line (Inter 14px muted) |
| 4 | Duration & what's included (NEW) | "DURATION & INCLUDES" | Duration line ("2 hours") + bulleted deliverables list. Bullets use mono `›` prefix, not disc — aligns with cyberpunk. Each item Inter 14px fg. |
| 5 | Features (existing) | "HIGHLIGHTS" | Existing `features` array, rendered as 2-column grid on desktop, single column mobile. |
| 6 | Process / timeline (NEW) | "PROCESS" | 4-step numbered list: (1) prep → (2) session → (3) revisions → (4) delivery. Each step: mono 12px step number + Inter 14px step body. Generic template used across all services (executor discretion per D-08; see "Open Decisions" below). |
| 7 | Policies (NEW) | "POLICIES" | 3-line block: deposit amount line, cancellation window line, refund policy line — mirror the Terms block format from Step 4. All pulled from `service_booking_config`. |
| 8 | Example work (NEW) | "EXAMPLE WORK" | Horizontal scroll row of 2–3 portfolio cards (existing portfolio item component). If no matching portfolio items exist, section is hidden entirely (no empty state in this sub-section). |
| 9 | CTA | — (button) | "BOOK THIS SERVICE" → `/book?service={slug}` (deep link, pre-existing pattern). Primary accent button. |

Vertical rhythm: 24px between sections. Each section heading has 8px bottom margin before its body.

---

## Coming-Soon Manifesto — Layout Contract (D-02, D-05)

Rendered at `/services` and `/book` when `booking_live = false` OR when zero bookable services exist.

### Structure

```
[ Hero block ]
  - GlitchLogo component (existing) at top, size="md" or "lg" (executor discretion)
  - h1: "WE'RE BUILDING GLITCH STUDIOS."  (mono 28/48px uppercase, hover-only glitch)
  - Lede paragraph (Inter 16px, line-height 1.6, max-width 640px, fg): 2-3 sentences. See copywriting contract.
  - Spacer: 2xl (48px)

[ What's Coming block ]
  - Section heading (mono 11px uppercase muted): "WHAT WE'RE BUILDING"
  - 3-up tile grid (desktop), stacked (mobile): MUSIC PRODUCTION / VIDEO PRODUCTION / CREATIVE SERVICES
  - Each tile: #111111 bg, #222222 border, 16px padding, mono 14px uppercase title + Inter 14px muted 2-line description.
  - Spacer: 2xl (48px)

[ Notify-me block ]
  - Section heading (mono 20px uppercase, hover glitch): "BE FIRST IN THE DOOR"
  - Sub-copy (Inter 14px muted, max-width 480px): "Drop your email. We'll let you know the day bookings open. No spam, no filler."
  - Newsletter form (reuse <NewsletterForm />) — email input (Inter 14px) + "NOTIFY ME" button (mono 11px uppercase accent)
  - Success state: replace form with mono 14px "YOU'RE ON THE LIST." + Inter 14px muted sub-line
  - Spacer: 3xl (64px)
```

- Hero background: plain `#000000`. No image, no WebGL, no dither — the existing Dither WebGL hero is reserved for the homepage (per `feedback_dither_hero`). Manifesto is intentionally stark.
- No auto-running animations. The only motion allowed is: (a) GlitchLogo hover-only glitch, (b) h1 hover-only RGB-split, (c) Framer Motion scroll-fade on section entry (existing `ScrollSection` variant pattern from Phase 6.1).
- Notify-me form signup gets tagged with `source = "launch-notify"` (or equivalent field decided by the planner per D-19) so admin can distinguish launch-list from general newsletter subscribers.

---

## Admin Booking-Live Toggle — UI Contract (D-01, D-04)

### Toggle card (rendered on both `/admin/settings` and `/admin/services`)

```
┌─────────────────────────────────────────────────────┐
│ BOOKING STATUS                                       │ mono 11px uppercase muted
│                                                      │
│ [ Switch ]  BOOKING: LIVE                           │ shadcn Switch + mono 16px fg
│                                                      │
│ When OFF, every Book CTA on the site redirects to   │ Inter 14px muted
│ the coming-soon page. Toggle ON to accept bookings. │
│                                                      │
│                             [ Save Changes ]        │ primary accent button
└─────────────────────────────────────────────────────┘
```

- Switch track: OFF state `#222222`, ON state `#f5f5f0`.
- Switch thumb: `#f5f5f0` when OFF (on dark track), `#000000` when ON (on light track).
- Status label: "BOOKING: LIVE" (when ON) or "BOOKING: COMING SOON" (when OFF). Mono 16px uppercase.
- "Save Changes" button is only enabled when the switch state differs from persisted state. Clicking opens the confirmation modal.

### Confirmation modal (shadcn `AlertDialog`)

Two copy variants depending on direction:

**ON → OFF (disabling):**
- Title (mono 20px): "TURN OFF BOOKING?"
- Body (Inter 14px): "Every Book Session and Book Now button across the site will redirect clients to the coming-soon page. The booking wizard will be hidden until you turn this back on."
- Cancel button: secondary, "Keep Booking Live"
- Confirm button: destructive (`#dc2626` bg, `#f5f5f0` text), "Turn Off Booking"

**OFF → ON (enabling):**
- Title (mono 20px): "TURN ON BOOKING?"
- Body (Inter 14px): "The booking wizard will become available to clients and Book CTAs will route to /book. Make sure at least one service has a booking config."
- Cancel button: secondary, "Keep Coming Soon"
- Confirm button: destructive style retained (for visual emphasis that this is site-wide), "Turn On Booking"

Post-confirmation: success toast ("Booking turned off — site now shows coming soon." / "Booking turned on — wizard is live."), card re-renders with new state.

---

## Copywriting Contract

All copy matches the cyberpunk brand voice — short, confident, mono-uppercase headings, sentence-case body. No exclamation points except where noted.

| Element | Copy |
|---------|------|
| Primary CTA — wizard Step 1→2 | "CONTINUE" (mono uppercase button) |
| Primary CTA — wizard Step 2→3 | "CONTINUE" |
| Primary CTA — wizard Step 3→4 | "CONTINUE TO DETAILS" |
| Primary CTA — wizard Step 4→5 | "CONTINUE TO PAYMENT" |
| Primary CTA — wizard Step 5 final | "COMPLETE BOOKING" |
| Back link (all wizard steps after step 1) | "← Back" (Inter 14px muted, no uppercase) |
| Primary CTA — service detail panel | "BOOK THIS SERVICE" |
| Primary CTA — non-bookable service on `/services` | "CONTACT FOR QUOTE" (routes to `/contact?service={slug}`) |
| Primary CTA — manifesto notify-me | "NOTIFY ME" |
| Primary CTA — admin toggle save | "Save Changes" (sentence case — matches admin copy convention) |
| Manifesto h1 | "WE'RE BUILDING GLITCH STUDIOS." |
| Manifesto lede | "A full-service music, video, and creative studio is almost online. Rooms are wiring up, sessions are locking in. Bookings open soon." |
| Manifesto what's-coming section heading | "WHAT WE'RE BUILDING" |
| Music production tile title | "MUSIC PRODUCTION" |
| Music production tile body | "Beats, mixing, mastering, and full-session studio time." |
| Video production tile title | "VIDEO PRODUCTION" |
| Video production tile body | "Music videos, performance capture, and narrative shoots." |
| Creative services tile title | "CREATIVE SERVICES" |
| Creative services tile body | "Graphic design, SFX, and brand-building for artists." |
| Notify-me section heading | "BE FIRST IN THE DOOR" |
| Notify-me sub-copy | "Drop your email. We'll let you know the day bookings open. No spam, no filler." |
| Notify-me success state | "YOU'RE ON THE LIST." + "We'll email you when bookings open." |
| Empty state — no dates in month | "No slots this month — try next." |
| Empty state — no time slots on date | "Session closed. Grab one next week." |
| Empty state — zero bookable services (fallback) | Same as full manifesto (D-06 / D-18) |
| Error — payment failed | "Payment didn't land. Retry or try another card." |
| Error — session load failed | "Couldn't load this session. Refresh and try again." |
| Error — network / generic wizard | "Connection dropped. Check your signal and retry." |
| Step subtitles | See per-step table above |
| Terms block heading (Step 4) | "DEPOSIT & CANCELLATION" |
| Service detail section — duration heading | "DURATION & INCLUDES" |
| Service detail section — process heading | "PROCESS" |
| Service detail section — policies heading | "POLICIES" |
| Service detail section — example work heading | "EXAMPLE WORK" |
| Destructive confirmation — turn off booking | Title: "TURN OFF BOOKING?" / Confirm: "Turn Off Booking" / Cancel: "Keep Booking Live" |
| Destructive confirmation — turn on booking | Title: "TURN ON BOOKING?" / Confirm: "Turn On Booking" / Cancel: "Keep Coming Soon" |
| Toast — toggle off success | "Booking turned off — site now shows coming soon." |
| Toast — toggle on success | "Booking turned on — wizard is live." |
| Toast — toggle save error | "Couldn't save. Try again." |
| Notify-me form error | "Couldn't save your email. Try again." |
| Admin status label (ON) | "BOOKING: LIVE" |
| Admin status label (OFF) | "BOOKING: COMING SOON" |

Tone guardrails enforced:
- No exclamation points in user-facing copy except in manifesto/notify-me (zero in this spec — keep it confident, not enthusiastic).
- No "oops", "whoops", "uh oh" — matches cyberpunk pragmatic tone.
- Uppercase only inside mono typography — sentence case for Inter body.

---

## Interaction States

| Component | Default | Hover | Active/Selected | Disabled | Focus |
|-----------|---------|-------|-----------------|----------|-------|
| Service tile (Step 1 wizard) | bg `#111111` border `#222222` | bg `#1a1a1a` border `#444444` + heading glitch | bg `#f5f5f0` text `#000000` | opacity-50 not-allowed | outline `#f5f5f0` 2px |
| Calendar day tile | pre-existing (unchanged) | pre-existing | pre-existing | pre-existing | pre-existing |
| Time-slot tile | pre-existing (unchanged) | pre-existing | pre-existing | pre-existing | pre-existing |
| Wizard primary CTA | bg `#f5f5f0` text `#000000` | opacity-90 | opacity-80 | opacity-50 + loading copy | ring `#f5f5f0` |
| Wizard "Back" link | text `#888888` underline on hover | text `#f5f5f0` | — | — | ring `#f5f5f0` |
| Manifesto notify-me input | bg `#111111` border `#222222` | border `#444444` | — | opacity-50 | ring `#555555` |
| Manifesto notify-me submit | bg `#f5f5f0` text `#000000` | opacity-90 | opacity-80 | loading copy "SAVING..." | ring `#f5f5f0` |
| Admin toggle switch | pre-existing shadcn Switch | pre-existing | pre-existing | pre-existing | pre-existing |
| Admin Save button | bg `#f5f5f0` text `#000000` | opacity-90 | opacity-80 | opacity-50 when no change | ring `#f5f5f0` |
| Confirmation modal confirm button (destructive) | bg `#dc2626` text `#f5f5f0` | brightness-110 | brightness-90 | loading copy "SAVING..." | ring `#dc2626` |
| Service detail panel | bg `#111111` border `#222222` | — | — | — | — |
| Example-work portfolio card (inside detail panel) | pre-existing portfolio card | pre-existing | — | — | pre-existing |

Loading states: all buttons use a text-swap pattern (e.g., "COMPLETE BOOKING" → "PROCESSING..."). No spinner components added. Matches Phase 8 convention.

---

## Motion Contract

- No auto-running animations on any heading (site-wide rule).
- Wizard step transitions: existing `motion/react` fade + slide on step change — do not modify.
- Manifesto section entry: existing `ScrollSection` fade-up variant per Phase 6.1 pattern — reuse, no new variants.
- Admin toggle switch: default shadcn Switch transition (200ms).
- Confirmation modal open/close: default shadcn AlertDialog fade + scale.
- Summary sidebar (desktop): no animation on value changes — immediate render. Collapsible summary (mobile): height-auto spring, 200ms.
- All motion respects `prefers-reduced-motion` via existing globals.css `@media (prefers-reduced-motion: reduce)` rule — no new overrides.

---

## Mobile Rendering Contract (success criterion #4, D-15)

Target viewports: 375px, 390px, 430px. Tested in Plan 01 (Playwright mobile audit).

Non-negotiables:
- Zero horizontal overflow on `/services`, `/book` (all 5 steps), `/services` coming-soon, `/book` coming-soon.
- Stepper dots remain visible on 375px — compact mode only shows current step number + total (pre-existing behavior, verify not regressed).
- Summary sidebar becomes the collapsible top-pinned block (56px header, expands inline).
- Service detail panel sections stack vertically with 24px gaps; no 2-column layouts below `lg` breakpoint (except `HIGHLIGHTS` which stays single-column on mobile).
- All tap targets remain ≥48px.
- Manifesto 3-up tile grid becomes single column; tiles retain 16px padding and 48px min-height per content section.
- Notify-me form: input + button stack vertically on mobile, full-width each.

Concrete break list is produced by Plan 01 audit and resolved in subsequent plans — this spec locks the targets, not the fixes.

---

## Components Touched / Created in This Phase

| Component | Action | Change |
|-----------|--------|--------|
| `src/components/services/service-grid.tsx` | Modify | Extend `ServiceDetailPanel` with 4 new sections (D-08). |
| `src/components/services/coming-soon-manifesto.tsx` | **Create** | New. Renders manifesto hero + what's-coming + notify-me. Shared by `/services` and `/book` when toggle OFF. |
| `src/components/booking/booking-flow.tsx` | Modify | Add persistent summary sidebar layout (D-10). Add step subtitles (D-11). Wire terms block into Step 4 (D-12). |
| `src/components/booking/booking-summary.tsx` | Modify | Upgrade to full 5-row sidebar format (service / date / time / duration / deposit). Add mobile collapsible variant. |
| `src/components/booking/service-selector.tsx` | Modify | Enrich tiles with description + price + duration (D-13). |
| `src/components/booking/booking-flow-stepper.tsx` | Modify (light) | Verify mobile compact mode renders correctly at 375px. No structural changes. |
| `src/app/(public)/services/page.tsx` | Modify | Read `booking_live` flag, branch between `<ServiceGrid>` and `<ComingSoonManifesto>`. |
| `src/app/(public)/book/page.tsx` | Modify | Read `booking_live` flag + check for bookable services. Branch between wizard and manifesto. |
| `src/app/admin/settings/page.tsx` | Modify | Add `<BookingLiveToggle>` card. |
| `src/app/admin/services/page.tsx` (or `/admin/bookings/`) | Modify | Add secondary `<BookingLiveToggle>` card. |
| `src/components/admin/booking-live-toggle.tsx` | **Create** | New. Switch + save button + confirmation modal. |
| `src/actions/settings/set-booking-live.ts` | **Create** | New server action. Updates `siteSettings` row, revalidates `/services`, `/book`, `/`. |
| `src/lib/get-booking-live.ts` | **Create** | New shared server util. Reads `siteSettings` once per request. Used by all Book CTA consumers. |
| `src/components/home/hero-section.tsx`, `src/components/layout/tile-nav.tsx`, footer, any Book CTA | Modify | Consult `getBookingLive()`; route to `/services` (manifesto) instead of `/book` when OFF. Link text unchanged (D-03). |
| `src/components/forms/newsletter-form.tsx` | Modify (light) | Accept optional `source` prop; pass through to server action for tagging launch-notify signups. |

No new shadcn components need to be installed for this phase. All UI uses pre-installed: `Button`, `Input`, `Label`, `Switch`, `AlertDialog`, `Card`, `Sonner` (toast). `Popover` / `Drawer` already available if mobile summary uses `Drawer` — planner's choice.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | Button, Input, Label, Switch, AlertDialog, Card, Sonner (all pre-existing) | not required |

No third-party registries declared. No new shadcn blocks added. Registry vetting gate: not applicable.

---

## Open Decisions (flagged for planner to resolve during plan-phase)

These are Claude's Discretion items from CONTEXT.md — they remain open for the planner to decide based on service data diversity and layout iteration:

1. **Process/timeline copy: generic template vs per-service-type** (D-08). This spec locks the 4-step shape (prep → session → revisions → delivery). Planner decides if the body copy is identical across all services or varies by `service.type`. Recommendation: start generic; upgrade to per-type only if generic reads wrong for e.g. graphic-design vs studio-session.
2. **Notify-me form placement within manifesto** (D-05). This spec puts it in its own "BE FIRST IN THE DOOR" block after the what's-coming grid. Planner may promote it to the hero if mobile length becomes unwieldy.
3. **GlitchLogo size on manifesto** — `md` or `lg`. Planner calls based on mobile hero fit.
4. **Summary sidebar on mobile — Drawer vs inline collapsible**. This spec assumes inline collapsible (pushes content down). Planner may switch to `Drawer` if inline proves to break wizard step vertical rhythm.
5. **Newsletter source tag schema touch** (D-19). If `newsletter_subscribers` has no `source` field, planner includes schema change + migration as a task.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
