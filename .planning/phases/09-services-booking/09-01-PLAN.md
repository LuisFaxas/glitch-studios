---
phase: 09-services-booking
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/09-services-booking-mobile-audit.spec.ts
  - .planning/phases/09-services-booking/audit/MOBILE-BREAKS.md
  - .planning/phases/09-services-booking/screenshots/audit/
autonomous: true
requirements: [BOOK-06, BOOK-07, BOOK-08]
must_haves:
  truths:
    - "Playwright spec exists that captures screenshots at 375px for all 8 target routes/states"
    - "A MOBILE-BREAKS.md document lists every concrete mobile rendering break with file reference"
    - "Screenshots are committed under .planning/phases/09-services-booking/screenshots/audit/"
  artifacts:
    - path: "tests/09-services-booking-mobile-audit.spec.ts"
      provides: "Playwright mobile audit spec per D-15"
      min_lines: 80
    - path: ".planning/phases/09-services-booking/audit/MOBILE-BREAKS.md"
      provides: "Concrete break list to drive Wave 4 tasks"
      contains: "# Mobile Audit — Break List"
  key_links:
    - from: "tests/09-services-booking-mobile-audit.spec.ts"
      to: ".planning/phases/09-services-booking/screenshots/audit/"
      via: "page.screenshot({ path })"
      pattern: "screenshot.*audit"
---

<objective>
Run the Playwright mobile audit locked by D-15. Capture 375px screenshots of `/services` (toggle ON), `/services` (toggle OFF — may not yet exist — placeholder OK), `/book` (toggle OFF — same), and `/book` wizard steps 1-5 with `isMobile: true, hasTouch: true`. Produce a concrete MOBILE-BREAKS.md listing every overflow, overlap, and sub-48px tap target observed, scoped to the files they live in. This break list feeds Wave 4 fix tasks — it replaces a generic "mobile pass."

Purpose: Ground Wave 4 mobile fixes in observed reality, not guesswork. Matches user feedback `feedback_playwright_verification`.
Output: Committed Playwright spec + audit markdown + screenshots directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-services-booking/09-CONTEXT.md
@.planning/phases/09-services-booking/09-UI-SPEC.md
@.planning/phases/09-services-booking/09-RESEARCH.md
@tests/mobile-audit.spec.ts
@tests/07.3-mobile-menu.spec.ts
@playwright.config.ts

<interfaces>
<!-- Existing mobile context pattern from Phase 07.2 (STATE.md decision) -->

From tests/mobile-audit.spec.ts (reference pattern):
```ts
import { test, expect } from "@playwright/test"

test("mobile audit", async ({ browser }) => {
  const context = await browser.newContext({
    viewport: { width: 375, height: 812 },
    isMobile: true,
    hasTouch: true,
    userAgent: "Mozilla/5.0 (iPhone; CPU iPhone OS 17_0 like Mac OS X) AppleWebKit/605.1.15"
  })
  const page = await context.newPage()
  // ...
})
```

Wizard step URLs (booking-flow uses internal state, not URL — must drive via UI):
- Step 1 (service): `/book` lands here by default
- Step 2 (date): click a service tile then "CONTINUE TO DATE"
- Step 3 (time): pick a date tile, continue
- Step 4 (details): pick a time, continue
- Step 5 (payment): fill form, continue (note: Stripe Embedded Checkout may not render in test env — screenshot the form-submit loading state is acceptable fallback)

Deep-link shortcut: `/book?service={slug}` lands on Step 2 directly (preserved from Phase 3 per 03-CONTEXT.md D-26).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Playwright mobile audit spec capturing 8 screenshots at 375px</name>
  <files>tests/09-services-booking-mobile-audit.spec.ts, .planning/phases/09-services-booking/screenshots/audit/.gitkeep</files>
  <read_first>
    - tests/mobile-audit.spec.ts (reference pattern for browser.newContext with isMobile/hasTouch)
    - tests/07.3-mobile-menu.spec.ts (reference pattern for interaction-driven Playwright flow)
    - .planning/phases/09-services-booking/09-UI-SPEC.md § "Mobile Rendering Contract" (non-negotiables to audit)
    - .planning/phases/09-services-booking/09-RESEARCH.md § "Mobile Audit Scope (D-15, Wave 0)" (8 target screenshots table)
    - playwright.config.ts (verify baseURL and projects)
    - src/app/(public)/book/page.tsx (understand `?service=slug` deep-link behavior)
    - src/components/booking/booking-flow.tsx (understand step-advance CTA labels — "CONTINUE TO DATE", etc.)
  </read_first>
  <action>
    Create `tests/09-services-booking-mobile-audit.spec.ts` that captures all 8 target screenshots at viewport 375x812 using `browser.newContext({ viewport: { width: 375, height: 812 }, isMobile: true, hasTouch: true, userAgent: "Mozilla/5.0 (iPhone; CPU iPhone OS 17_0 like Mac OS X) AppleWebKit/605.1.15" })`.

    Create the directory `.planning/phases/09-services-booking/screenshots/audit/` with a `.gitkeep` file so it is trackable.

    The spec must:

    1. Group into a single `test.describe("Phase 09 mobile audit (375px)", ...)` block.
    2. Write screenshots to `.planning/phases/09-services-booking/screenshots/audit/{name}.png` with `fullPage: true` for long pages and viewport-only for short ones (see list below).
    3. For each screenshot:
       - 01-services-toggle-on.png — `await page.goto("/services"); await page.waitForLoadState("networkidle"); await page.screenshot({ path: ..., fullPage: true });`
       - 02-services-toggle-off.png — same route. At audit time the manifesto may not yet exist; spec captures whatever renders. ACCEPTED result is either the manifesto OR current grid — the screenshot documents the pre-Wave-2 state.
       - 03-book-toggle-off.png — `await page.goto("/book")` — same caveat.
       - 04-book-step1-service.png — `await page.goto("/book"); await page.waitForSelector('[data-step="service"]', { timeout: 5000 }).catch(()=>{}); await page.screenshot(...)`. If the selector is absent, fall back to capturing the page as-rendered.
       - 05-book-step2-date.png — use the deep-link `/book?service=${firstServiceSlug}` where `firstServiceSlug` is resolved by reading the first service tile's link href on Step 1. If no bookable service exists, write a `.skip.md` marker in the audit folder and skip the remaining step screenshots.
       - 06-book-step3-time.png — click the first non-disabled calendar day tile. Locate via `page.locator('button:not([disabled])').filter({ has: page.locator('text=/^\\d+$/') }).first()` OR `page.getByRole("button", { name: /^\\d+$/ }).first()` — whichever matches the existing `calendar-day-tile.tsx` markup.
       - 07-book-step4-details.png — click first non-disabled time slot then the "CONTINUE TO DETAILS" (currently labelled "Continue" — locate via `page.getByRole("button", { name: /continue/i }).first()`) — CTA copy is not yet in the "CONTINUE TO DATE/TIME/DETAILS" form in pre-Phase-9 code.
       - 08-book-step5-payment.png — fill the form fields (name, email, phone if present) with `test@example.com` / "Test User" / "+1 555 555 5555", click continue. If Stripe Embedded Checkout iframe fails to load (test env limitation), wait 3s then screenshot whatever renders — typically the payment-card wrapper before the iframe hydrates.

    4. After each screenshot, run an assertion of horizontal overflow by evaluating `document.documentElement.scrollWidth <= document.documentElement.clientWidth + 1`. On FAIL, do NOT throw (this is an audit, not a regression gate) — instead `console.log(\`[OVERFLOW] ${name}: scrollWidth=... clientWidth=...\`)`. Capture a second screenshot suffixed `-overflow.png` showing the overflow at the right edge: `await page.evaluate(() => window.scrollTo(document.documentElement.scrollWidth, 0))`.

    5. After each screenshot, for ALL buttons and links within the viewport, collect `box.height` via `element.boundingBox()`. Log any button/link with `height < 48` with its `innerText().slice(0, 40)` — these become `[TAP-TARGET]` entries in MOBILE-BREAKS.md.

    The `.gitkeep` file contents: empty file.

    Run the spec once to confirm it executes end-to-end without thrown errors before committing (non-throwing logs are acceptable).
  </action>
  <verify>
    <automated>cd /home/faxas/workspaces/projects/personal/glitch_studios && pnpm exec playwright test tests/09-services-booking-mobile-audit.spec.ts --reporter=list 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/09-services-booking-mobile-audit.spec.ts` exists and contains the literal string `isMobile: true` and `hasTouch: true`.
    - Directory `.planning/phases/09-services-booking/screenshots/audit/` exists and contains at least 6 PNG files after spec runs (some steps may skip if no bookable service).
    - Playwright run exits 0 (audit is non-throwing).
    - `grep -c "fullPage: true" tests/09-services-booking-mobile-audit.spec.ts` ≥ 8.
    - `grep -c "OVERFLOW" tests/09-services-booking-mobile-audit.spec.ts` ≥ 1 (overflow logger present).
    - `grep -c "TAP-TARGET" tests/09-services-booking-mobile-audit.spec.ts` ≥ 1 (tap target logger present).
  </acceptance_criteria>
  <done>
    Spec runs and emits 6-8 PNGs plus structured `[OVERFLOW]` / `[TAP-TARGET]` logs to stdout.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author MOBILE-BREAKS.md documenting every observed issue with file references</name>
  <files>.planning/phases/09-services-booking/audit/MOBILE-BREAKS.md</files>
  <read_first>
    - Output of Task 1 (screenshots and stdout logs from `pnpm exec playwright test tests/09-services-booking-mobile-audit.spec.ts`)
    - .planning/phases/09-services-booking/09-UI-SPEC.md § "Mobile Rendering Contract" non-negotiables
    - src/components/booking/booking-summary.tsx (understand current mobile collapse height vs 48px target)
    - src/components/booking/booking-flow-stepper.tsx (verify mobile compact mode at 375px)
    - src/components/booking/service-selector.tsx (verify rich tile does not overflow at 375px)
    - src/components/services/service-grid.tsx (verify detail panel accordion pattern at 375px)
  </read_first>
  <action>
    Re-run the audit spec `pnpm exec playwright test tests/09-services-booking-mobile-audit.spec.ts --reporter=list 2>&1 | tee /tmp/09-audit.log`.

    Open each of the 8 screenshots (use `ls -la .planning/phases/09-services-booking/screenshots/audit/` to confirm existence; use the Read tool on each PNG to inspect visually).

    Produce `.planning/phases/09-services-booking/audit/MOBILE-BREAKS.md` with EXACTLY this structure (keep headings literal):

    ```markdown
    # Mobile Audit — Break List

    **Audited:** {YYYY-MM-DD}
    **Viewport:** 375x812 (iPhone emulation, isMobile+hasTouch)
    **Source:** tests/09-services-booking-mobile-audit.spec.ts
    **Screenshots:** .planning/phases/09-services-booking/screenshots/audit/

    ## Summary

    | Route/State | Overflow? | Tap targets <48px | Stacking issues | Priority |
    |-------------|-----------|-------------------|-----------------|----------|
    | /services (toggle ON) | yes/no | N | <description or none> | P0/P1/P2 |
    | /services (toggle OFF) | yes/no | N | ... | ... |
    | /book (toggle OFF) | yes/no | N | ... | ... |
    | /book Step 1 (Service) | yes/no | N | ... | ... |
    | /book Step 2 (Date) | yes/no | N | ... | ... |
    | /book Step 3 (Time) | yes/no | N | ... | ... |
    | /book Step 4 (Details) | yes/no | N | ... | ... |
    | /book Step 5 (Payment) | yes/no | N | ... | ... |

    ## Breaks

    ### B-01 — {short title}

    - **Route:** /book Step 2
    - **Screenshot:** screenshots/audit/05-book-step2-date.png
    - **File:** src/components/booking/booking-summary.tsx (or whichever)
    - **Observed:** {what is wrong — e.g. "Summary card renders 72px collapsed header — spec mandates 48px"}
    - **UI-SPEC reference:** § Mobile Rendering Contract / § Persistent Booking Summary Sidebar
    - **Fix hint:** {which line(s) to change — e.g. "Change `h-[72px]` to `h-12` on collapsed container"}
    - **Priority:** P0 (breaks non-negotiable) / P1 (visible regression) / P2 (polish)

    ### B-02 — ...
    ```

    Rules for populating breaks:
    - Every `[OVERFLOW]` stdout log from Task 1 becomes a P0 break.
    - Every `[TAP-TARGET]` log below 48px becomes a P0 break (non-negotiable per UI-SPEC).
    - Visually observed stacking/overlap/truncation issues become P1 breaks — include the exact pixel/tailwind class to change in the "Fix hint" field.
    - Stepper dots not visible at 375px → P0 (explicit non-negotiable).
    - Rich Step 1 tiles overflowing → P0.
    - If no breaks are found on a route, write `B-{NN} — (none observed)` entry for that route explicitly — do NOT omit.

    Minimum 8 `### B-NN` entries (one per route, even if the entry is "(none observed)"). If genuine breaks are more numerous, add all of them.

    Close the file with a `## Wave 4 Task Mapping` section that maps each P0/P1 break to a proposed fix file path (the executor in Wave 4 will consume this map).
  </action>
  <verify>
    <automated>test -f /home/faxas/workspaces/projects/personal/glitch_studios/.planning/phases/09-services-booking/audit/MOBILE-BREAKS.md && grep -c "^### B-" /home/faxas/workspaces/projects/personal/glitch_studios/.planning/phases/09-services-booking/audit/MOBILE-BREAKS.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/09-services-booking/audit/MOBILE-BREAKS.md` exists.
    - Contains literal heading `# Mobile Audit — Break List`.
    - Contains literal heading `## Summary` with a table of 8 routes.
    - `grep -c "^### B-" MOBILE-BREAKS.md` ≥ 8.
    - Contains literal heading `## Wave 4 Task Mapping`.
    - Each `### B-NN` entry contains the literal keys `**Route:**`, `**Screenshot:**`, `**File:**`, `**Observed:**`, `**Fix hint:**`, `**Priority:**`.
  </acceptance_criteria>
  <done>
    MOBILE-BREAKS.md documents ≥8 entries; Wave 4 plan can consume it directly.
  </done>
</task>

</tasks>

<verification>
- `pnpm exec playwright test tests/09-services-booking-mobile-audit.spec.ts --reporter=list` exits 0.
- `ls .planning/phases/09-services-booking/screenshots/audit/*.png | wc -l` ≥ 6.
- `grep -c "^### B-" .planning/phases/09-services-booking/audit/MOBILE-BREAKS.md` ≥ 8.
</verification>

<success_criteria>
- Playwright mobile audit captures all reachable routes/states at 375px.
- MOBILE-BREAKS.md is authored with per-break file references and fix hints.
- Screenshots folder committed (enables before/after comparison in Wave 4).
</success_criteria>

<output>
After completion, create `.planning/phases/09-services-booking/09-01-SUMMARY.md` summarizing:
- Number of screenshots captured
- Number of breaks documented by priority (P0/P1/P2)
- Top 3 most critical breaks to drive Wave 4 scope
</output>
