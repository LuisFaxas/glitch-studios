---
phase: 06-homepage
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/components/home/featured-carousel.tsx
  - src/components/home/services-overview.tsx
  - src/components/home/testimonials-carousel.tsx
  - src/components/home/blog-section.tsx
autonomous: true
requirements:
  - HOME-01
  - HOME-03

must_haves:
  truths:
    - "Featured beats section shows real beat data (title, genre, BPM, cover art) from database, not placeholder 'Coming Soon' cards"
    - "Services section uses Metro tile grid with tight 2px gaps, not loose gap-6 cards"
    - "Testimonials use square avatars (rounded-none), not rounded-full circles"
    - "Homepage sections flow in logical narrative order with consistent visual rhythm"
    - "Blog section renders latest posts as tiles on the homepage"
  artifacts:
    - path: "src/components/home/featured-carousel.tsx"
      provides: "Carousel rendering real beats with cover art, title, genre, BPM"
      contains: "beat.title"
    - path: "src/components/home/services-overview.tsx"
      provides: "Metro tile grid with tight gaps"
      contains: "gap-[2px]"
    - path: "src/components/home/testimonials-carousel.tsx"
      provides: "Testimonials with square avatars"
      contains: "rounded-none"
    - path: "src/components/home/blog-section.tsx"
      provides: "Blog section showing latest 3 posts"
      contains: "BlogSection"
  key_links:
    - from: "src/components/home/featured-carousel.tsx"
      to: "src/app/(public)/page.tsx"
      via: "beats prop passed from page.tsx"
      pattern: "beats.*Beat"
    - from: "src/components/home/blog-section.tsx"
      to: "src/app/(public)/page.tsx"
      via: "posts prop passed from page.tsx"
      pattern: "posts.*BlogPost"
---

<objective>
Polish all below-fold homepage sections: replace placeholder featured beats with real DB data, apply Metro tile language to services and testimonials, fix design violations, and create the blog section component.

Purpose: Below-fold content must flow logically and look cohesive (HOME-03). The featured beats section currently shows fake "Coming Soon" cards despite real beats in the DB (D-06). Services use loose gaps that violate Metro tile language (D-02). Testimonials use rounded avatars which violate the "no rounded corners" rule. A blog section adds freshness and SEO value.

Output: Four polished homepage section components that render real data with consistent Metro tile styling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/DESIGN-LANGUAGE.md
@.planning/phases/06-homepage/06-CONTEXT.md
@.planning/phases/06-homepage/06-RESEARCH.md
@.planning/phases/06-homepage/06-01-SUMMARY.md

<interfaces>
<!-- From Plan 01, page.tsx now passes these props -->

Beat data shape (from src/db/schema.ts beats table):
```typescript
type Beat = {
  id: string
  title: string
  slug: string
  bpm: number
  key: string
  genre: string
  moods: string[] | null
  description: string | null
  coverArtKey: string | null
  previewAudioKey: string | null
  status: string
  createdAt: Date
}
```

Blog post data shape (from src/db/schema.ts blogPosts table):
```typescript
type BlogPost = {
  id: string
  title: string
  slug: string
  excerpt: string | null
  coverImageUrl: string | null
  status: string
  publishedAt: Date | null
  createdAt: Date
}
```

From page.tsx (after Plan 01):
- FeaturedCarousel receives: `beats={beatsList}` (Beat[])
- BlogSection receives: `posts={blogList}` (BlogPost[])
- ServicesOverview receives: `services={servicesList}` (unchanged)
- TestimonialsCarousel receives: `testimonials={testimonialsList}` (unchanged)

Design Language tokens (from DESIGN-LANGUAGE.md):
- Tile bg: `bg-[#111]`
- Tile border: `border border-[#222]`
- Tile hover: `hover:bg-[#1a1a1a] hover:border-[#444]`
- Grid gap: `gap-[2px]`
- No rounded corners: `rounded-none`
- Headings: `font-mono font-bold uppercase tracking-[0.05em]`
- Sublabels: `text-[#888] text-sm`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace placeholder featured beats carousel with real beat data</name>
  <files>src/components/home/featured-carousel.tsx</files>
  <read_first>
    - src/components/home/featured-carousel.tsx
    - src/db/schema.ts (lines 218-236 for beats table)
    - .planning/DESIGN-LANGUAGE.md
  </read_first>
  <action>
Rewrite FeaturedCarousel to render real beats from the database instead of hardcoded placeholder cards. Per D-06, the "Coming Soon" placeholder data must be completely removed.

1. Change the component props from `{ beatIds?: string[] }` to accept full beat objects:
   ```typescript
   type Beat = {
     id: string
     title: string
     slug: string
     bpm: number
     key: string
     genre: string
     coverArtKey: string | null
   }

   interface FeaturedCarouselProps {
     beats: Beat[]
   }
   ```

2. Delete the entire `placeholderBeats` array and the `Array.from({ length: 6 }, ...)` code. Remove the `beatIds` prop. Remove the `"Coming soon in the beat store"` subtitle.

3. If `beats.length === 0`, render a minimal empty state tile (similar to portfolio empty state) with text "No beats available yet" -- NOT the old placeholder cards.

4. For each beat, render a card with:
   - Cover art: If `beat.coverArtKey` exists, render `<img src={"/api/media/" + beat.coverArtKey} alt={beat.title} className="absolute inset-0 w-full h-full object-cover" />` inside a `relative aspect-square bg-[#0a0a0a]` container. If no cover art, show a centered `<Music className="size-10 text-[#333]" />` icon.
   - Title: `font-mono font-bold text-[#f5f5f0] text-sm truncate`
   - Genre badge: `text-xs bg-[#1a1a1a] text-[#888] px-2 py-0.5 rounded-none`
   - BPM + Key line: `text-[#555] text-xs font-mono` showing `{beat.bpm} BPM // {beat.key}`
   - Link wrapping the card: `<Link href={"/beats/" + beat.slug}>` for navigation

5. Card container classes: `flex-shrink-0 w-[220px] bg-[#111] border border-[#222] rounded-none overflow-hidden hover:border-[#444] hover:bg-[#1a1a1a] transition-colors group`

6. Card info section below image: `p-3 flex flex-col gap-1`

7. Keep Embla carousel setup unchanged (loop, align start, prev/next buttons). Keep ScrollSection wrapper. Keep the "Featured Beats" heading but remove the "Coming soon" subtext.

8. Add `import Link from "next/link"` at top.

9. Use `gap-[2px]` instead of `gap-4` between carousel items for Metro tile spacing.
  </action>
  <verify>
    <automated>cd /home/faxas/workspaces/projects/personal/glitch_studios && grep -c "Coming Soon" src/components/home/featured-carousel.tsx && grep -c "placeholderBeats" src/components/home/featured-carousel.tsx && grep -c "beat.title" src/components/home/featured-carousel.tsx && grep -c "beat.bpm" src/components/home/featured-carousel.tsx && grep -c "beat.genre" src/components/home/featured-carousel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Coming Soon" src/components/home/featured-carousel.tsx` returns 0 matches (placeholder removed)
    - `grep "placeholderBeats" src/components/home/featured-carousel.tsx` returns 0 matches (placeholder array removed)
    - `grep "beat.title" src/components/home/featured-carousel.tsx` returns matches (real data rendering)
    - `grep "beat.bpm" src/components/home/featured-carousel.tsx` returns a match (BPM displayed)
    - `grep "beat.genre" src/components/home/featured-carousel.tsx` returns a match (genre displayed)
    - `grep "beat.slug" src/components/home/featured-carousel.tsx` returns a match (links to beat detail)
    - `grep "coverArtKey" src/components/home/featured-carousel.tsx` returns a match (cover art rendered)
    - `grep "gap-\[2px\]" src/components/home/featured-carousel.tsx` returns a match (Metro gap)
    - `grep "rounded-none" src/components/home/featured-carousel.tsx` returns matches (no rounded corners)
    - `grep "beats: Beat\[\]" src/components/home/featured-carousel.tsx` returns a match (correct prop type)
  </acceptance_criteria>
  <done>FeaturedCarousel renders real beat data (title, genre, BPM, key, cover art) with links to beat detail pages. No placeholder cards remain. Metro tile styling with tight gaps. Empty state handles zero beats gracefully.</done>
</task>

<task type="auto">
  <name>Task 2: Apply Metro tile language to services and testimonials, create blog section</name>
  <files>src/components/home/services-overview.tsx, src/components/home/testimonials-carousel.tsx, src/components/home/blog-section.tsx</files>
  <read_first>
    - src/components/home/services-overview.tsx
    - src/components/home/testimonials-carousel.tsx
    - .planning/DESIGN-LANGUAGE.md
    - .planning/phases/06-homepage/06-RESEARCH.md
  </read_first>
  <action>
Apply Metro tile design language to services and testimonials sections, fix design violations, and create the blog section component.

**Services (src/components/home/services-overview.tsx):**

1. Change the grid gap from `gap-6` (24px, too loose) to `gap-[2px]` (Metro tight gap per D-02 and DESIGN-LANGUAGE.md).
2. Keep all existing functionality (service data, Link to service detail).
3. Ensure all elements use `rounded-none` (already correct but verify).
4. Section heading style is already correct (`font-mono font-bold uppercase`).

**Testimonials (src/components/home/testimonials-carousel.tsx):**

1. Fix the design violation: change avatar `className` from `rounded-full` to `rounded-none` on line 99. This is a D-02 compliance fix -- the design language explicitly forbids rounded corners.
2. Change the carousel item gap from `gap-4` to `gap-[2px]` for Metro spacing.
3. Keep all existing functionality (Embla autoplay, star ratings, prev/next buttons).
4. Ensure `rounded-none` is present on the card container (already has it, verify).

**Blog Section (NEW: src/components/home/blog-section.tsx):**

Create a new server-compatible component that renders the latest 2-3 blog posts as Metro tiles on the homepage. This section is registered in the admin homepage editor as "blog" type (done in Plan 01).

```typescript
import Link from "next/link"
import { ScrollSection } from "@/components/home/scroll-section"

type BlogPost = {
  id: string
  title: string
  slug: string
  excerpt: string | null
  coverImageUrl: string | null
  publishedAt: Date | null
  createdAt: Date
}

interface BlogSectionProps {
  posts: BlogPost[]
}
```

Component structure:
- Wrap in `ScrollSection` with `className="py-16 md:py-24"`
- Container: `max-w-7xl mx-auto px-4`
- Heading: "Latest News" using `font-mono font-bold uppercase text-3xl md:text-4xl tracking-tight text-[#f5f5f0] mb-8`
- Grid: `grid grid-cols-1 md:grid-cols-3 gap-[2px]`
- Each post tile:
  - Container: `bg-[#111] border border-[#222] rounded-none overflow-hidden hover:border-[#444] hover:bg-[#1a1a1a] transition-colors group`
  - If `coverImageUrl`: render image in `relative aspect-video bg-[#0a0a0a]` container
  - Content area `p-4 flex flex-col gap-2`:
    - Date: format `publishedAt || createdAt` as `YYYY-MM-DD` using `.toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' })` -- `text-[#555] text-xs font-mono uppercase`
    - Title: `font-mono font-bold text-[#f5f5f0] text-base line-clamp-2`
    - Excerpt (if exists): `text-[#888] text-sm line-clamp-2`
  - Wrap entire tile in `<Link href={"/blog/" + post.slug}>`
- If `posts.length === 0`, return `null` (don't render the section at all)

Export: `export function BlogSection({ posts }: BlogSectionProps)`
  </action>
  <verify>
    <automated>cd /home/faxas/workspaces/projects/personal/glitch_studios && grep -c "gap-\[2px\]" src/components/home/services-overview.tsx && grep "rounded-full" src/components/home/testimonials-carousel.tsx; echo "rounded-full check: $?" && grep -c "gap-\[2px\]" src/components/home/testimonials-carousel.tsx && test -f src/components/home/blog-section.tsx && echo "blog-section exists" && grep -c "BlogSection" src/components/home/blog-section.tsx && grep -c "Latest News" src/components/home/blog-section.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "gap-\[2px\]" src/components/home/services-overview.tsx` returns a match (tight Metro gap)
    - `grep "gap-6" src/components/home/services-overview.tsx` returns 0 matches (old loose gap removed)
    - `grep "rounded-full" src/components/home/testimonials-carousel.tsx` returns 0 matches (design violation fixed)
    - `grep "rounded-none" src/components/home/testimonials-carousel.tsx` returns matches (square avatars)
    - `grep "gap-\[2px\]" src/components/home/testimonials-carousel.tsx` returns a match (tight Metro gap)
    - File `src/components/home/blog-section.tsx` exists
    - `grep "export function BlogSection" src/components/home/blog-section.tsx` returns a match
    - `grep "Latest News" src/components/home/blog-section.tsx` returns a match
    - `grep "gap-\[2px\]" src/components/home/blog-section.tsx` returns a match (Metro grid)
    - `grep "rounded-none" src/components/home/blog-section.tsx` returns matches
    - `grep "post.slug" src/components/home/blog-section.tsx` returns a match (links to blog posts)
  </acceptance_criteria>
  <done>Services grid uses tight 2px Metro gaps. Testimonial avatars are square (rounded-none). Blog section component renders latest posts as Metro tiles with cover images, dates, titles, and excerpts. All sections follow Cyberpunk Metro design language consistently.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep "Coming Soon" src/components/home/featured-carousel.tsx` returns 0 (no placeholders)
2. `grep "placeholderBeats" src/components/home/featured-carousel.tsx` returns 0 (placeholder array gone)
3. `grep "rounded-full" src/components/home/testimonials-carousel.tsx` returns 0 (violation fixed)
4. `grep "gap-6" src/components/home/services-overview.tsx` returns 0 (loose gap removed)
5. `grep "gap-\[2px\]"` returns matches in services-overview.tsx, testimonials-carousel.tsx, featured-carousel.tsx, blog-section.tsx (Metro consistency)
6. blog-section.tsx exists and exports BlogSection
7. `tsc --noEmit` passes (type safety across all changes)
</verification>

<success_criteria>
- Featured beats carousel shows real beat data from DB -- cover art, title, genre, BPM, key -- with links to detail pages
- Zero placeholder "Coming Soon" cards remain anywhere
- Services section uses 2px Metro tile gaps instead of 24px
- Testimonial avatars are square (rounded-none), not circular (rounded-full)
- Blog section renders latest 3 posts as Metro tiles with dates, titles, excerpts
- All homepage sections follow consistent Cyberpunk Metro visual language (D-02)
- Homepage content flows logically: Hero -> Services -> Featured Beats -> Portfolio -> Testimonials -> Blog (HOME-03)
</success_criteria>

<output>
After completion, create `.planning/phases/06-homepage/06-02-SUMMARY.md`
</output>
