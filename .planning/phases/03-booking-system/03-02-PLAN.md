---
phase: 03-booking-system
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/app/admin/rooms/page.tsx
  - src/app/admin/rooms/actions.ts
  - src/app/admin/availability/page.tsx
  - src/app/admin/availability/actions.ts
  - src/app/admin/services/[id]/booking-config/page.tsx
  - src/app/admin/services/[id]/booking-config/actions.ts
  - src/app/admin/packages/page.tsx
  - src/app/admin/packages/actions.ts
  - src/components/admin/admin-room-manager.tsx
  - src/components/admin/admin-availability-editor.tsx
  - src/components/admin/admin-service-booking-config.tsx
  - src/components/admin/admin-package-manager.tsx
autonomous: true
requirements:
  - ADMN-02
  - BOOK-03

must_haves:
  truths:
    - "Admin can create, edit, and delete rooms with name, description, features, and buffer time"
    - "Admin can set weekly availability hours per room per day of week"
    - "Admin can add date-specific overrides (closures, altered hours)"
    - "Admin can configure per-service booking settings: duration, deposit type/amount, auto-confirm, cancellation policy"
    - "Admin can create and manage session packages with discount pricing"
  artifacts:
    - path: "src/app/admin/rooms/page.tsx"
      provides: "Room management admin page"
      contains: "export default"
    - path: "src/app/admin/availability/page.tsx"
      provides: "Availability configuration admin page"
      contains: "export default"
    - path: "src/app/admin/services/[id]/booking-config/page.tsx"
      provides: "Per-service booking config admin page"
      contains: "export default"
    - path: "src/app/admin/packages/page.tsx"
      provides: "Session package management admin page"
      contains: "export default"
  key_links:
    - from: "src/app/admin/rooms/actions.ts"
      to: "src/db/schema.ts"
      via: "CRUD operations on rooms table"
      pattern: "import.*rooms.*from.*schema"
    - from: "src/app/admin/availability/actions.ts"
      to: "src/db/schema.ts"
      via: "CRUD on weeklyAvailability and availabilityOverrides"
      pattern: "import.*weeklyAvailability.*from.*schema"
---

<objective>
Build all admin configuration surfaces for the booking system: room management, weekly availability editor with date overrides, per-service booking config (duration, deposit, cancellation policy, auto-confirm), and session package management.

Purpose: Admin must configure rooms, availability, and policies BEFORE the public booking flow can work. These surfaces provide the data that the availability/slot logic queries.
Output: 4 admin pages with full CRUD via server actions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-booking-system/03-CONTEXT.md
@.planning/phases/03-booking-system/03-UI-SPEC.md
@.planning/phases/03-booking-system/03-01-SUMMARY.md
@src/db/schema.ts
@src/types/booking.ts
@src/app/admin/layout.tsx
@src/app/admin/beats/page.tsx

<interfaces>
<!-- Admin layout pattern from existing beat admin -->
From src/app/admin/layout.tsx — admin layout wraps all /admin routes with role check.
From src/app/admin/beats/page.tsx — existing admin page pattern: server component, queries DB, passes data to client component.
From src/db/schema.ts (after Plan 01):
```typescript
export const rooms = pgTable("rooms", { id, name, slug, description, features, photos, hourlyRateOverride, bufferMinutes, isActive, sortOrder, createdAt, updatedAt })
export const serviceBookingConfig = pgTable("service_booking_config", { id, serviceId, durationMinutes, depositType, depositValue, autoConfirm, cancellationWindowHours, refundPolicy, maxAdvanceBookingDays, prepInstructions, createdAt, updatedAt })
export const weeklyAvailability = pgTable("weekly_availability", { id, roomId, dayOfWeek, startTime, endTime, isActive })
export const availabilityOverrides = pgTable("availability_overrides", { id, roomId, date, isClosed, startTime, endTime, reason })
export const sessionPackages = pgTable("session_packages", { id, serviceId, name, sessionCount, discountPercent, isActive, createdAt })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Room management and availability editor admin pages</name>
  <files>src/app/admin/rooms/page.tsx, src/app/admin/rooms/actions.ts, src/app/admin/availability/page.tsx, src/app/admin/availability/actions.ts, src/components/admin/admin-room-manager.tsx, src/components/admin/admin-availability-editor.tsx</files>
  <read_first>
    - src/app/admin/beats/page.tsx (existing admin page pattern)
    - src/app/admin/layout.tsx (admin layout/role check)
    - src/db/schema.ts (rooms, weeklyAvailability, availabilityOverrides tables)
    - src/types/booking.ts (RoomInfo type)
    - .planning/phases/03-booking-system/03-UI-SPEC.md (admin room and availability layouts)
  </read_first>
  <action>
    **Room Management — `/admin/rooms`:**

    1. Create `src/app/admin/rooms/actions.ts` with server actions:
       - `createRoom(formData: FormData)`: Insert into rooms table. Fields: name (required), slug (auto-generated from name via kebab-case), description, features (comma-separated string → array), hourlyRateOverride (nullable number), bufferMinutes (default 15). Revalidate `/admin/rooms`.
       - `updateRoom(id: string, formData: FormData)`: Update rooms row by id. Same fields as create.
       - `deleteRoom(id: string)`: Delete from rooms where id matches. Revalidate.
       - `getRooms()`: Select all rooms ordered by sortOrder. Return array.
       - All actions use `"use server"` directive.

    2. Create `src/components/admin/admin-room-manager.tsx` — client component:
       - Props: `rooms: RoomInfo[]`
       - Tile grid layout matching UI-SPEC `/admin/rooms` wireframe
       - Each room renders as a Tile with name, description, active status badge
       - "+ Add Room" button opens a Dialog with form fields: Room Name (text, required), Description (textarea), Features (text, comma-separated), Hourly Rate Override (number), Buffer Time (number, default 15), Active (Switch toggle, default on)
       - Edit: click room tile opens same Dialog pre-filled with room data
       - Delete: destructive button in edit dialog with confirmation
       - Form inputs: flat style, 1px `#333333` border, `#f5f5f0` text, `#000000` bg per UI-SPEC form spec
       - Use sonner toast for success/error feedback

    3. Create `src/app/admin/rooms/page.tsx` — server component with `export const dynamic = "force-dynamic"`:
       - Query all rooms via `getRooms()`
       - Pass to `<AdminRoomManager rooms={rooms} />`
       - Page heading: "ROOMS" in JetBrains Mono 28px bold

    **Availability Editor — `/admin/availability`:**

    4. Create `src/app/admin/availability/actions.ts` with server actions:
       - `getWeeklySchedule(roomId: string)`: Select weeklyAvailability where roomId, ordered by dayOfWeek
       - `upsertWeeklySchedule(roomId: string, schedules: Array<{dayOfWeek: number, startTime: string, endTime: string, isActive: boolean}>)`: Delete existing entries for roomId, insert new entries. Use transaction.
       - `getOverrides(roomId: string)`: Select availabilityOverrides where roomId, ordered by date
       - `createOverride(roomId: string, formData: FormData)`: Insert into availabilityOverrides. Fields: date (YYYY-MM-DD), isClosed (boolean), startTime (nullable), endTime (nullable), reason (text).
       - `deleteOverride(id: string)`: Delete from availabilityOverrides.

    5. Create `src/components/admin/admin-availability-editor.tsx` — client component:
       - Props: `rooms: RoomInfo[]` (for room selector dropdown)
       - Room selector at top (Select component or tab per room)
       - Per UI-SPEC layout: Accordion per day of week (Mon-Sun). Each accordion item shows: day name, current hours or "CLOSED". Expanded: startTime input (time type or text "HH:mm"), endTime input, Active toggle (Switch). Days: ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"] indexed 0-6.
       - Default schedule for new rooms: Mon-Fri 09:00-21:00, Sat 10:00-18:00, Sun closed
       - "Save Schedule" button calls upsertWeeklySchedule
       - Date Overrides section below: Table with columns Date, Hours/Closed, Reason, Delete action. "+ Add Override" button opens Dialog with date input, isClosed checkbox (if checked, hide time inputs), startTime, endTime, reason.
       - Use the shadcn Accordion component for day sections

    6. Create `src/app/admin/availability/page.tsx` — server component:
       - Query rooms and pass to `<AdminAvailabilityEditor rooms={rooms} />`
       - Availability data fetched client-side when room is selected (via actions called from component)
       - Page heading: "AVAILABILITY" in JetBrains Mono 28px bold
  </action>
  <verify>
    <automated>cd /home/faxas/workspaces/projects/personal/glitch_studios && grep "export default" src/app/admin/rooms/page.tsx && grep "export default" src/app/admin/availability/page.tsx && grep "createRoom" src/app/admin/rooms/actions.ts && grep "upsertWeeklySchedule" src/app/admin/availability/actions.ts && grep "AdminRoomManager" src/components/admin/admin-room-manager.tsx && grep "AdminAvailabilityEditor" src/components/admin/admin-availability-editor.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/admin/rooms/page.tsx contains `export const dynamic = "force-dynamic"` and `export default`
    - src/app/admin/rooms/actions.ts contains `"use server"` and functions `createRoom`, `updateRoom`, `deleteRoom`, `getRooms`
    - src/components/admin/admin-room-manager.tsx contains `"use client"` and imports `Dialog` from shadcn
    - src/app/admin/availability/page.tsx contains `export const dynamic = "force-dynamic"` and `export default`
    - src/app/admin/availability/actions.ts contains `upsertWeeklySchedule` and `createOverride` and `deleteOverride`
    - src/components/admin/admin-availability-editor.tsx contains `"use client"` and imports `Accordion` from shadcn
    - Room form includes fields: name, description, features, bufferMinutes, hourlyRateOverride, isActive (Switch)
    - Availability editor includes 7 day-of-week accordion items
  </acceptance_criteria>
  <done>Admin can CRUD rooms and configure weekly availability schedules per room with date-specific overrides for holidays/closures</done>
</task>

<task type="auto">
  <name>Task 2: Service booking config and session package management admin pages</name>
  <files>src/app/admin/services/[id]/booking-config/page.tsx, src/app/admin/services/[id]/booking-config/actions.ts, src/app/admin/packages/page.tsx, src/app/admin/packages/actions.ts, src/components/admin/admin-service-booking-config.tsx, src/components/admin/admin-package-manager.tsx</files>
  <read_first>
    - src/app/admin/beats/page.tsx (existing admin page pattern)
    - src/db/schema.ts (serviceBookingConfig, sessionPackages, services tables)
    - src/types/booking.ts (ServiceBookingInfo, PackageInfo types)
    - .planning/phases/03-booking-system/03-UI-SPEC.md (admin service booking config form spec, admin color-coding)
  </read_first>
  <action>
    **Service Booking Config — `/admin/services/[id]/booking-config`:**

    1. Create `src/app/admin/services/[id]/booking-config/actions.ts`:
       - `getServiceBookingConfig(serviceId: string)`: Select from serviceBookingConfig where serviceId. Return config or null.
       - `upsertServiceBookingConfig(serviceId: string, formData: FormData)`: If config exists, update. If not, insert. Fields from UI-SPEC form:
         - durationMinutes: number (mapped from select: 30, 60, 120, 240, 480, or custom number)
         - depositType: "percentage" | "flat" (radio group)
         - depositValue: number
         - autoConfirm: boolean (switch)
         - cancellationWindowHours: number (default 48)
         - refundPolicy: "full" | "partial" | "none" (select)
         - maxAdvanceBookingDays: number (default 90)
         - prepInstructions: text (textarea, nullable)
       - Revalidate path.

    2. Create `src/components/admin/admin-service-booking-config.tsx` — client component:
       - Props: `serviceId: string, serviceName: string, config: ServiceBookingInfo | null`
       - Form layout per UI-SPEC Admin Service Booking Config section:
         - Session Duration: Select with options "30 min", "1 hour", "2 hours", "4 hours", "8 hours (full day)", "Custom". If custom, show number input for minutes.
         - Deposit Type: RadioGroup with "Percentage" and "Flat Amount" options
         - Deposit Value: Number input. Label changes based on type: "Deposit %" or "Deposit $"
         - Auto-Confirm: Switch toggle with label "Auto-confirm bookings" / "Require admin approval"
         - Cancellation Window: Number input, label "Hours before session"
         - Refund Policy: Select with "Full refund", "50% refund", "No refund"
         - Buffer Time: inherited from room config (show note: "Buffer time is configured per room")
         - Max Advance Booking: Number input, label "Days in advance"
         - Prep Instructions: Textarea, placeholder "Instructions included in confirmation email"
       - Default values when config is null: durationMinutes=120, depositType="percentage", depositValue=50, autoConfirm=true, cancellationWindowHours=48, refundPolicy="full", maxAdvanceBookingDays=90
       - Submit calls upsertServiceBookingConfig server action
       - Sonner toast on success

    3. Create `src/app/admin/services/[id]/booking-config/page.tsx` — server component:
       - Query service by id, query serviceBookingConfig
       - If service not found, notFound()
       - Pass to `<AdminServiceBookingConfig serviceId={id} serviceName={service.name} config={config} />`
       - Page heading: "{SERVICE NAME} — BOOKING CONFIG" in JetBrains Mono 28px bold

    **Session Package Management — `/admin/packages`:**

    4. Create `src/app/admin/packages/actions.ts`:
       - `getPackages()`: Select all sessionPackages joined with services (to show service name). Order by serviceName, sessionCount.
       - `createPackage(formData: FormData)`: Insert into sessionPackages. Fields: serviceId (select), name (text), sessionCount (number), discountPercent (number), isActive (boolean).
       - `updatePackage(id: string, formData: FormData)`: Update sessionPackages row.
       - `deletePackage(id: string)`: Delete from sessionPackages.
       - `getServicesWithConfig()`: Select services that have a serviceBookingConfig entry (only those are bookable).

    5. Create `src/components/admin/admin-package-manager.tsx` — client component:
       - Props: `packages: (PackageInfo & { serviceName: string })[], services: { id: string, name: string }[]`
       - Table layout using shadcn Table: columns Service, Package Name, Sessions, Discount %, Active, Actions (Edit/Delete)
       - "+ Add Package" button opens Dialog with: Service (Select dropdown), Package Name (text, e.g. "4-Session Pack"), Session Count (number), Discount Percent (number), Active (Switch)
       - Example packages to guide admin: "4-Session Pack" 15% off, "8-Session Pack" 25% off, "12-Session Pack" 30% off
       - Edit via clicking table row or edit icon
       - Delete with confirmation dialog

    6. Create `src/app/admin/packages/page.tsx` — server component:
       - Query packages and services with config
       - Pass to `<AdminPackageManager packages={packages} services={services} />`
       - Page heading: "SESSION PACKAGES" in JetBrains Mono 28px bold
  </action>
  <verify>
    <automated>cd /home/faxas/workspaces/projects/personal/glitch_studios && grep "export default" src/app/admin/services/\[id\]/booking-config/page.tsx && grep "upsertServiceBookingConfig" src/app/admin/services/\[id\]/booking-config/actions.ts && grep "export default" src/app/admin/packages/page.tsx && grep "createPackage" src/app/admin/packages/actions.ts && grep "AdminServiceBookingConfig" src/components/admin/admin-service-booking-config.tsx && grep "AdminPackageManager" src/components/admin/admin-package-manager.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/admin/services/[id]/booking-config/page.tsx contains `export const dynamic = "force-dynamic"`
    - src/app/admin/services/[id]/booking-config/actions.ts contains `upsertServiceBookingConfig` with "use server"
    - src/components/admin/admin-service-booking-config.tsx contains RadioGroup for deposit type, Switch for autoConfirm
    - src/app/admin/packages/page.tsx contains `export const dynamic = "force-dynamic"`
    - src/app/admin/packages/actions.ts contains `createPackage`, `updatePackage`, `deletePackage`
    - src/components/admin/admin-package-manager.tsx uses shadcn Table component
    - Service booking config form has all 8 fields from UI-SPEC: duration, deposit type, deposit value, auto-confirm, cancellation window, refund policy, max advance days, prep instructions
  </acceptance_criteria>
  <done>Admin can configure per-service booking settings (duration, deposit, cancellation policy, auto-confirm mode) and manage session packages with discount pricing for recurring bookings</done>
</task>

</tasks>

<verification>
- All 4 admin pages render with `force-dynamic`
- Room CRUD works via server actions
- Availability editor has 7-day accordion with time inputs and date override table
- Service booking config has all 8 fields per UI-SPEC
- Package CRUD works with service association
- All forms use flat monochrome styling per design language
</verification>

<success_criteria>
- Admin can create/edit/delete rooms from `/admin/rooms`
- Admin can set weekly hours per room with day-of-week accordion at `/admin/availability`
- Admin can add date overrides (holidays, closures) per room
- Admin can configure each service's booking parameters at `/admin/services/[id]/booking-config`
- Admin can manage session packages at `/admin/packages`
- All admin pages follow existing admin layout pattern (role-checked, force-dynamic)
</success_criteria>

<output>
After completion, create `.planning/phases/03-booking-system/03-02-SUMMARY.md`
</output>
