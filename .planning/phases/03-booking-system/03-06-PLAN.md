---
phase: 03-booking-system
plan: 06
type: execute
wave: 4
depends_on: ["03-04", "03-05"]
files_modified:
  - src/app/(public)/dashboard/bookings/page.tsx
  - src/app/(public)/dashboard/page.tsx
  - src/components/booking/client-booking-list.tsx
  - src/components/booking/client-booking-card.tsx
  - src/lib/email/booking-confirmation.tsx
  - src/lib/email/booking-reminder.tsx
  - src/app/api/cron/booking-reminders/route.ts
  - src/app/api/webhooks/stripe/route.ts
  - src/components/layout/tile-nav.tsx
  - src/components/services/service-grid.tsx
  - vercel.json
autonomous: true
requirements:
  - AUTH-04
  - MAIL-02
  - BOOK-02

must_haves:
  truths:
    - "Logged-in client can view upcoming and past bookings in their dashboard"
    - "Client can cancel or reschedule bookings within policy window from dashboard"
    - "Booking confirmation email is sent after payment with service details, room, date, time, .ics link"
    - "24hr reminder email and SMS are sent before each session via cron"
    - "/book nav tile appears in sidebar navigation"
    - "Service page CTAs link to /book?service=X"
  artifacts:
    - path: "src/app/(public)/dashboard/bookings/page.tsx"
      provides: "Client booking list dashboard page"
      contains: "export default"
    - path: "src/lib/email/booking-confirmation.tsx"
      provides: "React Email booking confirmation template"
      contains: "export function BookingConfirmationEmail"
    - path: "src/lib/email/booking-reminder.tsx"
      provides: "React Email 24hr reminder template"
      contains: "export function BookingReminderEmail"
    - path: "src/app/api/cron/booking-reminders/route.ts"
      provides: "Vercel Cron job for 24hr booking reminders"
      exports: ["GET"]
    - path: "src/components/layout/tile-nav.tsx"
      provides: "Updated nav with Book Session tile"
      contains: "Book Session"
  key_links:
    - from: "src/app/api/webhooks/stripe/route.ts"
      to: "src/lib/email/booking-confirmation.tsx"
      via: "sends confirmation email after booking deposit"
      pattern: "BookingConfirmationEmail"
    - from: "src/app/api/cron/booking-reminders/route.ts"
      to: "src/lib/email/booking-reminder.tsx"
      via: "sends reminder email 24hr before session"
      pattern: "BookingReminderEmail"
    - from: "src/app/api/cron/booking-reminders/route.ts"
      to: "src/lib/sms.ts"
      via: "sends SMS reminder"
      pattern: "import.*sendSms.*from.*sms"
    - from: "src/components/layout/tile-nav.tsx"
      to: "/book"
      via: "nav tile href"
      pattern: 'href.*"/book"'
---

<objective>
Complete the booking system with: client dashboard bookings tab (view/cancel/reschedule), confirmation and reminder email templates, 24hr reminder cron job with email+SMS, sidebar nav tile for booking, and service page "Book Now" CTAs. Also wire the confirmation email sending into the existing webhook.

Purpose: This final plan delivers the client-facing booking management (AUTH-04), email notifications (MAIL-02), and navigational integration (D-26, D-27) that make the booking system discoverable and complete.
Output: Dashboard bookings page, 2 email templates, cron job, nav tile, service CTAs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-booking-system/03-CONTEXT.md
@.planning/phases/03-booking-system/03-UI-SPEC.md
@.planning/phases/03-booking-system/03-01-SUMMARY.md
@.planning/phases/03-booking-system/03-04-SUMMARY.md
@.planning/phases/03-booking-system/03-05-SUMMARY.md
@src/lib/email/purchase-receipt.tsx
@src/components/layout/tile-nav.tsx
@src/app/(public)/dashboard/page.tsx
@src/app/(public)/dashboard/purchases/page.tsx
@src/lib/sms.ts
@src/lib/booking/ics.ts

<interfaces>
From src/lib/email/purchase-receipt.tsx (pattern to follow):
```typescript
import { Html, Head, Body, Container, Section, Heading, Text, Link, Hr } from "@react-email/components"
export function PurchaseReceiptEmail({ customerName, items, totalAmount, downloadUrl }: PurchaseReceiptProps) { ... }
```

From src/components/layout/tile-nav.tsx:
```typescript
const navItems = [
  { label: "Beats", href: "/beats", icon: Music, size: "wide" as const },
  { label: "Services", href: "/services", icon: Wrench, size: "wide" as const },
  // Add booking tile here
]
```

From src/app/(public)/dashboard/page.tsx:
```typescript
// Currently redirects to /dashboard/purchases
// Update to show tabs or keep redirect
```

From src/lib/sms.ts:
```typescript
export async function sendSms(to: string, body: string): Promise<void>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Client dashboard bookings tab with cancel/reschedule actions</name>
  <files>src/app/(public)/dashboard/bookings/page.tsx, src/app/(public)/dashboard/page.tsx, src/app/(public)/dashboard/layout.tsx, src/components/booking/client-booking-list.tsx, src/components/booking/client-booking-card.tsx</files>
  <read_first>
    - src/app/(public)/dashboard/page.tsx (current redirect to /purchases — update)
    - src/app/(public)/dashboard/purchases/page.tsx (existing dashboard page pattern to follow)
    - src/db/schema.ts (bookings table)
    - src/types/booking.ts (BookingWithRelations type)
    - src/lib/auth.ts (session auth pattern)
    - .planning/phases/03-booking-system/03-UI-SPEC.md (dashboard bookings tab layout, client booking card, copywriting, responsive behavior)
  </read_first>
  <action>
    1. Update `src/app/(public)/dashboard/page.tsx`:
       - Change from redirecting to /dashboard/purchases to rendering a tab view
       - Or: keep redirect but change to smart default (redirect to /dashboard/bookings if user has bookings, else /purchases)
       - Simpler approach: just redirect to /dashboard/purchases (keep existing behavior). The tab navigation handles switching.

    2. Create or update `src/app/(public)/dashboard/layout.tsx` (if it doesn't exist as a layout with tabs):
       - If a layout already exists, add tab navigation
       - Tabs at top: "PURCHASES" linking to /dashboard/purchases, "BOOKINGS" linking to /dashboard/bookings
       - Use shadcn Tabs with links (or simple nav links styled as tabs)
       - Tab styling: font-mono text-[13px] font-bold uppercase tracking-[0.05em]. Active: text-[#f5f5f0] border-b-2 border-[#f5f5f0]. Inactive: text-[#555555]
       - Auth check in layout: redirect to /login if no session

    3. Create `src/components/booking/client-booking-card.tsx` — client component:
       - Props: `booking: BookingWithRelations, onCancel: (id: string) => void, onReschedule: (id: string) => void`
       - Flat tile (bg-[#111111] border border-[#222222]) showing:
         - Service name: font-mono text-[15px] font-bold
         - Date + time: font-sans text-[15px] text-[#f5f5f0]
         - Room name: font-sans text-[13px] text-[#888888]
         - Status badge (same colors as admin: confirmed=#f5f5f0, pending=#888888, cancelled=#dc2626)
       - Action buttons (only for upcoming, non-cancelled bookings):
         - "Cancel" button: text-[#dc2626] border border-[#dc2626] (destructive style per UI-SPEC)
         - "Reschedule" button: text-[#f5f5f0] border border-[#333333]
       - Cancel click: open Dialog with cancellation policy info per UI-SPEC copywriting:
         - Within policy: "Cancel Booking: Your deposit of {amount} will be refunded within 5-10 business days. This cannot be undone."
         - Outside policy: "Cancel Booking: Your session is within the cancellation window. Your deposit of {amount} will be forfeited. Continue?"
         - Confirm button: "Cancel Booking" (destructive)
         - Calls POST /api/bookings/cancel with `{ bookingId, isAdmin: false }`
       - Reschedule click: open Dialog or navigate to `/book/reschedule?bookingId={id}` (or inline calendar in a Sheet)
         - Simpler approach: navigate to the booking page in reschedule mode. For MVP, use a Dialog stating the reschedule flow opens the calendar. Call POST /api/bookings/reschedule after new slot selected.
       - Mobile: card renders as full-width stacked tile (per UI-SPEC responsive)

    4. Create `src/components/booking/client-booking-list.tsx` — client component:
       - Props: `upcomingBookings: BookingWithRelations[], pastBookings: BookingWithRelations[]`
       - "UPCOMING BOOKINGS" section heading: font-mono text-[13px] font-bold uppercase tracking-[0.05em] text-[#888888]
       - Render ClientBookingCard for each upcoming booking
       - "PAST BOOKINGS" section heading (same style)
       - Render ClientBookingCard for each past booking (no action buttons — status display only)
       - Empty state per UI-SPEC copywriting:
         - Heading: "No Bookings Yet" — font-mono text-[15px] font-bold
         - Body: "Book your first studio session to get started." — font-sans text-[15px] text-[#888888]
         - CTA: "Browse Services" linking to /services — bg-[#f5f5f0] text-[#000000] font-mono font-bold
       - Use sonner for cancel/reschedule success/error toasts

    5. Create `src/app/(public)/dashboard/bookings/page.tsx` — server component:
       - `export const dynamic = "force-dynamic"`
       - Auth check: redirect to /login if no session
       - Query bookings where userId matches session user id OR guestEmail matches session email
       - Split into upcoming (date >= today, status in ["pending", "confirmed"]) and past (date < today or status in ["completed", "cancelled", "no_show"])
       - Join with services and rooms for names
       - Render `<ClientBookingList upcomingBookings={upcoming} pastBookings={past} />`
  </action>
  <verify>
    <automated>cd /home/faxas/workspaces/projects/personal/glitch_studios && grep "export default" src/app/\(public\)/dashboard/bookings/page.tsx && grep "export function ClientBookingList" src/components/booking/client-booking-list.tsx && grep "export function ClientBookingCard" src/components/booking/client-booking-card.tsx && grep "No Bookings Yet" src/components/booking/client-booking-list.tsx && grep "force-dynamic" src/app/\(public\)/dashboard/bookings/page.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(public)/dashboard/bookings/page.tsx has `export const dynamic = "force-dynamic"`, auth check, and queries bookings by userId
    - src/components/booking/client-booking-list.tsx separates "UPCOMING BOOKINGS" and "PAST BOOKINGS" sections
    - src/components/booking/client-booking-list.tsx has empty state with "No Bookings Yet" heading and "Browse Services" CTA
    - src/components/booking/client-booking-card.tsx shows Cancel and Reschedule buttons for upcoming non-cancelled bookings
    - src/components/booking/client-booking-card.tsx has cancellation Dialog with policy info text
    - Dashboard has tab navigation between PURCHASES and BOOKINGS
    - Status badges use correct hex colors: confirmed #f5f5f0, pending #888888, cancelled #dc2626
  </acceptance_criteria>
  <done>Client dashboard has a Bookings tab showing upcoming and past bookings. Clients can cancel (with policy-based refund info) and reschedule from their booking cards. Empty state guides to services page.</done>
</task>

<task type="auto">
  <name>Task 2: Email templates, webhook email sending, cron reminders, nav tile, service CTAs</name>
  <files>src/lib/email/booking-confirmation.tsx, src/lib/email/booking-reminder.tsx, src/app/api/webhooks/stripe/route.ts, src/app/api/cron/booking-reminders/route.ts, src/components/layout/tile-nav.tsx, src/components/services/service-grid.tsx, vercel.json</files>
  <read_first>
    - src/lib/email/purchase-receipt.tsx (React Email template pattern — follow structure, import style, dark theme styling)
    - src/app/api/webhooks/stripe/route.ts (current webhook — add email sending to booking_deposit branch)
    - src/lib/sms.ts (sendSms function signature)
    - src/lib/booking/ics.ts (generateBookingIcs function)
    - src/components/layout/tile-nav.tsx (navItems array — add booking tile)
    - src/components/services/service-grid.tsx (service cards — add Book Now CTA)
    - .planning/phases/03-booking-system/03-UI-SPEC.md (copywriting contract for email subjects, nav tile copy)
  </read_first>
  <action>
    1. Create `src/lib/email/booking-confirmation.tsx` — React Email template per D-23:
       - Follow the exact pattern of `purchase-receipt.tsx` (imports, Html/Head/Body/Container structure)
       - Props: `clientName: string, serviceName: string, roomName: string, date: string, time: string, depositAmount: string, totalPrice: string, address: string, prepInstructions: string | null, icsUrl: string`
       - Dark theme styling matching existing template: bg #000, text #f5f5f0, headings in monospace
       - Content per D-23:
         - "GLITCH STUDIOS" header (monospace, 24px bold)
         - "SESSION BOOKED" heading (monospace, 20px)
         - Key-value details: Service, Date, Time, Room, Deposit Paid, Balance Due
         - Prep instructions section (if provided): bordered box with prep text
         - "Add to Calendar" link pointing to icsUrl
         - Studio address
         - Social links footer
       - Subject line per copywriting: "Booking Confirmed: {service} at Glitch Studios"

    2. Create `src/lib/email/booking-reminder.tsx` — React Email template:
       - Same dark theme pattern
       - Props: `clientName: string, serviceName: string, roomName: string, date: string, time: string, address: string, prepInstructions: string | null`
       - Content:
         - "GLITCH STUDIOS" header
         - "REMINDER: SESSION TOMORROW" heading
         - Session details (same key-value layout as confirmation)
         - Prep instructions (if any)
         - Studio address
       - Subject line per copywriting: "Reminder: {service} session tomorrow at {time}"

    3. Update `src/app/api/webhooks/stripe/route.ts` — add email sending to the booking_deposit branch:
       - After updating booking status, send confirmation email:
         ```typescript
         // Query full booking details for email
         const bookingDetails = await db.select().from(bookings)
           .innerJoin(services, eq(bookings.serviceId, services.id))
           .innerJoin(rooms, eq(bookings.roomId, rooms.id))
           .where(eq(bookings.id, bookingId))
         // Compute icsUrl
         const icsUrl = `${process.env.NEXT_PUBLIC_SITE_URL}/api/bookings/ics?bookingId=${bookingId}`
         // Send email via Resend
         await resend.emails.send({
           from: "Glitch Studios <bookings@glitchstudios.com>",
           to: booking.guestEmail,
           subject: `Booking Confirmed: ${serviceName} at Glitch Studios`,
           react: BookingConfirmationEmail({ ... }),
         })
         ```
       - Import BookingConfirmationEmail from `@/lib/email/booking-confirmation`
       - Also send SMS confirmation if guestPhone present:
         ```typescript
         import { sendSms } from "@/lib/sms"
         await sendSms(booking.guestPhone, `Glitch Studios: Your ${serviceName} session is confirmed for ${date} at ${time}. See you there!`)
         ```
       - Send admin notification email: simple email to `process.env.ADMIN_EMAIL` notifying of new booking

    4. Create `src/app/api/cron/booking-reminders/route.ts`:
       - `export async function GET(request: Request)`
       - Auth: verify `request.headers.get("authorization") === Bearer ${process.env.CRON_SECRET}`
       - Query bookings where: date = tomorrow (using date-fns `addDays(new Date(), 1)` formatted as YYYY-MM-DD), status = "confirmed", reminderSent = false
       - For each booking:
         - Send reminder email via Resend with BookingReminderEmail
         - Send reminder SMS via sendSms: "Reminder: Your {service} session at Glitch Studios is tomorrow at {time}. {address}"
         - Update booking: set reminderSent = true
       - Return `{ sent: count }`
       - Add `export const dynamic = "force-dynamic"`

    5. Create or update `vercel.json` to add cron:
       ```json
       {
         "crons": [
           { "path": "/api/cron/booking-reminders", "schedule": "0 * * * *" }
         ]
       }
       ```
       If vercel.json already exists, merge the crons array.

    6. Update `src/components/layout/tile-nav.tsx`:
       - Add booking nav tile to navItems array (per D-27):
         ```typescript
         { label: "Book Session", href: "/book", icon: Calendar, size: "wide" as const },
         ```
       - Import `Calendar` from lucide-react
       - Place it after "Services" tile and before any other items (logical position: Beats, Services, Book Session, ...)

    7. Update `src/components/services/service-grid.tsx` (or equivalent component rendering service cards per D-26):
       - Add "Book Now" CTA button/link on each service card
       - Link to `/book?service={service.slug}` for pre-selection
       - Button style: bg-[#f5f5f0] text-[#000000] font-mono font-bold uppercase text-[13px] px-4 py-2
       - Only show "Book Now" if the service has a serviceBookingConfig entry (query in the server component that feeds this)
  </action>
  <verify>
    <automated>cd /home/faxas/workspaces/projects/personal/glitch_studios && grep "export function BookingConfirmationEmail" src/lib/email/booking-confirmation.tsx && grep "export function BookingReminderEmail" src/lib/email/booking-reminder.tsx && grep "BookingConfirmationEmail" src/app/api/webhooks/stripe/route.ts && grep "sendSms" src/app/api/webhooks/stripe/route.ts && grep "reminderSent" src/app/api/cron/booking-reminders/route.ts && grep "Book Session" src/components/layout/tile-nav.tsx && grep "booking-reminders" vercel.json && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/email/booking-confirmation.tsx follows React Email pattern with Html/Head/Body/Container from @react-email/components
    - src/lib/email/booking-confirmation.tsx has dark theme (bg #000, text #f5f5f0) and includes Add to Calendar link
    - src/lib/email/booking-reminder.tsx has "REMINDER: SESSION TOMORROW" heading
    - src/app/api/webhooks/stripe/route.ts imports BookingConfirmationEmail and sendSms, sends both after booking confirmation
    - src/app/api/cron/booking-reminders/route.ts checks CRON_SECRET auth header
    - src/app/api/cron/booking-reminders/route.ts queries bookings for tomorrow with reminderSent=false
    - src/app/api/cron/booking-reminders/route.ts sets reminderSent=true after sending
    - vercel.json contains crons array with /api/cron/booking-reminders at "0 * * * *"
    - src/components/layout/tile-nav.tsx contains { label: "Book Session", href: "/book", icon: Calendar }
    - Service grid/cards have "Book Now" link pointing to /book?service={slug}
  </acceptance_criteria>
  <done>Confirmation email sent after booking payment with full details and .ics link. 24hr reminder cron sends email + SMS. Sidebar nav has "Book Session" tile. Service page cards have "Book Now" CTAs linking to /book?service=X. The booking system is fully integrated into the site navigation and notification pipeline.</done>
</task>

</tasks>

<verification>
- Client dashboard has working Bookings tab with upcoming/past sections
- Cancel dialog shows policy-appropriate message
- Confirmation email template renders with dark theme and all booking details
- Webhook sends confirmation email + SMS after booking deposit
- Cron endpoint sends 24hr reminders and marks bookings as reminded
- Nav tile "Book Session" appears in sidebar
- Service page CTAs link to /book?service={slug}
- vercel.json has cron configured
</verification>

<success_criteria>
- Logged-in client sees Bookings tab in dashboard with upcoming and past bookings
- Client can cancel within policy (sees refund info) and reschedule from dashboard
- Booking confirmation email is branded with all session details and .ics download
- 24hr reminder email + SMS sent via Vercel Cron
- "Book Session" tile in sidebar navigation
- "Book Now" buttons on service pages pre-select the service in the booking flow
</success_criteria>

<output>
After completion, create `.planning/phases/03-booking-system/03-06-SUMMARY.md`
</output>
