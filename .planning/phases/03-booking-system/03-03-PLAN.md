---
phase: 03-booking-system
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/app/(public)/book/page.tsx
  - src/app/api/bookings/slots/route.ts
  - src/components/booking/booking-flow-stepper.tsx
  - src/components/booking/service-selector.tsx
  - src/components/booking/booking-calendar.tsx
  - src/components/booking/calendar-day-tile.tsx
  - src/components/booking/time-slot-list.tsx
  - src/components/booking/time-slot-tile.tsx
  - src/components/booking/booking-summary.tsx
  - src/components/booking/booking-flow.tsx
autonomous: true
requirements:
  - BOOK-02
  - BOOK-03

must_haves:
  truths:
    - "User can select a service type from a tile grid on the /book page"
    - "User can navigate a month calendar and select an available date"
    - "User can see available time slots for the selected date and pick one"
    - "Unavailable dates are greyed out, selected items are inverted (white bg/black text)"
    - "Calendar tiles have glitch hover effect on available days"
    - "Step indicator shows 5-step progress"
    - "Booking summary updates as user makes selections"
  artifacts:
    - path: "src/app/(public)/book/page.tsx"
      provides: "Public booking page"
      contains: "export default"
    - path: "src/components/booking/booking-flow.tsx"
      provides: "Client-side booking flow state machine"
      contains: "export function BookingFlow"
    - path: "src/components/booking/booking-calendar.tsx"
      provides: "Month grid calendar with tile-styled days"
      contains: "export function BookingCalendar"
    - path: "src/components/booking/service-selector.tsx"
      provides: "Tile grid of bookable services"
      contains: "export function ServiceSelector"
    - path: "src/app/api/bookings/slots/route.ts"
      provides: "GET endpoint returning available slots for service+date"
      contains: "export async function GET"
  key_links:
    - from: "src/components/booking/booking-flow.tsx"
      to: "/api/bookings/slots"
      via: "fetch when date is selected"
      pattern: "fetch.*api/bookings/slots"
    - from: "src/app/api/bookings/slots/route.ts"
      to: "src/lib/booking/slots.ts"
      via: "calls getAvailableSlots"
      pattern: "import.*getAvailableSlots.*from.*booking/slots"
    - from: "src/app/(public)/book/page.tsx"
      to: "src/components/booking/booking-flow.tsx"
      via: "renders BookingFlow with services data"
      pattern: "BookingFlow"
---

<objective>
Build the public booking page at `/book` with the first 3 steps of the booking flow: service selection (tile grid), date selection (month calendar with Cyberpunk Metro tiles), and time slot selection. Plus the step indicator, booking summary sidebar, and the slots API endpoint.

Purpose: The core booking UI that clients interact with. Per D-01, this is service-first flow: pick service, then date, then time. The calendar and slot tiles use the same Cyberpunk Metro visual language as the rest of the site (per D-03).
Output: Working /book page with steps 1-3 interactive, slots API, summary sidebar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-booking-system/03-CONTEXT.md
@.planning/phases/03-booking-system/03-UI-SPEC.md
@.planning/phases/03-booking-system/03-01-SUMMARY.md
@src/db/schema.ts
@src/types/booking.ts
@src/components/tiles/tile.tsx
@src/lib/booking/slots.ts
@src/lib/booking/availability.ts

<interfaces>
From src/components/tiles/tile.tsx — base Tile component for calendar tiles:
```typescript
// Tile renders as Link (href), button (onClick), or div (widget)
// Props include: size, layout, className, children, onClick, href
```

From src/types/booking.ts:
```typescript
export interface TimeSlot { roomId: string; roomName: string; startTime: string; endTime: string }
export interface ServiceBookingInfo { serviceId: string; serviceName: string; durationMinutes: number; depositType: DepositType; depositValue: number; ... }
```

From src/lib/booking/slots.ts:
```typescript
export function getAvailableSlots(serviceId: string, date: string): Promise<TimeSlot[]>
```

From src/lib/booking/availability.ts:
```typescript
export function getAvailableDates(serviceId: string, month: Date): Promise<Map<string, boolean>>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Slots API endpoint, booking flow orchestrator, service selector, and step indicator</name>
  <files>src/app/api/bookings/slots/route.ts, src/components/booking/booking-flow.tsx, src/components/booking/service-selector.tsx, src/components/booking/booking-flow-stepper.tsx, src/components/booking/booking-summary.tsx, src/app/(public)/book/page.tsx</files>
  <read_first>
    - src/components/tiles/tile.tsx (base Tile visual language — reuse for service tiles)
    - src/types/booking.ts (ServiceBookingInfo, TimeSlot types)
    - src/lib/booking/slots.ts (getAvailableSlots function signature)
    - src/db/schema.ts (services, serviceBookingConfig tables for data loading)
    - src/app/(public)/layout.tsx (public layout structure)
    - .planning/phases/03-booking-system/03-UI-SPEC.md (booking flow steps, step indicator, service selector, page layout, copywriting)
  </read_first>
  <action>
    1. Create `src/app/api/bookings/slots/route.ts` — GET endpoint:
       - Query params: `serviceId` (required), `date` (required, "YYYY-MM-DD")
       - Validate both params present, return 400 if missing
       - Call `getAvailableSlots(serviceId, date)` from `@/lib/booking/slots`
       - Return `NextResponse.json({ slots: TimeSlot[] })`
       - Add `export const dynamic = "force-dynamic"`

    2. Create `src/components/booking/booking-flow-stepper.tsx` — client component:
       - Props: `currentStep: number` (1-5), `completedSteps: number[]`
       - 5 steps: ["SERVICE", "DATE", "TIME", "DETAILS", "PAYMENT"]
       - Horizontal bar at top per UI-SPEC Step Indicator section:
         - Completed: `#f5f5f0` text
         - Current: `#f5f5f0` text + 2px bottom border `#f5f5f0`
         - Future: `#555555` text
         - All caps, font-mono text-[13px] font-bold tracking-[0.05em]
       - Flex row with equal spacing, gap-4

    3. Create `src/components/booking/service-selector.tsx` — client component:
       - Props: `services: ServiceBookingInfo[], selectedId: string | null, onSelect: (id: string) => void`
       - Tile grid of services. Each tile shows: service icon (use Lucide icons mapped by service type: Headphones for studio_session, Sliders for mixing, Volume2 for mastering, Video for video_production, Waves for sfx, Palette for graphic_design), service name, duration (e.g. "2 hours"), starting price from priceLabel
       - Selected tile: inverted — bg-[#f5f5f0] text-[#000000] (per D-03, UI-SPEC)
       - Unselected tile: bg-[#111111] text-[#f5f5f0] border border-[#222222]
       - Glitch hover on unselected tiles (add `group` class and glitch overlay on hover matching existing tile hover pattern)
       - Grid: 2 columns on mobile, 3 columns on desktop
       - Heading above: "SELECT SERVICE" per copywriting contract — font-mono text-[13px] font-bold tracking-[0.05em] uppercase text-[#f5f5f0]

    4. Create `src/components/booking/booking-summary.tsx` — client component:
       - Props: `selectedService: ServiceBookingInfo | null, selectedDate: string | null, selectedTime: TimeSlot | null, depositAmount: number | null`
       - Flat tile (bg-[#111111] border border-[#222222]) showing:
         - Key-value pairs per UI-SPEC: service name, date, time, room, price, deposit
         - Keys: font-mono text-[13px] font-bold uppercase tracking-[0.05em] text-[#888888]
         - Values: font-sans text-[15px] text-[#f5f5f0]
         - Price display: font-mono text-[28px] font-bold
         - Empty state: "Select a service to begin" message
       - Desktop: sticky sidebar (40% width per UI-SPEC layout)
       - Mobile: collapsible accordion at top per UI-SPEC responsive spec

    5. Create `src/components/booking/booking-flow.tsx` — client component (orchestrator):
       - Props: `services: ServiceBookingInfo[], initialServiceSlug?: string` (from query param `?service=mixing`)
       - State: `step` (1-5), `selectedServiceId`, `selectedDate`, `selectedSlot: TimeSlot | null`, `availableDates: Map<string, boolean>`, `availableSlots: TimeSlot[]`, `currentMonth: Date`
       - Step 1: Render `<ServiceSelector>`. On select, set selectedServiceId, fetch available dates for current month, advance to step 2.
       - Step 2: Render `<BookingCalendar>` (created in Task 2). On date select, fetch slots from `/api/bookings/slots?serviceId=X&date=Y`, advance to step 3.
       - Step 3: Render `<TimeSlotList>` (created in Task 2). On slot select, advance to step 4.
       - Steps 4-5: placeholder div "Step {N} coming in Plan 04" for now.
       - Step transition animation: Framer Motion AnimatePresence with opacity + x slide (300ms, per animation contract). Import from `motion/react`. Reduced motion: opacity only, 200ms.
       - Layout: Desktop two-column (60/40 per UI-SPEC). Mobile single-column with summary accordion above step content.
       - Pre-select service from `initialServiceSlug` if provided (find matching service by slug).
       - Deposit calculation: when service + slot selected, call `calculateDeposit` (import from `@/lib/booking/deposit` — this is a pure function so safe for client if we extract it, OR fetch via API).
       - For simplicity: compute deposit client-side since it's a pure math function. Pass depositType, depositValue, and compute: if percentage → priceLabel-derived * value/100, if flat → value directly.

    6. Create `src/app/(public)/book/page.tsx` — server component:
       - `export const dynamic = "force-dynamic"`
       - Query services that have a serviceBookingConfig entry (inner join services with serviceBookingConfig). Select: serviceId, serviceName, serviceSlug, durationMinutes, depositType, depositValue, autoConfirm, cancellationWindowHours, refundPolicy, maxAdvanceBookingDays, prepInstructions, priceLabel.
       - Read `searchParams.service` for pre-selection (from services page CTA per D-26)
       - Render `<BookingFlow services={services} initialServiceSlug={searchParams.service} />`
       - Page title: "BOOK SESSION" in JetBrains Mono 40px bold (display size per typography spec)
  </action>
  <verify>
    <automated>cd /home/faxas/workspaces/projects/personal/glitch_studios && grep "export async function GET" src/app/api/bookings/slots/route.ts && grep "getAvailableSlots" src/app/api/bookings/slots/route.ts && grep "export function BookingFlow" src/components/booking/booking-flow.tsx && grep "export function ServiceSelector" src/components/booking/service-selector.tsx && grep "export function BookingFlowStepper" src/components/booking/booking-flow-stepper.tsx && grep "export function BookingSummary" src/components/booking/booking-summary.tsx && grep "export default" src/app/\(public\)/book/page.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/api/bookings/slots/route.ts exports GET function, imports getAvailableSlots from @/lib/booking/slots, validates serviceId and date params
    - src/components/booking/booking-flow-stepper.tsx renders 5 steps: SERVICE, DATE, TIME, DETAILS, PAYMENT with current/completed/future styling
    - src/components/booking/service-selector.tsx renders tile grid with inverted selection state (bg-[#f5f5f0] text-[#000000])
    - src/components/booking/booking-summary.tsx shows key-value pairs with JetBrains Mono labels
    - src/components/booking/booking-flow.tsx manages step state (1-5), renders ServiceSelector at step 1, fetches slots at step 2-3
    - src/components/booking/booking-flow.tsx imports from `motion/react` for step transitions
    - src/app/(public)/book/page.tsx has `export const dynamic = "force-dynamic"` and reads searchParams.service
    - Booking flow reads `?service=` query param for pre-selection per D-26
  </acceptance_criteria>
  <done>Slots API returns available time slots. Booking page renders at /book with step indicator, service tile grid selection, booking summary sidebar, and flow orchestrator managing step state with Framer Motion transitions.</done>
</task>

<task type="auto">
  <name>Task 2: Calendar month grid and time slot list with Cyberpunk Metro tile styling</name>
  <files>src/components/booking/booking-calendar.tsx, src/components/booking/calendar-day-tile.tsx, src/components/booking/time-slot-list.tsx, src/components/booking/time-slot-tile.tsx</files>
  <read_first>
    - src/components/tiles/tile.tsx (base Tile component — glitch hover pattern to replicate)
    - src/styles/globals.css (glitch-hover keyframes, scan-line CSS)
    - .planning/phases/03-booking-system/03-UI-SPEC.md (calendar color tokens, animation contract, responsive breakpoints, interaction patterns for Step 2 and Step 3)
    - src/components/booking/booking-flow.tsx (how BookingCalendar and TimeSlotList will be consumed)
  </read_first>
  <action>
    1. Create `src/components/booking/calendar-day-tile.tsx` — client component:
       - Props: `date: string, dayNumber: string, isCurrentMonth: boolean, isAvailable: boolean, isSelected: boolean, isToday: boolean, isPast: boolean, onClick: () => void`
       - Renders a button element styled as a flat tile per UI-SPEC Booking-Specific Color Tokens:
         - Available (not selected): bg-[#111111], text-[#f5f5f0], border border-[#222222], hover:bg-[#1a1a1a] hover:border-[#444444], cursor-pointer
         - Unavailable/Past: bg-[#0a0a0a], text-[#555555], border border-[#222222], cursor-default, no hover effect, no onClick
         - Selected: bg-[#f5f5f0], text-[#000000], border border-[#f5f5f0]
         - Today (not selected): add `border-b-2 border-b-[#f5f5f0]` (2px bottom border indicator)
         - Not current month: text-[#333333] (dimmed)
       - Day number: font-mono text-[15px] font-bold centered
       - Minimum size: 36px on mobile, 44px tablet, 48px desktop (per responsive breakpoints)
       - Glitch hover: On available (not selected) tiles, add the glitch-hover effect. Use the same pattern as the base Tile component: a pseudo-element overlay with the `glitch-hover` keyframes from globals.css on hover. Include `@media (prefers-reduced-motion: reduce)` fallback (static bg change only).
       - Transition for selection: `transition-colors duration-150` (150ms per animation contract)
       - `aria-label` with full date (e.g., "April 15, 2026")
       - `aria-disabled="true"` on unavailable/past days

    2. Create `src/components/booking/booking-calendar.tsx` — client component:
       - Props: `currentMonth: Date, availableDates: Map<string, boolean>, selectedDate: string | null, onSelectDate: (date: string) => void, onMonthChange: (month: Date) => void`
       - Calendar grid generation using date-fns: `startOfWeek(startOfMonth(currentMonth))` through `endOfWeek(endOfMonth(currentMonth))` via `eachDayOfInterval`. Use `format(day, "yyyy-MM-dd")` for keys, `format(day, "d")` for display, `isSameMonth(day, currentMonth)` for current month check, `isBefore(day, startOfDay(new Date()))` for past check.
       - Header: month/year display centered — `format(currentMonth, "MMMM yyyy")` — font-mono text-[28px] font-bold (per typography spec)
       - Navigation: left arrow and right arrow buttons flanking the month heading. Style as small tiles (bg-[#111111] border border-[#222222] hover:bg-[#1a1a1a]). `aria-label="Previous month"` and `aria-label="Next month"` per UI-SPEC. Use Lucide `ChevronLeft` and `ChevronRight` icons. Use `subMonths` and `addMonths` from date-fns.
       - Weekday headers row: SUN, MON, TUE, WED, THU, FRI, SAT — font-mono text-[13px] font-bold uppercase tracking-[0.05em] text-[#888888]
       - Day grid: 7 columns, gap-[4px] (--gap-tile). Render `CalendarDayTile` for each day.
       - Month change animation: Framer Motion AnimatePresence with opacity + x direction (250ms per animation contract). Slide left when going forward, right when going back. Import from `motion/react`. Reduced motion: opacity only, 200ms.
       - Heading above grid: "PICK A DATE" per copywriting contract — same step heading style

    3. Create `src/components/booking/time-slot-tile.tsx` — client component:
       - Props: `slot: TimeSlot, isSelected: boolean, onClick: () => void`
       - Renders a button element styled as a flat tile:
         - Default: bg-[#111111], text-[#f5f5f0], border border-[#222222], hover:bg-[#1a1a1a] hover:border-[#444444]
         - Selected: bg-[#f5f5f0], text-[#000000]
       - Content: time range (e.g., "2:00 PM - 4:00 PM") in font-mono text-[15px]. Format from 24h "HH:mm" to 12h display.
       - Room name as sublabel: font-sans text-[13px] text-[#888888] (only shown if multiple rooms have slots)
       - Height: min-h-[44px] per UI-SPEC touch accessibility spec
       - Full width
       - Glitch hover on unselected tiles (same pattern as CalendarDayTile)
       - Transition: `transition-colors duration-150`

    4. Create `src/components/booking/time-slot-list.tsx` — client component:
       - Props: `slots: TimeSlot[], selectedSlot: TimeSlot | null, onSelectSlot: (slot: TimeSlot) => void, isLoading: boolean`
       - Heading: "CHOOSE A TIME" per copywriting contract
       - If isLoading: render 4 Skeleton tiles (44px tall, full width, pulsing) per UI-SPEC skeleton spec
       - If slots.length === 0 and not loading: empty state per copywriting contract:
         - Heading: "No Available Times" — font-mono text-[15px] font-bold
         - Body: "All time slots for this date are booked. Try another date or check back later." — font-sans text-[15px] text-[#888888]
       - If slots exist: vertical list of TimeSlotTile components, gap-[4px]
       - Show room name on slot tiles only if `new Set(slots.map(s => s.roomId)).size > 1`
  </action>
  <verify>
    <automated>cd /home/faxas/workspaces/projects/personal/glitch_studios && grep "export function BookingCalendar" src/components/booking/booking-calendar.tsx && grep "export function CalendarDayTile" src/components/booking/calendar-day-tile.tsx && grep "export function TimeSlotList" src/components/booking/time-slot-list.tsx && grep "export function TimeSlotTile" src/components/booking/time-slot-tile.tsx && grep "eachDayOfInterval" src/components/booking/booking-calendar.tsx && grep "aria-label" src/components/booking/booking-calendar.tsx && grep "aria-label" src/components/booking/calendar-day-tile.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/booking/booking-calendar.tsx imports startOfMonth, endOfMonth, startOfWeek, endOfWeek, eachDayOfInterval, isSameMonth from date-fns
    - src/components/booking/booking-calendar.tsx contains `aria-label="Previous month"` and `aria-label="Next month"`
    - src/components/booking/booking-calendar.tsx renders 7-column grid with gap-[4px]
    - src/components/booking/calendar-day-tile.tsx has 5 visual states: available, unavailable, selected (bg-[#f5f5f0]), today (border-b-2), not-current-month
    - src/components/booking/calendar-day-tile.tsx contains `prefers-reduced-motion` media query or check
    - src/components/booking/time-slot-tile.tsx has min-h-[44px] for touch accessibility
    - src/components/booking/time-slot-list.tsx renders Skeleton loading state and "No Available Times" empty state
    - All components import from `motion/react` (not `framer-motion`)
  </acceptance_criteria>
  <done>Calendar renders as a month grid of Cyberpunk Metro day tiles with glitch hover, proper color states (available/unavailable/selected/today), accessible navigation. Time slots render as tappable tile list with loading and empty states. All animations respect prefers-reduced-motion.</done>
</task>

</tasks>

<verification>
- `/book` page renders with step indicator and service tile grid
- Selecting a service shows the calendar for current month
- Calendar days have correct color states per UI-SPEC tokens
- Navigating months triggers Framer Motion animation
- Selecting a date fetches slots from `/api/bookings/slots`
- Time slots render with room sublabel when multiple rooms
- Empty slot state shows "No Available Times" message
- Booking summary sidebar updates as selections are made
- Mobile layout: single column with summary accordion
</verification>

<success_criteria>
- Client can visit `/book`, see service tiles, select one, navigate the calendar, see available dates highlighted, pick a date, and choose a time slot from the list
- Pre-selection via `?service=mixing` query param works
- All visual styling matches UI-SPEC color tokens exactly
- Calendar is keyboard accessible and has ARIA labels
- Step indicator correctly shows current/completed/future states
</success_criteria>

<output>
After completion, create `.planning/phases/03-booking-system/03-03-SUMMARY.md`
</output>
