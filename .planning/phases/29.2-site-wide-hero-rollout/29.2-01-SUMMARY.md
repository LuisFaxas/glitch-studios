---
phase: 29.2-site-wide-hero-rollout
plan: 01
subsystem: ui
tags: [tech-hero, size-variants, wcag, playwright]

requires:
  - phase: 29.1-master-leaderboard-polish
    provides: TechHero component (D-08) with eyebrow/title/subhead/CTA + tone variants
provides:
  - TechHero now accepts size?: "compact" | "default" | "tall" (200/280/400px)
  - Default value preserves existing surfaces (rankings hub, rankings/slug, categories/slug)
  - WCAG 2.5.5 touch-target compliance on CTA (min-h-[44px] inline-flex items-center)
affects: [29.2-02-about-tall-hero, 29.2-05-reviews-hero, 29.2-06-categories-hub-hero, 29.2-08-compare-hero, 29.2-09-benchmarks-hero, 29.2-10-blog-hero]

tech-stack:
  added: []
  patterns:
    - HEIGHT_MAP constant pattern (parallel to GRADIENTS) keeps variant heights typed and centralized

key-files:
  created:
    - tests/29.2-01-tech-hero-size-variants.spec.ts
  modified:
    - src/components/tech/tech-hero.tsx

key-decisions:
  - "Use Tailwind arbitrary heights (h-[200px]/[280px]/[400px]) inside a typed HEIGHT_MAP rather than separate components — single source of truth, no duplication of gradients/markup"
  - "Default size = 'default' so 3 existing callers (rankings hub, rankings/slug, categories/slug) remain unchanged with no edit required"
  - "CTA uses inline-flex + min-h-[44px] (not a fixed height) so existing px-6/py-2 padding still drives the visual baseline"

patterns-established:
  - "Variant maps: typed Record<NonNullable<Props['variantKey']>, string> with destructured default value in props signature"

requirements-completed: [HERO-SIZE-VARIANT]

duration: ~6min
completed: 2026-04-27
---

# Phase 29.2-01: TechHero Size Variants Summary

**TechHero now ships compact/default/tall heights via a single size prop — default behavior preserved, ready for the tall variant on /tech/about and the standard variant on remaining /tech/* surfaces.**

## Performance

- **Duration:** ~6 min
- **Started:** 2026-04-27T17:30Z
- **Completed:** 2026-04-27T17:36Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Added `size?: "compact" | "default" | "tall"` to `TechHeroProps`
- HEIGHT_MAP constant (200/280/400px) with TS narrowing
- WCAG 2.5.5 touch-target on CTA (min-h-[44px] inline-flex items-center)
- Playwright spec: 8/8 pass (4 tests × desktop+mobile) — default-size regression on /tech/rankings + /tech/rankings/laptops, data-tone preserved, no auto-running animation on h1

## Task Commits

1. **Task 1+2: size prop + HEIGHT_MAP + WCAG CTA + Playwright spec** — `dc8e863` (feat)

## Files Created/Modified
- `src/components/tech/tech-hero.tsx` — added size prop, HEIGHT_MAP, default value, WCAG CTA classes
- `tests/29.2-01-tech-hero-size-variants.spec.ts` — default-size regression + data-tone + no-animation checks

## Decisions Made
- Bundled both tasks in one commit since the spec is the verification of the prop addition (atomic unit).

## Deviations from Plan
None — plan executed exactly as written.

## Issues Encountered
- PM2 `glitch_studios` (dev) was stopped due to a prior crash; `glitch_studios_prod` was holding port 3004. Stopped prod and restarted dev to unblock Playwright.
- Pre-existing TS error in `tests/forensics-overlay-leak.spec.ts` (offsetParent) and 53 pre-existing lint errors are unrelated to this plan; verified by stashing changes and re-running `tsc`/`lint`.

## Next Phase Readiness
- TechHero size="tall" ready for Plan 02 (/tech/about hero mount)
- All other plans (05/06/08/09/10) can use default size with the new WCAG-compliant CTA

---
*Phase: 29.2-site-wide-hero-rollout*
*Completed: 2026-04-27*
