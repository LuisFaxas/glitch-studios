---
phase: 29.2-site-wide-hero-rollout
plan: 08
type: execute
wave: 8
depends_on: ["29.2-07"]
files_modified:
  - src/app/(tech)/tech/compare/page.tsx
  - tests/29.2-08-compare-hero.spec.ts
autonomous: true
requirements: [COMPARE-HERO]
must_haves:
  truths:
    - "/tech/compare renders TechHero with tone='cyan', size='default', eyebrow='COMPARE'"
    - "h1 reads 'Compare Devices' wrapped in GlitchHeading — no bare h1 outside TechHero"
    - "CTA 'Pick devices' links to '#compare-picker'"
    - "Subhead reads 'Put any two devices side-by-side. Benchmark for benchmark, score for score.'"
    - "ComparePageClient still renders below the hero — compare functionality unchanged"
    - "A #compare-picker anchor exists so the CTA scroll target resolves"
    - "pnpm tsc --noEmit and pnpm lint pass clean"
  artifacts:
    - path: "src/app/(tech)/tech/compare/page.tsx"
      provides: "Compare page with TechHero replacing the bare h1"
      contains: "<TechHero"
    - path: "tests/29.2-08-compare-hero.spec.ts"
      provides: "Playwright spec confirming hero visible with correct copy, CTA href, tone"
  key_links:
    - from: "src/app/(tech)/tech/compare/page.tsx"
      to: "src/components/tech/tech-hero.tsx"
      via: "import { TechHero } + render"
      pattern: "<TechHero"
---

<objective>
Mount TechHero on /tech/compare. Replace the bare h1 "COMPARE" + subhead with TechHero
(tone="cyan", size="default", eyebrow="COMPARE", CTA "Pick devices" → "#compare-picker").
Add a #compare-picker anchor so the CTA scroll target resolves to the ComparePageClient area.
ComparePageClient remains intact.

Output: /tech/compare opens with a cyan TechHero. Old bare h1 gone. CTA scrolls to the picker.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29.2-site-wide-hero-rollout/29.2-CONTEXT.md
@.planning/phases/29.2-site-wide-hero-rollout/29.2-UI-SPEC.md

<interfaces>
<!-- Current /tech/compare/page.tsx structure: -->
- <main className="min-h-screen bg-black">
  - First <section>: h1 "COMPARE" + GlitchHeading + p subhead
  - <ComparePageClient> component (inline, not in a section)
  - <TechNewsletter>

<!-- TechHeroProps for this surface: -->
```tsx
eyebrow="COMPARE"
title="Compare Devices"
subhead="Put any two devices side-by-side. Benchmark for benchmark, score for score."
ctaLabel="Pick devices"
ctaHref="#compare-picker"
tone="cyan"
// size omitted → defaults to "default"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace bare h1 section with TechHero on /tech/compare</name>
  <files>src/app/(tech)/tech/compare/page.tsx</files>
  <read_first>
    - src/app/(tech)/tech/compare/page.tsx (read the FULL current file)
    - src/components/tech/tech-hero.tsx (confirm import path)
    - .planning/phases/29.2-site-wide-hero-rollout/29.2-UI-SPEC.md (Hero Surface Map row for /tech/compare + Copywriting Contract)
  </read_first>
  <action>
    Edit `src/app/(tech)/tech/compare/page.tsx`:

    1. Add import: `import { TechHero } from "@/components/tech/tech-hero"`

    2. REPLACE the entire first `<section className="mx-auto max-w-7xl px-4 pt-16 pb-4 md:px-6 md:pt-24">` block (h1 + GlitchHeading + p subhead) with TechHero as first child of `<main>`:

       ```tsx
       <TechHero
         eyebrow="COMPARE"
         title="Compare Devices"
         subhead="Put any two devices side-by-side. Benchmark for benchmark, score for score."
         ctaLabel="Pick devices"
         ctaHref="#compare-picker"
         tone="cyan"
       />
       ```

    3. Add a `#compare-picker` anchor span immediately BEFORE `<ComparePageClient>` so the CTA scroll target resolves:

       ```tsx
       <span id="compare-picker" />
       <ComparePageClient
         initialProducts={resolvedProducts}
         pickerOptions={pickerOptions}
         benchmarkRuns={benchmarkRuns}
         maxSlots={MAX_SLOTS}
       />
       ```

    4. Remove `GlitchHeading` import if no longer used in this file.

    5. Keep TechNewsletter and all ComparePageClient props unchanged.
  </action>
  <verify>
    <automated>
      grep -q 'TechHero' src/app/\(tech\)/tech/compare/page.tsx &&
      grep -q 'eyebrow="COMPARE"' src/app/\(tech\)/tech/compare/page.tsx &&
      grep -q 'Compare Devices' src/app/\(tech\)/tech/compare/page.tsx &&
      grep -q 'ctaHref="#compare-picker"' src/app/\(tech\)/tech/compare/page.tsx &&
      grep -q 'id="compare-picker"' src/app/\(tech\)/tech/compare/page.tsx &&
      grep -q 'Pick devices' src/app/\(tech\)/tech/compare/page.tsx &&
      grep -q 'tone="cyan"' src/app/\(tech\)/tech/compare/page.tsx &&
      pnpm tsc --noEmit &&
      pnpm lint
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'TechHero' "src/app/(tech)/tech/compare/page.tsx"` exits 0
    - `grep -q '"COMPARE"' "src/app/(tech)/tech/compare/page.tsx"` exits 0 (eyebrow)
    - `grep -q '"Compare Devices"' "src/app/(tech)/tech/compare/page.tsx"` exits 0 (h1 title)
    - `grep -q 'tone="cyan"' "src/app/(tech)/tech/compare/page.tsx"` exits 0
    - `grep -q 'ctaHref="#compare-picker"' "src/app/(tech)/tech/compare/page.tsx"` exits 0
    - `grep -q 'id="compare-picker"' "src/app/(tech)/tech/compare/page.tsx"` exits 0
    - `grep -q '"Pick devices"' "src/app/(tech)/tech/compare/page.tsx"` exits 0
    - `grep -q 'Put any two devices' "src/app/(tech)/tech/compare/page.tsx"` exits 0
    - `grep -q 'ComparePageClient' "src/app/(tech)/tech/compare/page.tsx"` exits 0 (unchanged)
    - `pnpm tsc --noEmit` exits 0
    - `pnpm lint` exits 0
  </acceptance_criteria>
  <done>/tech/compare opens with cyan TechHero "Compare Devices". Old bare h1 removed. CTA "Pick devices" scrolls to #compare-picker. ComparePageClient intact.</done>
</task>

<task type="auto">
  <name>Task 2: Playwright spec — /tech/compare hero with correct copy and CTA</name>
  <files>tests/29.2-08-compare-hero.spec.ts</files>
  <read_first>
    - tests/29.2-05-reviews-hero.spec.ts (pattern to follow)
  </read_first>
  <action>
    Create `tests/29.2-08-compare-hero.spec.ts`:

    ```ts
    import { test, expect } from "@playwright/test"

    test.describe("29.2-08 — /tech/compare TechHero", () => {
      test.setTimeout(30_000)

      test("TechHero renders with cyan tone and COMPARE eyebrow", async ({ page }) => {
        await page.goto("/tech/compare")
        const hero = page.locator("[data-tech-hero]").first()
        await expect(hero).toBeVisible({ timeout: 5_000 })
        await expect(hero).toHaveAttribute("data-tone", "cyan")
        await expect(hero).toContainText("COMPARE")
      })

      test("h1 reads 'Compare Devices'", async ({ page }) => {
        await page.goto("/tech/compare")
        await expect(
          page.getByRole("heading", { level: 1, name: /Compare Devices/i }),
        ).toBeVisible()
      })

      test("CTA 'Pick devices' links to #compare-picker", async ({ page }) => {
        await page.goto("/tech/compare")
        const hero = page.locator("[data-tech-hero]").first()
        const cta = hero.locator('a[href="#compare-picker"]')
        await expect(cta).toBeVisible()
        await expect(cta).toContainText(/Pick devices/i)
      })

      test("Subhead contains 'Benchmark for benchmark, score for score'", async ({ page }) => {
        await page.goto("/tech/compare")
        const hero = page.locator("[data-tech-hero]").first()
        await expect(hero).toContainText("Benchmark for benchmark, score for score")
      })

      test("ComparePageClient still renders below the hero", async ({ page }) => {
        await page.goto("/tech/compare")
        // The compare picker UI should be present (even if empty state)
        const main = page.locator("main")
        await expect(main).toBeVisible()
        // #compare-picker anchor exists
        await expect(page.locator("#compare-picker")).toBeAttached()
      })
    })
    ```
  </action>
  <verify>
    <automated>
      test -f tests/29.2-08-compare-hero.spec.ts &&
      pnpm exec playwright test tests/29.2-08-compare-hero.spec.ts --project=desktop
    </automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/29.2-08-compare-hero.spec.ts` exits 0
    - `pnpm exec playwright test tests/29.2-08-compare-hero.spec.ts --project=desktop` exits 0 (all 5 tests pass)
    - `pnpm tsc --noEmit` exits 0
    - `pnpm lint` exits 0
  </acceptance_criteria>
  <done>All 5 Playwright assertions pass: cyan hero with COMPARE eyebrow, h1 "Compare Devices", CTA Pick-devices → #compare-picker, subhead correct, ComparePageClient intact.</done>
</task>

</tasks>

<verification>
- /tech/compare renders TechHero (cyan, default, "COMPARE" eyebrow, "Compare Devices" h1)
- CTA "Pick devices" links to #compare-picker anchor
- ComparePageClient intact
- Playwright spec passes
</verification>

<success_criteria>
/tech/compare opens with a cyan TechHero "Compare Devices". Old bare h1 replaced. CTA scrolls to the compare picker.
</success_criteria>

<output>
After completion, create `.planning/phases/29.2-site-wide-hero-rollout/29.2-08-SUMMARY.md`
</output>
