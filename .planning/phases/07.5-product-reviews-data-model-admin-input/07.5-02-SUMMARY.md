---
plan: 07.5-02
status: completed
completed: 2026-04-17T05:00:00Z
tasks_total: 2
tasks_completed: 2
---

## Plan 07.5-02 — Admin Context Switcher

### Outcome

Studios↔Tech context switcher lives at the top of the admin sidebar. Pathname-aware — no internal state. Sidebar branches on `pathname.startsWith("/admin/tech")` and renders mode-specific sections + shared sections in both modes.

### Key Files

**Created:**
- `src/components/admin/admin-context-switcher.tsx` — segmented pill component, route-aware via `usePathname`

**Modified:**
- `src/components/admin/admin-sidebar.tsx` — split into `getStudiosSections()` / `getTechSections()` / `getSharedSections()`; renders switcher at top of SidebarContent

### Nav Relocations (D-17)

Moved from Studios groups → Shared section:
- `Media Library` (from old standalone nav)
- `Client List` (from old Commerce group)
- `Roles & Permissions`
- `Site Settings`

### Tech Sidebar Items (D-19) — routes currently 404 until Plan 03 scaffolds them

- `/admin/tech` (Tech Dashboard)
- `/admin/tech/products`
- `/admin/tech/reviews`
- `/admin/tech/categories`
- `/admin/tech/benchmarks`

### Verification

- `pnpm tsc --noEmit` → exit 0
- AdminContextSwitcher has `role="tablist"` and 2x `role="tab"`
- Sidebar imports `AdminContextSwitcher` and uses it in both desktop + mobile sheet

### Self-Check

- [x] Pathname-derived active state (no internal state)
- [x] Tech mode has 5 items matching D-19
- [x] Studios mode preserves existing nav
- [x] Shared section renders in both modes
- [x] Mobile sheet passes `onNavigate` to close on tap
- [x] Compiles clean
