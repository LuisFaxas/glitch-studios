---
plan: 07.5-01
status: completed
completed: 2026-04-17T04:55:00Z
tasks_total: 3
tasks_completed: 3
---

## Plan 07.5-01 — Foundation (shadcn command + DB schema + migration)

### Outcome

12 `tech_*` DB tables + 3 enums + Drizzle `relations()` added to `src/db/schema.ts`. Tables applied directly to the dev DB. shadcn `command` + `input-group` components installed. `slugify` helper extracted.

### Key Files

**Created:**
- `src/components/ui/command.tsx` — shadcn Command primitives (via CLI)
- `src/components/ui/input-group.tsx` — transitive dependency from command registry item
- `src/lib/slugify.ts` — extracted slugify helper
- `src/db/migrations/0002_glitch_tech_foundation.sql` — DDL for history/new-env setup

**Modified:**
- `src/db/schema.ts` — appended 12 tables, 3 enums, 12 relations blocks (607 → 940 lines). Added `import type { AnyPgColumn }` to preserve TS inference on the `tech_categories.parent_id` self-reference.
- `package.json` — `cmdk` dep added by shadcn CLI
- `pnpm-lock.yaml` — regenerated

### 12 Tables Created

| # | Table | Purpose |
|---|---|---|
| 1 | `tech_categories` | 3-level tree (parent_id self-ref, level enforced in server actions) |
| 2 | `tech_spec_templates` | One template per category (unique category_id) |
| 3 | `tech_spec_fields` | Typed fields per template (text/number/enum/boolean + unit + enum_options JSON) |
| 4 | `tech_products` | Products with base fields + FK to category + FK to media hero image |
| 5 | `tech_product_specs` | Normalized join: product ↔ spec field ↔ typed value columns (text/number/boolean) |
| 6 | `tech_reviews` | Multi-dim ratings, Tiptap HTML body, draft/published workflow, reviewer FK |
| 7 | `tech_review_pros` | Sortable pros bullets per review |
| 8 | `tech_review_cons` | Sortable cons bullets per review |
| 9 | `tech_review_gallery` | Sortable media gallery per review (FK to media_assets) |
| 10 | `tech_benchmark_templates` | One template per category |
| 11 | `tech_benchmark_tests` | Named tests with unit + direction (higher/lower-is-better) |
| 12 | `tech_benchmark_runs` | Scores per product ↔ test, with recorded_at + notes |

### Deviation from Plan

Plan Task 3 specified `pnpm db:generate` + `pnpm db:push`. Drizzle-kit's generate detected **pre-existing drift on `orders.total_cents`** (schema had the column but migration snapshot didn't) and blocked on an interactive "is this a rename?" prompt. Running via `script`/`pexpect` couldn't reliably answer the TUI.

**Resolution:** Wrote DDL directly via a temp `tsx` script using the existing `postgres-js` driver (bypassing the `server-only` restriction on `src/lib/db.ts`), then authored the canonical `.sql` file at `src/db/migrations/0002_glitch_tech_foundation.sql` for history. Temp script deleted.

Effect: dev DB has all 12 tables, `src/db/migrations/` has a `.sql` file containing `CREATE TABLE tech_*` statements. Plan acceptance criteria satisfied; the unrelated `orders.total_cents` drift is unaddressed (out of scope for 07.5).

### Verification

- `pnpm tsc --noEmit` → exit 0
- DB confirms 12 tech_* tables via `information_schema.tables`
- `grep -c "export const techCategories" src/db/schema.ts` = 1
- `grep -c "AnyPgColumn" src/db/schema.ts` = 2 (import + usage)
- `grep -c "CREATE TABLE.*tech_reviews\|CREATE TABLE.*tech_product_specs\|CREATE TYPE.*tech_spec_field_type" src/db/migrations/0002_glitch_tech_foundation.sql` = 3

### Self-Check

- [x] 12 tables + 3 enums exist in schema.ts and compile
- [x] Self-reference FK preserves typing (no `any`)
- [x] `tech_review_gallery` added (per UI-SPEC media block requirement)
- [x] Migration file exists with all CREATE TABLE statements
- [x] shadcn command installed
- [x] slugify helper extracted
- [x] No existing tables modified (purely additive)
