---
plan: "01.3-01"
phase: "01.3"
status: complete
started: "2026-03-25T13:15:00.000Z"
completed: "2026-03-25T13:20:00.000Z"
tasks_completed: 1
tasks_total: 1
---

# Summary: 01.3-01 — Switch Neon HTTP driver to postgres-js

## What Changed

Replaced `@neondatabase/serverless` with `postgres` (postgres-js) as the Drizzle ORM database driver. The Neon HTTP driver could not connect to the Supabase pooler connection string, causing a blank page on all database-querying routes.

## Key Files

### Modified
- `src/lib/db.ts` — Switched from `neon()` + `drizzle-orm/neon-http` to `postgres()` + `drizzle-orm/postgres-js`
- `package.json` — Added `postgres ^3.4.8`, removed `@neondatabase/serverless`
- `pnpm-lock.yaml` — Updated lockfile

## Self-Check: PASSED

- [x] `src/lib/db.ts` uses `postgres` import (not `@neondatabase/serverless`)
- [x] `src/lib/db.ts` uses `drizzle-orm/postgres-js` adapter (not `neon-http`)
- [x] `package.json` has `postgres` in dependencies
- [x] `@neondatabase/serverless` removed from package.json
- [x] DB connection test passes (5 services returned from Supabase)

## Deviations

- Site still shows 500 error due to **pre-existing Turbopack issue** with `_share` symlink (unrelated to DB driver). The symlink at `_share -> /home/faxas/shared/glitch_studios` causes Turbopack's CSS processor to resolve paths outside the filesystem root. This existed before the DB fix.

## Decisions

- Chose `postgres` (postgres-js) over `@supabase/supabase-js` because it's a direct Drizzle adapter with zero API changes needed in existing queries.
