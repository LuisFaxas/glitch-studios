---
phase: 26-brand-aware-auth-ui-redesign
plan: 12
type: execute
wave: 6
depends_on: [26-05-PLAN, 26-06-PLAN, 26-07-PLAN, 26-08-PLAN, 26-09-PLAN, 26-10-PLAN, 26-11-PLAN]
files_modified:
  - .planning/phases/26-brand-aware-auth-ui-redesign/26-VERIFICATION.md
autonomous: false
requirements: [AUTH-01, AUTH-32]

must_haves:
  truths:
    - "pnpm tsc --noEmit exits 0"
    - "pnpm lint exits 0"
    - "GlitchTech spelling sweep complete across the 5 auth surfaces + admin/applications + artist-approval-invite email template (no 'GlitchTek' anywhere this phase introduced)"
    - "Vercel env-var checklist documented for production deploy: GOOGLE_CLIENT_ID/SECRET (live), META_CLIENT_ID/SECRET (optional), GITHUB_CLIENT_ID/SECRET (optional), ADMIN_NOTIFICATION_EMAIL"
    - "Manual Playwright pass evidence captured for both brand hosts × 5 auth surfaces × Google OAuth"
    - "Route audit confirms /verify-email, /register/customer, /register/artist, /admin/applications all return 200 (or appropriate redirect)"
    - "Grandfather migration verified: phase26_migration_meta has 'grandfather_email_verified' row; existing users have emailVerified=true"
    - "GitHub single-callback pitfall verified on staging (or documented as 'will validate at deploy time')"
  artifacts:
    - path: ".planning/phases/26-brand-aware-auth-ui-redesign/26-VERIFICATION.md"
      provides: "Documented checklist of every verification step + result + screenshots/logs as evidence"
  key_links:
    - from: "26-VERIFICATION.md"
      to: "All 5 auth surfaces + /admin/applications + email templates"
      via: "Manual checklist with PASS/FAIL per surface per brand host"
      pattern: "PASS|FAIL"
---

<objective>
Final verification + ship checklist for Phase 26. Run `pnpm tsc --noEmit` and `pnpm lint`, sweep "GlitchTek" spelling across all phase-touched surfaces (replace with "GlitchTech"), execute the manual Playwright smoke pass through both brand hosts × 5 auth surfaces × at least Google OAuth end-to-end, document Vercel env-var requirements for prod, route-audit the new pages, and verify the grandfather migration ran cleanly.

Purpose: AUTH-32 ships only when verified end-to-end. This plan is the gate between merge-ready code and prod deploy.
Output: `26-VERIFICATION.md` checklist file with results captured. NOT autonomous — checkpoint task waits for manual confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-brand-aware-auth-ui-redesign/26-CONTEXT.md
@.planning/phases/26-brand-aware-auth-ui-redesign/26-RESEARCH.md
@.planning/phases/26-brand-aware-auth-ui-redesign/26-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated checks + GlitchTech spelling sweep + write verification checklist</name>
  <files>.planning/phases/26-brand-aware-auth-ui-redesign/26-VERIFICATION.md</files>
  <read_first>
    - All Plan 01..11 outputs in this phase: each PLAN.md (for must_haves) and SUMMARY.md (for as-built notes) under `.planning/phases/26-brand-aware-auth-ui-redesign/`
    - .planning/phases/26-brand-aware-auth-ui-redesign/26-RESEARCH.md (Risks & Pitfalls — items #1, #4, #11 are this plan's verification targets)
    - MEMORY note `project_brand_name.md` re GlitchTech (NOT GlitchTek)
  </read_first>
  <action>
**Step A — automated checks:**

Run, in this exact order:

1. `pnpm tsc --noEmit` — must exit 0. Capture output.
2. `pnpm lint` — must exit 0. Capture output.
3. `grep -rn "GlitchTek" src/app/\\(auth\\)/ src/app/admin/applications/ src/components/auth/ src/components/admin/ src/lib/auth.ts src/lib/auth-client.ts src/lib/auth-providers.ts src/lib/auth-guards.ts src/lib/brand.ts src/lib/email/artist-approval-invite.tsx 2>/dev/null || echo "NO MATCHES"` — must print "NO MATCHES". If any match, FIX (replace `GlitchTek` -> `GlitchTech`) and re-run.
4. `grep -rn "GlitchTek" .planning/phases/26-brand-aware-auth-ui-redesign/*.md 2>/dev/null || echo "NO MATCHES"` — same rule, fix in planning docs if any.

**Step B — write `.planning/phases/26-brand-aware-auth-ui-redesign/26-VERIFICATION.md`** documenting results:

```markdown
# Phase 26 — Verification Report

**Date:** {YYYY-MM-DD}
**Branch:** master (or current)

## Automated Checks

| Check | Result | Evidence |
|-------|--------|----------|
| `pnpm tsc --noEmit` | PASS / FAIL | {first 5 lines of output if FAIL} |
| `pnpm lint` | PASS / FAIL | {first 5 lines of output if FAIL} |
| GlitchTek -> GlitchTech sweep | PASS (NO MATCHES) | {paths grepped} |

## Brand Spelling Audit

Files swept for `GlitchTek`:
- `src/app/(auth)/**/*.{ts,tsx}` — clean
- `src/app/admin/applications/**/*.{ts,tsx}` — clean
- `src/components/auth/**/*.tsx` — clean
- `src/components/admin/application-*.tsx` — clean
- `src/lib/auth.ts`, `src/lib/auth-client.ts`, `src/lib/auth-providers.ts`, `src/lib/auth-guards.ts`, `src/lib/brand.ts` — clean
- `src/lib/email/artist-approval-invite.tsx` — clean

## Vercel Env-Var Checklist (Production)

Required at deploy time:

| Var | Required For | Status |
|-----|--------------|--------|
| `GOOGLE_CLIENT_ID` | Google OAuth (live launch) | ☐ set in Vercel |
| `GOOGLE_CLIENT_SECRET` | Google OAuth (live launch) | ☐ set in Vercel |
| `META_CLIENT_ID` | Meta OAuth (button hidden when missing) | ☐ optional |
| `META_CLIENT_SECRET` | Meta OAuth (button hidden when missing) | ☐ optional |
| `GITHUB_CLIENT_ID` | GitHub OAuth (button hidden when missing) | ☐ optional |
| `GITHUB_CLIENT_SECRET` | GitHub OAuth (button hidden when missing) | ☐ optional |
| `ADMIN_NOTIFICATION_EMAIL` | Artist application notification destination | ☐ set in Vercel |
| `RESEND_API_KEY` | Email delivery (already set from Phase 24) | ☐ confirm still set |
| `RESEND_FROM_EMAIL` | Email sender (already set from Phase 24) | ☐ confirm still set |

OAuth Provider Console redirect URIs (must register on each provider):
- Google: 5 URIs (`{4 brand hosts}/api/auth/callback/google` + `localhost:3004/api/auth/callback/google`)
- Meta: same 5 with `/facebook` (Better Auth provider key is `facebook`)
- GitHub: ONE callback URL — register against `glitchstudios.io/api/auth/callback/github` (per RESEARCH §Multi-host pitfall, Option A); verify on staging

## Database Migration Verification

Run on production DB (or staging) before flipping the soft gate:

```sql
-- Should return 1 row with key='grandfather_email_verified'
SELECT * FROM phase26_migration_meta WHERE key = 'grandfather_email_verified';

-- Should return 0 (every pre-existing user is now verified)
SELECT COUNT(*) FROM "user" WHERE "emailVerified" IS NOT TRUE;

-- Sanity check artist_applications
SELECT to_regclass('public.artist_applications');  -- should NOT be null
```

Migration runner: `pnpm db:migrate:phase26` is idempotent. Re-running is safe.

## Manual Playwright / Browser Smoke Pass

Test matrix: 2 brand hosts × 5 auth surfaces. Run dev locally on `localhost:3004` (defaults to studios) AND with `glitchtech.local` mapped via `/etc/hosts` for tech.

| # | Surface | Studios host | Tech host |
|---|---------|--------------|-----------|
| 1 | `/login` (form + social row + brand panel) | ☐ PASS / ☐ FAIL | ☐ PASS / ☐ FAIL |
| 2 | `/login?error=account_not_linked&attempted=google` (conflict copy) | ☐ PASS / ☐ FAIL | ☐ PASS / ☐ FAIL |
| 3 | `/register` role-select tiles | ☐ PASS / ☐ FAIL | ☐ PASS / ☐ FAIL |
| 4 | `/register/customer?step=1` -> step 2 -> step 3 -> account created | ☐ PASS / ☐ FAIL | ☐ PASS / ☐ FAIL |
| 5 | `/register/customer?step=1` with duplicate email -> inline error | ☐ PASS / ☐ FAIL | ☐ PASS / ☐ FAIL |
| 6 | `/register/artist` form submit -> on-page success state + DB row + admin notification email | ☐ PASS / ☐ FAIL | ☐ PASS / ☐ FAIL |
| 7 | `/forgot-password` always-success copy | ☐ PASS / ☐ FAIL | ☐ PASS / ☐ FAIL |
| 8 | `/reset-password` valid token -> success toast -> /login | ☐ PASS / ☐ FAIL | ☐ PASS / ☐ FAIL |
| 9 | `/reset-password` no token -> expired Alert | ☐ PASS / ☐ FAIL | ☐ PASS / ☐ FAIL |
| 10 | `/verify-email` pending state | ☐ PASS / ☐ FAIL | ☐ PASS / ☐ FAIL |
| 11 | `/verify-email?status=success` success state + Continue to dashboard CTA | ☐ PASS / ☐ FAIL | ☐ PASS / ☐ FAIL |
| 12 | Soft gate: unverified user navigates to `/dashboard` -> redirected to `/verify-email` | ☐ PASS / ☐ FAIL | ☐ PASS / ☐ FAIL |
| 13 | Soft gate: unverified user navigates to `/login` -> NOT redirected (auth group exempt) | ☐ PASS / ☐ FAIL | ☐ PASS / ☐ FAIL |

OAuth end-to-end (AUTH-32):

| # | Provider | Studios host | Tech host |
|---|---------|--------------|-----------|
| 14 | Google sign-in -> /dashboard | ☐ PASS / ☐ FAIL | ☐ PASS / ☐ FAIL |
| 15 | Google sign-in to a host whose email matches an existing email+password user -> account_not_linked redirect | ☐ PASS / ☐ FAIL | ☐ PASS / ☐ FAIL |

Admin queue (AUTH-14..19):

| # | Flow | Result |
|---|------|--------|
| 16 | `/admin/applications` lists submitted application | ☐ PASS / ☐ FAIL |
| 17 | Detail drawer opens on row click; bio + portfolio + tags shown | ☐ PASS / ☐ FAIL |
| 18 | Approve modal -> Confirm -> user row created with role + emailVerified=true | ☐ PASS / ☐ FAIL |
| 19 | Approve email lands; recipient sets password; signs in successfully | ☐ PASS / ☐ FAIL |
| 20 | Reject sets status='rejected'; NO applicant email | ☐ PASS / ☐ FAIL |
| 21 | Request more info sends admin-composed email; status='info_requested' | ☐ PASS / ☐ FAIL |

## Known Limitations / Deferred

- GitHub OAuth single-callback-per-app pitfall (RESEARCH §Multi-host pitfall) — verified on STAGING / documented for prod-time check.
- Linked-accounts settings UI deferred (post-launch polish phase).
- Public artist self-serve signup deferred to v5.0.
- 2FA / MFA deferred.
- Email-template visual redesign deferred to backlog 999.5 (Phase 26 ships text-only ArtistApprovalInviteEmail).

## Sign-Off

- [ ] All automated checks pass
- [ ] Manual smoke pass complete (rows 1-21 all PASS)
- [ ] Vercel env-vars set for prod
- [ ] Migration runner executed against prod (or scheduled)
- [ ] User reviewed + approved
```

Save this file. Replace `{YYYY-MM-DD}` with today's date.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm lint && test -f .planning/phases/26-brand-aware-auth-ui-redesign/26-VERIFICATION.md && grep -q "Manual Playwright / Browser Smoke Pass" .planning/phases/26-brand-aware-auth-ui-redesign/26-VERIFICATION.md && grep -q "Vercel Env-Var Checklist" .planning/phases/26-brand-aware-auth-ui-redesign/26-VERIFICATION.md && ! grep -rn "GlitchTek" src/app/\(auth\)/ src/app/admin/applications/ src/components/auth/ src/components/admin/application-list-table.tsx src/components/admin/application-detail-sheet.tsx src/components/admin/application-approve-dialog.tsx src/lib/auth.ts src/lib/auth-client.ts src/lib/auth-providers.ts src/lib/auth-guards.ts src/lib/brand.ts src/lib/email/artist-approval-invite.tsx 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm tsc --noEmit` exits 0
    - `pnpm lint` exits 0
    - `grep -rn "GlitchTek" {phase-touched files}` returns no matches
    - File `.planning/phases/26-brand-aware-auth-ui-redesign/26-VERIFICATION.md` exists with sections: Automated Checks, Brand Spelling Audit, Vercel Env-Var Checklist, Database Migration Verification, Manual Playwright / Browser Smoke Pass (>=20 row checklist), Known Limitations, Sign-Off
    - All 21 manual smoke pass rows are present and unchecked (ready for human walkthrough)
  </acceptance_criteria>
  <done>Automated gates pass; verification checklist ready for human walkthrough.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human Playwright smoke pass + Vercel env-var setup + sign-off</name>
  <what-built>
    - All 5 auth surfaces rebuilt against AuthShell with brand-aware theming, social row, enumeration-safe error copy
    - Customer wizard with 3 steps + atomic create
    - Artist request flow with admin notification email
    - /admin/applications queue with Approve/Reject/Request-info
    - Email-verification soft gate at layout level
    - Grandfather migration applied (existing users keep access)
    - Verification checklist at `.planning/phases/26-brand-aware-auth-ui-redesign/26-VERIFICATION.md`
  </what-built>
  <how-to-verify>
1. Walk through every row in the `Manual Playwright / Browser Smoke Pass` table in `26-VERIFICATION.md`.
2. Mark each row PASS or FAIL with a short note. If any row FAILs, capture the screenshot path or repro steps.
3. Set production env vars in Vercel per the `Vercel Env-Var Checklist`:
   - GOOGLE_CLIENT_ID + GOOGLE_CLIENT_SECRET (required for live OAuth)
   - ADMIN_NOTIFICATION_EMAIL (required for artist application notifications)
   - META_*, GITHUB_* are optional (buttons hide when unset)
4. Register OAuth redirect URIs on Google Cloud Console (required), Meta Developers + GitHub Settings if those will be enabled.
5. Run the prod migration: `pnpm db:migrate:phase26` (or wire into your deploy script). Confirm `phase26_migration_meta` row exists.
6. Open `26-VERIFICATION.md`, fill in checkboxes, sign off at the bottom.
  </how-to-verify>
  <action>This is a human-verification checkpoint. Pause execution until the user types "approved". The user works through the 21-row checklist in 26-VERIFICATION.md, fills in PASS/FAIL marks, and sets Vercel env vars per the checklist. Do not proceed to commit/finalize until the user signals approval.</action>
  <verify>
    <automated>grep -q "Sign-Off" .planning/phases/26-brand-aware-auth-ui-redesign/26-VERIFICATION.md</automated>
  </verify>
  <done>User has typed "approved" and all 21 smoke-pass rows are marked PASS in 26-VERIFICATION.md.</done>
  <resume-signal>Type "approved" once all manual rows are PASS and env vars are set, or describe what failed so we can fix.</resume-signal>
</task>

</tasks>

<verification>
- All automated checks green
- Verification checklist filled in by human
- Vercel env vars confirmed
- Production migration applied
</verification>

<success_criteria>
- AUTH-32 verified end-to-end
- Phase 26 ready to ship
</success_criteria>

<output>
After completion, create `.planning/phases/26-brand-aware-auth-ui-redesign/26-12-SUMMARY.md`
</output>
