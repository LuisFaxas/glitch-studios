---
phase: 07.4-brand-architecture-glitch-tech-sub-brand-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - "07.4-01"
files_modified:
  - src/components/tiles/tech-cross-link-tile.tsx
  - src/components/home/glitch-tech-promo-section.tsx
  - src/app/(public)/layout.tsx
  - src/app/(public)/page.tsx
autonomous: true
requirements:
  - TECH-03
must_haves:
  truths:
    - "Glitch Studios sidebar (desktop, expanded) has a 'GLITCH TECH' cross-link tile at the bottom (above the Sign In / widgets scroll hint area) that navigates to /tech via Next.js Link"
    - "Glitch Studios homepage `/` renders a dedicated Glitch Tech promo section between the Portfolio and Testimonials sections (UI-SPEC Option A)"
    - "Clicking the cross-link tile uses client-side navigation (no target='_blank', no rel='noopener' to external)"
    - "Clicking the promo CTA navigates to /tech via Next.js Link"
    - "Neither change affects Studios `publicNavItems` or `mobileTabItems` — no 'Tech' item added to the existing nav arrays (per D-10)"
    - "pnpm tsc --noEmit exits 0"
  artifacts:
    - path: "src/components/tiles/tech-cross-link-tile.tsx"
      provides: "Cross-link tile rendering GLITCH TECH wordmark, navigating to /tech"
      contains: "href=\"/tech\""
    - path: "src/components/home/glitch-tech-promo-section.tsx"
      provides: "Homepage promo section promoting the Tech vertical"
      contains: "export function GlitchTechPromoSection"
    - path: "src/app/(public)/layout.tsx"
      provides: "Studios layout with cross-link tile wired via TileNav crossLinkTile prop"
      contains: "<TechCrossLinkTile"
    - path: "src/app/(public)/page.tsx"
      provides: "Studios homepage with GlitchTechPromoSection rendered between portfolio and testimonials"
      contains: "GlitchTechPromoSection"
  key_links:
    - from: "src/components/tiles/tech-cross-link-tile.tsx"
      to: "/tech (Tech route)"
      via: "Next.js <Link href='/tech'>"
      pattern: "Link href=\"/tech\""
    - from: "src/components/home/glitch-tech-promo-section.tsx"
      to: "/tech"
      via: "Next.js <Link href='/tech'>"
      pattern: "Link href=\"/tech\""
    - from: "src/app/(public)/layout.tsx"
      to: "src/components/tiles/tech-cross-link-tile.tsx"
      via: "<TileNav crossLinkTile={<TechCrossLinkTile />} />"
      pattern: "crossLinkTile=\\{<TechCrossLinkTile"
    - from: "src/app/(public)/page.tsx"
      to: "src/components/home/glitch-tech-promo-section.tsx"
      via: "Import + render between VideoPortfolioCarousel and TestimonialsCarousel"
      pattern: "<GlitchTechPromoSection"
---

<objective>
Wire the Studios side of the cross-brand relationship. Add the "GLITCH TECH" cross-link tile to the bottom of the Studios sidebar (via the `crossLinkTile` prop added to TileNav in Plan 01) and insert a dedicated `<GlitchTechPromoSection />` into the Studios homepage between Portfolio and Testimonials. This plan does NOT touch the Tech side (that's Plan 04). It runs in parallel with Plan 02 because they modify different directories.

Purpose: Make Glitch Tech discoverable from Studios. TECH-03 success criterion requires a cross-link tile on each sidebar AND a dedicated homepage promo section.
Output: Studios layout has a bottom cross-link tile; Studios homepage has a Tech promo section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.4-brand-architecture-glitch-tech-sub-brand-foundation/07.4-CONTEXT.md
@.planning/phases/07.4-brand-architecture-glitch-tech-sub-brand-foundation/07.4-UI-SPEC.md
@.planning/phases/07.4-brand-architecture-glitch-tech-sub-brand-foundation/07.4-01-SUMMARY.md

<interfaces>
<!-- After Plan 01, TileNav accepts crossLinkTile?: ReactNode -->
<!-- Current (public)/page.tsx section order (for placement): Hero → Services → Featured Beats → VideoPortfolio → Testimonials → Blog -->
<!-- UI-SPEC Option A: place promo between Portfolio and Testimonials -->

UI-SPEC copywriting contract for the promo section (UI-SPEC.md ~line 598-602):
- Heading: "GLITCH TECH" (wordmark-style)
- Subheading: "Advanced product reviews & benchmarks"
- Description: "Discover in-depth tech reviews, benchmarks, and comparisons. From gaming rigs to creative workstations."
- CTA: "Explore Tech Reviews →"
- Href: /tech

UI-SPEC cross-link tile (UI-SPEC.md ~line 502-510):
- Label/wordmark: "GLITCH TECH" (mono, uppercase, tracking-[0.05em])
- Size: wide (full sidebar width, spans both grid columns)
- Visual: identical to regular nav tiles — dark bg (#111), border (#222), white text (#f5f5f0)
- Hover: full glitch hover animation (matches Tile component default)
- No icon required (wordmark-only) — optional arrow affordance via user discretion
- `<Link href="/tech">` — client-side nav, NO target="_blank"

Scroll section available primitives (from src/components/home/):
- `ScrollSection` with `variant` prop (used throughout homepage for motion-wrapped sections)

Tile primitive is at src/components/tiles/tile.tsx — the `Tile` component can be used directly OR we can build a bespoke wrapper. Research and UI-SPEC both recommend REUSE of Tile with custom content for consistency.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build TechCrossLinkTile component and wire into Studios sidebar</name>
  <files>src/components/tiles/tech-cross-link-tile.tsx, src/app/(public)/layout.tsx</files>
  <read_first>
    - src/components/tiles/tile.tsx (Tile component API — size="wide" support, href, className, children slot)
    - src/components/layout/tile-nav.tsx (after Plan 01 — confirm crossLinkTile prop placement and grid context; the tile is placed via `<div className="mt-2">{crossLinkTile}</div>` between widgets and scroll hint)
    - src/components/tiles/logo-tile.module.css (glitch animation CSS — optional for wordmark rendering)
    - src/app/(public)/layout.tsx (current consumer wiring, updated in Plan 01)
  </read_first>
  <behavior>
    - Behavior 1: TechCrossLinkTile renders as a wide tile (spans full sidebar column span) with the text "GLITCH TECH" in font-mono uppercase tracking.
    - Behavior 2: Clicking it navigates to `/tech` via client-side Next.js Link (no full page reload).
    - Behavior 3: On hover, the tile shows the default Tile glitch hover animation (no additional effect needed).
    - Behavior 4: Studios sidebar (desktop, expanded) shows this tile BELOW the widget section and ABOVE any scroll-hint gradient. Collapsed sidebar does NOT render cross-link tiles (only nav icons; Tech discovery happens via the full view — acceptable simplification for 07.4).
  </behavior>
  <action>
    Step 1 — Create `src/components/tiles/tech-cross-link-tile.tsx`:

    ```tsx
    "use client"

    import Link from "next/link"
    import { usePathname } from "next/navigation"

    /**
     * Cross-link tile on Glitch Studios sidebar.
     * Displays "GLITCH TECH" wordmark, navigates to /tech.
     *
     * Implemented via raw markup (not reusing Tile) because:
     * - We need a wordmark-only display (no icon, no label in Tile's standard slot layout)
     * - Uses same visual treatment as a wide nav tile (dark bg, border, glitch hover)
     * - Acts as a "sister site portal" — visually identical to regular nav tiles per UI-SPEC
     */
    export function TechCrossLinkTile() {
      const pathname = usePathname()
      const isActive = pathname.startsWith("/tech")

      return (
        <Link
          href="/tech"
          aria-label="Glitch Tech — sister site"
          className={`group relative col-span-2 flex items-center justify-center overflow-hidden border px-4 py-5 font-mono font-bold uppercase tracking-[0.05em] outline-none transition-colors duration-200 focus-visible:outline-1 focus-visible:outline-[#f5f5f0] focus-visible:outline-offset-2 ${
            isActive
              ? "border-[#f5f5f0] bg-[#f5f5f0] text-[#000000] shadow-[0_0_20px_rgba(255,255,255,0.12)]"
              : "border-[#222222] bg-[#111111] text-[#f5f5f0] hover:border-[#444444] hover:bg-[#1a1a1a] active:bg-[#0a0a0a]"
          }`}
        >
          {/* Glitch hover overlay — same effect as Tile primitive */}
          {!isActive && (
            <span
              className="pointer-events-none absolute inset-0 bg-[#f5f5f0]/10 opacity-0 transition-opacity duration-150 group-hover:animate-glitch-hover group-hover:opacity-100 motion-reduce:hidden"
              aria-hidden="true"
            />
          )}
          <span className="relative z-10 text-xl">GLITCH TECH</span>
        </Link>
      )
    }
    ```

    Step 2 — Edit `src/app/(public)/layout.tsx`.

    **SURGICAL EDIT ONLY.** After Plan 01 completes, this file already contains:
    - `import { publicNavItems, mobileTabItems } from "@/components/layout/public-nav-config"` (added in Plan 01 Task 2/3)
    - `<TileNav navItems={publicNavItems} latestPostSlot={<WidgetLatestPost />} />` (updated in Plan 01 Task 2)
    - `<BottomTabBar items={mobileTabItems} />` (updated in Plan 01 Task 3)
    - `<FloatingCartButton />` already REMOVED (Plan 01 Task 3 moved it to root layout)

    Do NOT overwrite the whole file. Make only these two targeted changes:

    1. Add import at the top of the file:
    ```ts
    import { TechCrossLinkTile } from "@/components/tiles/tech-cross-link-tile"
    ```

    2. Update ONLY the existing `<TileNav ... />` JSX line to add the `crossLinkTile` prop. Change:
    ```tsx
    <TileNav navItems={publicNavItems} latestPostSlot={<WidgetLatestPost />} />
    ```
    to:
    ```tsx
    <TileNav
      navItems={publicNavItems}
      latestPostSlot={<WidgetLatestPost />}
      crossLinkTile={<TechCrossLinkTile />}
    />
    ```

    Leave `<BottomTabBar items={mobileTabItems} />` and all other JSX in the file UNCHANGED. Do NOT re-add `<FloatingCartButton />` — it was intentionally removed by Plan 01 and now lives at root. If you see FloatingCartButton already absent from (public)/layout.tsx, that is correct post-Plan 01 state.

    Do NOT add any Tech item to `publicNavItems` or `mobileTabItems` (per D-10).
  </action>
  <verify>
    <automated>cd /home/faxas/workspaces/projects/personal/glitch_studios && pnpm tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/tiles/tech-cross-link-tile.tsx` exists
    - `grep -c "export function TechCrossLinkTile" src/components/tiles/tech-cross-link-tile.tsx` returns `1`
    - `grep -c "href=\"/tech\"" src/components/tiles/tech-cross-link-tile.tsx` returns `1`
    - `grep -c "GLITCH TECH" src/components/tiles/tech-cross-link-tile.tsx` returns `1`
    - `grep -c "target=\"_blank\"" src/components/tiles/tech-cross-link-tile.tsx` returns `0` (must be same-tab)
    - `grep -c "TechCrossLinkTile" src/app/\(public\)/layout.tsx` returns at least `2` (import + usage)
    - `grep -c "crossLinkTile=" src/app/\(public\)/layout.tsx` returns `1`
    - `grep -c "Tech\|tech" src/components/layout/public-nav-config.ts` returns `0` (no Tech item added to Studios nav per D-10)
    - `pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Cross-link tile exists and is wired into Studios sidebar via TileNav's crossLinkTile prop.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build GlitchTechPromoSection and insert into Studios homepage</name>
  <files>src/components/home/glitch-tech-promo-section.tsx, src/app/(public)/page.tsx</files>
  <read_first>
    - src/components/home/scroll-section.tsx (ScrollSection variant API — look at how existing sections wrap content)
    - src/components/home/video-portfolio-carousel.tsx (section before the insertion point — to understand the styling tone)
    - src/components/home/testimonials-carousel.tsx (section after the insertion point)
    - src/app/(public)/page.tsx (current render order of homepage sections — look at both the dynamic DB-driven render map AND the fallback `<>...</>` block)
    - src/components/home/hero-section.tsx (reference for the GlitchLink hover pattern if you want to reuse it)
  </read_first>
  <behavior>
    - Behavior 1: GlitchTechPromoSection renders a full-width section with "GLITCH TECH" heading (64px-ish), subheading, description, and a CTA button linking to /tech.
    - Behavior 2: Homepage `/` renders this section between VideoPortfolioCarousel and TestimonialsCarousel (UI-SPEC Option A).
    - Behavior 3: Clicking the CTA navigates via client-side Next.js Link.
    - Behavior 4: Section respects reduced motion and mobile responsiveness (stacks vertically on mobile per UI-SPEC).
  </behavior>
  <action>
    Step 1 — Create `src/components/home/glitch-tech-promo-section.tsx`:

    ```tsx
    "use client"

    import Link from "next/link"
    import { ArrowRight } from "lucide-react"
    import { ScrollSection } from "@/components/home/scroll-section"
    import { GlitchLogo } from "@/components/layout/glitch-logo"

    /**
     * Homepage promo section announcing the Glitch Tech sub-brand.
     * Renders on Studios homepage between the Portfolio and Testimonials sections
     * (UI-SPEC Option A placement).
     *
     * Purpose: provide a dedicated discovery entry point for Glitch Tech without
     * cluttering the main Studios navigation (per D-10 / D-14).
     */
    export function GlitchTechPromoSection() {
      return (
        <ScrollSection variant="clip-reveal" className="py-16 md:py-24">
          <div className="mx-auto max-w-7xl px-4">
            <div className="relative overflow-hidden border border-[#222222] bg-[#111111] p-8 md:p-16">
              {/* Grid layout: content left, visual right on desktop; stacks on mobile */}
              <div className="grid grid-cols-1 items-center gap-8 md:grid-cols-2 md:gap-12">
                {/* Left column — copy */}
                <div className="flex flex-col gap-4 text-left">
                  {/* Subheading */}
                  <span className="font-mono text-[11px] font-bold uppercase tracking-[0.1em] text-[#888888]">
                    Introducing
                  </span>

                  {/* Wordmark heading — uses the parameterized GlitchLogo from Plan 01 */}
                  <div>
                    <GlitchLogo text="GLITCH TECH" size="lg" animate={false} />
                  </div>

                  {/* Description */}
                  <p className="font-mono text-sm font-bold uppercase tracking-[0.1em] text-[#f5f5f0]">
                    Advanced product reviews &amp; benchmarks
                  </p>
                  <p className="font-sans text-[15px] leading-relaxed text-[#888888]">
                    Discover in-depth tech reviews, benchmarks, and comparisons.
                    From gaming rigs to creative workstations.
                  </p>

                  {/* CTA */}
                  <Link
                    href="/tech"
                    className="group mt-2 inline-flex w-fit items-center gap-2 border border-[#f5f5f0] bg-transparent px-6 py-3 font-mono text-xs font-bold uppercase tracking-[0.05em] text-[#f5f5f0] transition-colors duration-200 hover:bg-[#f5f5f0] hover:text-[#000000]"
                    aria-label="Explore Glitch Tech Reviews"
                  >
                    <span>Explore Tech Reviews</span>
                    <ArrowRight className="h-4 w-4 transition-transform duration-200 group-hover:translate-x-1" />
                  </Link>
                </div>

                {/* Right column — visual placeholder */}
                <div className="relative aspect-[4/3] w-full overflow-hidden border border-[#222222] bg-[#0a0a0a]">
                  {/* Abstract tech-themed placeholder: radial gradient + scanlines */}
                  <div
                    className="absolute inset-0"
                    style={{
                      background:
                        "radial-gradient(ellipse at center, rgba(255,255,255,0.05) 0%, transparent 70%)",
                    }}
                    aria-hidden="true"
                  />
                  <div
                    className="absolute inset-0 opacity-30 mix-blend-overlay"
                    style={{
                      backgroundImage:
                        "repeating-linear-gradient(0deg, rgba(255,255,255,0.03) 0px, rgba(255,255,255,0.03) 1px, transparent 1px, transparent 3px)",
                    }}
                    aria-hidden="true"
                  />
                  <div className="absolute inset-0 flex items-center justify-center">
                    <span className="font-mono text-[10px] uppercase tracking-[0.2em] text-[#444444]">
                      Reviews // Benchmarks // Compare
                    </span>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </ScrollSection>
      )
    }
    ```

    Step 2 — Edit `src/app/(public)/page.tsx` to insert the promo section between portfolio and testimonials.

    Find the import block at the top (lines 7-16). Add:
    ```ts
    import { GlitchTechPromoSection } from "@/components/home/glitch-tech-promo-section"
    ```

    Find the `sectionRenderers` object (lines 80-97). Add a new entry:
    ```ts
    glitch_tech_promo: () => <GlitchTechPromoSection />,
    ```
    (Place it AFTER `portfolio` and BEFORE `testimonials` in the object literal to mirror the fallback order.)

    Find the fallback render block (lines 120-127, `<> <HeroSection /> ... </>`). Insert `<GlitchTechPromoSection />` BETWEEN `<VideoPortfolioCarousel portfolioItems={portfolioList} />` and `<TestimonialsCarousel testimonials={testimonialsList} />`:
    ```tsx
    <>
      <HeroSection />
      <ServicesOverview services={servicesList} />
      <FeaturedCarousel beats={beatsList} />
      <VideoPortfolioCarousel portfolioItems={portfolioList} />
      <GlitchTechPromoSection />
      <TestimonialsCarousel testimonials={testimonialsList} />
      <BlogSection posts={blogList} />
    </>
    ```

    Note on the dynamic render path: if `homepageSections` is populated from the DB (admin-configured order), the promo section will only render if an admin inserts a `glitch_tech_promo` section in the homepage builder. For the fallback (which ships by default and runs whenever DB has no sections), the promo will always appear. This is consistent with how other homepage sections work.
  </action>
  <verify>
    <automated>cd /home/faxas/workspaces/projects/personal/glitch_studios && pnpm tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/home/glitch-tech-promo-section.tsx` exists
    - `grep -c "export function GlitchTechPromoSection" src/components/home/glitch-tech-promo-section.tsx` returns `1`
    - `grep -c "href=\"/tech\"" src/components/home/glitch-tech-promo-section.tsx` returns `1`
    - `grep -c "GLITCH TECH" src/components/home/glitch-tech-promo-section.tsx` returns at least `1`
    - `grep -c "Explore Tech Reviews" src/components/home/glitch-tech-promo-section.tsx` returns `1`
    - `grep -c "Advanced product reviews" src/components/home/glitch-tech-promo-section.tsx` returns `1`
    - `grep -c "GlitchTechPromoSection" src/app/\(public\)/page.tsx` returns at least `2` (import + render, plus possibly render map entry)
    - `grep -c "glitch_tech_promo" src/app/\(public\)/page.tsx` returns at least `1` (section renderer entry)
    - In src/app/(public)/page.tsx, `<GlitchTechPromoSection />` appears between `<VideoPortfolioCarousel` and `<TestimonialsCarousel` in the fallback block — verify with `grep -A 1 "VideoPortfolioCarousel" src/app/\(public\)/page.tsx | grep "GlitchTechPromoSection"` returns a match
    - `pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Promo section component exists and renders on Studios homepage between portfolio and testimonials.</done>
</task>

</tasks>

<verification>
Manual sanity check: `pnpm dev` → visit `/`:
- Studios sidebar shows "GLITCH TECH" tile at the bottom of the expanded sidebar (between widgets and scroll hint)
- Homepage scrolls through sections: Hero → Services → Featured Beats → Portfolio → **Glitch Tech Promo** (NEW) → Testimonials → Blog
- Clicking the cross-link tile navigates to /tech in-tab
- Clicking "Explore Tech Reviews" CTA navigates to /tech in-tab

Automated: `pnpm tsc --noEmit` exits 0.
</verification>

<success_criteria>
- TechCrossLinkTile exists and renders at the bottom of the Studios sidebar (desktop, expanded view)
- GlitchTechPromoSection exists and renders between VideoPortfolioCarousel and TestimonialsCarousel on `/`
- Neither change touches `publicNavItems` or `mobileTabItems` (per D-10)
- Both use client-side Next.js Link (no target="_blank")
- Type check passes
</success_criteria>

<output>
After completion, create `.planning/phases/07.4-brand-architecture-glitch-tech-sub-brand-foundation/07.4-03-SUMMARY.md`
</output>
