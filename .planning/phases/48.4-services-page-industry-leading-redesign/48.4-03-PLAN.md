---
phase: 48.4
plan: 03
type: execute
wave: 2
depends_on:
  - 48.4-02
files_modified:
  - src/components/services/service-grid.tsx
  - src/components/services/service-detail-overlay.tsx
  - src/components/services/service-detail-panel.tsx
autonomous: true
gap_closure: false
requirements:
  - UI-SVC-04
  - UI-SVC-07
  - UI-SVC-08
  - UI-SVC-09
  - UI-SVC-10
  - UI-SVC-11
  - UI-SVC-16
tags:
  - frontend
  - grid
  - drawer
  - dialog
  - deep-link
  - responsive-overlay
must_haves:
  truths:
    - "Mobile (<768px) renders a 2-column grid (`grid-cols-2 gap-1`) of service cards"
    - "Desktop (≥768px) renders a 3-column grid (`md:grid-cols-3 gap-1`) of service cards"
    - "Custom Request tile occupies position 6 using the SAME ServiceCard primitive (no separate component)"
    - "Tapping a service card opens a Drawer (mobile) or Dialog (desktop) at the matching service"
    - "Tapping Custom Request tile navigates to `/contact?service=custom` — NO overlay"
    - "Overlay opens on first paint when URL hash matches a service slug (e.g. `/services#mixing-mastering`)"
    - "Overlay close clears the hash via `window.history.replaceState(null, '', window.location.pathname)`"
    - "Overlay content order matches UI-SPEC §8: description → pricing → duration & includes → highlights → process → policies → example work"
    - "Sticky bottom CTA reads `BOOK THIS SERVICE` when service.isBookable, else `REQUEST A QUOTE`"
    - "Drawer max-height is 92vh (not Vaul's 80vh default); Dialog max-width is 720px (not license-modal's 640px)"
    - "Drawer scroll body uses `grid grid-rows-[auto_minmax(0,1fr)_auto]` layout so sticky CTA never collapses"
    - "Empty state (services.length === 0) renders one tile spanning the grid (`col-span-2 md:col-span-3`)"
  artifacts:
    - path: "src/components/services/service-grid.tsx"
      provides: "ServiceGrid client component — owns selectedSlug state, deep-link hash sync, opens overlay"
      exports: ["ServiceGrid"]
      min_lines: 100
    - path: "src/components/services/service-detail-overlay.tsx"
      provides: "ServiceDetailOverlay — responsive Drawer (mobile) / Dialog (desktop) shell"
      exports: ["ServiceDetailOverlay"]
      min_lines: 120
    - path: "src/components/services/service-detail-panel.tsx"
      provides: "ServiceDetailPanel — overlay body content (description, pricing, duration, highlights, process, policies, example work)"
      exports: ["ServiceDetailPanel", "type Service", "type PortfolioItemLite"]
      min_lines: 150
  key_links:
    - from: "ServiceGrid handleTileClick"
      to: "window.history.replaceState"
      via: "direct DOM API call"
      pattern: "window\\.history\\.replaceState\\(null, \"\", `#"
    - from: "ServiceGrid deep-link useEffect"
      to: "setTimeout(..., 0) wrapping setSelectedSlug"
      via: "load-bearing deferral per RESEARCH §Pitfall 6 + CLAUDE.md ranking filter safety rule"
      pattern: "window\\.setTimeout\\(.*setSelectedSlug"
    - from: "ServiceDetailOverlay useMediaQuery"
      to: "DialogContent or DrawerContent"
      via: "isDesktop ternary"
      pattern: "isDesktop \\? <Dialog"
    - from: "ServiceDetailOverlay DrawerContent override classes"
      to: "Vaul defaults (drawer.tsx line 59)"
      via: "!-important classes overriding data-attribute variants"
      pattern: "data-\\[vaul-drawer-direction=bottom\\]:!"
    - from: "ServiceDetailOverlay handleClose"
      to: "history.replaceState clearing hash"
      via: "window.history.replaceState(null, \"\", window.location.pathname)"
      pattern: "history\\.replaceState\\(null, \"\", window\\.location\\.pathname"
---

<objective>
Replace `src/components/services/service-grid.tsx` (currently a master-detail + accordion implementation) with three new components:

1. `ServiceGrid` — slim wrapper that renders a 2-col/3-col grid of `ServiceCard` instances, owns the open/close state for the overlay, syncs the URL hash on open/close, and auto-opens the overlay on first paint when the page loads with a matching hash. Also renders the Custom Request tile at position 6 (direct nav, no overlay) and the empty-state tile when no services are seeded.
2. `ServiceDetailOverlay` — responsive shell that switches between Vaul `<Drawer>` (mobile) and Radix `<Dialog>` (desktop) via the same private `useMediaQuery` pattern as `license-modal.tsx`. Owns the sticky-bottom CTA layout. Critically applies the `!`-important Vaul overrides (RESEARCH §Pitfall 1) and the `grid-rows-[auto_minmax(0,1fr)_auto]` body layout (RESEARCH §Pitfall 2).
3. `ServiceDetailPanel` — the body of the overlay, extracted from the current inline `ServiceDetailPanel` inside `service-grid.tsx` lines 76-266. Ported verbatim minus the outer `border + p-6 + rounded-none` wrapper (overlay provides the chrome).

Purpose: This is the core navigation layer. The grid is what the user lands on; the overlay is what they tap into. The current implementation violates UI-SPEC §6 (master-detail on desktop, accordion on mobile, selected-tile inversion) and AP-03/AP-09 (single-col mobile stack, inline detail panel). This plan eradicates those patterns.

Output: 3 new/rewritten files in `src/components/services/`. Total ~400 LOC. ServiceGrid imports ServiceCard (Plan 02) and ServiceDetailOverlay; ServiceDetailOverlay imports ServiceDetailPanel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/48.4-services-page-industry-leading-redesign/48.4-UI-SPEC.md
@.planning/phases/48.4-services-page-industry-leading-redesign/48.4-RESEARCH.md
@./CLAUDE.md
@src/components/beats/license-modal.tsx
@src/components/services/service-grid.tsx
@src/components/ui/drawer.tsx
@src/components/ui/dialog.tsx

<interfaces>
<!-- ServiceGrid receives the same enriched service shape that src/app/(public)/services/page.tsx already constructs. Plan 04 wires the page; this plan only changes what the page imports underneath. -->

interface Service {
  id: string
  name: string
  slug: string
  type: string
  description: string
  shortDescription: string
  priceLabel: string
  features: string[] | null
  ctaText: string | null
  sortOrder: number | null
  isActive: boolean | null
  isBookable?: boolean
  durationMinutes: number | null
  depositType: "flat" | "percentage" | null
  depositValue: number | null
  cancellationWindowHours: number | null
  refundPolicy: string | null
  deliverables: string[]
}

interface PortfolioItemLite {
  id: string
  title: string
  slug: string
  type: string
  category: string | null
  thumbnailUrl: string | null
}

interface ServiceGridProps {
  services: Service[]
  portfolioByServiceId: Record<string, PortfolioItemLite[]>
}

export function ServiceGrid(props: ServiceGridProps): JSX.Element

interface ServiceDetailOverlayProps {
  service: Service | null    // null = closed (overlay returns null)
  portfolioItems: PortfolioItemLite[]
  onClose: () => void
}

export function ServiceDetailOverlay(props: ServiceDetailOverlayProps): JSX.Element | null

interface ServiceDetailPanelProps {
  service: Service
  portfolioItems: PortfolioItemLite[]
}

export function ServiceDetailPanel(props: ServiceDetailPanelProps): JSX.Element

<!-- From license-modal.tsx (lines 29-46) — port verbatim into service-detail-overlay.tsx as a PRIVATE hook (do NOT extract to shared util this phase per RESEARCH Q2) -->

function useMediaQuery(query: string): boolean {
  const [matches, setMatches] = useState(
    () => typeof window !== "undefined" && window.matchMedia(query).matches
  )
  useEffect(() => {
    const mql = window.matchMedia(query)
    function handleChange(e: MediaQueryListEvent) { setMatches(e.matches) }
    mql.addEventListener("change", handleChange)
    return () => mql.removeEventListener("change", handleChange)
  }, [query])
  return matches
}

<!-- From src/components/ui/drawer.tsx line 59 — the defaults we MUST override -->

// data-[vaul-drawer-direction=bottom]:mt-24
// data-[vaul-drawer-direction=bottom]:max-h-[80vh]
// data-[vaul-drawer-direction=bottom]:rounded-t-xl

// Override classes for ServiceDetailOverlay's <DrawerContent>:
//   "data-[vaul-drawer-direction=bottom]:!rounded-none"
//   "data-[vaul-drawer-direction=bottom]:!mt-0"
//   "data-[vaul-drawer-direction=bottom]:!max-h-[92vh]"

<!-- Locked process steps content (UI-SPEC §17 Process step copy) -->

const PROCESS_STEPS = [
  { n: "01", title: "PREP", body: "We confirm your goals, tech requirements, and session brief." },
  { n: "02", title: "SESSION", body: "Full-focus studio time on the service you booked." },
  { n: "03", title: "REVISIONS", body: "Up to two revision passes on deliverables where applicable." },
  { n: "04", title: "DELIVERY", body: "Final files delivered digitally within the agreed window." },
]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port ServiceDetailPanel from current service-grid.tsx into its own file (no chrome wrapper)</name>
  <files>src/components/services/service-detail-panel.tsx</files>
  <read_first>
    - src/components/services/service-grid.tsx (FULL FILE — lines 76-266 are the `ServiceDetailPanel` body to port, lines 53-74 are PROCESS_STEPS to also port)
    - .planning/phases/48.4-services-page-industry-leading-redesign/48.4-UI-SPEC.md §8 (overlay content order) + §17 (section labels + process step copy)
    - .planning/phases/48.4-services-page-industry-leading-redesign/48.4-RESEARCH.md §Anti-Patterns ("Don't reuse ServiceDetailPanel verbatim ... drop the wrapper border + padding")
  </read_first>
  <action>
    Create NEW file `src/components/services/service-detail-panel.tsx`. Must start with `"use client"` directive (uses Next/Link, no client hooks itself, but kept client to avoid mixing server/client in the overlay tree).

    Re-export the `Service` and `PortfolioItemLite` types as a shared module-level export (other files in the services tree import these):
    ```
    export type Service = { /* same 19 fields from current service-grid.tsx lines 9-28 */ }
    export type PortfolioItemLite = { /* same 6 fields from current service-grid.tsx lines 30-37 */ }
    ```

    Port the PROCESS_STEPS constant from current service-grid.tsx lines 53-74 verbatim (UI-SPEC §17 process step copy is locked).

    Port the local `formatDuration(minutes)` helper from current service-grid.tsx lines 44-51 verbatim — this returns long-form `"2 hours"` / `"45 minutes"` for the body text (DIFFERENT from `formatDurationChip` which returns short-form `"2 HRS"`; both forms coexist intentionally per UI-SPEC §6 + §8).

    Implement `ServiceDetailPanel({ service, portfolioItems })`:
    - Compute `depositHint`, `depositPolicy`, `hasPolicies`, `portfolio`, `hasExamples` the same way as current service-grid.tsx lines 83-99.
    - Root wrapper: `<div className="space-y-6 p-4 md:p-6">` (NO outer border, NO bg, NO rounded — overlay provides chrome per RESEARCH anti-pattern note).
    - Render sections in this exact order (UI-SPEC §8):
      1. Description: `<p className="font-sans text-[14px] md:text-[15px] leading-[1.5] text-[#f5f5f0] max-w-2xl">{service.description || service.shortDescription}</p>` — REMOVE the H1 / GlitchHeading from the current implementation (overlay header owns the H1).
      2. Pricing section: `<section><h3 className="font-mono text-[12px] font-bold uppercase tracking-[0.05em] text-[#888] mb-2">PRICING</h3>` containing the price + optional depositHint paragraph (same shape as current).
      3. Duration & Includes section (conditional on `service.durationMinutes !== null`): same shape as current.
      4. Highlights section (conditional on `service.features?.length > 0`): same shape as current.
      5. Process section: same shape as current (uses PROCESS_STEPS).
      6. Policies section (conditional on `hasPolicies`): same shape as current — REMINDER: `service.refundPolicy` is a DB string; React renders it as text (auto-escaped) which is safe; do NOT use `dangerouslySetInnerHTML`.
      7. Example Work section (conditional on `hasExamples`): same shape as current — uses `<Link href={`/portfolio/${item.slug}`}>` and `<img>` (with eslint-disable-next-line @next/next/no-img-element comment as in current).
    - DO NOT render the bottom CTA inside this panel — the OVERLAY owns the sticky bottom CTA (per UI-SPEC §8 layout: sticky CTA lives in the overlay shell, not in the scrollable body).

    Strip the GlitchHeading import from this file (the overlay header uses a plain `<h2>` with the service name — GlitchHeading was for the inline panel's H1 and is not needed in the drawer/dialog header per UI-SPEC §8 which shows a plain service-name header).

    Anti-pattern locks for this file (UI-SPEC §13 + §14):
    - NO Tailwind-named color tokens; ONLY literal hex from the palette
    - NO `rounded-md/lg/xl/2xl/3xl`; only `rounded-none` (or no rounding at all)
    - NO `shadow-*`
    - NO `duration-300` or higher
    - DO NOT import lucide-react in this file (the overlay shell owns the close icon)
  </action>
  <verify>
    <automated>
      pnpm tsc --noEmit --pretty false 2>&1 | grep -E "service-detail-panel" | head -10
    </automated>
    <automated>
      pnpm lint src/components/services/service-detail-panel.tsx 2>&1 | tail -20
    </automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/services/service-detail-panel.tsx` exists
    - First non-blank line is `"use client"` — `head -1 src/components/services/service-detail-panel.tsx | grep -q '"use client"'`
    - Exports `ServiceDetailPanel`, `Service` type, `PortfolioItemLite` type — `grep -c 'export function ServiceDetailPanel' src/components/services/service-detail-panel.tsx` equals 1 AND `grep -c 'export type Service' src/components/services/service-detail-panel.tsx` is at least 1 AND `grep -c 'export type PortfolioItemLite' src/components/services/service-detail-panel.tsx` equals 1
    - All 6 section labels present verbatim (UI-SPEC §17): `grep -c '>PRICING<' src/components/services/service-detail-panel.tsx` equals 1, `grep -c '>DURATION & INCLUDES<' src/components/services/service-detail-panel.tsx` equals 1, `grep -c '>HIGHLIGHTS<' src/components/services/service-detail-panel.tsx` equals 1, `grep -c '>PROCESS<' src/components/services/service-detail-panel.tsx` equals 1, `grep -c '>POLICIES<' src/components/services/service-detail-panel.tsx` equals 1, `grep -c '>EXAMPLE WORK<' src/components/services/service-detail-panel.tsx` equals 1
    - PROCESS_STEPS copy verbatim — `grep -c "We confirm your goals" src/components/services/service-detail-panel.tsx` equals 1, `grep -c "Full-focus studio time" src/components/services/service-detail-panel.tsx` equals 1
    - NO outer chrome wrapper (the overlay provides it): `grep -cE 'className="[^"]*\bborder\s[^"]*p-6[^"]*rounded-none' src/components/services/service-detail-panel.tsx` equals 0
    - NO GlitchHeading import (overlay header has plain H2): `grep -c 'GlitchHeading' src/components/services/service-detail-panel.tsx` equals 0
    - NO lucide-react import (overlay owns close X): `grep -c 'lucide-react' src/components/services/service-detail-panel.tsx` equals 0
    - Anti-pattern lock — NO rounded except none: `grep -vE 'rounded-none' src/components/services/service-detail-panel.tsx | grep -cE 'rounded-(md|lg|xl|2xl|3xl|full)'` equals 0
    - Anti-pattern lock — NO shadows: `grep -cE '\bshadow-' src/components/services/service-detail-panel.tsx` equals 0
    - Anti-pattern lock — NO Tailwind named colors: `grep -cE 'bg-(gray|zinc|neutral|slate)-|text-(gray|zinc|neutral|slate)-' src/components/services/service-detail-panel.tsx` equals 0
    - Anti-pattern lock — NO dangerouslySetInnerHTML: `grep -c 'dangerouslySetInnerHTML' src/components/services/service-detail-panel.tsx` equals 0
    - TypeScript clean — `pnpm tsc --noEmit --pretty false 2>&1 | grep -cE "service-detail-panel.*error"` equals 0
    - Lint clean — `pnpm lint src/components/services/service-detail-panel.tsx` returns 0 errors
  </acceptance_criteria>
  <done>
    ServiceDetailPanel renders all 7 content sections in spec order, no chrome wrapper, no H1 (overlay owns the heading), Service + PortfolioItemLite types exported for consumption by ServiceGrid and ServiceDetailOverlay.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement ServiceDetailOverlay with Drawer/Dialog responsive switching + sticky bottom CTA + grid-rows layout</name>
  <files>src/components/services/service-detail-overlay.tsx</files>
  <read_first>
    - src/components/beats/license-modal.tsx (FULL FILE — 1:1 useMediaQuery + responsive Drawer/Dialog pattern to port)
    - src/components/ui/drawer.tsx (FULL FILE — line 59 has the data-attribute variant classes we must override)
    - src/components/ui/dialog.tsx (existence + showCloseButton API)
    - .planning/phases/48.4-services-page-industry-leading-redesign/48.4-UI-SPEC.md §8 (overlay spec, all wireframes) + §17 (CTA labels — "BOOK THIS SERVICE" / "REQUEST A QUOTE")
    - .planning/phases/48.4-services-page-industry-leading-redesign/48.4-RESEARCH.md §Pattern 3 (useMediaQuery port) + §Pattern 4 (Vaul override layout + grid-rows pattern — the killing-pattern fix) + §Pitfall 1 (Vaul baked classes) + §Pitfall 2 (sticky CTA collapse) + §Pitfall 4 (hydration mismatch)
  </read_first>
  <action>
    Create NEW file `src/components/services/service-detail-overlay.tsx`. Must start with `"use client"` directive.

    Imports (exact):
    - `useState`, `useEffect` from "react"
    - `X` from "lucide-react" (the only allowed lucide import in this file — for the close button)
    - `Link` from "next/link"
    - `Dialog`, `DialogContent`, `DialogHeader`, `DialogTitle`, `DialogDescription` from "@/components/ui/dialog"
    - `Drawer`, `DrawerContent`, `DrawerHeader`, `DrawerTitle`, `DrawerDescription`, `DrawerClose` from "@/components/ui/drawer"
    - `ServiceDetailPanel`, `type Service`, `type PortfolioItemLite` from "./service-detail-panel"
    - `cn` from "@/lib/utils"

    Define a PRIVATE `useMediaQuery(query: string): boolean` hook (port verbatim from license-modal.tsx lines 29-46). DO NOT extract to a shared file in this phase (RESEARCH §Pattern 3 + Open Question 2 explicitly defer).

    Add a hydration-safe mount guard (RESEARCH §Pitfall 4 strong fix — recommended because this overlay auto-opens on deep-link, which is exactly when the mismatch is most visible):
    ```
    const [mounted, setMounted] = useState(false)
    useEffect(() => setMounted(true), [])
    ```
    Return `null` from the component when `!mounted` (defer all overlay rendering until post-hydration).

    Component signature: `export function ServiceDetailOverlay({ service, portfolioItems, onClose }: ServiceDetailOverlayProps)`.

    Behavior:
    - If `service === null`: return null
    - If `!mounted`: return null
    - `isDesktop = useMediaQuery("(min-width: 768px)")`
    - `isOpen = service !== null` (controlled by parent; passed to `<Drawer open={...}>` and `<Dialog open={...}>`)
    - `ctaLabel = service.isBookable ? "BOOK THIS SERVICE" : "REQUEST A QUOTE"` (UI-SPEC §17)
    - `ctaHref = service.isBookable ? `/book?service=${service.slug}` : `/contact?service=${service.slug}``

    Render branching:

    Desktop (`isDesktop === true`):
    ```
    <Dialog open={isOpen} onOpenChange={(open) => !open && onClose()}>
      <DialogContent
        showCloseButton={false}
        className={cn(
          "max-w-[720px] rounded-none border border-[#222] bg-[#111] p-0",
          "!grid !grid-rows-[auto_minmax(0,1fr)_auto] !gap-0",
          "max-h-[85vh]"
        )}
      >
        <DialogHeader className="border-b border-[#222] p-4 m-0">
          <DialogTitle asChild>
            <h2 className="font-mono font-bold uppercase tracking-[0.05em] text-[#f5f5f0] text-[22px] md:text-[28px] leading-[1.15]">
              {service.name}
            </h2>
          </DialogTitle>
          <DialogDescription className="sr-only">
            Details and booking for {service.name}.
          </DialogDescription>
        </DialogHeader>
        <div className="overflow-y-auto">
          <ServiceDetailPanel service={service} portfolioItems={portfolioItems} />
        </div>
        <div className="border-t border-[#222] bg-[#111] p-4">
          <Link
            href={ctaHref}
            className="block w-full text-center bg-[#f5f5f0] text-[#000] font-mono font-bold text-[13px] uppercase tracking-[0.05em] px-6 py-3 rounded-none transition-colors duration-150 hover:bg-[#e5e5e0]"
          >
            {ctaLabel}
          </Link>
        </div>
        <button
          type="button"
          onClick={onClose}
          aria-label="Close"
          className="absolute right-3 top-3 cursor-pointer text-[#555] transition-colors duration-150 hover:text-[#f5f5f0]"
        >
          <X className="h-4 w-4" />
        </button>
      </DialogContent>
    </Dialog>
    ```

    Mobile (`isDesktop === false`):
    ```
    <Drawer open={isOpen} onOpenChange={(open) => !open && onClose()}>
      <DrawerContent
        data-testid="service-detail-drawer"
        className={cn(
          "rounded-none border-t border-[#222] bg-[#111]",
          "data-[vaul-drawer-direction=bottom]:!rounded-none",
          "data-[vaul-drawer-direction=bottom]:!mt-0",
          "data-[vaul-drawer-direction=bottom]:!max-h-[92vh]",
          "!grid grid-rows-[auto_minmax(0,1fr)_auto]"
        )}
      >
        <DrawerHeader className="border-b border-[#222] p-4 text-left">
          <DrawerTitle asChild>
            <h2 className="font-mono font-bold uppercase tracking-[0.05em] text-[#f5f5f0] text-[22px] leading-[1.15]">
              {service.name}
            </h2>
          </DrawerTitle>
          <DrawerDescription className="sr-only">
            Details and booking for {service.name}.
          </DrawerDescription>
        </DrawerHeader>
        <div className="overflow-y-auto">
          <ServiceDetailPanel service={service} portfolioItems={portfolioItems} />
        </div>
        <div className="border-t border-[#222] bg-[#111] p-4">
          <Link
            href={ctaHref}
            className="block w-full text-center bg-[#f5f5f0] text-[#000] font-mono font-bold text-[13px] uppercase tracking-[0.05em] px-6 py-3 rounded-none transition-colors duration-150 hover:bg-[#e5e5e0]"
          >
            {ctaLabel}
          </Link>
        </div>
        <DrawerClose
          aria-label="Close"
          className="absolute right-3 top-3 cursor-pointer text-[#555] transition-colors duration-150 hover:text-[#f5f5f0]"
        >
          <X className="h-4 w-4" />
          <span className="sr-only">Close</span>
        </DrawerClose>
      </DrawerContent>
    </Drawer>
    ```

    CRITICAL implementation notes (RESEARCH §Pattern 4):
    - The `!grid grid-rows-[auto_minmax(0,1fr)_auto]` on DrawerContent (and DialogContent) is LOAD-BEARING. Without it, the scroll body collapses to 0 height when content is short and the sticky CTA falls below the fold. DO NOT replace with `flex flex-col` — that fails on short content (see RESEARCH §Pitfall 2).
    - The `!` prefix on Vaul classes is required because Vaul's defaults (`drawer.tsx` line 59) apply via data-attribute variants which have higher specificity than `cn()` ordering can resolve (RESEARCH §Pitfall 1).
    - The sticky CTA is the BOTTOM grid row, NOT `sticky bottom-0`. Both patterns together cause double-pin bugs (RESEARCH §Pattern 4 "Don't also add sticky bottom-0").
    - `DialogDescription` + `DrawerDescription` with `sr-only` class satisfies the a11y `aria-describedby` requirement (Radix warns when missing).
    - `DialogTitle asChild` + plain `<h2>` overrides the default `font-heading` styling from `dialog.tsx`. Same for `DrawerTitle asChild`.

    Anti-pattern lock-outs:
    - NO `flex flex-col flex-1 overflow-y-auto` on the body (that's the failing pattern from prior attempts — use grid)
    - NO `sticky bottom-0` on the CTA wrapper (it IS the bottom grid row)
    - NO Tailwind-named color tokens
    - NO `rounded-md/lg/xl/2xl/3xl` (only `rounded-none`)
    - NO `shadow-*`
    - NO `duration-300` or higher
  </action>
  <verify>
    <automated>
      pnpm tsc --noEmit --pretty false 2>&1 | grep -E "service-detail-overlay" | head -10
    </automated>
    <automated>
      pnpm lint src/components/services/service-detail-overlay.tsx 2>&1 | tail -20
    </automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/services/service-detail-overlay.tsx` exists
    - First non-blank line is `"use client"` — `head -1 src/components/services/service-detail-overlay.tsx | grep -q '"use client"'`
    - Exports `ServiceDetailOverlay` — `grep -c 'export function ServiceDetailOverlay' src/components/services/service-detail-overlay.tsx` equals 1
    - `useMediaQuery` private hook present — `grep -c 'function useMediaQuery' src/components/services/service-detail-overlay.tsx` equals 1
    - Hydration mount guard present — `grep -c 'setMounted(true)' src/components/services/service-detail-overlay.tsx` equals 1 AND `grep -c 'if (!mounted)' src/components/services/service-detail-overlay.tsx` is at least 1
    - Imports both Drawer and Dialog primitives — `grep -c 'from "@/components/ui/drawer"' src/components/services/service-detail-overlay.tsx` equals 1 AND `grep -c 'from "@/components/ui/dialog"' src/components/services/service-detail-overlay.tsx` equals 1
    - CRITICAL — Vaul override classes ALL present (RESEARCH §Pitfall 1):
      - `grep -c 'data-\[vaul-drawer-direction=bottom\]:!rounded-none' src/components/services/service-detail-overlay.tsx` equals 1
      - `grep -c 'data-\[vaul-drawer-direction=bottom\]:!mt-0' src/components/services/service-detail-overlay.tsx` equals 1
      - `grep -c 'data-\[vaul-drawer-direction=bottom\]:!max-h-\[92vh\]' src/components/services/service-detail-overlay.tsx` equals 1
    - CRITICAL — grid-rows layout present on BOTH Dialog and Drawer (RESEARCH §Pattern 4): `grep -c 'grid-rows-\[auto_minmax(0,1fr)_auto\]' src/components/services/service-detail-overlay.tsx` equals 2 (one Dialog, one Drawer)
    - Dialog max-width 720px present — `grep -c 'max-w-\[720px\]' src/components/services/service-detail-overlay.tsx` equals 1
    - CTA labels (UI-SPEC §17): `grep -c 'BOOK THIS SERVICE' src/components/services/service-detail-overlay.tsx` is at least 1 AND `grep -c 'REQUEST A QUOTE' src/components/services/service-detail-overlay.tsx` is at least 1
    - `isBookable` ternary drives CTA: `grep -c 'service\.isBookable' src/components/services/service-detail-overlay.tsx` is at least 2 (once for label, once for href)
    - a11y description nodes present — `grep -c 'DialogDescription' src/components/services/service-detail-overlay.tsx` is at least 1 AND `grep -c 'DrawerDescription' src/components/services/service-detail-overlay.tsx` is at least 1 AND `grep -c 'sr-only' src/components/services/service-detail-overlay.tsx` is at least 2
    - useMediaQuery break at 768px: `grep -c 'min-width: 768px' src/components/services/service-detail-overlay.tsx` equals 1
    - Anti-pattern lock — NO `flex-1 overflow-y-auto` pattern: `grep -cE 'flex-1[^"]*overflow-y-auto|overflow-y-auto[^"]*flex-1' src/components/services/service-detail-overlay.tsx` equals 0
    - Anti-pattern lock — NO `sticky bottom-0` (CTA is the bottom grid row): `grep -c 'sticky bottom-0' src/components/services/service-detail-overlay.tsx` equals 0
    - Anti-pattern lock — NO Tailwind named colors: `grep -cE 'bg-(gray|zinc|neutral|slate)-|text-(gray|zinc|neutral|slate)-' src/components/services/service-detail-overlay.tsx` equals 0
    - Anti-pattern lock — NO non-zero rounded: `grep -vE 'rounded-none' src/components/services/service-detail-overlay.tsx | grep -cE 'rounded-(md|lg|xl|2xl|3xl|full)'` equals 0
    - Anti-pattern lock — NO shadows: `grep -cE '\bshadow-' src/components/services/service-detail-overlay.tsx` equals 0
    - Anti-pattern lock — NO long durations: `grep -cE 'duration-(300|500|700|1000)' src/components/services/service-detail-overlay.tsx` equals 0
    - TypeScript clean — `pnpm tsc --noEmit --pretty false 2>&1 | grep -cE "service-detail-overlay.*error"` equals 0
    - Lint clean — `pnpm lint src/components/services/service-detail-overlay.tsx` returns 0 errors
  </acceptance_criteria>
  <done>
    Overlay switches Drawer↔Dialog at 768px, sticky CTA pinned via grid-rows layout (NOT flex-col + sticky), Vaul defaults overridden with `!`-important classes, hydration-safe mount guard prevents auto-open race, a11y description nodes present.
  </done>
</task>

<task type="auto">
  <name>Task 3: Rewrite ServiceGrid as grid wrapper + Custom Request tile + deep-link hash sync</name>
  <files>src/components/services/service-grid.tsx</files>
  <read_first>
    - src/components/services/service-grid.tsx (current FULL FILE — discard master-detail/accordion, KEEP the deep-link hash effect at lines 277-295 and the handleTileClick pattern at lines 291-295)
    - .planning/phases/48.4-services-page-industry-leading-redesign/48.4-UI-SPEC.md §3 (grid breakpoints) + §6 (wireframes for 2-col mobile / 3-col desktop) + §7 (Custom Request tile spec) + §10 (empty state spec)
    - .planning/phases/48.4-services-page-industry-leading-redesign/48.4-RESEARCH.md §Pattern 6 (deep-link preserve verbatim) + §Pitfall 6 (auto-open setTimeout deferral is load-bearing) + Open Question 3 (Custom Request index = N+1)
    - ./CLAUDE.md "Ranking filter safety" rule — the `setTimeout(0)` deferral is the SAME pattern that fixed the rankings filter crash (commit `6af8177`). Preserve it.
  </read_first>
  <action>
    REWRITE existing file `src/components/services/service-grid.tsx` (this is a full rewrite — the existing master-detail + accordion implementation is discarded). Must start with `"use client"` directive.

    Imports (exact):
    - `useState`, `useEffect` from "react"
    - `Link` from "next/link"
    - `ServiceCard` from "./service-card"
    - `ServiceDetailOverlay` from "./service-detail-overlay"
    - `type Service`, `type PortfolioItemLite` from "./service-detail-panel"

    Re-export the types for downstream use (page.tsx may already import them):
    ```
    export type { Service, PortfolioItemLite } from "./service-detail-panel"
    ```

    Props interface (export):
    ```
    interface ServiceGridProps {
      services: Service[]
      portfolioByServiceId: Record<string, PortfolioItemLite[]>
    }
    ```

    Custom Request tile shape — define as a module-level constant or inline literal inside the component:
    ```
    const CUSTOM_REQUEST_TILE_SERVICE_SHAPE = {
      slug: "custom",
      name: "CUSTOM REQUEST",
      shortDescription: "Anything we didn't list. Tell us what you need.",
      priceLabel: "custom",            // formatPriceChip → "CUSTOM"
      durationMinutes: null,           // chip omitted; we'll override visually below for "BY BRIEF"
    }
    ```
    NOTE: The Custom Request tile needs `BY BRIEF` as the SECOND chip (UI-SPEC §7), which `formatDurationChip(null)` will return null for. Therefore the Custom Request tile cannot use the standard ServiceCard primitive 1:1 without modification. Resolution per RESEARCH Open Question 3 and UI-SPEC §7 (which says "same card primitive"): render the Custom Request tile as a SIBLING `<Link>` element with the same className/styles as ServiceCard but with both chips hardcoded (`CUSTOM` + `BY BRIEF`), wrapped in a `<Link href="/contact?service=custom">` instead of a `<button>`. This keeps visual parity (UI-SPEC §7: "reads as the sixth equal option") while honoring §7's direct-nav-no-overlay rule.

    Inline the Custom Request tile JSX (use the SAME className contract as ServiceCard from Plan 02 — wrap in a `<Link>` to `/contact?service=custom`, render the same dotted-grid background style, same index + name + description + two chips, same hover state). The duplication is acceptable because (a) the click target differs (Link vs button), (b) the chip set is locked literals not formatted from DB, (c) extracting a shared "TileShell" primitive is over-engineering for one extra tile. Document inline why duplicated.

    Component:
    `export function ServiceGrid({ services, portfolioByServiceId }: ServiceGridProps)`

    State:
    - `const [selectedSlug, setSelectedSlug] = useState<string | null>(null)` (NOT services[0]?.slug — overlay starts closed; was a master-detail leftover)

    Deep-link useEffect — port from current service-grid.tsx lines 277-287 with the `setTimeout(0)` deferral PRESERVED VERBATIM (RESEARCH §Pitfall 6 + CLAUDE.md ranking filter safety — load-bearing):
    ```
    useEffect(() => {
      if (typeof window === "undefined") return
      const hash = window.location.hash.slice(1)
      if (!hash || !services.some((s) => s.slug === hash)) return

      const timeoutId = window.setTimeout(() => {
        setSelectedSlug(hash)
      }, 0)

      return () => window.clearTimeout(timeoutId)
    }, [services])
    ```

    Handler — `handleTileOpen(slug: string)`:
    ```
    const handleTileOpen = (slug: string) => {
      setSelectedSlug(slug)
      if (typeof window !== "undefined") {
        window.history.replaceState(null, "", `#${slug}`)
      }
    }
    ```

    Handler — `handleOverlayClose()`:
    ```
    const handleOverlayClose = () => {
      setSelectedSlug(null)
      if (typeof window !== "undefined") {
        window.history.replaceState(null, "", window.location.pathname)
      }
    }
    ```

    Derived: `const selectedService = selectedSlug ? services.find((s) => s.slug === selectedSlug) ?? null : null`

    Render:

    1. Empty state (UI-SPEC §10, UI-SVC-16): if `services.length === 0`:
       ```
       <section id="service-grid" className="grid grid-cols-2 gap-1 md:grid-cols-3">
         <div className="col-span-2 md:col-span-3 border border-[#222] bg-[#111] py-16 px-6 flex flex-col items-center text-center gap-4">
           <h2 className="font-mono text-[20px] font-bold uppercase tracking-[0.05em] text-[#f5f5f0]">
             NO SERVICES LIVE YET
           </h2>
           <p className="font-sans text-[14px] max-w-md text-[#888]">
             We're finishing the booking setup. In the meantime, send us a message and we'll get back to you about your project.
           </p>
           <Link
             href="/contact"
             className="mt-2 border border-[#f5f5f0] px-6 py-2 font-mono text-[12px] uppercase tracking-[0.05em] text-[#f5f5f0] transition-colors duration-150 hover:bg-[#f5f5f0] hover:text-[#000]"
           >
             CONTACT US
           </Link>
         </div>
       </section>
       ```

    2. Normal state — fragment containing the grid + the overlay:
       ```
       <>
         <section id="service-grid" className="grid grid-cols-2 gap-1 md:grid-cols-3">
           {services.map((service, i) => (
             <ServiceCard
               key={service.slug}
               service={{
                 slug: service.slug,
                 name: service.name,
                 shortDescription: service.shortDescription,
                 priceLabel: service.priceLabel,
                 durationMinutes: service.durationMinutes,
               }}
               indexLabel={String(i + 1).padStart(2, "0")}
               onOpen={handleTileOpen}
             />
           ))}
           {/* Custom Request tile at position N+1 (UI-SPEC §7, RESEARCH Open Question 3) */}
           <Link
             href="/contact?service=custom"
             data-testid="custom-request-tile"
             className="group relative flex aspect-square w-full flex-col justify-between overflow-hidden md:aspect-auto md:min-h-[220px] border border-[#222] bg-[#111] p-3 md:p-5 text-left transition-colors duration-150 hover:border-[#444] hover:bg-[#1a1a1a] active:bg-[#0a0a0a] focus-visible:outline-1 focus-visible:outline-offset-2 focus-visible:outline-[#f5f5f0] rounded-none"
             style={{ backgroundImage: "radial-gradient(rgba(245,245,240,0.025) 1px, transparent 1px)", backgroundSize: "8px 8px" }}
           >
             <span
               aria-hidden
               className="pointer-events-none absolute inset-0 z-10 bg-[#f5f5f0]/10 opacity-0 group-hover:opacity-100 group-hover:animate-glitch-hover motion-reduce:hidden"
             />
             <div className="relative z-20 flex w-full items-start justify-between">
               <span className="font-mono text-[11px] font-bold uppercase tracking-[0.15em] text-[#555]">
                 {String(services.length + 1).padStart(2, "0")}
               </span>
               <span aria-hidden className="hidden md:block opacity-0 group-hover:opacity-100 transition-opacity duration-150 font-mono text-[14px] text-[#555] tracking-[0.2em]">··</span>
             </div>
             <div className="relative z-20 flex flex-col gap-1">
               <h3 className="font-mono font-bold uppercase tracking-[0.05em] text-[#f5f5f0] text-[16px] leading-[1.1] md:text-[22px] md:leading-[1.15]">
                 CUSTOM REQUEST
               </h3>
               <p className="font-sans text-[#888] text-[11px] line-clamp-1 md:text-[13px] md:line-clamp-2">
                 Anything we didn't list. Tell us what you need.
               </p>
             </div>
             <div className="relative z-20 flex flex-wrap gap-1">
               <span className="rounded-none border border-[#333] bg-[#222] px-1.5 py-0.5 font-mono text-[11px] uppercase text-[#f5f5f0] md:px-2 md:py-1">CUSTOM</span>
               <span className="rounded-none border border-[#333] bg-[#222] px-1.5 py-0.5 font-mono text-[11px] uppercase text-[#f5f5f0] md:px-2 md:py-1">BY BRIEF</span>
             </div>
           </Link>
         </section>
         <ServiceDetailOverlay
           service={selectedService}
           portfolioItems={selectedService ? (portfolioByServiceId[selectedService.id] ?? []) : []}
           onClose={handleOverlayClose}
         />
       </>
       ```

    CRITICAL preserved patterns (do NOT remove):
    - `setTimeout(window.setTimeout(() => setSelectedSlug(hash), 0))` — deferral is load-bearing (CLAUDE.md ranking filter safety, RESEARCH §Pitfall 6)
    - `window.history.replaceState(null, "", `#${slug}`)` — opens hash without router re-render (RESEARCH §Pattern 6)
    - `window.history.replaceState(null, "", window.location.pathname)` — close clears hash; `pathname` (not literal `/services`) supports preview deploys

    Anti-pattern lock-outs (this file is THE critical guardrail file for mobile-grid-cols and master-detail eradication):
    - NO `grid-cols-1` on mobile (AP-03 lock): grid MUST be `grid-cols-2`
    - NO master-detail layout: `md:grid-cols-[1fr_1fr]` pattern from old file MUST NOT appear
    - NO accordion: no `<AnimatePresence>` with `height: "auto"` animations
    - NO selected-tile inversion: do NOT render a `bg-[#f5f5f0] text-[#000]` selected card state
    - NO inline ServiceDetailPanel rendering: panel lives in overlay only
    - NO motion/react / AnimatePresence imports (the overlay-open animation is handled by Vaul/Radix transitions)
    - NO clsx import (use cn from @/lib/utils if any conditional className is needed, but the rewrite shouldn't need it)
    - NO Tailwind-named color tokens
    - NO `rounded-md/lg/xl/2xl/3xl/full` (only `rounded-none`)
    - NO `shadow-*`
    - NO `duration-300` or higher
  </action>
  <verify>
    <automated>
      pnpm tsc --noEmit --pretty false 2>&1 | grep -E "service-grid" | head -10
    </automated>
    <automated>
      pnpm lint src/components/services/service-grid.tsx 2>&1 | tail -20
    </automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/services/service-grid.tsx` exists (rewrite, not new file)
    - First non-blank line is `"use client"` — `head -1 src/components/services/service-grid.tsx | grep -q '"use client"'`
    - Exports `ServiceGrid` — `grep -c 'export function ServiceGrid' src/components/services/service-grid.tsx` equals 1
    - Re-exports types — `grep -c 'export type { Service, PortfolioItemLite }' src/components/services/service-grid.tsx` equals 1 (or two `export type` lines if separated)
    - Grid root has `id="service-grid"` (Hero CTAs scroll here): `grep -c 'id="service-grid"' src/components/services/service-grid.tsx` is at least 1
    - Mobile 2-col grid present — `grep -cE 'grid-cols-2[^"]*md:grid-cols-3|grid-cols-2.*md:grid-cols-3' src/components/services/service-grid.tsx` is at least 1 (in either order)
    - HARD anti-pattern lock — NO `grid-cols-1` (AP-03): `grep -c 'grid-cols-1' src/components/services/service-grid.tsx` equals 0
    - HARD anti-pattern lock — NO master-detail pattern: `grep -c 'grid-cols-\[1fr_1fr\]' src/components/services/service-grid.tsx` equals 0
    - HARD anti-pattern lock — NO AnimatePresence (accordion was the only use here): `grep -c 'AnimatePresence' src/components/services/service-grid.tsx` equals 0 AND `grep -c "from \"motion/react\"" src/components/services/service-grid.tsx` equals 0
    - HARD anti-pattern lock — NO inline ServiceDetailPanel rendering: `grep -c 'ServiceDetailPanel' src/components/services/service-grid.tsx` equals 0 (panel is consumed by overlay, not directly by grid)
    - HARD anti-pattern lock — NO selected-tile inversion: `grep -cE 'bg-\[#f5f5f0\][^"]*text-\[#000\]' src/components/services/service-grid.tsx` returns 0 OR exactly 1 if it appears in the empty-state CONTACT US button (acceptable — that's a CTA, not a tile state). To be safe, the lock should be: `grep -cE 'aria-selected=true.*bg-\[#f5f5f0\]' src/components/services/service-grid.tsx` equals 0.
    - Deep-link useEffect with setTimeout deferral PRESERVED — `grep -c 'window.setTimeout' src/components/services/service-grid.tsx` is at least 1 AND `grep -c 'setSelectedSlug(hash)' src/components/services/service-grid.tsx` equals 1
    - handleTileOpen pushes hash — `grep -c 'history.replaceState(null, "", `#' src/components/services/service-grid.tsx` is at least 1
    - handleOverlayClose clears hash via pathname — `grep -c 'window.location.pathname' src/components/services/service-grid.tsx` is at least 1
    - Custom Request tile present at N+1 — `grep -c 'CUSTOM REQUEST' src/components/services/service-grid.tsx` is at least 1 AND `grep -c 'href="/contact?service=custom"' src/components/services/service-grid.tsx` is at least 1
    - Custom Request tile has BOTH `CUSTOM` and `BY BRIEF` chips: `grep -c '>CUSTOM<' src/components/services/service-grid.tsx` is at least 1 AND `grep -c '>BY BRIEF<' src/components/services/service-grid.tsx` is at least 1
    - Custom Request tile has data-testid for Playwright: `grep -c 'data-testid="custom-request-tile"' src/components/services/service-grid.tsx` equals 1
    - Empty state — `grep -c 'NO SERVICES LIVE YET' src/components/services/service-grid.tsx` equals 1 AND `grep -c 'col-span-2 md:col-span-3' src/components/services/service-grid.tsx` is at least 1
    - ServiceDetailOverlay rendered with service + portfolioItems + onClose props — `grep -c '<ServiceDetailOverlay' src/components/services/service-grid.tsx` equals 1
    - Anti-pattern lock — NO Tailwind named colors: `grep -cE 'bg-(gray|zinc|neutral|slate)-|text-(gray|zinc|neutral|slate)-' src/components/services/service-grid.tsx` equals 0
    - Anti-pattern lock — NO non-zero rounded: `grep -vE 'rounded-none' src/components/services/service-grid.tsx | grep -cE 'rounded-(md|lg|xl|2xl|3xl|full)'` equals 0
    - Anti-pattern lock — NO shadows: `grep -cE '\bshadow-' src/components/services/service-grid.tsx` equals 0
    - Anti-pattern lock — NO long durations: `grep -cE 'duration-(300|500|700|1000)' src/components/services/service-grid.tsx` equals 0
    - TypeScript clean — `pnpm tsc --noEmit --pretty false 2>&1 | grep -cE "service-grid.*error"` equals 0
    - Lint clean — `pnpm lint src/components/services/service-grid.tsx` returns 0 errors
  </acceptance_criteria>
  <done>
    Grid renders 2-col mobile / 3-col desktop, Custom Request tile is the (N+1)th cell as a direct `/contact?service=custom` Link, overlay opens on tile click + closes via callback, hash sync preserved with load-bearing setTimeout deferral, empty state covered. Zero master-detail/accordion/AP-03 patterns remain.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL hash ↔ ServiceGrid effect | `window.location.hash.slice(1)` is user-controllable; validated against `services.some((s) => s.slug === hash)` before any state write — no XSS or open-redirect risk |
| ServiceGrid → Overlay | `service` prop is a server-fetched DB row passed through internal state; no user-input flows here |
| Overlay → /book and /contact Links | hrefs interpolate `service.slug` (DB-controlled identifier); slugs are admin-controlled (operator trust) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48.4-03-01 | Tampering | URL hash → setSelectedSlug | mitigate | `services.some((s) => s.slug === hash)` guard before state write rejects unknown slugs |
| T-48.4-03-02 | Tampering | history.replaceState arguments | accept | Hash is `#${validated slug}`; pathname-clear uses `window.location.pathname` (origin/path Vercel-controlled) |
| T-48.4-03-03 | Information Disclosure | Service refundPolicy / description rendered in overlay | accept | React text auto-escapes; refundPolicy is admin-controlled DB content; no end-user input crosses |
| T-48.4-03-04 | Denial of Service | Deep-link auto-open race | mitigate | `setTimeout(0)` deferral defers Drawer/Dialog mount to next task tick (RESEARCH §Pitfall 6 — same fix as rankings filter crash) |
| T-48.4-03-05 | Spoofing | Custom Request Link → /contact?service=custom | accept | Static string literal; no slug interpolation; the `/contact` handler is responsible for query-param sanitization (out of scope, pre-existing) |
</threat_model>

<verification>
- `pnpm tsc --noEmit --pretty false` passes (no NEW errors in any of the 3 files)
- `pnpm lint src/components/services/{service-grid,service-detail-overlay,service-detail-panel}.tsx` returns 0 errors
- All acceptance_criteria greps return the expected counts
- ServiceGrid imports build path: ServiceCard (Plan 02) + ServiceDetailOverlay (this plan) + types from ServiceDetailPanel (this plan) all resolve at module-load time
</verification>

<success_criteria>
- UI-SVC-04 (2-col mobile, 3-col desktop), UI-SVC-07 (Custom Request position 6 direct-nav), UI-SVC-08 (Drawer/Dialog 768px split), UI-SVC-09 (sticky bottom CTA + grid-rows layout — implementation contract), UI-SVC-10 (overlay content order from ServiceDetailPanel), UI-SVC-11 (deep-link hash with setTimeout deferral + close clear), UI-SVC-16 (empty state spanning grid) all satisfied at component layer
- Zero AP-03 (mobile 1-col), zero AP-09 (inline detail in grid), zero selected-tile inversion patterns remain
- Vaul defaults overridden with `!`-important classes; grid-rows-[auto_minmax(0,1fr)_auto] body layout in place — sticky CTA cannot collapse on short content
- Page composition (Plan 04) can now import { ServiceGrid } from this file and the grid will JUST WORK
</success_criteria>

<output>
Create `.planning/phases/48.4-services-page-industry-leading-redesign/48.4-03-SUMMARY.md` when done.
</output>
