---
phase: 06.1-homepage-flair
plan: 02
type: execute
wave: 2
depends_on: ["06.1-01"]
files_modified:
  - tests/homepage-flair.spec.ts
  - .planning/phases/06.1-homepage-flair/screenshots/
autonomous: false
requirements: [HOME-01, HOME-03]

must_haves:
  truths:
    - "Playwright screenshots captured at 1440px and 375px confirming all visual changes"
    - "User has verified the homepage flair looks correct at both viewport sizes"
  artifacts:
    - path: "tests/homepage-flair.spec.ts"
      provides: "Playwright screenshot test for homepage flair"
      contains: "screenshot"
    - path: ".planning/phases/06.1-homepage-flair/screenshots/"
      provides: "Visual evidence of implementation at both viewports"
  key_links:
    - from: "tests/homepage-flair.spec.ts"
      to: "https://glitch-studios.codebox.local"
      via: "Playwright page.goto"
      pattern: "glitch-studios"
---

<objective>
Capture Playwright screenshots of the homepage at 1440px and 375px to verify all Phase 06.1 visual changes, then present to user for approval.

Purpose: v2 Quality Overhaul requires Playwright-verified visual output at both desktop and mobile viewports.
Output: Screenshots at both viewports + user approval
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06.1-homepage-flair/06.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create and run Playwright screenshot test</name>
  <files>tests/homepage-flair.spec.ts</files>
  <read_first>
    src/app/(public)/page.tsx,
    src/components/home/hero-section.tsx,
    src/components/home/splash-overlay.tsx
  </read_first>
  <action>
    **1. Create Playwright config if not exists (`playwright.config.ts`):**
    - Only create if the file does not already exist at project root
    - Base URL: `https://glitch-studios.codebox.local`
    - `ignoreHTTPSErrors: true` (Caddy self-signed cert)
    - Projects: `desktop` (viewport 1440x900) and `mobile` (viewport 375x812)
    - Output dir: `test-results`
    - No `webServer` block (dev server managed by PM2)

    **2. Create `tests/homepage-flair.spec.ts`:**
    - Test: "Homepage flair visual verification"
    - Before screenshots: set `sessionStorage.setItem("glitch-splash-seen", "true")` via `page.addInitScript()` to skip splash animation (we want to capture the actual page content, not a black overlay)
    - Navigate to `/` and wait for network idle
    - Take full-page screenshot at current viewport, save to `.planning/phases/06.1-homepage-flair/screenshots/homepage-{project}-full.png`
    - Take hero-section-only screenshot (clip to first viewport height or use locator `section` first child), save to `.planning/phases/06.1-homepage-flair/screenshots/hero-{project}.png`
    - Second test: "Splash overlay renders on first visit"
    - Do NOT set sessionStorage before this test
    - Navigate to `/`, wait 500ms (splash should be visible)
    - Screenshot the splash overlay, save to `.planning/phases/06.1-homepage-flair/screenshots/splash-{project}.png`

    **3. Ensure screenshots directory exists:**
    - `mkdir -p .planning/phases/06.1-homepage-flair/screenshots`

    **4. Run the test:**
    - `npx playwright test tests/homepage-flair.spec.ts --reporter=list`
    - If Playwright browsers not installed, run `npx playwright install chromium` first

    **5. Verify screenshots were created:**
    - List files in `.planning/phases/06.1-homepage-flair/screenshots/`
    - Confirm at least 4 screenshots exist (hero desktop, hero mobile, full desktop, full mobile)
  </action>
  <verify>
    <automated>ls -la /home/faxas/workspaces/projects/personal/glitch_studios/.planning/phases/06.1-homepage-flair/screenshots/*.png 2>&1 | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/homepage-flair.spec.ts` exists and contains `screenshot`, `glitch-splash-seen`, `sessionStorage`
    - At least 4 PNG files exist in `.planning/phases/06.1-homepage-flair/screenshots/`
    - Screenshots include both 1440px and 375px viewport captures
  </acceptance_criteria>
  <done>
    - Playwright test created and executed successfully
    - Screenshots captured at both viewports
    - Screenshots saved to phase screenshots directory
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of homepage flair</name>
  <files>.planning/phases/06.1-homepage-flair/screenshots/</files>
  <action>
    Present screenshots to user and ask them to verify the homepage flair implementation at both desktop (1440px) and mobile (375px) viewports. Guide them through the verification steps below.
  </action>
  <verify>User types "approved"</verify>
  <done>User has approved the visual implementation of all homepage flair changes</done>
  <what-built>
    Homepage flair implementation: splash logo animation, real logo in hero and footer, animated scroll indicator, enhanced scroll effects with variants, and increased beat card spacing. Playwright screenshots captured at 1440px and 375px.
  </what-built>
  <how-to-verify>
    1. Review the screenshots in `.planning/phases/06.1-homepage-flair/screenshots/`
    2. Visit https://glitch-studios.codebox.local in a browser
    3. Open an incognito/private window to trigger the splash animation (fresh session)
    4. Verify the splash shows the real Glitch logo with a dramatic entrance, then transitions to the hero
    5. Verify the hero section shows the real logo image (not "GLITCH" text), with glitch effect on hover
    6. Verify the scroll indicator arrow bounces at the bottom of the hero
    7. Scroll down to verify beat cards have visible spacing between them
    8. Scroll through all sections to see scroll-driven entrance effects
    9. Verify the footer shows the real logo image
    10. Resize browser to 375px width to check mobile layout
    11. Refresh the page to confirm splash does NOT replay (sessionStorage gate)
  </how-to-verify>
  <resume-signal>Type "approved" to complete the phase, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. Playwright tests pass
2. Screenshots exist at both viewports
3. User approves visual output
</verification>

<success_criteria>
- Screenshots captured at 1440px and 375px showing all homepage flair changes
- User approves the visual implementation
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-homepage-flair/06.1-02-SUMMARY.md`
</output>
