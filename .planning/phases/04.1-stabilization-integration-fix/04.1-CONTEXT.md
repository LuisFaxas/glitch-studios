# Phase 04.1: Stabilization & Integration Fix - Context

**Gathered:** 2026-03-27
**Status:** Ready for planning

<domain>
## Phase Boundary

Make every built feature actually work end-to-end in a browser. Fix the postgres client-bundle crash that blocks all public pages, fix admin auth 500, configure all missing third-party services, seed the database with realistic data, and verify every route renders without errors. This phase produces zero new features — it makes existing features functional.

</domain>

<decisions>
## Implementation Decisions

### Service Configuration
- **D-01:** Configure ALL missing services now (Resend, CRON_SECRET). No graceful degradation — everything should work for real.
- **D-02:** Sign up for Resend, use test domain (`onboarding@resend.dev`) for now since no custom domain is available yet. Plan includes Resend account setup and API key generation.
- **D-03:** Set ADMIN_EMAIL env var for contact form notification routing and email reply-to.
- **D-04:** Generate and set CRON_SECRET for Vercel Cron authentication (booking reminders, scheduled blog posts).
- **D-05:** Twilio SMS already gracefully degrades (existing `sendSms` returns silently when unconfigured) — leave unconfigured for now.

### Testing & Verification
- **D-06:** Route verification approach to be determined by research — investigate industry-leading smoke testing solutions for Next.js apps.
- **D-07:** Full Stripe test mode for payment flows — use test cards to complete a beat purchase and booking deposit end-to-end. Verify webhooks fire and emails send.

### Database Seeding
- **D-08:** Test BOTH empty state (verify graceful fallbacks work) AND seeded state (verify full experience).
- **D-09:** Seed data must be realistic — Glitch Studios branding, real-sounding beat names, proper service pricing, studio-quality content. Good for demos and screenshots.
- **D-10:** Create admin seed user with configurable credentials so admin dashboard is immediately testable.

### Fix Priority Order
- **D-11:** Follow dependency order: 1) Fix postgres bundle leak (unblocks all public pages), 2) Fix auth 500 (unblocks admin), 3) Configure services (Resend, CRON_SECRET, env vars), 4) Seed database + verify all routes.

### Claude's Discretion
- Exact approach to fixing postgres client-bundle leak (may involve dynamic imports, barrel file restructuring, or "use server" annotations)
- Route verification tooling choice (Playwright, curl scripts, custom test runner — research will determine)
- Seed script structure and data volume (enough to demonstrate all features)
- Whether to create a shared seed utility or inline script

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Database & Bundling
- `src/lib/db.ts` — Database client using postgres-js + Drizzle. This is the import that leaks into client bundles.
- `src/components/tiles/widget-latest-post.tsx` — Server component importing db. Passed as slot to client TileNav.
- `src/components/layout/tile-nav.tsx` — "use client" component receiving server component slot.

### Authentication
- `src/lib/auth.ts` — Better Auth config with Drizzle adapter, admin plugin, owner/admin roles.
- `src/lib/auth-client.ts` — Client-side auth hooks.

### Payment Integration
- `src/lib/stripe.ts` — Stripe server client singleton.
- `src/lib/stripe-client.ts` — Stripe client-side (publishable key).
- `src/components/booking/booking-flow.tsx` — Client-side booking with Stripe Embedded Checkout.

### Email & Services
- `src/lib/email/` — Email templates and send functions (requires RESEND_API_KEY).
- `src/lib/r2.ts` — R2 storage client (already configured).
- `src/lib/sms.ts` — Twilio SMS with graceful degradation.

### Schema & Seed
- `src/db/schema.ts` — All database tables (Drizzle schema).
- `src/db/seed.ts` — Existing seed file (uses old @neondatabase/serverless — needs updating).

### Environment
- `.env.local` — Currently has: DATABASE_URL, R2_*, STRIPE_*, NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY, NEXT_PUBLIC_SITE_URL. Missing: RESEND_API_KEY, ADMIN_EMAIL, CRON_SECRET.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `src/lib/db.ts`: Database singleton — fix here propagates everywhere
- `src/db/seed.ts`: Existing seed script — needs updating from Neon to postgres-js driver
- Promise.allSettled pattern on home page for resilient rendering
- try/catch in widget-latest-post for DB unavailability

### Established Patterns
- Server component slots passed to client components (WidgetLatestPost → TileNav)
- "use client" boundary on all interactive components
- force-dynamic on all DB-querying pages
- Native HTML elements for forms (not shadcn inputs) per Cyberpunk Metro
- R2 direct upload pattern for media

### Integration Points
- All public pages import `db` from `@/lib/db` — fixing the bundle leak fixes all routes
- Admin routes all go through Better Auth middleware
- Stripe webhooks at `/api/stripe/webhook` handle both beat purchases and booking deposits via `metadata.type`
- Vercel Cron at scheduled endpoints authenticated by CRON_SECRET

</code_context>

<specifics>
## Specific Ideas

- Use Resend test domain initially, switch to custom domain when available
- Admin seed user must be created programmatically (not manual registration)
- Full Stripe test card flow: add beat to cart → checkout → webhook → receipt email → download
- Full booking test: select service → pick slot → deposit payment → confirmation email

</specifics>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope

</deferred>

---

*Phase: 04.1-stabilization-integration-fix*
*Context gathered: 2026-03-27*
