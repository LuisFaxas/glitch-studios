---
phase: 04.1-stabilization-integration-fix
plan: 02
subsystem: database
tags: [postgres-js, drizzle, seed, better-auth, scrypt]

requires:
  - phase: 04.1-01
    provides: server-only guard and postgres-js driver fix in db.ts
provides:
  - Comprehensive seed script populating ALL Phase 1-4 tables with realistic demo data
  - Standalone admin seed script with scrypt password hashing (no dev server required)
affects: [04.1-03, verification, demo]

tech-stack:
  added: []
  patterns: [standalone postgres-js connection for CLI scripts, Better Auth hashPassword for direct DB user creation]

key-files:
  created: []
  modified: [src/db/seed.ts, src/db/seed-admin.ts]

key-decisions:
  - "Standalone postgres connection in seed scripts (cannot import @/lib/db due to server-only guard)"
  - "Better Auth hashPassword (scrypt via @noble/hashes) for admin user creation instead of bcrypt"
  - "Owner role (not admin) for seed admin user to bypass all RBAC permission checks"
  - "ON CONFLICT upsert for idempotent re-runs of both seed scripts"

patterns-established:
  - "CLI seed scripts use standalone postgres() connection, not @/lib/db"
  - "Direct DB insert with hashPassword for user seeding outside Next.js runtime"

requirements-completed: [INFR-02, AUTH-01]

duration: 5min
completed: 2026-03-28
---

# Phase 04.1 Plan 02: Seed Script Rewrite Summary

**Postgres-js seed scripts with comprehensive Phase 1-4 data and standalone admin user creation via Better Auth scrypt hashing**

## Performance

- **Duration:** 5 min
- **Started:** 2026-03-28T04:05:05Z
- **Completed:** 2026-03-28T04:09:55Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Rewrote seed.ts from @neondatabase/serverless to postgres-js with standalone connection
- Expanded seed data from Phase 1 tables only to ALL 20+ tables across Phases 1-4 (services, beats, rooms, availability, license tiers, blog posts/tags, homepage sections, site settings, admin roles, newsletter subscribers)
- Rewrote seed-admin.ts as standalone CLI tool that creates owner user with properly hashed password via direct DB insert (no dev server dependency)

## Task Commits

Each task was committed atomically:

1. **Task 1: Rewrite seed.ts with postgres-js driver and comprehensive data** - `2aca73c` (feat)
2. **Task 2: Rewrite seed-admin.ts as standalone script with password hashing** - `42c4d03` (feat)

## Files Created/Modified
- `src/db/seed.ts` - Comprehensive seed script using postgres-js, populates all Phase 1-4 tables with realistic Glitch Studios data
- `src/db/seed-admin.ts` - Standalone admin user seeder with Better Auth scrypt password hashing, idempotent via ON CONFLICT

## Decisions Made
- Standalone postgres connection in seed scripts because db.ts has server-only guard that throws outside Next.js runtime
- Used Better Auth's hashPassword (scrypt via @noble/hashes) producing salt:hash format compatible with verifyPassword
- Set admin user role to "owner" (not "admin") so it bypasses all RBAC permission checks per permissions.ts logic
- ON CONFLICT upsert pattern for idempotent re-runs of both scripts
- Get actual userId after upsert in seed-admin.ts to handle case where user already exists with different ID

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Both seed scripts ready for execution against the database (Plan 03 verification will run them)
- Admin user creation enables login testing at /login
- Comprehensive seed data enables full site demo across all pages

---
*Phase: 04.1-stabilization-integration-fix*
*Completed: 2026-03-28*
