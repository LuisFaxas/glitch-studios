---
phase: 29.3-rebuild-filter
plan: 02
status: awaiting_user_verification
preview_url: https://glitchstudios-n8hdo5k5b-luis-faxas-projects.vercel.app
target_route: /tech/rankings/laptops
deployed_at: 2026-04-27T01:39:35Z
---

# Phase 29.3-02 — Baseline Verification (sort-header click)

## What was deployed

Plan 29.3-01 baseline fixes (commits `4960885` → `b668bba`, all on `master`):

1. **Footer hover-gate** (`src/components/layout/footer.tsx`) — no idle GPU layer for the LogoTile glitch overlays. Footer marked `"use client"`; glitchLayer1/2 only mount on hover.
2. **AudioPlayerProvider value memoization** (`src/components/player/audio-player-provider.tsx`) — context value wrapped in `useMemo` with explicit deps; no consumer re-render on incidental parent renders (PlayerBar + WidgetNowPlaying no longer cascade).
3. **WidgetNowPlaying idle branch** (`src/components/tiles/widget-now-playing.tsx`) — no `<canvas>`-backed `<Waveform>` when `currentBeat === null`; replaced with static `<div className="h-8 w-full" />` placeholder.
4. **LeaderboardTable columns memo deps cleanup** (`src/components/tech/leaderboard-table.tsx:541-542`) — deps reduced from `[benchmarkColumns, filters.sort, filters.dir]` to `[benchmarkColumns]`. No full column-def rebuild on sort.
5. **LeaderboardTable min-width: 1600px → 1280px** (`src/components/tech/leaderboard-table.tsx:624` desktop + `:708` mobile clone) — smaller GPU rasterization surface (~20% reduction at 1280px viewport, ~30% at 1600px).

Filter UI is **STILL HIDDEN**. The atomic-fix block in `leaderboard-table.tsx:596-607` is unchanged. Plan 29.3-03 will re-mount the filter only after this checkpoint passes.

## Diagnostic premise

Sort header click in the table and filter chip click both write through `setFilters` in `leaderboard-table.tsx`. If sort survives rapid interaction with the filter hidden, the filter chip will too when re-mounted in Plan 29.3-03. If sort still crashes with the filter hidden, the baseline fixes were insufficient and Plan 29.3-03 must NOT be executed without re-investigating.

## Pre-verification: Vercel SSO sign-in

The preview URL is protected by Vercel team SSO. Before running the verification steps:

1. In Safari (and again in Firefox), visit the preview URL once: `https://glitchstudios-n8hdo5k5b-luis-faxas-projects.vercel.app`
2. You will be redirected to a Vercel SSO sign-in page. Sign in with the `luisfaxas` account.
3. After sign-in completes, the preview app loads. Cookie persists for the session.

If SSO sign-in fails or the page does not load, stop and report — the rest of the verification cannot proceed without preview access.

## Verification steps

Open the preview URL on a physical macOS machine. Run the steps in BOTH browsers.

### macOS Safari

1. Open Safari → navigate to `https://glitchstudios-n8hdo5k5b-luis-faxas-projects.vercel.app/tech/rankings/laptops`
2. Wait for the leaderboard table to fully render (≥1 row visible).
3. Click the **sort header** for **BPR** 10 times rapidly (no pauses).
4. Click the **sort header** for **Glitchmark** 10 times rapidly.
5. Click the **sort header** for **Year** 10 times rapidly.
6. Click the **sort header** for **Price** 10 times rapidly.
7. Observe: tab stays responsive, no spinning beachball, no "this page is using significant memory" banner, no Web Process crash.
8. Bonus: open DevTools → Activity Monitor and confirm CPU stays under 100% sustained.

### macOS Firefox

Repeat steps 1-8 above in Firefox.

## Pass criteria

- [ ] Safari: tab survived all sort-header click batches without crashing or hanging
- [ ] Firefox: tab survived all sort-header click batches without crashing or hanging

## Result fields (user fills in)

Safari result: pending  ← change to `passed` or `failed: <description>`

Firefox result: pending  ← change to `passed` or `failed: <description>`

Status: awaiting_user_verification  ← change to `passed` (both passed) or `failed` (either crashed)

## Next step

- If both browsers pass → user updates `status: passed` in the frontmatter at the top of this file, then runs `/gsd:execute-phase 29.3-rebuild-filter` to continue with Plan 29.3-03 (filter re-mount).
- If either browser fails → user updates `status: failed` and STOPS. Do NOT execute Plan 29.3-03. Re-open the dropdown-rebuild question with a fresh `/gsd:research-phase` or `/gsd:debug` workflow.

## Deploy notes (informational)

- **Deployment ID:** `dpl_EbnYtKU3gYjrwS2zR8zaCbyVnoji`
- **Inspect URL:** `https://vercel.com/luis-faxas-projects/glitch_studios/EbnYtKU3gYjrwS2zR8zaCbyVnoji`
- **Target:** `preview`
- **Build:** Ready
- **Branch source state:** clean `master` HEAD at commit `2556282 docs(29.3-01): complete reduce-filter-path-gpu-baseline plan` — uncommitted WIP edits in working tree were stashed before deploy and restored after.
- **Env-scope deviation (Rule 3):** Initial `vercel --yes` builds (`glitchstudios-4ka58vzxf` and `glitchstudios-hqt2qeeph`) failed because Preview environment had zero env vars set — only Production scope was configured. Per Rule 3 (auto-fix blocking issue), 15 user-defined env vars (`ADMIN_EMAIL`, `BETTER_AUTH_SECRET`, `BETTER_AUTH_URL`, `CRON_SECRET`, `DATABASE_URL`, `NEXT_PUBLIC_SITE_URL`, `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`, `RESEND_API_KEY`, `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `R2_BUCKET_NAME`, `R2_SECRET_ACCESS_KEY`, `R2_ACCESS_KEY_ID`, `R2_ACCOUNT_ID`, `R2_PUBLIC_URL`) were copied from Production scope to Preview scope (all branches) via `vercel env add <name> preview "" --value "..." --force --yes`. A second build attempt failed with `database "postgres\n" does not exist` because the first batch picked up literal `\n` escape sequences from the `.env.production.local` pull file; values were re-parsed with `printf "%b"` to expand escapes properly and trimmed of trailing whitespace, then re-uploaded with `--force`. The local `.env.production.local` file was deleted after each use (it is gitignored, but the value never needs to land on disk persistently). No production env scope was modified.
