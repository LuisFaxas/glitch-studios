---
phase: 29.3-rebuild-filter
plan: 05
type: execute
wave: 5
depends_on: ["29.3-04"]
files_modified:
  - .planning/phases/29.3-rebuild-filter/29.3-05-VERIFICATION.md
autonomous: false
requirements: []
gap_closure: false

must_haves:
  truths:
    - "Vercel preview URL is captured for the post-29.3-04 branch state (filter re-mounted + Playwright crash-repro spec in place)"
    - "VERIFICATION.md exists with macOS Safari + Firefox FILTER CHIP CLICK steps (Year facet + Medal facet)"
    - "User has confirmed (in writing, in VERIFICATION.md) that filter chip click on /tech/rankings/laptops does NOT crash macOS Safari and does NOT crash macOS Firefox"
    - "If user reports a crash, this plan stops the phase — the dropdown rebuild question is re-opened"
  artifacts:
    - path: ".planning/phases/29.3-rebuild-filter/29.3-05-VERIFICATION.md"
      provides: "Final preview URL + verification steps + status field + result fields"
      contains: "https://"
  key_links:
    - from: ".planning/phases/29.3-rebuild-filter/29.3-05-VERIFICATION.md"
      to: "Vercel preview deployment of the post-29.3-04 branch"
      via: "URL captured by `vercel --yes` and pasted into the file"
      pattern: "https://[a-z0-9-]+-luis-faxas-projects\\.vercel\\.app"
---

<objective>
Final phase gate. The filter is re-mounted (Plan 29.3-03), the Playwright cross-engine spec passes (Plan 29.3-04), but the only authoritative verification of the original crash class is a real macOS Safari + macOS Firefox interaction. Deploy a Vercel preview, hand off a VERIFICATION.md with chip-click steps, and wait for user confirmation.

Purpose: Phase 29.3 ships only when the user confirms — on physical macOS — that filter chip clicks no longer crash. This is the symmetric counterpart of Plan 29.3-02 (which verified the same on the FILTER-HIDDEN baseline) but now exercises the actual filter chip click interaction with the filter re-mounted.

Output: One Vercel preview deploy + one VERIFICATION.md handoff file. User runs the steps on physical macOS Safari + Firefox and posts results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29.3-rebuild-filter/29.3-CONTEXT.md
@.planning/phases/29.3-rebuild-filter/29.3-02-SUMMARY.md
@.planning/phases/29.3-rebuild-filter/29.3-03-SUMMARY.md
@.planning/phases/29.3-rebuild-filter/29.3-04-SUMMARY.md
@.planning/debug/filter-chip-crash-mac-browsers.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Deploy Vercel preview + write VERIFICATION.md handoff (chip-click)</name>
  <files>.planning/phases/29.3-rebuild-filter/29.3-05-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/29.3-rebuild-filter/29.3-CONTEXT.md (decisions: Vercel preview verification — Plans 02 + 05)
    - .planning/phases/29.3-rebuild-filter/29.3-03-SUMMARY.md (confirm filter is re-mounted)
    - .planning/phases/29.3-rebuild-filter/29.3-04-SUMMARY.md (confirm Playwright cross-engine spec passed)
    - .planning/phases/29.3-rebuild-filter/29.3-02-VERIFICATION.md (reference: shape of the prior VERIFICATION.md)
    - .planning/debug/filter-chip-crash-mac-browsers.md (the crash class the user is checking against)
  </read_first>
  <action>
Two-step task: (1) deploy current branch to Vercel preview, capture URL; (2) write the handoff file the user reads.

Step 1 — Deploy. From the project root, run:

  vercel --yes

Capture the preview URL from stdout. It will match `https://[a-z0-9-]+-luis-faxas-projects\.vercel\.app`. If `vercel` returns non-zero or stdout doesn't contain a matching URL, STOP — do NOT write a partial VERIFICATION.md.

Step 2 — Use the Write tool (NOT bash heredoc) to create `.planning/phases/29.3-rebuild-filter/29.3-05-VERIFICATION.md`. The file must have a YAML frontmatter block delimited by triple-dash lines. The literal `+++` markers below denote where each YAML frontmatter delimiter line goes — the executor MUST convert each `+++` to a literal triple-dash line (three hyphens, no spaces, on its own line) when writing the file.

File content shape (substitute the captured URL):

  +++
  phase: 29.3-rebuild-filter
  plan: 05
  status: awaiting_user_verification
  preview_url: <PASTE THE CAPTURED URL HERE>
  target_route: /tech/rankings/laptops
  deployed_at: <ISO 8601 timestamp>
  +++

  # Phase 29.3-05 — Final Verification (filter chip click)

  ## What was deployed

  All five Plan 29.3 changes:
  1. (Plan 01) Footer hover-gate — no idle GPU layer for LogoTile glitch overlays
  2. (Plan 01) AudioPlayerProvider value memoization
  3. (Plan 01) WidgetNowPlaying idle branch (no <Waveform> canvas)
  4. (Plan 01) LeaderboardTable columns memo deps cleanup
  5. (Plan 01) LeaderboardTable min-width: 1600px → 100%
  6. (Plan 03) Filter UI re-mounted (LeaderboardFilters above table + LeaderboardFilterSheet on mobile)
  7. (Plan 03) data-testid="leaderboard-filters" added to filter root
  8. (Plan 04) Playwright cross-engine crash-repro spec landed (webkit + firefox both exit 0)

  The atomic-fix block in `leaderboard-table.tsx` is gone. The filter is now visible and interactive.

  ## Diagnostic premise (verified twice)

  - Plan 29.3-02 confirmed on physical macOS Safari + Firefox that sort-header click (same `setFilters` write path as filter chips) survives rapid interaction with the filter HIDDEN. So the baseline fixes worked.
  - Plan 29.3-04 confirmed that Playwright cross-engine (webkit + firefox) crash-repro test on the chip click passes — no DOM leaks, no console errors, no page exceptions.

  This plan is the final authoritative check: do REAL macOS Safari and REAL macOS Firefox tolerate the chip click now?

  ## Verification steps

  Open the preview URL on a physical macOS machine. Run the steps in BOTH browsers.

  ### macOS Safari

  1. Open Safari → navigate to `<preview_url>/tech/rankings/laptops`
  2. Wait for the leaderboard table to fully render (≥1 row visible).
  3. Confirm the filter bar is visible above the table (Price | Year | CPU | RAM | Storage | Medal | Sub-cat dropdowns).
  4. **Year facet:** Click the "Year" dropdown trigger to open it. Click the first year chip 10 times rapidly. Click a different year chip 10 times rapidly. Press Escape.
  5. **Medal facet:** Click the "Medal" dropdown trigger. Click the "Gold" chip 10 times rapidly. Click "Silver" 10 times rapidly. Press Escape.
  6. **Cross-facet:** Open Year, click a chip. Open Medal, click a chip. Open Year again, click another chip. Reset filters via the bar's reset button.
  7. **Sort header:** Click the BPR sort header 5 times. Click GlitchMark 5 times.
  8. Observe: tab stays responsive, no spinning beachball, no Web Process crash, no "this page is using significant memory" banner.
  9. Bonus: open DevTools → Activity Monitor and confirm CPU stays under 100% sustained.

  ### macOS Firefox

  Repeat steps 1-9 above in Firefox.

  ## Pass criteria

  - [ ] Safari: tab survived all chip-click and sort-click batches without crashing or hanging
  - [ ] Firefox: tab survived all chip-click and sort-click batches without crashing or hanging

  ## Result fields (user fills in)

  **Safari result:** pending  ← change to `passed` or `failed: <description>`
  **Firefox result:** pending  ← change to `passed` or `failed: <description>`
  **Status:** awaiting_user_verification  ← change to `passed` (both passed) or `failed` (either crashed)

  ## Next step

  - If both browsers pass → user updates `status: passed`. Phase 29.3 is COMPLETE. Run `/gsd:reflect` or proceed to the next phase.
  - If either browser fails → user updates `status: failed` and STOPS. Re-open the dropdown-rebuild question. The deferred items in `.planning/phases/29.3-rebuild-filter/29.3-CONTEXT.md` (TechHero scanlines, Base UI Slider, dropdown rebuild itself) become active candidates for a new debug or research phase.

When writing the file, the executor MUST replace each leading `+++` line with a triple-dash line so the resulting file has valid YAML frontmatter delimiters.
  </action>
  <verify>
    <automated>test -f .planning/phases/29.3-rebuild-filter/29.3-05-VERIFICATION.md && grep -qE 'https://[a-z0-9-]+-luis-faxas-projects\.vercel\.app' .planning/phases/29.3-rebuild-filter/29.3-05-VERIFICATION.md && grep -q 'status: awaiting_user_verification' .planning/phases/29.3-rebuild-filter/29.3-05-VERIFICATION.md && grep -q '/tech/rankings/laptops' .planning/phases/29.3-rebuild-filter/29.3-05-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/29.3-rebuild-filter/29.3-05-VERIFICATION.md` exists
    - `grep -cE 'https://[a-z0-9-]+-luis-faxas-projects\\.vercel\\.app' .planning/phases/29.3-rebuild-filter/29.3-05-VERIFICATION.md` returns at least 1
    - `grep -c 'status: awaiting_user_verification' .planning/phases/29.3-rebuild-filter/29.3-05-VERIFICATION.md` returns 1
    - `grep -c '/tech/rankings/laptops' .planning/phases/29.3-rebuild-filter/29.3-05-VERIFICATION.md` returns at least 1
    - `grep -c 'Safari result: pending' .planning/phases/29.3-rebuild-filter/29.3-05-VERIFICATION.md` returns 1
    - `grep -c 'Firefox result: pending' .planning/phases/29.3-rebuild-filter/29.3-05-VERIFICATION.md` returns 1
    - `grep -c 'Year facet' .planning/phases/29.3-rebuild-filter/29.3-05-VERIFICATION.md` returns at least 1
    - `grep -c 'Medal facet' .planning/phases/29.3-rebuild-filter/29.3-05-VERIFICATION.md` returns at least 1
    - `grep -c 'chip' .planning/phases/29.3-rebuild-filter/29.3-05-VERIFICATION.md` returns at least 5 (chip-click is the diagnostic interaction)
  </acceptance_criteria>
  <done>
    Vercel preview is live for the post-29.3-04 branch (filter re-mounted + Playwright spec landed). VERIFICATION.md handoff exists with the captured URL, target route, chip-click steps for Year + Medal facets in both Safari and Firefox, and pending result fields.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User final-verifies on physical macOS Safari + Firefox</name>
  <files>.planning/phases/29.3-rebuild-filter/29.3-05-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/29.3-rebuild-filter/29.3-05-VERIFICATION.md (the handoff written by Task 1; read to retrieve the preview URL and steps)
  </read_first>
  <action>
This is a blocking human-verification gate. The agent does NOT advance until the user posts a result.

What was built (by the full Phase 29.3 chain): Vercel preview of `master` branch with baseline GPU/render fixes (Plan 01), filter re-mounted (Plan 03), Playwright cross-engine spec (Plan 04). Filter is visible and interactive on `/tech/rankings/laptops`.

How the user verifies:
1. Open `.planning/phases/29.3-rebuild-filter/29.3-05-VERIFICATION.md` to retrieve the preview URL.
2. On a physical macOS machine, follow the "Verification steps" section in that file for BOTH macOS Safari and macOS Firefox. Steps target FILTER CHIP CLICKS (Year facet + Medal facet + cross-facet + sort-header).
3. Update the "Result fields" section in 29.3-05-VERIFICATION.md:
   - Set `Safari result:` to `passed` or `failed: <one-line description>`
   - Set `Firefox result:` to `passed` or `failed: <one-line description>`
   - Set top-level `status:` (in frontmatter AND inline) to `passed` if BOTH browsers passed, otherwise `failed`
4. Type "approved" in chat ONLY if both browsers passed. If either failed, type "blocked: <browser> crashed on chip click" and DO NOT mark the phase complete.

Resume signal: "approved" (both browsers passed — Phase 29.3 complete) OR "blocked: <browser> crashed" (either failed — re-open dropdown rebuild).
  </action>
  <verify>
    <automated>grep -qE '^status:\s*(passed|failed)$' .planning/phases/29.3-rebuild-filter/29.3-05-VERIFICATION.md</automated>
  </verify>
  <done>
    User has updated VERIFICATION.md status to either `passed` or `failed` and posted the resume signal in chat. If `passed`, Phase 29.3 is COMPLETE. If `failed`, the deferred items list in CONTEXT.md becomes the candidate set for the next investigation.
  </done>
</task>

</tasks>

<verification>
- VERIFICATION.md is committed to git (commit message documents the captured URL)
- Status field updated by user before resume
- Phase 29.3 is COMPLETE only when status: passed
- If status: failed, the deferred items list in CONTEXT.md becomes active for a new investigation phase
</verification>

<success_criteria>
- [ ] Vercel preview deployed (URL captured)
- [ ] VERIFICATION.md exists with all required fields filled (preview_url, target_route, deployed_at, chip-click steps for Year + Medal, pending result fields)
- [ ] User has run the verification on physical macOS Safari AND Firefox
- [ ] User has updated VERIFICATION.md with results
- [ ] Status is `passed` (Phase 29.3 complete) OR `failed` (re-open dropdown rebuild)
</success_criteria>

<output>
After completion, create `.planning/phases/29.3-rebuild-filter/29.3-05-SUMMARY.md` with:
- Captured Vercel preview URL
- Final status (passed | failed)
- Safari + Firefox results
- If passed: phase 29.3 is complete; note any follow-up observations the user shared
- If failed: copy of the user's failure description and the next-step decision (which deferred item becomes the next investigation candidate)
</output>
