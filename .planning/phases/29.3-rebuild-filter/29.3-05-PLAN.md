---
phase: 29.3-rebuild-filter
plan: 05
type: execute
wave: 5
depends_on: ["29.3-04"]
files_modified:
  - .planning/phases/29.3-rebuild-filter/29.3-05-VERIFICATION.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "Current branch is deployed to Vercel preview successfully."
    - "Preview URL is captured in 29.3-05-VERIFICATION.md."
    - "User has tested /tech/rankings/laptops on real macOS Safari and confirmed the tab does not die after chip click."
    - "User has tested /tech/rankings/laptops on real macOS Firefox and confirmed the tab does not die after chip click."
    - "User has confirmed first chip click registers (aria-pressed flips immediately, no spinning beachball)."
  artifacts:
    - path: ".planning/phases/29.3-rebuild-filter/29.3-05-VERIFICATION.md"
      provides: "Records the Vercel preview URL, the verification steps run, and the user's pass/fail confirmation per browser."
      contains: "Preview URL"
  key_links:
    - from: "Vercel preview deploy"
      to: "/tech/rankings/laptops"
      via: "filter chip click on real macOS Safari + Firefox"
      pattern: "tab does not die"
---

<objective>
Deploy the current branch to Vercel preview and hand off to the user for real-browser verification on macOS Safari and Firefox. This is the human gate. Per CONTEXT.md `<specifics>`: "Playwright passing is necessary but not sufficient" — the actual GPU crash only manifests on real macOS hardware, so this plan is the only path to confirm the rebuild actually fixed the bug.

Purpose: Close the loop between Plans 01-04 (which built and tested the rebuild on headless Linux) and the original failing condition (real macOS browsers crashing). The phase MUST NOT be marked complete until the user posts confirmation.

Output: A Vercel preview URL recorded in `29.3-05-VERIFICATION.md`, with the user's pass/fail signature for both Safari and Firefox.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29.3-rebuild-filter/29.3-CONTEXT.md
@.planning/phases/29.3-rebuild-filter/29.3-04-SUMMARY.md
@.planning/debug/filter-chip-crash-mac-browsers.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Deploy current branch to Vercel preview and capture URL</name>
  <files>.planning/phases/29.3-rebuild-filter/29.3-05-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/29.3-rebuild-filter/29.3-CONTEXT.md (`<decisions>` section, "Vercel Preview + Real-Browser Handoff")
    - CLAUDE.md (vercel auth context — `vercel` CLI is logged in as luisfaxas / team luis-faxas-projects)
  </read_first>
  <action>
    1. Confirm the working tree is clean enough to deploy. If there are uncommitted changes from Plans 01-04 not yet on the current branch, surface them and stop — this plan deploys what is on disk; it does not commit.

    2. Run a Vercel preview deploy from the project root:
       ```bash
       vercel --yes
       ```
       (Use `vercel --yes` for non-interactive preview, NOT `vercel --prod` — production deploy is out of scope for this plan.)

    3. Capture the preview URL printed by the CLI (typically `https://glitch-studios-<hash>-luis-faxas-projects.vercel.app`).

    4. Write `.planning/phases/29.3-rebuild-filter/29.3-05-VERIFICATION.md` with this exact structure (fill in the captured URL):

       ```markdown
       # Phase 29.3 — Real-Browser Verification

       **Status:** awaiting_user_verification
       **Preview deployed:** {ISO timestamp}
       **Preview URL:** {paste the URL from `vercel --yes` output}
       **Page to test:** {preview-url}/tech/rankings/laptops

       ## Why this gate exists

       Per `.planning/debug/filter-chip-crash-mac-browsers.md`, the original
       crash is GPU-compositor-only on macOS Safari + Firefox. Headless
       Linux Playwright tests (Plan 04) cannot reproduce it — they verify
       the architectural contract (no DOM leaks, no listener leaks, no
       page exceptions) but not the actual GPU pressure cascade. Real
       macOS browser verification is the only proof the bug is fixed.

       ## Steps the user must run

       ### Safari (macOS)

       1. Open Safari on your macOS device.
       2. Navigate to: {preview-url}/tech/rankings/laptops
       3. Wait for the leaderboard to render (rows visible).
       4. Click any filter chip trigger (the simplest is **Year**).
       5. Inside the open dropdown, click any year chip.
       6. Observe:
          - The chip's selected state flips on the FIRST click (no need to click twice).
          - The leaderboard rows update.
          - The tab stays responsive (no spinning beachball, no fan ramp-up, no tab crash).
          - Activity Monitor: Safari's per-tab CPU should stay under ~50% during/after the click.
       7. Click another chip (toggle off, toggle on a different one) at least 5 times in succession.
       8. After 5 toggles, the tab should still be fully interactive — scrolling, hovering, opening another dropdown all work.

       ### Firefox (macOS)

       Repeat the same 8 steps in Firefox. Activity Monitor's Firefox content
       process CPU should stay under ~50% during/after the click batch. No
       tab crash, no "page is unresponsive" warning.

       ## Pass criteria

       - [ ] Safari: tab does not die; first chip click registers; CPU stays moderate
       - [ ] Firefox: tab does not die; first chip click registers; CPU stays moderate

       ## How to record your verdict

       Edit this file. Change `Status:` to one of:
       - `pass — both browsers` (then this phase is shippable)
       - `fail — Safari` / `fail — Firefox` / `fail — both` (then create a debug
         artifact at `.planning/debug/29.3-followup-{date}.md` with what you
         observed, and the phase stays open for a follow-up plan)

       Add a bottom section:
       ```
       ## Verification outcome

       **Verified by:** {your name / "user"}
       **Date:** {ISO date}
       **Safari:** PASS / FAIL — {notes}
       **Firefox:** PASS / FAIL — {notes}
       **Verdict:** {pass — both browsers | fail — ... }
       ```
       ```

    5. Print the preview URL to stdout in a clearly delimited block so the user can copy it from the agent transcript:
       ```
       ============================
       VERCEL PREVIEW URL: {url}
       TEST THIS PAGE ON macOS:
       {url}/tech/rankings/laptops
       ============================
       ```

    6. DO NOT run `vercel --prod`. DO NOT mark the phase complete. DO NOT update STATE.md or ROADMAP.md to "done" — this plan completes when the user records `pass — both browsers` in the verification file.

    **CRITICAL — Failed-deploy guard:** If `vercel --yes` exits non-zero, DO NOT write a partial VERIFICATION.md with a placeholder URL. The automated verify gate below explicitly enforces a Vercel-URL pattern grep so a half-written file from a failed deploy cannot pass the gate and silently advance the phase. If the deploy fails, surface the error and stop.
  </action>
  <verify>
    <automated>test -f .planning/phases/29.3-rebuild-filter/29.3-05-VERIFICATION.md &amp;&amp; grep -q "Preview URL:" .planning/phases/29.3-rebuild-filter/29.3-05-VERIFICATION.md &amp;&amp; grep -qE 'https://[a-z0-9-]+-luis-faxas-projects\.vercel\.app' .planning/phases/29.3-rebuild-filter/29.3-05-VERIFICATION.md &amp;&amp; grep -q "/tech/rankings/laptops" .planning/phases/29.3-rebuild-filter/29.3-05-VERIFICATION.md &amp;&amp; grep -q "Status: awaiting_user_verification" .planning/phases/29.3-rebuild-filter/29.3-05-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/29.3-rebuild-filter/29.3-05-VERIFICATION.md` exits 0
    - `grep -c "Preview URL:" .planning/phases/29.3-rebuild-filter/29.3-05-VERIFICATION.md` returns at least 1
    - `grep -cE 'https://[a-z0-9-]+-luis-faxas-projects\\.vercel\\.app' .planning/phases/29.3-rebuild-filter/29.3-05-VERIFICATION.md` returns at least 1 (a real Vercel preview URL was captured — not a placeholder, not a partial write from a failed deploy)
    - `grep -c "/tech/rankings/laptops" .planning/phases/29.3-rebuild-filter/29.3-05-VERIFICATION.md` returns at least 1
    - `grep -c "Status: awaiting_user_verification" .planning/phases/29.3-rebuild-filter/29.3-05-VERIFICATION.md` returns 1
    - The agent stdout contains the preview URL in a clearly-delimited block
    - If `vercel --yes` exited non-zero, the agent must NOT write VERIFICATION.md with a placeholder URL — the URL-pattern grep above mechanically enforces this
  </acceptance_criteria>
  <done>The preview is deployed, the URL is recorded (with mechanical Vercel-URL pattern enforcement), and the verification document exists with the exact macOS test steps for the user.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User verifies on real macOS Safari + Firefox</name>
  <files>.planning/phases/29.3-rebuild-filter/29.3-05-VERIFICATION.md</files>
  <action>Pause execution and wait for the user to complete the macOS verification steps documented in 29.3-05-VERIFICATION.md (and reproduced in <how-to-verify> below). Do not proceed; the user MUST run the steps on real macOS Safari and Firefox and edit the verification file with their verdict.</action>
  <what-built>
    The rebuilt filter UI (Plans 01-03) is live at the Vercel preview URL recorded in `29.3-05-VERIFICATION.md`. Plan 04's headless Playwright tests passed on both Webkit and Firefox engines, proving the architectural contract holds. This checkpoint is the macOS GPU-pressure verification — the only test that actually exercises the original failure condition.
  </what-built>
  <how-to-verify>
    Follow the exact 8 steps documented in `29.3-05-VERIFICATION.md` for each browser:

    **Safari (macOS):**
    1. Open Safari, navigate to `{preview-url}/tech/rankings/laptops` (URL is in the verification file).
    2. Wait for rows to render.
    3. Click the **Year** filter trigger.
    4. Click a year chip inside the open dropdown.
    5. Confirm: chip flipped on FIRST click, rows updated, tab stayed responsive, Activity Monitor CPU stayed moderate.
    6. Click 5+ chips in succession (different facets ok).
    7. After the batch, scroll the page, hover the table, open another dropdown — all should work.
    8. No tab crash, no spinning beachball.

    **Firefox (macOS):** Repeat all 8 steps in Firefox. Same pass criteria.

    **Record your verdict** at the bottom of `29.3-05-VERIFICATION.md`:
    ```
    ## Verification outcome
    Verified by: {you}
    Date: {date}
    Safari: PASS / FAIL — {notes}
    Firefox: PASS / FAIL — {notes}
    Verdict: pass — both browsers   (or: fail — ...)
    ```

    If either browser FAILS:
    - Write `.planning/debug/29.3-followup-{date}.md` with what you observed (CPU graph, console errors if any, video if you have one).
    - Reply with "fail" — this triggers `/gsd:plan-phase 29.3 --gaps` for a follow-up plan.

    If BOTH PASS:
    - Reply with "approved" or "pass — both browsers".
    - The phase is shippable; the executor can update ROADMAP.md to mark Phase 29.3 complete and commit.
  </how-to-verify>
  <verify><automated>grep -E "^\*\*Verdict:\*\* pass" .planning/phases/29.3-rebuild-filter/29.3-05-VERIFICATION.md</automated></verify>
  <done>User has recorded "pass — both browsers" verdict in 29.3-05-VERIFICATION.md after testing on real macOS Safari and Firefox.</done>
  <resume-signal>
    Type "approved" / "pass — both browsers" if Safari and Firefox both passed.
    Type "fail — Safari" / "fail — Firefox" / "fail — both" with a brief description if any browser crashed or behaved badly.
  </resume-signal>
</task>

</tasks>

<verification>
- Vercel preview deploy succeeded (mechanically enforced via URL-pattern grep in Task 1's automated verify).
- Preview URL captured in 29.3-05-VERIFICATION.md.
- User has confirmed pass on both macOS Safari and Firefox.
</verification>

<success_criteria>
The user has verified that the rebuilt filter UI does not crash macOS Safari or Firefox tabs on chip click. Phase 29.3 is shippable. The original failing condition documented in `.planning/debug/filter-chip-crash-mac-browsers.md` is closed.
</success_criteria>

<output>
After user verification passes, create `.planning/phases/29.3-rebuild-filter/29.3-05-SUMMARY.md` recording the preview URL, the verification outcome, and a one-line note that Phase 29.3 is complete pending the standard ROADMAP.md update.
</output>
</content>
