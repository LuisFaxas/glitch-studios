---
phase: 29.3-rebuild-filter
plan: 06
type: stabilization-verification
status: complete
verified: passed
files_touched:
  - .planning/phases/29.3-rebuild-filter/29.3-06-PLAN.md (new)
  - .planning/phases/29.3-rebuild-filter/29.3-06-VERIFICATION.md (new, status: passed)
  - .planning/phases/29.3-rebuild-filter/29.3-05-VERIFICATION.md (status: failed_superseded)
  - .planning/debug/filter-chip-crash-mac-browsers.md (Current Focus rewritten)
  - .planning/STATE.md
  - .planning/audit-screenshots/2026-04-27/AUDIT-FINDINGS.md
commits_landed:
  - 3cf5991 feat(29.3-06) final gating work — defer + Floating-UI portal cleanup
  - a9e7bba docs(29.3-06) stabilization plan + reconciled debug + audit artifacts
  - bf3751a docs(29.3-06) verification artifact with preview URL
preview_url: https://glitchstudios-dpdbse9e8-luis-faxas-projects.vercel.app
phase_complete: true
---

<one_liner>
Closes Phase 29.3 — real macOS Safari and Firefox both pass the 9-step filter chip-click verification on the post-`6af8177` preview, confirming the native-pointer/style feedback-loop root cause is resolved and the filter UI ships visible; the historical 03:15Z Safari freeze on commit `e6a1ba2` is preserved as `29.3-05-VERIFICATION.md status: failed_superseded` for timeline truth, and the debug session has been re-anchored to the correct hypothesis so the image-decode/drop-shadow/min-width history doesn't lead future investigations astray.
</one_liner>

## What shipped (the fix family)

| Commit | Author | Effect |
|---|---|---|
| `6af8177` | Codex | Defer `setFilters` + `setOpen` via `setTimeout(0)` — breaks the native pointer/style feedback loop |
| `12214c7` | Codex | Price slider commits on release, revert table-fixed (kept the GPU experiment narrowed) |
| `ba1e747` | Codex | Replace Base UI Slider with hand-rolled `PriceRangeSlider`; defer mobile sheet open |
| `c9d8c60` | Codex | Cross-engine Playwright timeline test passes 4/4 (chromium + webkit + firefox) |
| `3cf5991` | Claude | Replace BPR `<Tooltip>` with native `title=`; additional defer cycles in leaderboard-table; `closeBeforePageSuspends` lifecycle handler in filter-sidebar |

## Verification record

- **Linux-headless Playwright** (Codebox): 4/4 cross-engine pass on `chromium + webkit + firefox` against fresh dev server, zero console/page errors
- **Real macOS Safari**: passed all 9 steps (Year × 10 → Medal × 10 → cross-facet → Reset → Price slider × 5), no freeze, table re-rendered correctly
- **Real macOS Firefox**: passed all 9 steps, same scope, no freeze
- **Preview URL**: `https://glitchstudios-dpdbse9e8-luis-faxas-projects.vercel.app` (master @ `a9e7bba`)

## Why a 6th plan in a 5-plan phase

Plan 29.3-05 was the original verification gate. It ran on `e6a1ba2` and Safari froze. Codex then identified the native-event feedback-loop root cause and shipped commit `6af8177` plus follow-ons. Mutating Plan 05's `status: failed` to "passed" after the code changed would erase the timeline. Plan 06 supersedes 05 via frontmatter `superseded_by` so the phase reads truthfully:

```
01 — passed (baseline GPU fixes — necessary but NOT sufficient)
02 — skipped (premise invalidated, sort header atomic-killed)
03 — passed (filter re-mounted on main render path)
04 — passed (cross-engine Playwright crash-repro spec)
05 — failed (commit e6a1ba2, 03:15Z) → failed_superseded
06 — passed (post-6af8177 fix family, real macOS verified)
```

## Phase 29.3 final state

- Filter UI ships **visible** on `/tech/rankings/laptops` for all engines including macOS Safari + Firefox
- The 6+ hour debug session that started 2026-04-26 is **resolved**
- Hard rule preserved (now in PROJECT.md key decisions + Praxis SOT): **macOS Safari/Firefox ranking filter interactions must defer React state updates out of native input/focus/visibility events** — future filter / dropdown / slider / sheet changes that violate this pattern will reintroduce the crash class
- Phase 29.2 (Site-Wide Hero Rollout, 10 plans drafted, 0 executed) unblocks immediately
