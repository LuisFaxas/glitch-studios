---
phase: 29.3-rebuild-filter
plan: 06
type: stabilization-verification
wave: 6
depends_on: ["29.3-05"]
files_modified:
  - .planning/phases/29.3-rebuild-filter/29.3-06-VERIFICATION.md
  - .planning/debug/filter-chip-crash-mac-browsers.md
autonomous: false
requirements: []
gap_closure: true
supersedes: 29.3-05-VERIFICATION.md (status: failed_superseded)

must_haves:
  truths:
    - "Vercel preview URL is captured for the post-6af8177 + post-c9d8c60 + post-uncommitted-gating-work tree state"
    - "29.3-06-VERIFICATION.md exists with macOS Safari + Firefox FILTER CHIP CLICK steps (Year facet + Medal facet + cross-facet + Reset)"
    - "User has confirmed (in writing, in 29.3-06-VERIFICATION.md frontmatter) that filter chip click on /tech/rankings/laptops does NOT crash macOS Safari and does NOT crash macOS Firefox on the post-fix preview"
    - "Debug session .planning/debug/filter-chip-crash-mac-browsers.md is updated with the new root cause (native input event feedback loop) and no longer leads with the historical image-decode/drop-shadow/min-width hypotheses as the active focus"
  artifacts:
    - path: ".planning/phases/29.3-rebuild-filter/29.3-06-VERIFICATION.md"
      provides: "Final macOS preview URL + verification steps + status field + result fields, replacing the failed 29.3-05 record"
      contains: "https://"
    - path: ".planning/debug/filter-chip-crash-mac-browsers.md"
      provides: "Updated Current Focus section reflecting commit 6af8177 root cause; historical hypotheses retained below"
  key_links:
    - from: ".planning/phases/29.3-rebuild-filter/29.3-06-VERIFICATION.md"
      to: "Vercel preview deployment of the post-6af8177 + post-uncommitted-gating tree"
      via: "URL captured by `vercel --yes` from current working tree (after committing gating WIP) and pasted into the file"
      pattern: "https://[a-z0-9-]+-luis-faxas-projects\\.vercel\\.app"
    - from: ".planning/phases/29.3-rebuild-filter/29.3-05-VERIFICATION.md"
      to: ".planning/phases/29.3-rebuild-filter/29.3-06-VERIFICATION.md"
      via: "frontmatter `superseded_by` + banner explaining historical failure predated root-cause fix"
      pattern: "superseded_by: 29.3-06-VERIFICATION.md"
---

<objective>
Re-verify Phase 29.3 on real macOS Safari + Firefox after the root-cause fix shipped in commit `6af8177` (native pointer/style feedback loop on synchronous React state inside native input events). Plan 29.3-05 verification ran on commit `e6a1ba2` and FAILED at 03:15Z; that failure was real but predated `6af8177` and the four follow-on commits + uncommitted gating work that complete the fix family.

This plan does NOT re-attempt the dropdown rebuild, does NOT roll back the filter, and does NOT change the diagnostic premise of 29.3 — the rubric is unchanged: real macOS, real chip clicks, no headless emulation. It records what shipped between the failed verify and now, and routes the user to do the same physical macOS test on a fresh preview.

If Safari + Firefox both pass the chip-click cycles → Phase 29.3 closes. Transition to Phase 29.2 (Site-Wide Hero Rollout).

If either browser fails → STOP. Roll back filter visibility on master (revert leaderboard-table.tsx + leaderboard-filter-sidebar.tsx + bpr-medal.tsx working-tree changes), open a new debug phase scoped to the **native-event feedback-loop hypothesis** (NOT the historical image/drop-shadow/min-width list — those are already fixed). The next investigation surface is whatever still fires synchronous state inside a native input event after this fix family lands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

## What changed since Plan 29.3-05 failed verification

Codex reproduced the freeze locally on **headless Chromium with REAL MOUSE input** (not synthetic `.click()`) and isolated the root cause to a macOS Safari/Firefox pointer/style feedback loop. The original Plan 29.3-01 baseline fixes (image-decode placeholder swap, drop-shadow hover-gate, min-width 1600 → 1280) were necessary but **not sufficient**. Five commits + three uncommitted edits address the remaining cost.

| Commit | What it does |
|---|---|
| `6af8177` (root-cause fix, 2026-04-27 00:48 EDT) | `setFilters` defers `setFiltersState` via `setTimeout(0)`; `CustomDropdown.setOpen` defers via `setOpenAfterInput`. Verified by Codex on headless Chromium + Firefox + WebKit with real-mouse 6× cycles. |
| `12214c7` | Price slider commit-on-release + revert table-fixed. |
| `ba1e747` | Replace Base UI `Slider` with hand-rolled `PriceRangeSlider`; defer mobile sheet open. Removes another Floating-UI-portal source from the input-event path. |
| `c9d8c60` | Comprehensive timeline test — full interaction matrix on chromium + webkit + firefox. Re-passes after the deferred-update pattern. |
| (uncommitted) `bpr-medal.tsx` | Replace Base UI `Tooltip` with native `title=` attribute. 7 medals/page × `<DashCell>` × `<BuyButton>` — the Floating UI portals were exhausting Firefox. |
| (uncommitted) `leaderboard-table.tsx` | Additional defer cycles + cleanup `useEffect`. |
| (uncommitted) `leaderboard-filter-sidebar.tsx` | Adds `closeBeforePageSuspends` page-lifecycle handler so the dropdown closes cleanly when the tab is suspended/visibility-hidden. |

The diagnostic premise (Linux-headless can reproduce regression, only real macOS can authenticate the original crash class) is unchanged from Plan 29.3-05.

## Why a 29.3-06 instead of re-running 29.3-05

Plan 29.3-05's `status: failed` is a permanent historical record — that test ran, that test failed on commit `e6a1ba2`. Mutating it to "passed" after a code change would obscure the timeline. Plan 29.3-06 supersedes it via frontmatter `superseded_by` so the phase history reads truthfully:

```
05 — failed (commit e6a1ba2, 03:15Z)
06 — pending → passed/failed (post-6af8177 tree)
```

## Steps

1. **Commit the three uncommitted gating files** as a single `feat(29.3-06): final gating work — defer + Floating-UI portal cleanup` commit so the preview deploy reflects the full fix family.
2. **Deploy Vercel preview** via `vercel --yes` from project root. Project is already linked (`.vercel/project.json` exists). Preview-scope env vars were populated in Plan 29.3-02; production-scope is untouched.
3. **Capture preview URL** into `29.3-06-VERIFICATION.md` `preview_url` frontmatter field + body.
4. **Update debug session frontmatter** `updated:` field to today.
5. **User runs the 7-step verification on real macOS Safari + Firefox** (Year × 10 → Medal × 10 → cross-facet → Reset, in both browsers).
6. **User updates 29.3-06-VERIFICATION.md** `result_safari` + `result_firefox` + top-level `status` field.
7. **If both passed** → close 29.3, write 29.3-06-SUMMARY.md (3-line one-liner), `/gsd:transition` to Phase 29.2.
8. **If either failed** → revert filter visibility on master, open a new debug phase from the native-event feedback-loop hypothesis (do NOT reopen the image/drop-shadow/min-width hypotheses — those fixes shipped and stay shipped).

## Pass criteria (mirrored into VERIFICATION.md)

- Safari: tab survived all chip-click batches without crashing or hanging
- Firefox: same
- In both browsers, chip `aria-pressed` visibly toggled and table rows visibly filtered (proves the click handler actually fired through the deferred `setFilters` path)

## Out of scope

- Real product imagery (Plan 29.3-01 ships the placeholder tile; real images come post-launch)
- Sort header onClick re-enable (still atomic-killed at `leaderboard-table.tsx:146` per Plan 29.3-03 deferral; un-killing is a sibling task, NOT a blocker for filter chip-click pass)
- TechHero scanlines, deferred items in 29.3-CONTEXT.md
- Phase 29.2 work — gated on this plan passing
