---
phase: 29.3-rebuild-filter
plan: 05
status: awaiting_user_verification
preview_url: https://glitchstudios-7svm9lb7v-luis-faxas-projects.vercel.app
target_route: /tech/rankings/laptops
deployed_at: 2026-04-27T02:49:15Z
---

# Phase 29.3-05 — Final Verification (filter chip click)

## What was deployed

Preview reflects clean `master` HEAD at commit `e6a1ba2 docs(29.3-04): complete Playwright crash-repro plan`. All five Plan 29.3 changes are live:

1. (Plan 01) Footer hover-gate — no idle GPU layer for LogoTile glitch overlays
2. (Plan 01) AudioPlayerProvider value memoization
3. (Plan 01) WidgetNowPlaying idle branch (no `<Waveform>` canvas)
4. (Plan 01) LeaderboardTable columns memo deps cleanup
5. (Plan 01) LeaderboardTable min-width: 1600px → 1280px
6. (Plan 03) Filter UI re-mounted (`<LeaderboardFilters>` chip-bar above table on desktop + `<LeaderboardFilterSheet>` on mobile)
7. (Plan 03) `data-testid="leaderboard-filters"` added to filter root
8. (Plan 04) Playwright cross-engine crash-repro spec landed (chromium + webkit + firefox all exit 0 in 4–6s)

The atomic-fix block in `leaderboard-table.tsx` is gone. The filter chip-bar is now visible and interactive on `/tech/rankings/laptops`.

The SortHeader atomic-fix at `leaderboard-table.tsx:146` is **still in place** — sort header onClick is disabled. Plan 02 was SKIPPED for this reason (no live sort interaction to diagnose). This plan tests **filter chip clicks**, which is the original crash-class interaction.

## Diagnostic premise

- **Plan 29.3-02 was SKIPPED** — the diagnostic premise was invalid (sort header onClick was atomic-killed alongside the filter, so there was no live `setFilters` write path to verify the baseline fixes against). Methodology collapsed to one human gate at this plan.
- **Plan 29.3-04 confirmed** that automated cross-engine (chromium + webkit + firefox, headless Linux) crash-repro of the chip click passes — no DOM leaks, no console errors, no page exceptions, aria-pressed cycle correctness, row-count narrowing on filter activation, portal-scoped DOM-node delta within ±2.

This plan is the final authoritative check: **do REAL macOS Safari and REAL macOS Firefox tolerate filter chip clicks now?** Headless Linux Playwright can prove the regression class is gone, but only physical macOS hardware can prove the original Metal/WebRender crash class is gone.

## Pre-verification: Vercel SSO sign-in

The preview URL is protected by Vercel team SSO. Before running the verification steps:

1. In Safari (and again in Firefox), visit the preview URL once: `https://glitchstudios-7svm9lb7v-luis-faxas-projects.vercel.app`
2. You will be redirected to a Vercel SSO sign-in page. Sign in with the `luisfaxas` account.
3. After sign-in completes, the preview app loads. Cookie persists for the session.

If SSO sign-in fails or the page does not load, stop and report — the rest of the verification cannot proceed without preview access.

## Verification steps

Open the preview URL on a physical macOS machine. Run the steps in BOTH browsers. Each browser run should take under 5 minutes.

### macOS Safari

1. Open Safari → navigate to `https://glitchstudios-7svm9lb7v-luis-faxas-projects.vercel.app/tech/rankings/laptops`
2. Wait for the leaderboard table to fully render (≥1 row visible).
3. Confirm the **filter chip-bar** is visible above the table (Price | Year | CPU | RAM | Storage | Medal | Sub-cat dropdown triggers).
4. **Year facet:** Click the **"Year"** dropdown trigger to open it. Click the first year chip 10 times rapidly (no pauses). Click a different year chip 10 times rapidly. Close the dropdown by clicking the trigger again (or click outside).
5. **Medal facet:** Click the **"Medal"** dropdown trigger to open it. Click the **"Gold"** chip 10 times rapidly. Click **"Silver"** 10 times rapidly. Close the dropdown.
6. **Cross-facet:** Open Year, click a chip (table should reorder/filter). Open Medal, click a chip (table should narrow further). Open Year again, click another chip. Click the **Reset** button on the right of the chip-bar — table should return to unfiltered state.
7. Observe throughout: tab stays responsive, no spinning beachball, no "this page is using significant memory" banner, no Web Process crash, chips visibly toggle (aria-pressed flips), table rows visibly reorder/filter.
8. Bonus: open Activity Monitor and confirm CPU stays under 100% sustained.

### macOS Firefox

Repeat steps 1-8 above in Firefox.

## Pass criteria

- [ ] Safari: tab survived all chip-click batches (Year + Medal + cross-facet + Reset) without crashing or hanging
- [ ] Firefox: tab survived all chip-click batches (Year + Medal + cross-facet + Reset) without crashing or hanging
- [ ] In both browsers, chip aria-pressed visibly toggled and table rows visibly filtered (proves the click handler actually fired through `setFilters`)

## Result fields (user fills in)

Safari result: pending  ← change to `passed` or `failed: <one-line description>`

Firefox result: pending  ← change to `passed` or `failed: <one-line description>`

Status: awaiting_user_verification  ← change to `passed` (both passed) or `failed` (either crashed)

## Next step

- If both browsers pass → user updates `status: passed` in the frontmatter at the top of this file AND types **"approved"** in chat. Phase 29.3 is COMPLETE. The full chain (baseline GPU fixes + filter re-mount + Playwright cross-engine guard + macOS real-browser confirmation) shipped.
- If either browser fails → user updates `status: failed` AND types **"blocked: <browser> crashed on chip click"** in chat. STOP. The deferred items in `.planning/phases/29.3-rebuild-filter/29.3-CONTEXT.md` (TechHero scanlines, Base UI Slider in PriceFilterPopover, hand-rolled `<FilterDropdown>` rebuild, Toaster/CartDrawer always-mounted shell) become active candidates for a new debug or research phase.

## Deploy notes (informational)

- **Preview URL:** `https://glitchstudios-7svm9lb7v-luis-faxas-projects.vercel.app`
- **Deployed branch state:** clean `master` HEAD at commit `e6a1ba2 docs(29.3-04): complete Playwright crash-repro plan`. Uncommitted WIP edits in working tree (.gitignore, package.json, template.tsx, bottom-tab-bar.tsx, tile-nav.tsx, bpr-medal.tsx, buy-button.tsx, logo-tile.module.css, logo-tile.tsx, glitch-heading.tsx, untracked debug/exports/scripts) were stashed before deploy and restored after. The preview reflects only what is committed to `master`.
- **Target:** `preview` (not production)
- **Build:** Completed in 2m, deployment completed successfully.
- **Env scope:** Preview env vars were already populated from Plan 29.3-02's deploy work (15 vars copied from Production scope to Preview scope). No env modifications needed for this deploy. Production env scope was NOT touched.
- **Vercel team:** `luis-faxas-projects`
- **Vercel user:** `luisfaxas`
