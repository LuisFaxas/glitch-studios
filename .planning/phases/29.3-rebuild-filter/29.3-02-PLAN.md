---
phase: 29.3-rebuild-filter
plan: 02
type: execute
wave: 2
depends_on: ["29.3-01"]
files_modified:
  - .planning/phases/29.3-rebuild-filter/29.3-02-VERIFICATION.md
autonomous: false
requirements: []
gap_closure: false

must_haves:
  truths:
    - "Vercel preview URL is captured for the post-29.3-01 branch state"
    - "VERIFICATION.md exists with macOS Safari + Firefox sort-header click steps"
    - "User has confirmed (in writing, in VERIFICATION.md) that sort-header click on /tech/rankings/laptops does NOT crash macOS Safari and does NOT crash macOS Firefox"
    - "If user reports a crash, this plan stops the phase — Plan 29.3-03 is NOT to be executed without re-opening the dropdown-rebuild question"
  artifacts:
    - path: ".planning/phases/29.3-rebuild-filter/29.3-02-VERIFICATION.md"
      provides: "Preview URL + verification steps + status field + result fields"
      contains: "https://"
  key_links:
    - from: ".planning/phases/29.3-rebuild-filter/29.3-02-VERIFICATION.md"
      to: "Vercel preview deployment of the post-29.3-01 branch"
      via: "URL captured by `vercel --yes` and pasted into the file"
      pattern: "https://[a-z0-9-]+-luis-faxas-projects\\.vercel\\.app"
---

<objective>
Diagnostic checkpoint. Validate that the GPU/render-cycle baseline fixes from Plan 29.3-01 actually eliminated the macOS Safari + Firefox crash class — BEFORE re-mounting the filter UI in Plan 29.3-03. Sort header click and filter chip click share the same `setFilters` write path through `LeaderboardTable`. If sort header click survives rapid interaction on real macOS Safari and Firefox with the filter STILL HIDDEN, that's strong diagnostic evidence the baseline was the issue and re-mounting the filter is safe.

Purpose: Avoid blindly continuing to filter re-mount only to discover the baseline fixes were insufficient. Provides a clean rollback point if needed.

Output: One Vercel preview deploy + one VERIFICATION.md handoff file. User runs the steps on physical macOS Safari + Firefox and posts results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29.3-rebuild-filter/29.3-CONTEXT.md
@.planning/phases/29.3-rebuild-filter/29.3-01-SUMMARY.md
@.planning/debug/filter-chip-crash-mac-browsers.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Deploy Vercel preview + write VERIFICATION.md handoff</name>
  <files>.planning/phases/29.3-rebuild-filter/29.3-02-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/29.3-rebuild-filter/29.3-CONTEXT.md (decisions: Vercel preview verification — Plans 02 + 05)
    - .planning/phases/29.3-rebuild-filter/29.3-01-SUMMARY.md (confirm 29.3-01 actually shipped — files modified, tsc/lint passed)
    - .planning/debug/filter-chip-crash-mac-browsers.md (the crash-class definition the user is checking against)
  </read_first>
  <action>
Two-step task: (1) deploy current branch to Vercel preview, capture URL; (2) write the handoff file the user reads to perform verification.

Step 1 — Deploy. From the project root, run:

  vercel --yes

Capture the production-style preview URL from stdout. It will match the regex `https://[a-z0-9-]+-luis-faxas-projects\.vercel\.app` (the user's team is `luis-faxas-projects`). If `vercel` returns a non-zero exit code or stdout does not contain a matching URL, STOP and surface the error — do NOT write a partial VERIFICATION.md.

Step 2 — Use the Write tool (NOT bash heredoc) to create `.planning/phases/29.3-rebuild-filter/29.3-02-VERIFICATION.md`. The file must have a YAML frontmatter block (delimited by triple-dash lines on lines 1 and 8) followed by a markdown body. The literal `+++` markers below denote where each YAML frontmatter delimiter line goes — the executor MUST convert each `+++` to a literal triple-dash line (three hyphens, no spaces, on its own line) when writing the file.

File content shape (substitute the captured URL where indicated):

  +++
  phase: 29.3-rebuild-filter
  plan: 02
  status: awaiting_user_verification
  preview_url: <PASTE THE CAPTURED URL HERE>
  target_route: /tech/rankings/laptops
  deployed_at: <ISO 8601 timestamp>
  +++

  # Phase 29.3-02 — Baseline Verification (sort-header click)

  ## What was deployed

  Plan 29.3-01 baseline fixes:
  1. Footer hover-gate (no idle GPU layer for the LogoTile glitch overlays)
  2. AudioPlayerProvider value memoization (no consumer re-render on incidental parent renders)
  3. WidgetNowPlaying idle branch (no <Waveform> canvas when there's no current beat)
  4. LeaderboardTable columns memo deps cleanup (no full column-def rebuild on sort)
  5. LeaderboardTable min-width: 1600px → 100% (smaller GPU rasterization surface)

  Filter UI is STILL HIDDEN. The atomic-fix block in `leaderboard-table.tsx:596-607` is unchanged. Plan 29.3-03 will re-mount the filter only after this checkpoint passes.

  ## Diagnostic premise

  Sort header click in the table and filter chip click both write through `setFilters` in `leaderboard-table.tsx`. If sort survives rapid interaction with the filter hidden, the filter chip will too when re-mounted. If sort still crashes with the filter hidden, the baseline fixes were insufficient and Plan 29.3-03 must NOT be executed without re-investigating.

  ## Verification steps

  Open the preview URL on a physical macOS machine. Run the steps in BOTH browsers.

  ### macOS Safari

  1. Open Safari → navigate to `<preview_url>/tech/rankings/laptops`
  2. Wait for the leaderboard table to fully render (≥1 row visible).
  3. Click the sort header for **BPR** 10 times rapidly (no pauses).
  4. Click the sort header for **Glitchmark** 10 times rapidly.
  5. Click the sort header for **Year** 10 times rapidly.
  6. Click the sort header for **Price** 10 times rapidly.
  7. Observe: tab stays responsive, no spinning beachball, no "this page is using significant memory" banner, no Web Process crash.
  8. Bonus: open DevTools → Activity Monitor and confirm CPU stays under 100% sustained.

  ### macOS Firefox

  Repeat steps 1-8 above in Firefox.

  ## Pass criteria

  - [ ] Safari: tab survived all sort-header click batches without crashing or hanging
  - [ ] Firefox: tab survived all sort-header click batches without crashing or hanging

  ## Result fields (user fills in)

  **Safari result:** pending  ← change to `passed` or `failed: <description>`
  **Firefox result:** pending  ← change to `passed` or `failed: <description>`
  **Status:** awaiting_user_verification  ← change to `passed` (both passed) or `failed` (either crashed)

  ## Next step

  - If both browsers pass → user updates `status: passed`, then runs `/gsd:execute-phase 29.3-rebuild-filter` to continue with Plan 29.3-03 (filter re-mount).
  - If either browser fails → user updates `status: failed` and STOPS. Do NOT execute Plan 29.3-03. Re-open the dropdown-rebuild question with a fresh `/gsd:research-phase` or `/gsd:debug` workflow.

When writing the file, the executor MUST replace each leading `+++` line with a triple-dash line so the resulting file has valid YAML frontmatter delimiters. The file is committed verbatim afterward.
  </action>
  <verify>
    <automated>test -f .planning/phases/29.3-rebuild-filter/29.3-02-VERIFICATION.md && grep -qE 'https://[a-z0-9-]+-luis-faxas-projects\.vercel\.app' .planning/phases/29.3-rebuild-filter/29.3-02-VERIFICATION.md && grep -q 'status: awaiting_user_verification' .planning/phases/29.3-rebuild-filter/29.3-02-VERIFICATION.md && grep -q '/tech/rankings/laptops' .planning/phases/29.3-rebuild-filter/29.3-02-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/29.3-rebuild-filter/29.3-02-VERIFICATION.md` exists
    - `grep -cE 'https://[a-z0-9-]+-luis-faxas-projects\\.vercel\\.app' .planning/phases/29.3-rebuild-filter/29.3-02-VERIFICATION.md` returns at least 1
    - `grep -c 'status: awaiting_user_verification' .planning/phases/29.3-rebuild-filter/29.3-02-VERIFICATION.md` returns 1
    - `grep -c '/tech/rankings/laptops' .planning/phases/29.3-rebuild-filter/29.3-02-VERIFICATION.md` returns at least 1
    - `grep -c 'Safari result: pending' .planning/phases/29.3-rebuild-filter/29.3-02-VERIFICATION.md` returns 1
    - `grep -c 'Firefox result: pending' .planning/phases/29.3-rebuild-filter/29.3-02-VERIFICATION.md` returns 1
    - `grep -c 'sort header' .planning/phases/29.3-rebuild-filter/29.3-02-VERIFICATION.md` returns at least 1 (sort, not filter chips, is the diagnostic interaction)
  </acceptance_criteria>
  <done>
    Vercel preview is live for the post-29.3-01 branch. VERIFICATION.md handoff exists at the documented path with the captured URL, the explicit /tech/rankings/laptops target route, sort-header verification steps for both Safari and Firefox, and pending result fields.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User verifies on physical macOS Safari + Firefox</name>
  <files>.planning/phases/29.3-rebuild-filter/29.3-02-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/29.3-rebuild-filter/29.3-02-VERIFICATION.md (the handoff written by Task 1; read to retrieve the preview URL and steps)
  </read_first>
  <action>
This is a blocking human-verification gate. The agent does NOT advance until the user posts a result.

What was built (by Task 1): Vercel preview of `master` branch with Plan 29.3-01 baseline fixes applied. Filter UI remains hidden. Diagnostic interaction is sort-header click (same `setFilters` write path as filter chips would use).

How the user verifies:
1. Open `.planning/phases/29.3-rebuild-filter/29.3-02-VERIFICATION.md` to retrieve the preview URL.
2. On a physical macOS machine, follow the "Verification steps" section in that file for BOTH macOS Safari and macOS Firefox.
3. Update the "Result fields" section in 29.3-02-VERIFICATION.md:
   - Set `Safari result:` to `passed` or `failed: <one-line description>`
   - Set `Firefox result:` to `passed` or `failed: <one-line description>`
   - Set top-level `status:` (in frontmatter AND inline) to `passed` if BOTH browsers passed, otherwise `failed`
4. Type "approved" in chat ONLY if both browsers passed. If either failed, type "blocked: <browser> crashed" and DO NOT advance.

Resume signal: "approved" (both browsers passed) OR "blocked: <browser> crashed" (either failed).
  </action>
  <verify>
    <automated>grep -qE '^status:\s*(passed|failed)$' .planning/phases/29.3-rebuild-filter/29.3-02-VERIFICATION.md</automated>
  </verify>
  <done>
    User has updated VERIFICATION.md status to either `passed` or `failed` and posted the resume signal in chat. If `passed`, Plan 29.3-03 may proceed. If `failed`, the phase stops and the dropdown-rebuild question is re-opened.
  </done>
</task>

</tasks>

<verification>
- VERIFICATION.md is committed to git (commit message documents the captured URL)
- Status field updated by user before resume
- Phase does NOT auto-advance — Plan 29.3-03 only runs after explicit "approved" signal
</verification>

<success_criteria>
- [ ] Vercel preview deployed (URL captured)
- [ ] VERIFICATION.md exists with all required fields filled (preview_url, target_route, deployed_at, sort-header steps, pending result fields)
- [ ] User has run the verification on physical macOS Safari AND Firefox
- [ ] User has updated VERIFICATION.md with results
- [ ] Status is `passed` (allows Plan 29.3-03 to proceed) OR `failed` (blocks the phase)
- [ ] If failed: phase stops, dropdown-rebuild question is re-opened
</success_criteria>

<output>
After completion, create `.planning/phases/29.3-rebuild-filter/29.3-02-SUMMARY.md` with:
- Captured Vercel preview URL
- Final status (passed | failed)
- Safari + Firefox results
- If failed: copy of the user's failure description and the next-step decision
</output>
