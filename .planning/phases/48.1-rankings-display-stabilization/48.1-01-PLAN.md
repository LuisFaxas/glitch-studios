---
phase: 48.1-rankings-display-stabilization
plan: 01
slug: promote-stable-custom-rankings-display
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/tech/leaderboard-table.tsx
autonomous: true
requirements:
  - RANK-STAB-01
  - RANK-STAB-02
  - RANK-STAB-03
  - RANK-STAB-04
must_haves:
  truths:
    - "D-01: Treat the passing `?simple=1` diagnostics and failing default page as sufficient evidence that the unstable surface is the full rankings display path."
    - "D-02: Do not full-scrap the rankings page; preserve route, data fetch, filters, and visual direction while replacing the display layer."
    - "D-03: Default `/tech/rankings/laptops` must not import or execute `@tanstack/react-table`, `useReactTable`, or `flexRender`."
    - "D-04: Promote the stable lightweight `?simple=1` display idea into the default custom display architecture."
    - "D-05: The custom display renders rank, product link, BPR, GlitchMark, benchmarks or not-tested states, year, and price."
    - "D-06: Existing filter controls remain wired to the same local filter state and reset behavior."
    - "D-07: Do not re-enable risky all-column interactive sorting as part of this stabilization phase."
    - "D-08: Avoid mapping the full result set into separate hidden desktop and mobile display trees at the same time."
  artifacts:
    - path: "src/components/tech/leaderboard-table.tsx"
      provides: "Stable default custom rankings display"
      contains: "data-leaderboard-display"
---

<objective>
Replace the default full leaderboard table runtime with the stable custom display architecture proven by the live `?simple=1` diagnostic.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48.1-rankings-display-stabilization/48.1-CONTEXT.md
@.planning/phases/48.1-rankings-display-stabilization/48.1-RESEARCH.md
@.planning/phases/48.1-rankings-display-stabilization/48.1-UI-SPEC.md
@.planning/phases/48.1-rankings-display-stabilization/48.1-PATTERNS.md
@src/components/tech/leaderboard-table.tsx
@src/components/tech/leaderboard-filter-sidebar.tsx
@src/components/tech/leaderboard-filter-sheet.tsx
@src/lib/tech/leaderboard.ts
</context>

<threat_model>
No auth, payment, database schema, or privileged data paths are modified. Primary risk is production availability: a bad display replacement could hide rankings, break review links, or reintroduce the freeze. Mitigation: keep the server data contract unchanged, use stable keys, preserve filter controls, and gate production closure on real macOS Safari/Firefox verification.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Promote the simple diagnostic architecture into default display</name>
  <files>src/components/tech/leaderboard-table.tsx</files>
  <read_first>
    - .planning/phases/48.1-rankings-display-stabilization/48.1-CONTEXT.md
    - .planning/phases/48.1-rankings-display-stabilization/48.1-RESEARCH.md
    - .planning/phases/48.1-rankings-display-stabilization/48.1-UI-SPEC.md
    - .planning/phases/48.1-rankings-display-stabilization/48.1-PATTERNS.md
    - src/components/tech/leaderboard-table.tsx
    - src/components/tech/leaderboard-filter-sidebar.tsx
    - src/components/tech/leaderboard-filter-sheet.tsx
    - src/lib/tech/leaderboard.ts
  </read_first>
  <action>
    In `src/components/tech/leaderboard-table.tsx`, remove the TanStack import block and delete usage of `useReactTable`, `getCoreRowModel`, `getSortedRowModel`, `flexRender`, `ColumnDef`, and `SortingFn`.

    Delete the `LeaderboardTableDispatcher`, `LeaderboardTableSimpleInner`, and `useSearchParams`-based `simple=1` branching. Export `memo(LeaderboardTableInner)` directly.

    Rewrite `LeaderboardTableInner` so it keeps the existing local filter state, `setFilters`, `filterState`, `bounds`, `filteredRows`, `onResetFilters`, `onFilterChange`, and `activeFilterCount`, but renders `filteredRows` through a custom display:

    - root wrapper: `<section data-leaderboard-display aria-label="Ranked products">`
    - one mapped item per `filteredRows`, keyed by `row.reviewId`
    - product anchor href: `/tech/reviews/${row.reviewSlug}` with `prefetch={false}`
    - visible rank: `#${index + 1}`
    - visible product name
    - visible BPR tier and BPR score, using `BPRMedal` when present
    - visible GlitchMark value via `formatGlitchmarkDisplay`
    - visible benchmark cells for every `benchmarkColumns` item, using `row.benchmarkScores[bc.rubricKey]`
    - visible year and price

    Keep the desktop `LeaderboardFilters` bar and `LeaderboardFilterSheet` mounted as they are today. Remove the old desktop table wrapper, mobile duplicate result wrapper, `data-leaderboard-table-mobile-wrapper`, and `style={{ contain: "layout paint style" }}` result wrappers.

    Keep `LeaderboardEmptyState` behavior for no rows and no filtered results.
  </action>
  <acceptance_criteria>
    - `rg -n '@tanstack/react-table|useReactTable|flexRender|getCoreRowModel|getSortedRowModel|LeaderboardTableDispatcher|LeaderboardTableSimpleInner|searchParams\\?\\.get\\(\"simple\"\\)' src/components/tech/leaderboard-table.tsx` returns no matches.
    - `rg -n 'data-leaderboard-display|aria-label="Ranked products"|prefetch=\\{false\\}' src/components/tech/leaderboard-table.tsx` returns matches.
    - `rg -n 'data-leaderboard-table-mobile-wrapper|contain: \"layout paint style\"' src/components/tech/leaderboard-table.tsx` returns no matches.
    - `pnpm exec eslint src/components/tech/leaderboard-table.tsx` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec eslint src/components/tech/leaderboard-table.tsx</automated>
  </verify>
  <done>The default leaderboard display uses the stable custom path and contains no TanStack runtime.</done>
</task>

</tasks>

<verification>
Run the scoped ESLint command and all grep acceptance criteria.
</verification>

<success_criteria>
Default rankings display no longer uses the full TanStack/full table path and still renders filtered ranking rows with core scoring data.
</success_criteria>

<output>
After completion, create `.planning/phases/48.1-rankings-display-stabilization/48.1-01-SUMMARY.md`.
</output>
