---
phase: 48.1-rankings-display-stabilization
plan: 02
slug: update-ranking-regression-tests
type: execute
wave: 2
depends_on:
  - 48.1-01
files_modified:
  - tests/29.3-comprehensive-timeline.spec.ts
  - tests/29.1-06-filter-bar.spec.ts
  - tests/29-leaderboard.spec.ts
autonomous: true
requirements:
  - RANK-STAB-01
  - RANK-STAB-03
  - RANK-STAB-04
must_haves:
  truths:
    - "D-03: Regression tests target the new custom display contract, not the removed TanStack table DOM."
    - "D-05: Tests assert the visible display still exposes core ranking information such as product links, BPR, GlitchMark, year, and price."
    - "D-06: Filter interaction tests still prove filters change visible row counts and reset restores the baseline."
    - "D-07: Old URL-backed sort/filter assertions are not treated as current stabilization requirements."
---

<objective>
Update Playwright regression coverage so Phase 48.1 protects the stable custom rankings display and does not preserve stale table/URL assumptions.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48.1-rankings-display-stabilization/48.1-CONTEXT.md
@src/components/tech/leaderboard-table.tsx
@tests/29.3-comprehensive-timeline.spec.ts
@tests/29.1-06-filter-bar.spec.ts
@tests/29-leaderboard.spec.ts
</context>

<threat_model>
No security-sensitive runtime code is changed in this plan. Test risk is false confidence: stale selectors could pass without verifying the new display. Mitigation: tests must assert `data-leaderboard-display`, product links, filter count changes, reset recovery, and no console/page errors.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Retarget filter timeline test to custom display</name>
  <files>tests/29.3-comprehensive-timeline.spec.ts</files>
  <read_first>
    - .planning/phases/48.1-rankings-display-stabilization/48.1-CONTEXT.md
    - src/components/tech/leaderboard-table.tsx
    - tests/29.3-comprehensive-timeline.spec.ts
  </read_first>
  <action>
    In `tests/29.3-comprehensive-timeline.spec.ts`, replace `getRowCount` so it counts `[data-leaderboard-display] [data-leaderboard-row]` or the exact row selector implemented in Plan 01. Replace waits for `[data-leaderboard-table]` with waits for `[data-leaderboard-display]`.

    Keep the existing facet open/close helpers and price slider flow. Keep the stage timing budgets. Keep console/page error assertions.
  </action>
  <acceptance_criteria>
    - `rg -n 'data-leaderboard-display' tests/29.3-comprehensive-timeline.spec.ts` returns matches.
    - `rg -n 'data-leaderboard-table' tests/29.3-comprehensive-timeline.spec.ts` returns no matches.
    - `pnpm exec playwright test tests/29.3-comprehensive-timeline.spec.ts --project=desktop` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec playwright test tests/29.3-comprehensive-timeline.spec.ts --project=desktop</automated>
  </verify>
  <done>Comprehensive timeline test targets the new custom display.</done>
</task>

<task type="auto">
  <name>Task 2: Keep filter bar test aligned with local filter state</name>
  <files>tests/29.1-06-filter-bar.spec.ts</files>
  <read_first>
    - src/components/tech/leaderboard-filter-sidebar.tsx
    - tests/29.1-06-filter-bar.spec.ts
  </read_first>
  <action>
    Update `tests/29.1-06-filter-bar.spec.ts` only where it assumes URL-backed filter state. The current contract is local state. The CPU active-state test should open the CPU dropdown, click the first chip, assert the trigger label contains `CPU (1)`, click reset, and assert the trigger returns to `CPU`.

    Keep tests that verify desktop bar triggers, price popover, and mobile filter sheet.
  </action>
  <acceptance_criteria>
    - `rg -n '\\?cpu=AMD|toHaveURL\\(/cpu=' tests/29.1-06-filter-bar.spec.ts` returns no matches.
    - `rg -n 'CPU \\(1\\)|data-reset-filters|data-mobile-filter-sheet-trigger' tests/29.1-06-filter-bar.spec.ts` returns matches.
    - `pnpm exec playwright test tests/29.1-06-filter-bar.spec.ts --project=desktop` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec playwright test tests/29.1-06-filter-bar.spec.ts --project=desktop</automated>
  </verify>
  <done>Filter bar tests verify local filter behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Update leaderboard contract test for stabilization scope</name>
  <files>tests/29-leaderboard.spec.ts</files>
  <read_first>
    - .planning/phases/48.1-rankings-display-stabilization/48.1-CONTEXT.md
    - src/components/tech/leaderboard-table.tsx
    - tests/29-leaderboard.spec.ts
  </read_first>
  <action>
    In `tests/29-leaderboard.spec.ts`, keep route/CTA and mobile visibility coverage, but remove or rewrite tests that require URL-backed sorting/filtering or `tbody tr`.

    Add stabilization assertions:

    - `/tech/rankings/laptops` renders `[data-leaderboard-display]`.
    - at least one review link exists inside `[data-leaderboard-display]`.
    - the first display item has visible rank text beginning with `#`.
    - the display contains visible `GlitchMark`, `BPR`, `Year`, and `Price` labels or values.
    - clicking a filter chip changes the displayed row count or leaves it less than/equal to baseline.
    - reset restores the baseline displayed row count.
  </action>
  <acceptance_criteria>
    - `rg -n 'sort=bpr|sort=bprScore|cpu=|tbody tr|role=\"tooltip\"' tests/29-leaderboard.spec.ts` returns no matches.
    - `rg -n 'data-leaderboard-display|GlitchMark|BPR|Price|Year' tests/29-leaderboard.spec.ts` returns matches.
    - `pnpm exec playwright test tests/29-leaderboard.spec.ts --project=desktop --project=mobile` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec playwright test tests/29-leaderboard.spec.ts --project=desktop --project=mobile</automated>
  </verify>
  <done>Leaderboard tests match the custom display stabilization contract.</done>
</task>

</tasks>

<verification>
Run all three Playwright commands listed in task acceptance criteria.
</verification>

<success_criteria>
Automated regression tests verify the custom display, filter behavior, and mobile filter entry points without relying on the removed table DOM.
</success_criteria>

<output>
After completion, create `.planning/phases/48.1-rankings-display-stabilization/48.1-02-SUMMARY.md`.
</output>
