---
phase: 23-debug-broken-pages-missing-routes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/actions/admin-clients.ts
  - src/actions/admin-roles.ts
  - src/app/admin/clients/page.tsx
  - src/app/admin/roles/page.tsx
  - tests/23-03-admin-500-pages.spec.ts
  - .planning/phases/23-debug-broken-pages-missing-routes/23-03-DIAGNOSIS.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "Authenticated owner can load /admin/clients without a 500 error"
    - "Authenticated owner can load /admin/roles without a 500 error"
    - "Root cause of both 500s is documented in 23-03-DIAGNOSIS.md (not patched-over)"
    - "Unauthenticated visits to either route redirect to /login, NOT a 500"
  artifacts:
    - path: ".planning/phases/23-debug-broken-pages-missing-routes/23-03-DIAGNOSIS.md"
      provides: "Diagnosed root cause (stack trace + SQL/permission error) for both pages"
      contains: "Root cause:"
    - path: "tests/23-03-admin-500-pages.spec.ts"
      provides: "Playwright regression proving both pages render 200 for owner, redirect for guest"
    - path: "src/actions/admin-clients.ts"
      provides: "Fixed query/permission logic"
    - path: "src/actions/admin-roles.ts"
      provides: "Fixed query/permission logic"
  key_links:
    - from: "src/app/admin/clients/page.tsx"
      to: "src/actions/admin-clients.ts"
      via: "getClients() server function call"
      pattern: "getClients\\("
    - from: "src/app/admin/roles/page.tsx"
      to: "src/actions/admin-roles.ts"
      via: "getRoles + getAdminMembers calls"
      pattern: "getRoles\\(\\)|getAdminMembers\\(\\)"
---

<objective>
Fix Audit §D.1 bugs: `/admin/clients` and `/admin/roles` both 500 with the same "page couldn't load" symptom. Per D-13 the fix starts with Vercel runtime logs + local reproduction against the prod DB — NOT guessing. RESEARCH.md §Bug 2-3 ranks the suspects: raw-SQL column drift in `admin-clients.ts:54` / `admin-roles.ts:171`, or `requirePermission` cascade from audit §C.4, or a Drizzle relation missing for `adminRolePermissions`. Per D-01 we root-cause both in one diagnostic session (shared failure mode per audit) then ship a minimal query or permission fix — no `try/catch` cover-ups.

This plan contains a CHECKPOINT because the fix cannot be pre-specified until the stack trace is captured.

Purpose: Unbreak admin client/roles management before launch; document the root cause so future admin-list pages don't repeat it.
Output: Diagnosed root cause, minimal fix in the relevant action, guard test, and written diagnosis note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-debug-broken-pages-missing-routes/23-CONTEXT.md
@.planning/phases/23-debug-broken-pages-missing-routes/23-RESEARCH.md
@.planning/phases/22-visual-audit-discovery/22-AUDIT.md
@src/actions/admin-clients.ts
@src/actions/admin-roles.ts
@src/app/admin/clients/page.tsx
@src/app/admin/roles/page.tsx
@src/app/admin/roles/client.tsx
@src/lib/permissions.ts

<interfaces>
<!-- Existing server action contracts — do NOT change signatures -->
From src/actions/admin-clients.ts:
- `getClients({ page: number }): Promise<{ clients: Client[]; total: number; ... }>` — consumed by `/admin/clients/page.tsx`
- Raw SQL around line 54: `WHERE u.role = 'user'` + JOINs against orders / bookings

From src/actions/admin-roles.ts:
- `getRoles(): Promise<Role[]>` — uses `db.query.adminRoles.findMany({ with: { permissions: true } })`
- `getAdminMembers(): Promise<AdminMember[]>` — uses raw SQL `SELECT id, name, email, role, created_at FROM "user" WHERE role != 'user'`
- `new Date(r.created_at as string).toISOString()` at line 171

Permission gate:
- `requirePermission("view_clients")` and `requirePermission("manage_roles")` — defined in `src/lib/permissions.ts`; throws `Error` if the caller lacks the permission

Do NOT change any of these function signatures without updating call-sites in the `page.tsx` files identified in `files_modified`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reproduce + diagnose both 500s locally, write DIAGNOSIS.md</name>
  <files>.planning/phases/23-debug-broken-pages-missing-routes/23-03-DIAGNOSIS.md</files>
  <read_first>
    - src/actions/admin-clients.ts (full file — especially the raw SQL block and the `(rows as any[]).map(...)` coercion)
    - src/actions/admin-roles.ts (full file — especially line ~171 created_at cast and the relational query in getRoles)
    - src/app/admin/clients/page.tsx (Server Component shape — where does the error bubble from?)
    - src/app/admin/roles/page.tsx (Server Component shape)
    - src/lib/permissions.ts (requirePermission behavior — does it throw or redirect?)
    - src/lib/auth.ts (session read pattern)
    - src/db/schema.ts or src/db/schema/*.ts (confirm column names referenced by raw SQL actually exist in current schema — especially `role`, `user_id`, `guest_email`, `created_at`)
    - .planning/phases/22-visual-audit-discovery/22-AUDIT.md §C.4 (auth cascade lessons)
  </read_first>
  <behavior>
    - With `pnpm dev` running and an owner session cookie, hitting `/admin/clients` and `/admin/roles` produces an observable stack trace in the terminal.
    - DIAGNOSIS.md captures: the exact stack trace (source file + line), the specific failure class (raw-SQL column miss / Drizzle relation miss / permission throw / null-coercion), and the chosen fix strategy (one sentence per page).
    - If both pages fail for the same reason, that's noted and the fix is unified.
  </behavior>
  <action>
    1. Start the dev server: `pnpm dev` (runs on port 3004 via Caddy at `glitch-studios.codebox.local`).
    2. Authenticate as an owner/admin in the browser. If no dev admin account exists, use the seed script or create one via `src/actions/` helpers — DO NOT skip auth; the bug likely involves session state.
    3. Navigate to `/admin/clients`. Capture the full terminal stack trace (the Next.js 16 dev overlay also shows it in-browser; copy both). Save exact file:line refs.
    4. Navigate to `/admin/roles`. Repeat.
    5. Cross-reference column names in each raw SQL query against the live schema:
       ```bash
       grep -rn "export const user" src/db/schema*
       grep -rn "role" src/db/schema*
       ```
       Confirm every column referenced in `admin-clients.ts` and `admin-roles.ts` raw SQL exists in the Drizzle schema.
    6. Check the Drizzle relation: `db.query.adminRoles.findMany({ with: { permissions: true } })` — find the `adminRoles` table definition and confirm a `permissions` relation is declared (via `relations()`).
    7. Check `requirePermission` behavior in `src/lib/permissions.ts` — does it throw, or does it `redirect()`? A throwing implementation in a Server Component becomes a 500; a redirecting one becomes a 302.

    8. If local repro does NOT show the prod failure (e.g., works locally), the bug is prod-environment-dependent — STOP and create a checkpoint (Task 2) requesting user to pull Vercel logs.

    9. If local repro DOES show the failure, write `.planning/phases/23-debug-broken-pages-missing-routes/23-03-DIAGNOSIS.md` with this exact structure:
       ```markdown
       # Phase 23-03 Diagnosis

       ## /admin/clients
       - **Stack trace (top frame):** {file}:{line} — {error message}
       - **Root cause:** {one sentence}
       - **Fix strategy:** {one sentence — what file, what change}

       ## /admin/roles
       - (same shape)

       ## Shared root cause?
       YES/NO — if YES, describe the unified fix.

       ## Env notes
       - Local matches prod? YES/NO
       - Auth cascade (§C.4) relevance: YES/NO — explanation
       ```
    10. Commit DIAGNOSIS.md before any production code change.
  </action>
  <verify>
    <automated>test -f .planning/phases/23-debug-broken-pages-missing-routes/23-03-DIAGNOSIS.md && grep -q "Root cause:" .planning/phases/23-debug-broken-pages-missing-routes/23-03-DIAGNOSIS.md</automated>
  </verify>
  <done>DIAGNOSIS.md exists with concrete stack traces and one-sentence fix strategies for both pages.</done>
  <acceptance_criteria>
    - `test -f .planning/phases/23-debug-broken-pages-missing-routes/23-03-DIAGNOSIS.md`
    - `grep -q "## /admin/clients" .planning/phases/23-debug-broken-pages-missing-routes/23-03-DIAGNOSIS.md`
    - `grep -q "## /admin/roles" .planning/phases/23-debug-broken-pages-missing-routes/23-03-DIAGNOSIS.md`
    - `grep -q "Root cause:" .planning/phases/23-debug-broken-pages-missing-routes/23-03-DIAGNOSIS.md`
    - `grep -q "Fix strategy:" .planning/phases/23-debug-broken-pages-missing-routes/23-03-DIAGNOSIS.md`
  </acceptance_criteria>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Confirm fix strategy (or escalate if local repro fails)</name>
  <files>.planning/phases/23-debug-broken-pages-missing-routes/23-03-DIAGNOSIS.md</files>
  <what-built>Diagnosis in 23-03-DIAGNOSIS.md — exact stack traces + proposed minimal fix for each page.</what-built>
  <decision>Approve the diagnosed root cause and proposed fix strategy, OR escalate if local repro did not reveal the failure (prod-only bug requires Vercel log access).</decision>
  <context>
    Per D-01 we ship root-cause fixes, not symptom patches. Per D-13 and RESEARCH.md §Bug 2-3, the failure modes are narrow: raw-SQL column drift, Drizzle relation missing, or permission cascade. If the diagnosis matches one of these and the fix is a query/permission patch, approve and proceed. If the diagnosis reveals something unexpected (schema migration needed, role data seed required, auth redesign), escalate.

    Special case: if the bug ONLY reproduces in prod, user must either grant Vercel log access or run the reproduction themselves and share the trace.
  </context>
  <options>
    <option id="approve-fix">
      <name>Approve — proceed to Task 3 with the diagnosed fix strategy</name>
      <pros>Fast path; root cause confirmed; matches D-01.</pros>
      <cons>None if diagnosis is accurate.</cons>
    </option>
    <option id="escalate-migration">
      <name>Escalate — fix requires a schema migration or role-seed</name>
      <pros>Prevents landing an incomplete fix.</pros>
      <cons>Blocks Phase 23 until user decides scope (migration may belong in a separate phase).</cons>
    </option>
    <option id="escalate-prod-logs">
      <name>Escalate — local repro failed; need Vercel logs</name>
      <pros>Honest about missing data; prevents guessing.</pros>
      <cons>Blocks until user pulls logs or grants access.</cons>
    </option>
  </options>
  <action>
    Pause execution and present the diagnosis to the user for review. Show the contents of `23-03-DIAGNOSIS.md` and ask the user to select one of the options (approve-fix, escalate-migration, escalate-prod-logs). Wait for the resume-signal before continuing to Task 3.
  </action>
  <verify>
    <automated>test -f .planning/phases/23-debug-broken-pages-missing-routes/23-03-DIAGNOSIS.md</automated>
  </verify>
  <done>User has reviewed DIAGNOSIS.md and selected a resume option. Execution either proceeds to Task 3 (approve-fix) or halts Phase 23-03 for escalation.</done>
  <resume-signal>Select: approve-fix, escalate-migration, or escalate-prod-logs</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write Playwright regression (RED), then apply the diagnosed fix (GREEN)</name>
  <files>tests/23-03-admin-500-pages.spec.ts, src/actions/admin-clients.ts, src/actions/admin-roles.ts, src/app/admin/clients/page.tsx, src/app/admin/roles/page.tsx</files>
  <read_first>
    - .planning/phases/23-debug-broken-pages-missing-routes/23-03-DIAGNOSIS.md (the fix strategy you just approved)
    - tests/07.5-admin-context-switcher.spec.ts (admin auth fixture pattern)
    - All files listed in action for Task 1 (re-read after fix plan is clear)
  </read_first>
  <behavior>
    - RED: new Playwright spec asserts both pages return a 200 and show their expected heading for an authenticated owner; asserts unauth users get redirected to /login (NOT a 500). Initially fails because pages still 500.
    - GREEN: apply the fix described in DIAGNOSIS.md — scope is STRICTLY the one-liner(s) identified:
      - If the fix is a SQL column patch: change the column name / add COALESCE / drop the stale reference.
      - If the fix is a Drizzle relation miss: add the `relations()` declaration in the schema.
      - If the fix is a permission cascade: change `requirePermission` to `redirect('/login')` in the page where appropriate, OR update the permission data.
      - Do NOT wrap Server Components in try/catch to hide errors (per D-01).
      - Do NOT change function signatures of `getClients`, `getRoles`, `getAdminMembers`.
    - Run the spec — must pass.
  </behavior>
  <action>
    PART A — write RED spec:
    Create `tests/23-03-admin-500-pages.spec.ts`:
    ```ts
    import { test, expect } from '@playwright/test'
    // Reuse admin-auth fixture from tests/07.5-admin-context-switcher.spec.ts

    test.describe('admin 500-page regressions', () => {
      test('owner can load /admin/clients (no 500)', async ({ page }) => {
        // authenticate as owner (use existing fixture/helper)
        const res = await page.goto('/admin/clients')
        expect(res?.status()).toBeLessThan(400)
        await expect(page.getByRole('heading', { name: /clients/i })).toBeVisible()
        await expect(page.getByText(/page couldn.?t load|something went wrong/i)).toHaveCount(0)
      })

      test('owner can load /admin/roles (no 500)', async ({ page }) => {
        const res = await page.goto('/admin/roles')
        expect(res?.status()).toBeLessThan(400)
        await expect(page.getByRole('heading', { name: /roles/i })).toBeVisible()
      })

      test('unauthenticated /admin/clients redirects to /login (not 500)', async ({ page }) => {
        // ensure no session cookie
        await page.context().clearCookies()
        const res = await page.goto('/admin/clients')
        expect(page.url()).toMatch(/\/login/)
        expect(res?.status()).toBeLessThan(500)
      })

      test('unauthenticated /admin/roles redirects to /login', async ({ page }) => {
        await page.context().clearCookies()
        await page.goto('/admin/roles')
        expect(page.url()).toMatch(/\/login/)
      })
    })
    ```
    Run `pnpm exec playwright test tests/23-03-admin-500-pages.spec.ts` — expect owner tests to FAIL (500 still happens), commit RED.

    PART B — apply the fix:
    Open the files identified in DIAGNOSIS.md "Fix strategy" and make the minimal change. DO NOT exceed the diagnosed scope. Examples of the kind of change this looks like (pick whichever matches):

    - SQL column patch in `src/actions/admin-clients.ts`: replace `u.role = 'user'` with the correct column name, or wrap nullable columns in `COALESCE(u.x, '')`.
    - Drizzle relation in schema: add `export const adminRolesRelations = relations(adminRoles, ({ many }) => ({ permissions: many(adminRolePermissions) }))` — only if missing.
    - `src/actions/admin-roles.ts` line ~171 timestamp cast: replace `new Date(r.created_at as string).toISOString()` with `(r.created_at instanceof Date ? r.created_at : new Date(r.created_at as string)).toISOString()`.
    - Permission gate: if `requirePermission` throws and the Server Component has no error.tsx, either add `src/app/admin/clients/error.tsx` (NOT a try/catch hiding the bug — a proper error boundary that redirects unauth to /login) OR change the action to use `redirect('/login')` on missing session.

    Run `pnpm tsc --noEmit` — must exit 0.
    Run the Playwright spec — must pass all 4 tests.

    PART C — manual cross-check:
    After the fix, manually visit both pages in `pnpm dev` as owner. Confirm the page renders real data (not just a blank frame). If data is empty, verify that's legitimate (new DB) not a silent-failure cover-up.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm exec playwright test tests/23-03-admin-500-pages.spec.ts --reporter=line</automated>
  </verify>
  <done>Both admin pages return 200 for owner; redirect unauth to /login; Playwright green; fix scope matches DIAGNOSIS.md.</done>
  <acceptance_criteria>
    - `test -f tests/23-03-admin-500-pages.spec.ts`
    - `grep -q "owner can load /admin/clients" tests/23-03-admin-500-pages.spec.ts`
    - `grep -q "owner can load /admin/roles" tests/23-03-admin-500-pages.spec.ts`
    - `pnpm tsc --noEmit` exits 0
    - `pnpm exec playwright test tests/23-03-admin-500-pages.spec.ts --reporter=line` exits 0
    - `! grep -q 'try {[^}]*getClients\|try {[^}]*getRoles' src/app/admin/clients/page.tsx src/app/admin/roles/page.tsx` (no symptom-hiding try/catch wrapping in pages)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- DIAGNOSIS.md documents the actual root cause (not guesses).
- RED-then-GREEN Playwright cycle committed.
- `pnpm tsc --noEmit` clean.
- Fix honors D-01: root cause addressed, no symptom patches.
- Auth-cascade §C.4 discipline: if any auth/session change was made, the Playwright spec exercises both authenticated AND unauthenticated paths.
</verification>

<success_criteria>
- Audit §D.1 bugs `/admin/clients` 500 and `/admin/roles` 500 both closed.
- Root cause documented for future admin-list-page work (Phase 39).
- No regression to other admin pages (ad-hoc smoke: `/admin`, `/admin/settings/homepage` from plan 23-01 should still load).
</success_criteria>

<output>
After completion, create `.planning/phases/23-debug-broken-pages-missing-routes/23-03-SUMMARY.md` summarizing the diagnosed root cause, exact files/lines changed, and Playwright output.
</output>
