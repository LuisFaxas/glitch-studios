---
phase: 23-debug-broken-pages-missing-routes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/tech-nav-config.ts
  - src/app/(tech)/tech/blog/page.tsx
  - src/app/(tech)/tech/benchmarks/page.tsx
  - tests/23-02-about-link-removal.spec.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "No user-facing nav link labelled 'About' exists in GlitchTech navigation (sidebar or mobile menu)"
    - "No anchor in the codebase points to /tech/about (stale links redirected to /tech/methodology)"
    - "Studios navigation has no /about link (verified pre-existing zero occurrences, guarded by regression test)"
  artifacts:
    - path: "src/components/layout/tech-nav-config.ts"
      provides: "GlitchTech nav config without About entries"
      contains: "techNavItems"
    - path: "tests/23-02-about-link-removal.spec.ts"
      provides: "Playwright + grep regression proving About is gone from nav and stray anchors are redirected"
  key_links:
    - from: "src/app/(tech)/tech/blog/page.tsx"
      to: "/tech/methodology"
      via: "replaced anchor href"
      pattern: 'href="/tech/methodology"'
    - from: "src/app/(tech)/tech/benchmarks/page.tsx"
      to: "/tech/methodology"
      via: "replaced anchor href"
      pattern: 'href="/tech/methodology"'
---

<objective>
Fix Audit §B.8 / §E.1 dead-link bug. Phase 23 scope lists "/about dead link" but grep confirms Studios has zero `href="/about"`. The real bug is the GlitchTech "About" nav tile pointing to a placeholder `/tech/about` page, plus two stray `href="/tech/about"` anchors in tech pages. Per D-09 the fix is to REMOVE the link (not stub a page — Phase 44 owns the future `/about` experience). Per audit §B.8(b) — "for a review site, About IS methodology" — the stray anchors get redirected to `/tech/methodology` (which exists).

Purpose: Eliminate dead navigation before launch without creating throwaway content.
Output: No `About` entries in tech nav; stray `/tech/about` anchors redirected to `/tech/methodology`; regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-debug-broken-pages-missing-routes/23-CONTEXT.md
@.planning/phases/23-debug-broken-pages-missing-routes/23-RESEARCH.md
@.planning/phases/22-visual-audit-discovery/22-AUDIT.md
@src/components/layout/tech-nav-config.ts
@src/app/(tech)/tech/blog/page.tsx
@src/app/(tech)/tech/benchmarks/page.tsx

<interfaces>
<!-- Existing nav-config shapes the executor must honor -->
From src/components/layout/tech-nav-config.ts:
- `techNavItems`: NavItem[] — each entry has `{ label: string; href: string; icon?: ... }`
- `techMobileMenuItems`: MobileMenuItem[] — same shape or superset

The About entries live approximately at:
- techNavItems: lines 54-60 (one entry, label "About", href "/tech/about")
- techMobileMenuItems: line 99 (same shape)

DO NOT change any other entries. DO NOT re-order the arrays beyond what removing these specific entries forces. Preserve exact formatting and trailing commas.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write regression spec (RED)</name>
  <files>tests/23-02-about-link-removal.spec.ts</files>
  <read_first>
    - tests/16.1-cross-link-nav.spec.ts (existing nav-wiring spec pattern)
    - src/components/layout/tech-nav-config.ts (current About entries)
    - src/app/(tech)/tech/blog/page.tsx (stray anchor around line 36)
    - src/app/(tech)/tech/benchmarks/page.tsx (stray anchor around line 27)
  </read_first>
  <behavior>
    - Test A: On `/tech` (or wherever the tech sidebar renders), `page.getByRole('link', { name: /^about$/i })` has count 0 across all viewports (desktop + mobile).
    - Test B: On `/tech/blog`, no link with `href="/tech/about"` exists; the link that previously pointed there now points to `/tech/methodology`.
    - Test C: On `/tech/benchmarks`, same as Test B.
    - Test D: Across Studios routes (`/`, `/beats`, `/services`), no link with `href="/about"` exists (guardrail).
    - Tests initially FAIL on A/B/C (About still present), pass on D (pre-existing state).
  </behavior>
  <action>
    Create `tests/23-02-about-link-removal.spec.ts` mirroring the style of `tests/16.1-cross-link-nav.spec.ts`. The spec must:

    1. For Test A: visit `/tech`, open the mobile menu AND verify desktop sidebar. Assert `await expect(page.getByRole('link', { name: /^about$/i })).toHaveCount(0)` (use `^$` anchors to avoid matching "About Us" or partial labels in the future).
    2. For Test B: visit `/tech/blog`, assert `await expect(page.locator('a[href="/tech/about"]')).toHaveCount(0)`. Then assert a link that previously pointed to about now targets methodology — for example if the context around line 36 is a "Read our methodology" or equivalent anchor, assert `page.locator('a[href="/tech/methodology"]')` has count >= 1 on that page.
    3. For Test C: same pattern on `/tech/benchmarks`.
    4. For Test D (guardrail): visit `/`, `/beats`, `/services`; for each assert `await expect(page.locator('a[href="/about"]')).toHaveCount(0)`.
    5. Run two viewports: desktop (1440x900) and mobile (375x812 with `hasTouch`, `isMobile: true`) for Test A.

    Run `pnpm exec playwright test tests/23-02-about-link-removal.spec.ts` — expect A/B/C to FAIL. Commit RED before fix.
  </action>
  <verify>
    <automated>pnpm exec playwright test tests/23-02-about-link-removal.spec.ts --reporter=line 2>&1 | tail -5</automated>
  </verify>
  <done>Spec committed, initial run fails on Tests A/B/C (About still in nav + stray anchors still present), passes on D.</done>
  <acceptance_criteria>
    - `test -f tests/23-02-about-link-removal.spec.ts`
    - `grep -q 'name: /\^about\$/i' tests/23-02-about-link-removal.spec.ts` (exact-match regex used)
    - `grep -q '/tech/about' tests/23-02-about-link-removal.spec.ts` (checks for absence)
    - `pnpm exec playwright test tests/23-02-about-link-removal.spec.ts --reporter=line` exits non-zero
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove About nav entries and redirect stray anchors (GREEN)</name>
  <files>src/components/layout/tech-nav-config.ts, src/app/(tech)/tech/blog/page.tsx, src/app/(tech)/tech/benchmarks/page.tsx</files>
  <read_first>
    - src/components/layout/tech-nav-config.ts (full file — understand adjacent entries before deleting)
    - src/app/(tech)/tech/blog/page.tsx (line 36 anchor context)
    - src/app/(tech)/tech/benchmarks/page.tsx (line 27 anchor context)
    - src/app/(tech)/tech/methodology/page.tsx (confirm this route exists before redirecting to it)
  </read_first>
  <behavior>
    - After change, grep for `/tech/about` in `src/` returns NO matches outside the page file itself (`src/app/(tech)/tech/about/page.tsx` stays — Phase 44 may reuse the slot, and the file has no inbound links after this task).
    - `techNavItems` and `techMobileMenuItems` no longer contain any `About` entry.
    - Both stray anchors now point to `/tech/methodology`.
    - Playwright spec from Task 1 passes all four tests.
  </behavior>
  <action>
    1. Open `src/components/layout/tech-nav-config.ts`. Locate the `About` entry in the `techNavItems` array (research says around lines 54-60). Delete the entire object entry including its leading comma or trailing comma so the array stays syntactically valid. Repeat for the `About` entry in `techMobileMenuItems` (around line 99).
       - Do NOT change any other array entry.
       - Do NOT change the array names or exports.
       - Preserve original indentation on surrounding entries.

    2. Open `src/app/(tech)/tech/blog/page.tsx`. Locate the stray anchor at approximately line 36 that uses `href="/tech/about"`. Change the href to exactly `"/tech/methodology"`. If the link text implies "About" rather than "Methodology" (per audit §B.8), update the visible label to "Methodology" as well to keep UX honest — research confirms methodology IS the canonical about-like page for a review site. Include the exact before/after diff in your commit message.

    3. Open `src/app/(tech)/tech/benchmarks/page.tsx`. Same procedure as step 2 at approximately line 27.

    4. Leave `src/app/(tech)/tech/about/page.tsx` in place — do NOT delete it. Phase 44 decides the fate of that slot.

    5. Run `pnpm tsc --noEmit` — must exit 0.
    6. Run `grep -rn "/tech/about" src/ | grep -v "src/app/(tech)/tech/about/"` — must return nothing (zero references outside the page file itself).
    7. Run the Playwright spec — must pass all 4 tests.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && (grep -rn '/tech/about' src/ | grep -v 'src/app/(tech)/tech/about/'; [ $? -ne 0 ]) && pnpm exec playwright test tests/23-02-about-link-removal.spec.ts --reporter=line</automated>
  </verify>
  <done>Nav entries deleted, stray anchors redirected, TypeScript clean, Playwright green. Zero inbound references to /tech/about remain in src outside the page file.</done>
  <acceptance_criteria>
    - `! grep -n 'href: *"/tech/about"' src/components/layout/tech-nav-config.ts`
    - `! grep -n 'label: *"About"' src/components/layout/tech-nav-config.ts`
    - `! grep -n 'href="/tech/about"' src/app/\(tech\)/tech/blog/page.tsx`
    - `! grep -n 'href="/tech/about"' src/app/\(tech\)/tech/benchmarks/page.tsx`
    - `grep -q 'href="/tech/methodology"' src/app/\(tech\)/tech/blog/page.tsx`
    - `grep -q 'href="/tech/methodology"' src/app/\(tech\)/tech/benchmarks/page.tsx`
    - `test -f src/app/\(tech\)/tech/about/page.tsx` (file intentionally preserved)
    - `pnpm tsc --noEmit` exits 0
    - `pnpm exec playwright test tests/23-02-about-link-removal.spec.ts --reporter=line` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- RED spec committed before code change; GREEN after.
- `pnpm tsc --noEmit` clean.
- Manual spot-check at `pnpm dev`: visit `/tech`, confirm no "About" tile in the tech sidebar; visit `/tech/blog` and `/tech/benchmarks`, confirm the former "About" link now reads "Methodology" and targets `/tech/methodology`.
</verification>

<success_criteria>
- Audit §E.1 dead /about link closed (removal, not stubbing, per D-09).
- Audit §B.8 stray anchors redirected per user-preferred merge.
- Phase 44 inherits a clean slate: no inbound nav links to `/tech/about`, page file preserved for reuse.
</success_criteria>

<output>
After completion, create `.planning/phases/23-debug-broken-pages-missing-routes/23-02-SUMMARY.md` documenting exact lines deleted, anchors updated, and final grep state.
</output>
