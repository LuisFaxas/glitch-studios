---
phase: 23-debug-broken-pages-missing-routes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/admin/admin-sidebar.tsx
  - src/components/admin/quick-actions.tsx
  - tests/23-01-admin-homepage-editor.spec.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Clicking the 'Homepage' link in the admin sidebar lands on /admin/settings/homepage without a 404"
    - "The homepage editor is discoverable from /admin via a visible Quick Action tile"
    - "No other admin-sidebar entry accidentally 'active-highlights' when on /admin/settings/homepage"
  artifacts:
    - path: "src/components/admin/admin-sidebar.tsx"
      provides: "Corrected Homepage sidebar entry href"
      contains: 'href="/admin/settings/homepage"'
    - path: "src/components/admin/quick-actions.tsx"
      provides: "Quick Action tile linking to homepage editor"
      contains: "/admin/settings/homepage"
    - path: "tests/23-01-admin-homepage-editor.spec.ts"
      provides: "Playwright regression proving sidebar link + quick-action tile reach the editor"
  key_links:
    - from: "src/components/admin/admin-sidebar.tsx"
      to: "/admin/settings/homepage"
      via: "href attribute in Homepage nav entry"
      pattern: 'href="/admin/settings/homepage"'
    - from: "src/components/admin/quick-actions.tsx"
      to: "/admin/settings/homepage"
      via: "Link href on Homepage quick action"
      pattern: "/admin/settings/homepage"
---

<objective>
Fix Audit §D.1 bug 1: `/admin` homepage editor is unreachable because the admin sidebar's `Homepage` entry points to `/admin/homepage` (does not exist) while the actual route is `/admin/settings/homepage`. Simultaneously add a discoverable Quick Action tile on the admin dashboard per D-15.

Purpose: Close a one-line-fix launch-blocker and raise editor discoverability for daily non-technical admin use (see Glitch Studios CLAUDE.md "Operability" constraint).
Output: Corrected sidebar href, new Quick Action tile, green Playwright regression.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-debug-broken-pages-missing-routes/23-CONTEXT.md
@.planning/phases/23-debug-broken-pages-missing-routes/23-RESEARCH.md
@.planning/phases/22-visual-audit-discovery/22-AUDIT.md
@src/components/admin/admin-sidebar.tsx
@src/components/admin/quick-actions.tsx
@src/app/admin/settings/homepage/page.tsx

<interfaces>
<!-- Pre-existing contracts the executor must honor -->
Sidebar entry shape (from existing `admin-sidebar.tsx` `navGroups` / nav items):
```ts
{ label: string; href: string; icon: LucideIcon; permission?: PermissionKey }
```
- Keep `icon: Layout` and `permission: "manage_settings"` unchanged.
- `isActiveRoute(pathname, href)` uses `pathname.startsWith(href)`. Changing href to `/admin/settings/homepage` must not cause the sibling `Site Settings` entry (`/admin/settings`) to also activate when on the homepage editor. Verify by rendering both states in the Playwright test.

QuickActions tile shape (from existing `quick-actions.tsx`):
```ts
{ label: string; href: string; icon: LucideIcon; description?: string }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Playwright regression BEFORE the fix (RED)</name>
  <files>tests/23-01-admin-homepage-editor.spec.ts</files>
  <read_first>
    - tests/07.5-admin-context-switcher.spec.ts (for admin auth fixture pattern)
    - tests/16.1-wiring.spec.ts (for route-wiring assertion pattern)
    - src/components/admin/admin-sidebar.tsx (current nav structure)
    - src/app/admin/settings/homepage/page.tsx (heading text to assert on)
  </read_first>
  <behavior>
    - Test A: on /admin, Playwright finds a link with accessible name /homepage/i pointing to /admin/settings/homepage (not /admin/homepage). Clicking it lands on /admin/settings/homepage without a 404 and the editor heading is visible.
    - Test B: on /admin, Playwright finds a Quick Action tile / card (data-testid="quick-action-homepage" or accessible role + name) linking to /admin/settings/homepage.
    - Test C: when pathname is /admin/settings/homepage, only the Homepage sidebar entry has aria-current="page" (or the project's active-class data attribute) — NOT the Site Settings entry.
    - Tests initially FAIL (RED) — the href is wrong and the tile does not yet exist. This is expected; the fix tasks turn them GREEN.
  </behavior>
  <action>
    Create `tests/23-01-admin-homepage-editor.spec.ts` using the existing admin-auth fixture pattern from `tests/07.5-admin-context-switcher.spec.ts`. The spec must:

    1. Navigate to `/admin` as an authenticated owner/admin (reuse whatever login helper `07.5-admin-context-switcher.spec.ts` uses; if it uses a session-cookie fixture, copy it verbatim).
    2. Assert `page.getByRole('link', { name: /homepage/i }).first()` has `href` attribute exactly `/admin/settings/homepage`. Use `await expect(link).toHaveAttribute('href', '/admin/settings/homepage')`.
    3. Click the sidebar link, assert `await expect(page).toHaveURL(/\/admin\/settings\/homepage$/)` and assert the editor heading is visible (read `src/app/admin/settings/homepage/page.tsx` for the exact heading text — use a case-insensitive regex fallback).
    4. Navigate back to `/admin` and assert a Quick Action tile with `data-testid="quick-action-homepage"` exists and has `href="/admin/settings/homepage"`.
    5. Go to `/admin/settings/homepage` directly; assert the Homepage sidebar row has `aria-current="page"` OR the project's equivalent active-state attribute (read admin-sidebar.tsx for the attribute name — do NOT guess). Assert the Site Settings sibling row does NOT have the same attribute.

    Run `pnpm exec playwright test tests/23-01-admin-homepage-editor.spec.ts --reporter=line` — expect FAIL. Commit the RED spec before touching production code.
  </action>
  <verify>
    <automated>pnpm exec playwright test tests/23-01-admin-homepage-editor.spec.ts --reporter=line 2>&1 | grep -E "(failed|passed)" | head -3</automated>
  </verify>
  <done>Spec file committed; running it fails on at least Test A (href mismatch) and Test B (tile missing). No changes to production code yet.</done>
  <acceptance_criteria>
    - `test -f tests/23-01-admin-homepage-editor.spec.ts`
    - `grep -q "/admin/settings/homepage" tests/23-01-admin-homepage-editor.spec.ts`
    - `grep -q "quick-action-homepage" tests/23-01-admin-homepage-editor.spec.ts`
    - `pnpm exec playwright test tests/23-01-admin-homepage-editor.spec.ts --reporter=line` exits non-zero (expected RED)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix sidebar href and add Quick Action tile (GREEN)</name>
  <files>src/components/admin/admin-sidebar.tsx, src/components/admin/quick-actions.tsx</files>
  <read_first>
    - src/components/admin/admin-sidebar.tsx (line 85 and isActiveRoute logic)
    - src/components/admin/quick-actions.tsx (existing tile structure + icon imports)
    - src/app/admin/settings/homepage/page.tsx (confirm the route actually resolves)
  </read_first>
  <behavior>
    - After change, the Playwright spec from Task 1 passes all three tests (GREEN).
    - Sidebar `Homepage` entry has `href="/admin/settings/homepage"` with icon `Layout` and permission `manage_settings` unchanged.
    - Quick Actions component includes a new tile with `data-testid="quick-action-homepage"`, href `/admin/settings/homepage`, icon `Layout` (matching sidebar), label "Homepage", description one short sentence (e.g., "Edit the public homepage content").
    - Active-state logic: if `isActiveRoute(pathname, href)` uses `pathname.startsWith(href)`, ensure the new Homepage href (`/admin/settings/homepage`) does NOT cause the `/admin/settings` entry to also activate. If the current implementation would double-activate, convert that specific check to an exact-match OR re-order so Homepage is evaluated first. Document the chosen approach in the action with the exact diff.
  </behavior>
  <action>
    1. In `src/components/admin/admin-sidebar.tsx`, locate the Homepage nav entry (currently around line 85 per research):
       ```
       { label: "Homepage", href: "/admin/homepage", icon: Layout, permission: "manage_settings" },
       ```
       Change the href to exactly `/admin/settings/homepage` (leave label/icon/permission unchanged).

    2. Inspect the `isActiveRoute` helper (around line 108 per research). If it does `pathname.startsWith(href)`, this means when pathname is `/admin/settings/homepage`:
       - `/admin/settings/homepage` matches its own entry (correct)
       - `/admin/settings` (Site Settings) ALSO matches (incorrect — wrong entry highlights)

       Fix: change the active-check to use exact match when one entry's href is a prefix of another, OR evaluate the MOST specific href first and return early. Prefer the smaller diff:
       ```ts
       function isActiveRoute(pathname: string, href: string): boolean {
         if (href === pathname) return true
         // Only treat as "active via prefix" when there is no more-specific sibling entry that matches exactly
         return pathname.startsWith(href + "/")
       }
       ```
       Changing `startsWith(href)` → `startsWith(href + "/")` plus the exact-match short-circuit eliminates the sibling-highlight bug without changing semantics for any non-prefix pair.

    3. In `src/components/admin/quick-actions.tsx`:
       - Add a new tile to the existing quick-actions array:
         ```tsx
         {
           label: "Homepage",
           href: "/admin/settings/homepage",
           icon: Layout, // import from lucide-react if not already
           description: "Edit the public homepage content",
         }
         ```
       - Ensure the rendered element (Link or Card wrapping Link) includes `data-testid="quick-action-homepage"` on the anchor or its card root so the Playwright spec can locate it.
       - Import `Layout` from `lucide-react` if not already imported.

    4. Run `pnpm tsc --noEmit` — must exit 0. Do NOT run `next build`.
    5. Run the Playwright spec from Task 1 — must pass all three tests.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm exec playwright test tests/23-01-admin-homepage-editor.spec.ts --reporter=line</automated>
  </verify>
  <done>TypeScript clean; all three Playwright tests green; sidebar and Quick Actions correctly link to the homepage editor.</done>
  <acceptance_criteria>
    - `grep -q 'href: "/admin/settings/homepage"' src/components/admin/admin-sidebar.tsx`
    - `! grep -q 'href: "/admin/homepage"' src/components/admin/admin-sidebar.tsx` (old href gone)
    - `grep -q 'quick-action-homepage' src/components/admin/quick-actions.tsx`
    - `grep -q '/admin/settings/homepage' src/components/admin/quick-actions.tsx`
    - `pnpm tsc --noEmit` exits 0
    - `pnpm exec playwright test tests/23-01-admin-homepage-editor.spec.ts --reporter=line` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- RED test exists and failed before the fix (documented in Task 1 commit).
- GREEN after fix: all three sub-tests in `23-01-admin-homepage-editor.spec.ts` pass.
- No TypeScript errors (`pnpm tsc --noEmit`).
- Manual spot-check: hit `/admin/settings/homepage` in `pnpm dev`, confirm only the Homepage row in the sidebar appears active.
</verification>

<success_criteria>
- Audit §D.1 bug "admin homepage editor 404" is closed (grep-verifiable href fix + tile presence + green test).
- No regression to the Site Settings sidebar active-highlight behavior.
- Phase 23 must-haves `truths` all demonstrably true by Playwright output.
</success_criteria>

<output>
After completion, create `.planning/phases/23-debug-broken-pages-missing-routes/23-01-SUMMARY.md` documenting the exact lines changed, the chosen `isActiveRoute` strategy, and the Playwright test results.
</output>
