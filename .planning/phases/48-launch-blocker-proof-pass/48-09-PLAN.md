---
phase: 48-launch-blocker-proof-pass
plan: 09
slug: auth32-final-lint-and-command-proof
type: execute
wave: 2
depends_on: ["48-07", "48-08"]
files_modified:
  - src/app/(auth)/register/artist/artist-request-form.tsx
  - src/app/admin/team/[id]/edit/page.tsx
  - src/components/PixelCard.tsx
  - src/components/admin/admin-package-manager.tsx
  - .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-command-output.md
autonomous: true
gap_closure: true
requirements:
  - AUTH-32
must_haves:
  truths:
    - "The remaining reported lint errors are fixed in the four residual files."
    - "`pnpm tsc --noEmit --pretty false` exits 0 after Plans 48-07 through 48-09."
    - "`pnpm lint` exits 0 after Plans 48-07 through 48-09."
  artifacts:
    - path: ".planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-command-output.md"
      provides: "AUTH-32 command proof"
      contains: "exit status: 0"
    - path: "src/app/admin/team/[id]/edit/page.tsx"
      provides: "Team edit page without JSX construction inside try/catch"
      contains: "notFound"
  key_links:
    - from: "48-07"
      to: ".planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-command-output.md"
      via: "TypeScript/no-any fixes must land before final commands"
      pattern: "pnpm tsc"
    - from: "48-08"
      to: ".planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-command-output.md"
      via: "React compiler lint fixes must land before final commands"
      pattern: "pnpm lint"
---

<objective>
Fix the remaining reported AUTH-32 lint errors, then recapture the repo-wide TypeScript and lint command proof.

Purpose: Convert AUTH-32 from blocked by repo-wide command failures to evidence-backed command pass.
Output: Updated command artifact with exit status 0 for both required commands.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-launch-blocker-proof-pass/48-VERIFICATION.md
@.planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-command-output.md
@src/app/(auth)/register/artist/artist-request-form.tsx
@src/app/admin/team/[id]/edit/page.tsx
@src/components/PixelCard.tsx
@src/components/admin/admin-package-manager.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix residual lint errors in artist, team, PixelCard, and package manager files</name>
  <files>src/app/(auth)/register/artist/artist-request-form.tsx, src/app/admin/team/[id]/edit/page.tsx, src/components/PixelCard.tsx, src/components/admin/admin-package-manager.tsx</files>
  <read_first>
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-command-output.md
    - src/app/(auth)/register/artist/artist-request-form.tsx
    - src/app/admin/team/[id]/edit/page.tsx
    - src/components/PixelCard.tsx
    - src/components/admin/admin-package-manager.tsx
  </read_first>
  <behavior>
    - Local navigation in artist request success state uses `next/link`.
    - The team edit page returns JSX outside the try/catch block.
    - PixelCard uses `@ts-expect-error` rather than `@ts-ignore`.
    - Package example quotes are escaped.
  </behavior>
  <action>
    In `src/app/(auth)/register/artist/artist-request-form.tsx`, import `Link` from `next/link` and replace the local `<a href="/">` and `<a href="/contact">` elements in the success state with `<Link href="/">` and `<Link href="/contact">`. Keep class names and copy unchanged.

    In `src/app/admin/team/[id]/edit/page.tsx`, keep `getTeamMember(id)` inside try/catch, but move the returned JSX below the try/catch. Store the fetched member in a `const member` or `let member` that is assigned before the return. The catch block should still call `notFound()`. Do not construct `<div>`, `<h1>`, or `<TeamMemberForm>` inside the try block.

    In `src/components/PixelCard.tsx`, replace `// @ts-ignore` with `// @ts-expect-error -- dynamic pixel action is selected by fnName and guarded by the pixel implementation`. Keep the next line `pixel[fnName]()` unchanged unless TypeScript requires a narrower typed helper.

    In `src/components/admin/admin-package-manager.tsx`, escape the quote characters in the empty-state examples as `&quot;` or rewrite the example string without literal double quotes. Keep visible copy equivalent: `Examples: "4-Session Pack" 15% off, "8-Session Pack" 25% off, "12-Session Pack" 30% off`.

    Do not add eslint-disable comments.
  </action>
  <acceptance_criteria>
    - `rg -n 'import Link from \"next/link\"|<Link[[:space:]][^>]*href=\"/\"|<Link[[:space:]][^>]*href=\"/contact\"' 'src/app/(auth)/register/artist/artist-request-form.tsx'` returns Link import and both local links.
    - `rg -n '@ts-expect-error -- dynamic pixel action' src/components/PixelCard.tsx` returns the targeted PixelCard suppression, and `rg -n '@ts-ignore' src/components/PixelCard.tsx` returns no matches.
    - `rg -n 'try \\{|notFound\\(\\)|<TeamMemberForm member=\\{member\\}' 'src/app/admin/team/[id]/edit/page.tsx'` returns the fetch/error path and JSX return for review.
    - `rg -n '&quot;4-Session Pack&quot;|4-Session Pack' src/components/admin/admin-package-manager.tsx` returns escaped or equivalent package example copy.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec eslint 'src/app/(auth)/register/artist/artist-request-form.tsx' 'src/app/admin/team/[id]/edit/page.tsx' src/components/PixelCard.tsx src/components/admin/admin-package-manager.tsx</automated>
  </verify>
  <done>Scoped ESLint passes for the remaining residual lint files.</done>
</task>

<task type="auto">
  <name>Task 2: Recapture AUTH-32 command output with passing statuses</name>
  <files>.planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-command-output.md</files>
  <read_first>
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-command-output.md
    - .planning/phases/48-launch-blocker-proof-pass/48-VERIFICATION.md
    - package.json
  </read_first>
  <action>
    Run these commands from the repo root, in this order:
    - `pnpm tsc --noEmit --pretty false`
    - `pnpm lint`

    Replace `.planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-command-output.md` with updated AUTH-32 evidence. Keep these sections:
    - `## pnpm tsc --noEmit`
    - `Command: pnpm tsc --noEmit --pretty false`
    - `exit status: 0`
    - `## pnpm lint`
    - `Command: pnpm lint`
    - `exit status: 0`

    If either command still exits nonzero, keep `exit status: 1`, paste the concise failing file/rule summary, and do not claim AUTH-32 command pass.
  </action>
  <acceptance_criteria>
    - `rg -n 'pnpm tsc --noEmit --pretty false|pnpm lint|exit status: 0' .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-command-output.md` returns both commands and passing exit statuses.
    - `rg -n 'exit status: 1|TS2339|no-explicit-any|react-hooks/static-components|react-hooks/set-state-in-effect|react-hooks/refs|react-hooks/purity|react-hooks/error-boundaries|react/no-unescaped-entities|no-html-link-for-pages|ban-ts-comment' .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-command-output.md` returns no stale blocker text when both commands pass.
  </acceptance_criteria>
  <verify>
    <automated>pnpm tsc --noEmit --pretty false && pnpm lint && rg -n 'pnpm tsc --noEmit --pretty false|pnpm lint|exit status: 0' .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-command-output.md</automated>
  </verify>
  <done>AUTH-32 command artifact records passing TypeScript and lint output.</done>
</task>

</tasks>

<verification>
Run the final automated command exactly as written. If either command fails, create the summary with `status: blocked` and the remaining failing files rather than marking AUTH-32 passed.
</verification>

<success_criteria>
AUTH-32 has evidence-backed command pass for `pnpm tsc --noEmit --pretty false` and `pnpm lint`.
</success_criteria>

<output>
After completion, create `.planning/phases/48-launch-blocker-proof-pass/48-09-SUMMARY.md` with final command statuses and any residual blockers.
</output>
