---
phase: 48-launch-blocker-proof-pass
plan: 13
slug: ios-safari-checkout-proof
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/mobile-checkout-proof.md
  - .planning/phases/48-launch-blocker-proof-pass/48-13-SUMMARY.md
autonomous: false
gap_closure: true
requirements:
  - MOBILE-CHECKOUT-PROOF
user_setup:
  - "Physical iPhone or iPad running Safari"
  - "Stripe test-mode dashboard or screenshots for payment/session evidence"
must_haves:
  truths:
    - "The desktop checkout row remains passed from existing proof and is not rerun as required work."
    - "The real iOS Safari row changes from blocked to passed only after a physical iOS device Stripe test-card purchase."
    - "The iOS row includes Stripe session/payment evidence, Vercel checkout/webhook logs, and app order/receipt evidence."
  artifacts:
    - path: ".planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/mobile-checkout-proof.md"
      provides: "Physical iOS Safari checkout proof"
      contains: "real iOS device"
  key_links:
    - from: ".planning/phases/48-launch-blocker-proof-pass/48-VERIFICATION.md"
      to: ".planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/mobile-checkout-proof.md"
      via: "MOBILE-CHECKOUT-PROOF blocked row"
      pattern: "physical iOS Safari"
---

<objective>
Close the remaining mobile checkout proof gap with a real iOS Safari purchase.

Purpose: Desktop Stripe checkout already passed; this plan targets only the physical iOS Safari row.
Output: Updated mobile checkout proof artifact with iOS evidence or explicit blocked/failure reason.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-launch-blocker-proof-pass/48-VERIFICATION.md
@.planning/phases/48-launch-blocker-proof-pass/48-HUMAN-PROOF-GUIDE.md
@.planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/mobile-checkout-proof.md
@.planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/desktop-checkout-stripe-db-proof.json
@.planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/desktop-checkout-vercel-logs.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run physical iOS Safari checkout and update the proof row</name>
  <files>.planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/mobile-checkout-proof.md</files>
  <read_first>
    - .planning/phases/48-launch-blocker-proof-pass/48-HUMAN-PROOF-GUIDE.md
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/mobile-checkout-proof.md
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/desktop-checkout-stripe-db-proof.json
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/desktop-checkout-vercel-logs.json
  </read_first>
  <action>
    Human real-device proof is required. On a physical iPhone or iPad, open Safari and complete checkout at `https://glitchstudios.io/checkout` using Stripe test card `4242 4242 4242 4242`, any future expiration, any 3-digit CVC, and any valid-looking ZIP/postal code.

    If the cart is empty, add a beat from `https://glitchstudios.io/beats`, then return to checkout. Capture:
    - iOS checkout page before payment screenshot filename.
    - Stripe success or returned success page screenshot filename.
    - App order/receipt screenshot filename.
    - Approximate run time with timezone.
    - Stripe Checkout Session ID or PaymentIntent ID, or screenshot filename if dashboard access is unavailable.
    - Matching Vercel `[checkout]` and webhook log filename or query window.

    Update only the `real iOS device | Safari` row in `mobile-checkout-proof.md` from `blocked: awaiting physical iOS Safari proof` to `passed` when all evidence above exists. Keep the existing desktop row passed; do not downgrade it and do not require rerunning it.

    If iOS checkout fails, write `failed: <one-line failure>` in the iOS row and include available Stripe/Vercel/app evidence. Do not apply speculative code changes in this plan; create a follow-up diagnosis plan if failure evidence points to code.
  </action>
  <acceptance_criteria>
    - `rg -n 'real iOS device \\| Safari .*\\| passed|Stripe Checkout Session|PaymentIntent|iOS checkout page|success page|order/receipt|Vercel.*\\[checkout\\]' .planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/mobile-checkout-proof.md` returns iOS pass evidence, or `rg -n 'real iOS device \\| Safari .*\\| failed:|real iOS device \\| Safari .*\\| blocked:' .planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/mobile-checkout-proof.md` returns explicit non-pass status.
    - `rg -n 'desktop \\| Chrome or Safari .*\\| passed' .planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/mobile-checkout-proof.md` confirms the existing desktop row remains passed.
  </acceptance_criteria>
  <verify>
    <manual>Human confirms the iOS checkout row was run on a physical iOS device in Safari and the evidence references are accurate.</manual>
    <automated>rg -n 'real iOS device \\| Safari|desktop \\| Chrome or Safari|passed|failed:|blocked:' .planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/mobile-checkout-proof.md</automated>
  </verify>
  <done>Real iOS Safari checkout proof is passed or explicitly failed/blocked with evidence.</done>
</task>

</tasks>

<verification>
MOBILE-CHECKOUT-PROOF passes only when the `real iOS device | Safari` row is `passed` with physical-device evidence. Desktop proof is already passed and is not part of this gap closure.
</verification>

<success_criteria>
The iOS Safari row has Stripe, Vercel, and app order/receipt evidence, or remains visibly blocked/failed with the exact reason.
</success_criteria>

<output>
After completion, create `.planning/phases/48-launch-blocker-proof-pass/48-13-SUMMARY.md` with iOS run time, final row status, evidence filenames, and any failure diagnosis.
</output>
