---
phase: 48-launch-blocker-proof-pass
plan: 16
slug: auth32-navigation-carousel-effect-lint-fixes
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/home/splash-overlay.tsx
  - src/components/portfolio/portfolio-carousel.tsx
  - src/components/services/service-grid.tsx
  - src/components/services/service-tabs.tsx
  - src/components/tiles/studios-cross-link-tile.tsx
  - src/components/tiles/tech-cross-link-tile.tsx
autonomous: true
gap_closure: true
requirements:
  - AUTH-32
must_haves:
  truths:
    - "Reported navigation, deep-link, splash, and carousel set-state-in-effect lint errors are fixed without disabling rules."
    - "Splash, carousel, services hash deep links, and cross-brand tile same-tab behavior remain intact."
  artifacts:
    - path: "src/components/tiles/studios-cross-link-tile.tsx"
      provides: "Cross-brand href correction without direct effect setter"
      contains: "setTimeout"
    - path: "src/components/portfolio/portfolio-carousel.tsx"
      provides: "Carousel init/reInit deferred out of effect body"
      contains: "clearTimeout"
  key_links:
    - from: ".planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-command-output.md"
      to: "src/components/tiles/tech-cross-link-tile.tsx"
      via: "react-hooks/set-state-in-effect blocker"
      pattern: "set-state-in-effect"
---

<objective>
Fix the reported AUTH-32 React compiler `set-state-in-effect` lint blockers in navigation, deep-link, splash, and carousel components.

Purpose: Keep React compiler rules enabled while preserving user-visible navigation and interactive behavior.
Output: Scoped ESLint passes for the navigation/carousel/service effect files.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-command-output.md
@src/components/home/splash-overlay.tsx
@src/components/portfolio/portfolio-carousel.tsx
@src/components/services/service-grid.tsx
@src/components/services/service-tabs.tsx
@src/components/tiles/studios-cross-link-tile.tsx
@src/components/tiles/tech-cross-link-tile.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix navigation, deep-link, splash, and carousel set-state effects</name>
  <files>src/components/home/splash-overlay.tsx, src/components/portfolio/portfolio-carousel.tsx, src/components/services/service-grid.tsx, src/components/services/service-tabs.tsx, src/components/tiles/studios-cross-link-tile.tsx, src/components/tiles/tech-cross-link-tile.tsx</files>
  <read_first>
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-command-output.md
    - src/components/home/splash-overlay.tsx
    - src/components/portfolio/portfolio-carousel.tsx
    - src/components/services/service-grid.tsx
    - src/components/services/service-tabs.tsx
    - src/components/tiles/studios-cross-link-tile.tsx
    - src/components/tiles/tech-cross-link-tile.tsx
  </read_first>
  <behavior>
    - Splash mode `never` still starts in the completed state.
    - Portfolio carousel still initializes selected index, snap list, and filter reInit behavior.
    - Service hash deep links still select and expand the matching service after hydration.
    - Cross-brand tiles still use absolute URLs only on the matching production host.
  </behavior>
  <action>
    In `splash-overlay.tsx`, initialize `splashState` with `mode === "never" ? "done" : "pending"` and remove the direct `setSplashState("done")` branch from the effect body.

    In `portfolio-carousel.tsx`, defer the initial `onSelect()` and `setScrollSnaps(...)` calls with a zero-delay timer and cleanup. In the filter reset effect, defer `emblaApi.reInit()` and `setScrollSnaps(...)` with a zero-delay timer and cleanup. Keep Embla event subscriptions and unsubscriptions unchanged.

    In `service-grid.tsx` and `service-tabs.tsx`, keep hash deep-link behavior but schedule `setSelectedSlug`, `setExpandedSlug`, and `setActiveTab` inside a zero-delay timer with cleanup after confirming the hash matches an existing service slug.

    In `studios-cross-link-tile.tsx` and `tech-cross-link-tile.tsx`, keep the current default relative hrefs, but schedule the production-host `setHref(...)` correction with a zero-delay timer and cleanup. Do not change the same-tab behavior and do not add `target="_blank"`.

    Do not use eslint-disable comments.
  </action>
  <acceptance_criteria>
    - `rg -n 'mode === "never" \\? "done" : "pending"|setTimeout\\(|clearTimeout\\(' src/components/home/splash-overlay.tsx src/components/portfolio/portfolio-carousel.tsx src/components/services/service-grid.tsx src/components/services/service-tabs.tsx src/components/tiles/studios-cross-link-tile.tsx src/components/tiles/tech-cross-link-tile.tsx` returns lazy initialization and deferred setter patterns.
    - `rg -n 'target="_blank"' src/components/tiles/studios-cross-link-tile.tsx src/components/tiles/tech-cross-link-tile.tsx` returns no matches.
    - `pnpm exec eslint src/components/home/splash-overlay.tsx src/components/portfolio/portfolio-carousel.tsx src/components/services/service-grid.tsx src/components/services/service-tabs.tsx src/components/tiles/studios-cross-link-tile.tsx src/components/tiles/tech-cross-link-tile.tsx` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec eslint src/components/home/splash-overlay.tsx src/components/portfolio/portfolio-carousel.tsx src/components/services/service-grid.tsx src/components/services/service-tabs.tsx src/components/tiles/studios-cross-link-tile.tsx src/components/tiles/tech-cross-link-tile.tsx</automated>
  </verify>
  <done>Scoped ESLint passes for splash, carousel, service, and cross-link files.</done>
</task>

</tasks>

<verification>
Run the scoped ESLint command for all files in `files_modified` before writing the summary.
</verification>

<success_criteria>
AUTH-32 is no longer blocked by reported set-state-in-effect lint errors in navigation, deep-link, splash, and carousel components.
</success_criteria>

<output>
After completion, create `.planning/phases/48-launch-blocker-proof-pass/48-16-SUMMARY.md` with fixed files, scoped ESLint output, and any residual blockers.
</output>
