---
phase: 48-launch-blocker-proof-pass
plan: 02
slug: resend-email-proof
type: execute
wave: 2
depends_on: ["48-01"]
files_modified:
  - .planning/phases/48-launch-blocker-proof-pass/artifacts/email/
  - .planning/phases/48-launch-blocker-proof-pass/48-02-SUMMARY.md
autonomous: false
requirements:
  - EMAIL-01
  - EMAIL-02
  - EMAIL-03
  - EMAIL-04
  - EMAIL-05
  - EMAIL-06
  - EMAIL-07
  - EMAIL-08
  - AUTH-18
  - AUTH-19
  - AUTH-29
must_haves:
  truths:
    - "Both brand domains have Resend DNS/domain proof captured before transactional email proof is considered complete."
    - "Every launch-required transactional email path has an artifact row with trigger, recipient, Resend event/log ID or screenshot filename, and inbox result."
    - "EMAIL-08 is not marked passed unless SPF/DKIM/DMARC documentation and Resend verified status are captured for both brands."
  artifacts:
    - path: ".planning/phases/48-launch-blocker-proof-pass/artifacts/email/resend-domain-verification.md"
      provides: "Resend and DNS proof for glitchstudios.io and glitchtech.io"
      contains: "status: verified"
    - path: ".planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md"
      provides: "Transactional email smoke matrix"
      contains: "password reset | passed"
  key_links:
    - from: ".planning/phases/24-email-delivery-end-to-end/24-VERIFICATION.md"
      to: ".planning/phases/48-launch-blocker-proof-pass/artifacts/email/resend-domain-verification.md"
      via: "EMAIL-08 gap closure"
      pattern: "Both domains have ZERO email DNS records"
---

<objective>
Complete Resend domain/DNS verification for both brands and prove launch-required transactional email delivery with real dashboard and inbox artifacts.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-launch-blocker-proof-pass/48-CONTEXT.md
@.planning/phases/48-launch-blocker-proof-pass/48-RESEARCH.md
@.planning/phases/48-launch-blocker-proof-pass/48-01-SUMMARY.md
@.planning/phases/24-email-delivery-end-to-end/24-VERIFICATION.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Resend and Cloudflare domain verification</name>
  <files>.planning/phases/48-launch-blocker-proof-pass/artifacts/email/resend-domain-verification.md, .planning/phases/48-launch-blocker-proof-pass/artifacts/email/dns-after.txt</files>
  <read_first>
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/email/resend-domain-verification.md (if it exists)
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/email/dns-before.txt
    - .planning/phases/48-launch-blocker-proof-pass/48-RESEARCH.md
    - .planning/phases/24-email-delivery-end-to-end/24-VERIFICATION.md
  </read_first>
  <action>
    Human dashboard action is required. The user or operator must add both `glitchstudios.io` and `glitchtech.io` in Resend, copy the exact Resend-generated DNS records into Cloudflare, and click Verify in Resend.

    Do not guess DNS values. The artifact must capture the dashboard-generated values or screenshot filenames.

    Create `.planning/phases/48-launch-blocker-proof-pass/artifacts/email/resend-domain-verification.md` with one section per domain. Each domain section must include these exact fields:
    - `domain: glitchstudios.io` or `domain: glitchtech.io`
    - `resend_status: verified`
    - `spf_record: <exact value or screenshot filename>`
    - `dkim_records: <exact values or screenshot filename>`
    - `dmarc_record: <exact value or screenshot filename>`
    - `cloudflare_evidence: <screenshot filename or exported record filename>`
    - `resend_evidence: <screenshot filename>`

    DMARC can be `v=DMARC1; p=none; rua=mailto:<configured-report-mailbox>` for launch baseline if that is what the user configures, but the exact value used must be recorded.

    After DNS changes propagate, rerun the Phase 48 DNS command from `48-RESEARCH.md` and save stdout to `.planning/phases/48-launch-blocker-proof-pass/artifacts/email/dns-after.txt`.
  </action>
  <acceptance_criteria>
    - `rg -n 'domain: glitchstudios\\.io|domain: glitchtech\\.io|resend_status: verified|spf_record:|dkim_records:|dmarc_record:|cloudflare_evidence:|resend_evidence:' .planning/phases/48-launch-blocker-proof-pass/artifacts/email/resend-domain-verification.md` returns all required field names for both domains.
    - `rg -n 'glitchstudios\\.io|glitchtech\\.io' .planning/phases/48-launch-blocker-proof-pass/artifacts/email/dns-after.txt` returns both domains.
    - `rg -n 'v=DMARC1|dmarc_record:' .planning/phases/48-launch-blocker-proof-pass/artifacts/email/resend-domain-verification.md .planning/phases/48-launch-blocker-proof-pass/artifacts/email/dns-after.txt` returns at least one DMARC proof line.
  </acceptance_criteria>
  <verify>
    <manual>Human confirms Resend dashboard shows `verified` for both `glitchstudios.io` and `glitchtech.io` and provides screenshot/export filenames recorded in the artifact.</manual>
    <automated>rg -n 'domain: glitchstudios\\.io|domain: glitchtech\\.io|resend_status: verified|spf_record:|dkim_records:|dmarc_record:|cloudflare_evidence:|resend_evidence:' .planning/phases/48-launch-blocker-proof-pass/artifacts/email/resend-domain-verification.md && rg -n 'glitchstudios\\.io|glitchtech\\.io' .planning/phases/48-launch-blocker-proof-pass/artifacts/email/dns-after.txt</automated>
  </verify>
  <done>Both domains have recorded Resend verified status, DNS evidence, and after-change DNS output.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Transactional email smoke matrix</name>
  <files>.planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md</files>
  <read_first>
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md (if it exists)
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/email/resend-domain-verification.md
    - .planning/phases/48-launch-blocker-proof-pass/48-RESEARCH.md
    - .planning/phases/24-email-delivery-end-to-end/24-01-SUMMARY.md
    - .planning/phases/24-email-delivery-end-to-end/24-02-SUMMARY.md
    - .planning/phases/24-email-delivery-end-to-end/24-03-SUMMARY.md
  </read_first>
  <action>
    Trigger and verify each launch-required email path against a deployed preview or production-like environment that uses the verified Resend domains. Create `.planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md` with this exact markdown table header:

    `| flow | trigger | recipient | resend_event_or_screenshot | inbox_result | status | requirements |`

    Include exactly these flow rows and mark each `status` as `passed` only when Resend evidence and inbox evidence are both recorded:
    - `password reset`
    - `account verification`
    - `booking confirmation`
    - `booking modification`
    - `booking cancellation`
    - `order receipt`
    - `contact auto-reply`
    - `admin contact notification`
    - `newsletter broadcast`
    - `newsletter unsubscribe`
    - `artist approval invite`
    - `request more info`

    For any flow that cannot be triggered in the current environment, write `blocked: <reason>` in the status cell and do not mark its requirement passed in later rollup.
  </action>
  <acceptance_criteria>
    - `rg -n '\\| flow \\| trigger \\| recipient \\| resend_event_or_screenshot \\| inbox_result \\| status \\| requirements \\|' .planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md` returns the table header.
    - `rg -n 'password reset|account verification|booking confirmation|booking modification|booking cancellation|order receipt|contact auto-reply|admin contact notification|newsletter broadcast|newsletter unsubscribe|artist approval invite|request more info' .planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md` returns all twelve flow names.
    - `rg -n 'EMAIL-01|EMAIL-02|EMAIL-03|EMAIL-04|EMAIL-05|EMAIL-06|EMAIL-07|EMAIL-08|AUTH-18|AUTH-19|AUTH-29' .planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md` returns every requirement ID covered by this plan.
  </acceptance_criteria>
  <verify>
    <manual>Human confirms each `passed` row has an inbox result plus a Resend event ID, log ID, or screenshot filename.</manual>
    <automated>rg -n '\\| flow \\| trigger \\| recipient \\| resend_event_or_screenshot \\| inbox_result \\| status \\| requirements \\|' .planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md && rg -n 'password reset|account verification|booking confirmation|booking modification|booking cancellation|order receipt|contact auto-reply|admin contact notification|newsletter broadcast|newsletter unsubscribe|artist approval invite|request more info' .planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md</automated>
  </verify>
  <done>Email smoke matrix exists with each required flow marked passed or blocked with evidence.</done>
</task>

</tasks>

<verification>
Resend domain verification and transactional email proof are complete only when the artifacts contain verified statuses, exact DNS evidence, and real send/inbox proof. If any row remains `blocked`, the final Phase 48 verification must keep the relevant requirement unchecked.
</verification>

<success_criteria>
EMAIL-01 through EMAIL-08 have evidence-backed pass/blocked status, and email-dependent AUTH-18, AUTH-19, and AUTH-29 proof is available for the auth plan.
</success_criteria>

<output>
After completion, create `.planning/phases/48-launch-blocker-proof-pass/48-02-SUMMARY.md` with DNS status, email smoke matrix status counts, and any blocked flow names.
</output>
