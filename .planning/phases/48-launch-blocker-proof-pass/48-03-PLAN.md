---
phase: 48-launch-blocker-proof-pass
plan: 03
slug: auth-oauth-admin-smoke
type: execute
wave: 3
depends_on: ["48-02"]
files_modified:
  - .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/
  - .planning/phases/48-launch-blocker-proof-pass/48-03-SUMMARY.md
autonomous: false
requirements:
  - AUTH-14
  - AUTH-15
  - AUTH-16
  - AUTH-17
  - AUTH-18
  - AUTH-19
  - AUTH-20
  - AUTH-21
  - AUTH-22
  - AUTH-26
  - AUTH-28
  - AUTH-29
  - AUTH-32
must_haves:
  truths:
    - "Google OAuth is proven on both brand hosts with exact callback URI and Better Auth base URL evidence."
    - "Admin application approve, reject, request-info, invite delivery, and applicant password setup are smoke-tested with artifacts."
    - "AUTH-32 has command output for `pnpm tsc --noEmit`, `pnpm lint`, and a both-brand auth smoke matrix."
  artifacts:
    - path: ".planning/phases/48-launch-blocker-proof-pass/artifacts/auth/oauth-env-redirects.md"
      provides: "Vercel env and Google OAuth redirect proof"
      contains: "/api/auth/callback/google"
    - path: ".planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-smoke-matrix.md"
      provides: "Both-brand auth smoke matrix"
      contains: "Google OAuth | passed"
    - path: ".planning/phases/48-launch-blocker-proof-pass/artifacts/auth/admin-application-smoke.md"
      provides: "Admin application workflow proof"
      contains: "approve | passed"
  key_links:
    - from: ".planning/phases/26-brand-aware-auth-ui-redesign/26-VERIFICATION.md"
      to: ".planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-smoke-matrix.md"
      via: "AUTH launch smoke closure"
      pattern: "manual smoke"
---

<objective>
Prove the Phase 26 auth launch contract in a production-like environment: Google OAuth on both brands, email verification and reset, layout soft gate, grandfather migration, and admin application workflows with email delivery.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-launch-blocker-proof-pass/48-CONTEXT.md
@.planning/phases/48-launch-blocker-proof-pass/48-RESEARCH.md
@.planning/phases/48-launch-blocker-proof-pass/48-02-SUMMARY.md
@.planning/phases/26-brand-aware-auth-ui-redesign/26-VERIFICATION.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Capture OAuth env and redirect configuration</name>
  <files>.planning/phases/48-launch-blocker-proof-pass/artifacts/auth/oauth-env-redirects.md</files>
  <read_first>
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/oauth-env-redirects.md (if it exists)
    - .planning/phases/48-launch-blocker-proof-pass/48-RESEARCH.md
    - .planning/phases/26-brand-aware-auth-ui-redesign/26-04-SUMMARY.md
    - src/lib/auth.ts
    - .env.example
  </read_first>
  <action>
    Human dashboard action is required for Vercel and Google Cloud Console. Create `.planning/phases/48-launch-blocker-proof-pass/artifacts/auth/oauth-env-redirects.md` with these exact sections:

    `## Vercel Env`
    Include rows for `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `BETTER_AUTH_URL`, `BETTER_AUTH_SECRET`, `RESEND_API_KEY`, and `ADMIN_NOTIFICATION_EMAIL`. Each row must be `present`, `missing`, or `blocked: <reason>`. Do not write secret values.

    `## Google OAuth Redirect URIs`
    Include exactly these redirect URI rows, each with `status: configured` or `status: blocked: <reason>`:
    - `https://glitchstudios.io/api/auth/callback/google`
    - `https://glitchtech.io/api/auth/callback/google`

    `## Better Auth Base URL`
    Record the target `BETTER_AUTH_URL` used for each deployed host. If the app uses host-aware auth instead of one static base URL, record the exact implementation evidence from `src/lib/auth.ts`.
  </action>
  <acceptance_criteria>
    - `rg -n 'GOOGLE_CLIENT_ID|GOOGLE_CLIENT_SECRET|BETTER_AUTH_URL|BETTER_AUTH_SECRET|RESEND_API_KEY|ADMIN_NOTIFICATION_EMAIL' .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/oauth-env-redirects.md` returns all six env var names.
    - `rg -n 'https://glitchstudios\\.io/api/auth/callback/google|https://glitchtech\\.io/api/auth/callback/google|status: configured|status: blocked:' .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/oauth-env-redirects.md` returns both redirect URIs and a status value.
    - `rg -n 'secret|client_secret' .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/oauth-env-redirects.md` does not reveal raw secret values; only env var names may appear.
  </acceptance_criteria>
  <verify>
    <manual>Human confirms Google Cloud Console has both exact redirect URIs configured and Vercel has the required env vars present.</manual>
    <automated>rg -n 'GOOGLE_CLIENT_ID|GOOGLE_CLIENT_SECRET|BETTER_AUTH_URL|BETTER_AUTH_SECRET|RESEND_API_KEY|ADMIN_NOTIFICATION_EMAIL' .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/oauth-env-redirects.md && rg -n 'https://glitchstudios\\.io/api/auth/callback/google|https://glitchtech\\.io/api/auth/callback/google' .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/oauth-env-redirects.md</automated>
  </verify>
  <done>OAuth env and redirect configuration evidence is captured without exposing secrets.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Both-brand auth smoke matrix</name>
  <files>.planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-smoke-matrix.md</files>
  <read_first>
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-smoke-matrix.md (if it exists)
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/oauth-env-redirects.md
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md
    - .planning/phases/26-brand-aware-auth-ui-redesign/26-VERIFICATION.md
  </read_first>
  <action>
    Create `.planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-smoke-matrix.md` with this exact table header:

    `| brand_host | surface_or_flow | evidence | status | requirements |`

    Include rows for both `glitchstudios.io` and `glitchtech.io` for these flows:
    - `Google OAuth`
    - `login email/password`
    - `register customer`
    - `forgot password`
    - `reset password`
    - `verify email`
    - `unverified soft gate`
    - `sign out exception`

    Mark a row `passed` only when evidence includes a screenshot/log filename or a concrete browser result. Mark `blocked: <reason>` for missing credentials, missing deploy URL, missing env, or failed OAuth.

    Run `pnpm tsc --noEmit` and `pnpm lint` after any auth fixes required by the smoke. Save command summaries in `.planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-command-output.md`.
  </action>
  <acceptance_criteria>
    - `rg -n '\\| brand_host \\| surface_or_flow \\| evidence \\| status \\| requirements \\|' .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-smoke-matrix.md` returns the table header.
    - `rg -n 'glitchstudios\\.io|glitchtech\\.io|Google OAuth|login email/password|register customer|forgot password|reset password|verify email|unverified soft gate|sign out exception' .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-smoke-matrix.md` returns both hosts and all eight flows.
    - `rg -n 'pnpm tsc --noEmit|pnpm lint|exit status:' .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-command-output.md` returns both command names and statuses.
  </acceptance_criteria>
  <verify>
    <manual>Human confirms both brand hosts were tested in a real browser and all `passed` rows have evidence filenames or concrete browser results.</manual>
    <automated>rg -n 'glitchstudios\\.io|glitchtech\\.io|Google OAuth|forgot password|reset password|verify email|unverified soft gate' .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-smoke-matrix.md && rg -n 'pnpm tsc --noEmit|pnpm lint|exit status:' .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-command-output.md</automated>
  </verify>
  <done>Both-brand auth smoke matrix and command outputs are captured.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Admin application, invite, and migration smoke</name>
  <files>.planning/phases/48-launch-blocker-proof-pass/artifacts/auth/admin-application-smoke.md, .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/grandfather-migration.md</files>
  <read_first>
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/admin-application-smoke.md (if it exists)
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/grandfather-migration.md (if it exists)
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md
    - .planning/phases/26-brand-aware-auth-ui-redesign/26-10-SUMMARY.md
    - .planning/phases/26-brand-aware-auth-ui-redesign/26-11-SUMMARY.md
  </read_first>
  <action>
    Smoke `/admin/applications` in a deployed environment with admin credentials. Create `.planning/phases/48-launch-blocker-proof-pass/artifacts/auth/admin-application-smoke.md` with rows for:
    - `list | passed`
    - `detail drawer | passed`
    - `approve | passed`
    - `reject | passed`
    - `request more info | passed`
    - `artist approval invite email | passed`
    - `request more info email | passed`
    - `approved applicant password setup | passed`
    - `approved applicant sign in | passed`

    Use `blocked: <reason>` for any row that cannot be proven, and include evidence filenames or Resend event IDs for the invite/info email rows.

    Create `.planning/phases/48-launch-blocker-proof-pass/artifacts/auth/grandfather-migration.md` with the exact SQL evidence used in the target environment:
    - `SELECT * FROM phase26_migration_meta WHERE key = 'grandfather_email_verified';`
    - `SELECT COUNT(*) FROM "user" WHERE "emailVerified" IS NOT TRUE;`
    - `SELECT to_regclass('public.artist_applications');`

    If DB access is unavailable, write `status: blocked: no target DATABASE_URL or DB console access` and keep AUTH-28 blocked in final rollup.
  </action>
  <acceptance_criteria>
    - `rg -n 'list \\| passed|detail drawer \\| passed|approve \\| passed|reject \\| passed|request more info \\| passed|artist approval invite email|approved applicant password setup|approved applicant sign in|blocked:' .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/admin-application-smoke.md` returns admin workflow status rows.
    - `rg -n 'grandfather_email_verified|COUNT\\(\\*\\).*emailVerified|to_regclass\\(' .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/grandfather-migration.md` returns all three SQL evidence references, or `rg -n 'status: blocked: no target DATABASE_URL or DB console access' .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/grandfather-migration.md` returns the explicit blocked state.
    - `rg -n 'AUTH-14|AUTH-15|AUTH-16|AUTH-17|AUTH-18|AUTH-19|AUTH-28' .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/admin-application-smoke.md .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/grandfather-migration.md` returns the requirement IDs.
  </acceptance_criteria>
  <verify>
    <manual>Human confirms admin credentials were used and every `passed` admin row was tested in the deployed environment.</manual>
    <automated>rg -n 'approve|reject|request more info|artist approval invite email|grandfather_email_verified|AUTH-28|blocked:' .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/admin-application-smoke.md .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/grandfather-migration.md</automated>
  </verify>
  <done>Admin application workflow and migration evidence are captured with passed or blocked statuses.</done>
</task>

</tasks>

<verification>
All auth rows must have evidence or explicit blocked status. AUTH checkboxes may only be changed later for rows with passed proof and command output.
</verification>

<success_criteria>
AUTH-14 through AUTH-22, AUTH-26, AUTH-28, AUTH-29, and AUTH-32 have evidence-backed status across env, OAuth, email-dependent auth flows, admin workflows, migration, tsc, and lint.
</success_criteria>

<output>
After completion, create `.planning/phases/48-launch-blocker-proof-pass/48-03-SUMMARY.md` with OAuth status, auth smoke counts, admin smoke counts, migration status, and any blocked rows.
</output>
