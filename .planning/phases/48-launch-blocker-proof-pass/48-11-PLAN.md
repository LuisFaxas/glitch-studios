---
phase: 48-launch-blocker-proof-pass
plan: 11
slug: google-oauth-and-auth-smoke
type: execute
wave: 3
depends_on: ["48-09", "48-10"]
files_modified:
  - .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/oauth-env-redirects.md
  - .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-smoke-matrix.md
  - .planning/phases/48-launch-blocker-proof-pass/48-11-SUMMARY.md
autonomous: false
gap_closure: true
requirements:
  - AUTH-20
  - AUTH-21
  - AUTH-22
  - AUTH-26
  - AUTH-29
  - AUTH-32
user_setup:
  - "Google Cloud Console OAuth client access"
  - "Vercel Production env access for GOOGLE_CLIENT_ID and GOOGLE_CLIENT_SECRET"
  - "Test inbox and production-safe test account credentials"
must_haves:
  truths:
    - "Vercel Production has Google OAuth env vars present without exposing secret values."
    - "Google Cloud Console has both production brand callback URLs configured exactly."
    - "Google OAuth and auth surface smoke rows are updated from blocked to passed only with browser evidence."
  artifacts:
    - path: ".planning/phases/48-launch-blocker-proof-pass/artifacts/auth/oauth-env-redirects.md"
      provides: "Google env and redirect proof"
      contains: "GOOGLE_CLIENT_ID"
    - path: ".planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-smoke-matrix.md"
      provides: "Both-brand OAuth/auth smoke rows"
      contains: "Google OAuth"
  key_links:
    - from: ".planning/phases/48-launch-blocker-proof-pass/artifacts/auth/oauth-env-redirects.md"
      to: "src/lib/auth.ts"
      via: "Better Auth env-gated social provider"
      pattern: "socialProviders"
    - from: ".planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md"
      to: ".planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-smoke-matrix.md"
      via: "forgot/reset and verification email-dependent auth proof"
      pattern: "password reset"
---

<objective>
Configure and prove Google OAuth plus remaining auth smoke rows on both production brand hosts.

Purpose: Close AUTH-20, AUTH-21, AUTH-22, AUTH-26, AUTH-29, and the manual-smoke portion of AUTH-32.
Output: OAuth env/redirect artifact and auth smoke matrix with passed or explicitly blocked rows.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-launch-blocker-proof-pass/48-VERIFICATION.md
@.planning/phases/48-launch-blocker-proof-pass/48-HUMAN-PROOF-GUIDE.md
@.planning/phases/48-launch-blocker-proof-pass/artifacts/auth/oauth-env-redirects.md
@.planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-smoke-matrix.md
@.planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-command-output.md
@.planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md
@src/lib/auth.ts
@src/lib/auth-providers.ts
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Configure Google OAuth env and callback redirects</name>
  <files>.planning/phases/48-launch-blocker-proof-pass/artifacts/auth/oauth-env-redirects.md</files>
  <read_first>
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/oauth-env-redirects.md
    - .planning/phases/48-launch-blocker-proof-pass/48-HUMAN-PROOF-GUIDE.md
    - src/lib/auth.ts
    - src/lib/auth-providers.ts
    - .env.example
  </read_first>
  <action>
    Human dashboard action is required. Configure Vercel Production with `GOOGLE_CLIENT_ID` and `GOOGLE_CLIENT_SECRET`. Do not write raw secret values to any file.

    In Google Cloud Console, configure these exact authorized redirect URIs:
    - `https://glitchstudios.io/api/auth/callback/google`
    - `https://glitchtech.io/api/auth/callback/google`

    Update `oauth-env-redirects.md` so:
    - `GOOGLE_CLIENT_ID` status changes from `missing` to `present`.
    - `GOOGLE_CLIENT_SECRET` status changes from `missing` to `present`.
    - Each Google redirect URI status changes from `blocked: ...` to `configured`.
    - Meta and GitHub env rows may remain `missing` if still unconfigured; their hidden-button behavior is allowed and should remain documented.
    - `BETTER_AUTH_URL`, `BETTER_AUTH_SECRET`, and `RESEND_API_KEY` status remains present if still present.
  </action>
  <acceptance_criteria>
    - `rg -n '\\| GOOGLE_CLIENT_ID \\| present \\||\\| GOOGLE_CLIENT_SECRET \\| present \\|' .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/oauth-env-redirects.md` returns both Google env rows as present.
    - `rg -n 'https://glitchstudios\\.io/api/auth/callback/google.*configured|https://glitchtech\\.io/api/auth/callback/google.*configured' .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/oauth-env-redirects.md` returns both configured redirect rows.
    - `rg -n 'raw secret|client secret value|GOOGLE_CLIENT_SECRET=.*[A-Za-z0-9_-]{10,}' .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/oauth-env-redirects.md` returns no raw secret values.
  </acceptance_criteria>
  <verify>
    <manual>Human confirms Vercel Production env and Google Cloud Console redirect settings are configured exactly.</manual>
    <automated>rg -n '\\| GOOGLE_CLIENT_ID \\| present \\||\\| GOOGLE_CLIENT_SECRET \\| present \\||https://glitchstudios\\.io/api/auth/callback/google.*configured|https://glitchtech\\.io/api/auth/callback/google.*configured' .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/oauth-env-redirects.md</automated>
  </verify>
  <done>Google OAuth env and redirect proof is recorded without exposing secrets.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run both-brand Google OAuth and auth surface smoke</name>
  <files>.planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-smoke-matrix.md</files>
  <read_first>
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-smoke-matrix.md
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/oauth-env-redirects.md
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-command-output.md
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md
  </read_first>
  <action>
    Test both `https://glitchstudios.io` and `https://glitchtech.io` in a real browser. Update these rows in `auth-smoke-matrix.md` from `blocked` to `passed` only with screenshot filenames, browser result JSON, or concrete log evidence:
    - `Google OAuth`: click Google button, complete provider login, return to app, session exists on the same brand host.
    - `social row on /login`: Google visible; Meta/GitHub hidden if env vars are still missing or visible/configured if env vars were added.
    - `social row on /register?role=customer` or `/register/customer`: Google visible on the customer registration surface.
    - `login email/password`: production-safe test account can sign in.
    - `register customer`: registration reaches verification-required state; verification email evidence must be linked to `email-smoke-matrix.md`.
    - `forgot password` and `reset password`: use the passed password reset row from Plan 48-10; reset link works and new password signs in.
    - `verify email`: use the passed account verification row from Plan 48-10; verify link works.
    - `protected-page redirect for unverified user`: authenticated unverified test user is redirected to `/verify-email` or the configured soft-gate route.
    - `/verify-email allowed for unverified user`: remains accessible.
    - `/api/auth/* allowed for unverified user`: remains accessible.
    - `sign out allowed for unverified user`: unverified test user can sign out.

    Do not mark AUTH-32 manual smoke passed unless `auth-command-output.md` has both command exit statuses at 0 and the both-brand smoke rows above pass or have user-approved deferred status.
  </action>
  <acceptance_criteria>
    - `rg -n 'glitchstudios\\.io.*Google OAuth.*passed|glitchtech\\.io.*Google OAuth.*passed' .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-smoke-matrix.md` returns both brand Google OAuth pass rows.
    - `rg -n 'protected-page redirect for unverified user.*passed|sign out allowed for unverified user.*passed|/verify-email allowed for unverified user.*passed|/api/auth/\\* allowed for unverified user.*passed' .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-smoke-matrix.md` returns AUTH-26 soft-gate rows.
    - `rg -n 'forgot password.*passed|reset password.*passed|verify email.*passed|AUTH-29' .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-smoke-matrix.md` returns email-dependent auth rows.
    - `rg -n 'pnpm tsc --noEmit --pretty false|pnpm lint|exit status: 0' .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-command-output.md` returns command pass evidence.
  </acceptance_criteria>
  <verify>
    <manual>Human confirms Google OAuth and auth smoke rows marked `passed` were tested on both production brand hosts.</manual>
    <automated>rg -n 'glitchstudios\\.io.*Google OAuth.*passed|glitchtech\\.io.*Google OAuth.*passed|protected-page redirect for unverified user.*passed|sign out allowed for unverified user.*passed|forgot password.*passed|reset password.*passed|verify email.*passed' .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-smoke-matrix.md</automated>
  </verify>
  <done>Google OAuth, auth surfaces, soft gate, forgot/reset, and verification smoke rows are passed or explicitly blocked.</done>
</task>

</tasks>

<verification>
AUTH-20, AUTH-21, AUTH-22, AUTH-26, AUTH-29, and AUTH-32 can only pass where the artifacts show configured env/redirects, command pass, and both-brand browser proof.
</verification>

<success_criteria>
Google OAuth is live on both brand hosts, social rows behave correctly, soft-gate exceptions are proven, and email-dependent auth flows are linked to Resend/inbox proof.
</success_criteria>

<output>
After completion, create `.planning/phases/48-launch-blocker-proof-pass/48-11-SUMMARY.md` with OAuth config status, passed auth rows, and any blocked rows.
</output>
