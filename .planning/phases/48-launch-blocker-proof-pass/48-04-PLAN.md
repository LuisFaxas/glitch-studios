---
phase: 48-launch-blocker-proof-pass
plan: 04
slug: mobile-checkout-proof
type: execute
wave: 2
depends_on: ["48-01"]
files_modified:
  - .planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/
  - .planning/phases/48-launch-blocker-proof-pass/48-04-SUMMARY.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "The original Phase 23 mobile checkout carry-forward is closed only by real iOS Safari and desktop Stripe test-card purchase proof."
    - "Checkout failures produce Vercel/Stripe/app evidence before any fix is attempted."
    - "Any code or env fix is diagnosis-driven and followed by the same iOS and desktop purchase path."
  artifacts:
    - path: ".planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/checkout-env-diagnostics.md"
      provides: "Checkout env and deployed URL readiness"
      contains: "deployed_url:"
    - path: ".planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/mobile-checkout-proof.md"
      provides: "iOS Safari and desktop checkout proof"
      contains: "iOS Safari | passed"
  key_links:
    - from: ".planning/phases/23-debug-broken-pages-missing-routes/23-VERIFICATION.md"
      to: ".planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/mobile-checkout-proof.md"
      via: "mobile checkout purchase proof"
      pattern: "Phase 48 carry-forward: mobile checkout purchase proof remains open"
---

<objective>
Prove the Phase 23 mobile checkout carry-forward on the original target path: real iOS Safari plus desktop Stripe test-card purchase through a deployed preview or production-like URL.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-launch-blocker-proof-pass/48-CONTEXT.md
@.planning/phases/48-launch-blocker-proof-pass/48-RESEARCH.md
@.planning/phases/48-launch-blocker-proof-pass/48-01-SUMMARY.md
@.planning/phases/23-debug-broken-pages-missing-routes/23-VERIFICATION.md
@.planning/phases/23-debug-broken-pages-missing-routes/23-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare checkout diagnostics and deployed URL handoff</name>
  <files>.planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/checkout-env-diagnostics.md</files>
  <read_first>
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/checkout-env-diagnostics.md (if it exists)
    - .planning/phases/48-launch-blocker-proof-pass/48-RESEARCH.md
    - .planning/phases/23-debug-broken-pages-missing-routes/23-05-SUMMARY.md
    - src/app/api/checkout/route.ts
    - src/app/(public)/checkout/page.tsx
    - .env.example
  </read_first>
  <action>
    Create `.planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/checkout-env-diagnostics.md` with fields:
    - `deployed_url: <preview-or-production-url or blocked: no deployed URL>`
    - `stripe_mode: test`
    - `stripe_public_key_present: present|missing|blocked`
    - `stripe_secret_key_present: present|missing|blocked`
    - `stripe_webhook_secret_present: present|missing|blocked`
    - `checkout_route_error_logging: present|missing`
    - `vercel_log_query: [checkout]`
    - `stripe_test_card: 4242 4242 4242 4242`

    If no deployed URL exists, run `vercel --yes` only if Vercel CLI is authenticated and the repo is in a deployable state. If deployment fails, record `deployed_url: blocked: vercel deploy failed` plus the error summary; do not claim checkout proof.
  </action>
  <acceptance_criteria>
    - `rg -n 'deployed_url:|stripe_mode: test|stripe_public_key_present:|stripe_secret_key_present:|stripe_webhook_secret_present:|checkout_route_error_logging:|vercel_log_query: \\[checkout\\]|stripe_test_card: 4242 4242 4242 4242' .planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/checkout-env-diagnostics.md` returns all required fields.
    - `rg -n 'present|missing|blocked:' .planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/checkout-env-diagnostics.md` returns status values for env/deploy fields.
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'deployed_url:|stripe_mode: test|stripe_test_card: 4242 4242 4242 4242|vercel_log_query: \\[checkout\\]' .planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/checkout-env-diagnostics.md</automated>
  </verify>
  <done>Checkout diagnostics and deployed URL handoff are recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Real iOS Safari and desktop Stripe purchase proof</name>
  <files>.planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/mobile-checkout-proof.md</files>
  <read_first>
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/mobile-checkout-proof.md (if it exists)
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/checkout-env-diagnostics.md
    - .planning/phases/23-debug-broken-pages-missing-routes/23-VERIFICATION.md
  </read_first>
  <action>
    Human real-device proof is required. Create `.planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/mobile-checkout-proof.md` with this exact table header:

    `| device_path | browser | checkout_url | stripe_payment_or_session | vercel_log_evidence | app_order_or_receipt | status |`

    Include at least these rows:
    - `real iOS device | Safari | <url> | <Stripe test payment/session ID or screenshot> | <Vercel [checkout] log filename> | <order/receipt evidence> | passed`
    - `desktop | Chrome or Safari | <url> | <Stripe test payment/session ID or screenshot> | <Vercel [checkout] log filename> | <order/receipt evidence> | passed`

    Use Stripe test card `4242 4242 4242 4242`, any future expiry, and any 3-digit CVC. If either path fails, record `failed: <one-line failure>` plus Vercel and Stripe evidence, apply only diagnosis-driven code/env fixes, then rerun the same row until it is `passed` or remains explicitly `blocked`.
  </action>
  <acceptance_criteria>
    - `rg -n '\\| device_path \\| browser \\| checkout_url \\| stripe_payment_or_session \\| vercel_log_evidence \\| app_order_or_receipt \\| status \\|' .planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/mobile-checkout-proof.md` returns the table header.
    - `rg -n 'real iOS device \\| Safari|desktop \\| Chrome or Safari|4242 4242 4242 4242|passed|failed:|blocked:' .planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/mobile-checkout-proof.md` returns both device paths and final statuses.
    - `rg -n 'Stripe|Vercel|order|receipt|session|payment' .planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/mobile-checkout-proof.md` returns evidence references.
  </acceptance_criteria>
  <verify>
    <manual>Human confirms the iOS Safari row was run on a physical iOS device and the desktop row was run in a desktop browser with Stripe test payment/session proof.</manual>
    <automated>rg -n 'real iOS device \\| Safari|desktop \\| Chrome or Safari|passed|failed:|blocked:' .planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/mobile-checkout-proof.md</automated>
  </verify>
  <done>Checkout proof has passed or explicit blocked/failure evidence for iOS Safari and desktop.</done>
</task>

</tasks>

<verification>
This plan closes the mobile checkout carry-forward only when both required rows are `passed`. If either row is failed or blocked, Phase 48 final verification must keep mobile checkout proof open.
</verification>

<success_criteria>
Real iOS Safari and desktop checkout purchases complete with Stripe test payment/session evidence, Vercel checkout logs, and app order/receipt proof.
</success_criteria>

<output>
After completion, create `.planning/phases/48-launch-blocker-proof-pass/48-04-SUMMARY.md` with deployed URL, iOS result, desktop result, and any diagnosis-driven fixes.
</output>
