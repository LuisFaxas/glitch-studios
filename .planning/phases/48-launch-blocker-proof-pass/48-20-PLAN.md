---
phase: 48-launch-blocker-proof-pass
plan: 20
slug: ios-checkout-order-receipt-proof
type: execute
wave: 6
depends_on: ["48-17"]
files_modified:
  - .planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/mobile-checkout-proof.md
  - .planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md
  - .planning/phases/48-launch-blocker-proof-pass/48-20-SUMMARY.md
autonomous: false
gap_closure: true
requirements:
  - MOBILE-CHECKOUT-PROOF
  - EMAIL-05
user_setup:
  - "Physical iPhone or iPad running Safari"
  - "Stripe test-mode dashboard access or screenshots"
  - "Purchaser inbox access for the order receipt"
must_haves:
  truths:
    - "The remaining checkout proof is a real physical iOS Safari purchase, not desktop or emulation."
    - "The iOS row has Stripe session/payment, Vercel checkout/webhook, app order/receipt, device/browser, and screenshot evidence before it can pass."
    - "The EMAIL-05 order receipt row has matching Resend event, inbox, content, download-link, and Stripe session/payment evidence before it can pass."
  artifacts:
    - path: ".planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/mobile-checkout-proof.md"
      provides: "Physical iOS Safari checkout proof"
      contains: "device_browser: iOS Safari"
    - path: ".planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md"
      provides: "Order receipt email proof"
      contains: "flow: order receipt"
  key_links:
    - from: ".planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/mobile-checkout-proof.md"
      to: ".planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md"
      via: "Stripe session/payment ID tying checkout to order receipt"
      pattern: "stripe_payment_intent_or_session"
---

<objective>
Collect physical iOS Safari checkout proof and tie the resulting order receipt email to the same Stripe test purchase.

Purpose: Close MOBILE-CHECKOUT-PROOF and EMAIL-05 without rerunning already-passed desktop checkout as required work.
Output: Updated checkout proof and order receipt email proof artifacts with passed or explicit blocked/failed states.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-launch-blocker-proof-pass/48-VERIFICATION.md
@.planning/phases/48-launch-blocker-proof-pass/48-HUMAN-PROOF-GUIDE.md
@.planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/mobile-checkout-proof.md
@.planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/desktop-checkout-stripe-db-proof.json
@.planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/desktop-checkout-vercel-logs.json
@.planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run physical iOS Safari checkout and record payment proof</name>
  <files>.planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/mobile-checkout-proof.md</files>
  <read_first>
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/mobile-checkout-proof.md
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/desktop-checkout-stripe-db-proof.json
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/desktop-checkout-vercel-logs.json
    - .planning/phases/48-launch-blocker-proof-pass/48-HUMAN-PROOF-GUIDE.md
  </read_first>
  <action>
    On a physical iPhone or iPad, open Safari and complete checkout at `https://glitchstudios.io/checkout`. If the cart is empty, add a test beat from `https://glitchstudios.io/beats` and return to checkout.

    Use Stripe's public test card only: `4242 4242 4242 4242`, a future expiration date, any 3-digit CVC, and any valid-looking ZIP/postal code. Do not use or record a real card number.

    Update the `real iOS device | Safari` row or add a `## 48-20 iOS Checkout Evidence` section in `mobile-checkout-proof.md` with these exact fields:
    - `tested_at: <ISO timestamp with timezone>`
    - `device_browser: iOS Safari on physical iPhone/iPad`
    - `checkout_url: https://glitchstudios.io/checkout`
    - `pre_payment_screenshot: <filename>`
    - `stripe_success_or_return_screenshot: <filename>`
    - `app_order_or_receipt_screenshot: <filename>`
    - `stripe_payment_intent_or_session: <PaymentIntent ID, Checkout Session ID, or dashboard screenshot filename>`
    - `vercel_checkout_log_window_or_file: <filename or query window>`
    - `vercel_webhook_log_window_or_file: <filename or query window>`
    - `app_order_id: <order id or blocked: <reason>>`
    - `status: passed|failed: <reason>|blocked: <reason>`
    - `blocked_reason: <only if status is not passed>`
    - `secret_safety: no API keys, OAuth secrets, passwords, tokens, full cookies, or real card numbers in artifacts`

    Keep the existing desktop row passed. If iOS checkout fails, record the failure and available Stripe/Vercel/app evidence instead of guessing at a fix.
  </action>
  <acceptance_criteria>
    - `rg -n '## 48-20 iOS Checkout Evidence|tested_at:|device_browser: iOS Safari|checkout_url: https://glitchstudios\\.io/checkout|pre_payment_screenshot:|stripe_success_or_return_screenshot:|app_order_or_receipt_screenshot:|stripe_payment_intent_or_session:|vercel_checkout_log_window_or_file:|vercel_webhook_log_window_or_file:|status: (passed|failed:|blocked:)' .planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/mobile-checkout-proof.md` returns iOS evidence fields.
    - `rg -n 'desktop \\| Chrome or Safari .*\\| passed|desktop.*passed' .planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/mobile-checkout-proof.md` confirms desktop proof remains passed.
    - `! rg -n '(card number: [0-9]{12,19}|sk_(live|test)_[A-Za-z0-9]|STRIPE_WEBHOOK_SECRET=|Cookie:|password:)' .planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/mobile-checkout-proof.md`
  </acceptance_criteria>
  <verify>
    <manual>Human confirms the checkout was run on a physical iOS device in Safari and every passed evidence reference is accurate.</manual>
    <automated>rg -n 'device_browser: iOS Safari|stripe_payment_intent_or_session:|vercel_checkout_log_window_or_file:|vercel_webhook_log_window_or_file:|status: (passed|failed:|blocked:)' .planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/mobile-checkout-proof.md</automated>
  </verify>
  <done>Physical iOS Safari checkout proof is passed, failed, or blocked with concrete evidence and no secret leakage.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Prove the matching order receipt email</name>
  <files>.planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md, .planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/mobile-checkout-proof.md</files>
  <read_first>
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/mobile-checkout-proof.md
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/email/single-domain-testing-scope.md
    - .planning/phases/48-launch-blocker-proof-pass/48-HUMAN-PROOF-GUIDE.md
  </read_first>
  <action>
    Using the same Stripe checkout run from Task 1, update the `flow: order receipt` row in `email-smoke-matrix.md`.

    Fill these exact fields:
    - `flow: order receipt`
    - `tested_at: <same run timestamp or later inbox check timestamp>`
    - `trigger_url_or_action: physical iOS Safari checkout from 48-20`
    - `recipient_inbox_alias: <redacted purchaser inbox alias>`
    - `stripe_payment_intent_or_session: <same ID or screenshot filename as checkout artifact>`
    - `resend_event_id_or_screenshot: <event ID or screenshot filename>`
    - `inbox_screenshot: <filename>`
    - `content_assertions: items present; license tier per beat present; total present; download links present`
    - `link_result: download link opens or blocked: <reason>`
    - `status: passed|blocked: <reason>`
    - `blocked_reason: <only if status is not passed>`

    EMAIL-05 can pass only if the order receipt email ties to the same Stripe session/payment as the iOS checkout or a clearly documented replacement Stripe test purchase. If iOS checkout failed and no replacement purchase is run, keep EMAIL-05 blocked.
  </action>
  <acceptance_criteria>
    - `rg -n 'flow: order receipt|trigger_url_or_action: physical iOS Safari checkout from 48-20|stripe_payment_intent_or_session:|resend_event_id_or_screenshot:|inbox_screenshot:|content_assertions: items present; license tier per beat present; total present; download links present|link_result:|status: (passed|blocked:)' .planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md` returns order receipt fields.
    - `rg -n 'stripe_payment_intent_or_session:' .planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/mobile-checkout-proof.md .planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md` returns the cross-link field in both artifacts.
    - `! rg -n '(card number: [0-9]{12,19}|sk_(live|test)_[A-Za-z0-9]|STRIPE_WEBHOOK_SECRET=|Cookie:|password:|download_token=[A-Za-z0-9_-]{8,})' .planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md`
  </acceptance_criteria>
  <verify>
    <manual>Human confirms the order receipt email belongs to the checkout run and that the download/link proof is real.</manual>
    <automated>rg -n 'flow: order receipt|stripe_payment_intent_or_session:|resend_event_id_or_screenshot:|inbox_screenshot:|link_result:|status: (passed|blocked:)' .planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md</automated>
  </verify>
  <done>EMAIL-05 order receipt proof is passed or explicitly blocked with the missing evidence named.</done>
</task>

</tasks>

<verification>
Run both automated checks. Confirm the iOS checkout and order receipt artifacts share the same Stripe session/payment reference or clearly name why EMAIL-05 remains blocked.
</verification>

<success_criteria>
MOBILE-CHECKOUT-PROOF passes only with physical iOS Safari evidence. EMAIL-05 passes only with matching order receipt Resend, inbox, content, download-link, and Stripe session/payment proof.
</success_criteria>

<output>
After completion, create `.planning/phases/48-launch-blocker-proof-pass/48-20-SUMMARY.md` with iOS checkout status, order receipt status, evidence filenames, and secret-safety confirmation.
</output>
