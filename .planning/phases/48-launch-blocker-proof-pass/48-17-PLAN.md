---
phase: 48-launch-blocker-proof-pass
plan: 17
slug: glitchtech-transactional-email-evidence
type: execute
wave: 5
depends_on: ["48-14"]
files_modified:
  - .planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md
  - .planning/phases/48-launch-blocker-proof-pass/artifacts/email/resend-domain-verification.md
  - .planning/phases/48-launch-blocker-proof-pass/artifacts/email/single-domain-testing-scope.md
  - .planning/phases/48-launch-blocker-proof-pass/48-17-SUMMARY.md
autonomous: false
gap_closure: true
requirements:
  - EMAIL-01
  - EMAIL-02
  - EMAIL-03
  - EMAIL-04
  - EMAIL-05
  - EMAIL-06
  - EMAIL-07
  - EMAIL-08
  - AUTH-29
user_setup:
  - "Resend dashboard access for the verified glitchtech.io sender"
  - "Recipient inbox access for password reset, verification, booking, contact, newsletter, and receipt tests"
must_haves:
  truths:
    - "Current transactional email proof uses only the verified `glitchtech.io` Resend sender."
    - "`glitchstudios.io` multi-domain Resend/DMARC proof remains deferred by user decision and is not marked passed."
    - "Each passed email row has a real Resend event or screenshot, inbox proof, content assertions, and link result where applicable."
  artifacts:
    - path: ".planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md"
      provides: "Single-domain transactional email smoke matrix"
      contains: "resend_event_id_or_screenshot"
    - path: ".planning/phases/48-launch-blocker-proof-pass/artifacts/email/single-domain-testing-scope.md"
      provides: "Current sender and user-approved domain boundary"
      contains: "sender_domain: glitchtech.io"
    - path: ".planning/phases/48-launch-blocker-proof-pass/artifacts/email/resend-domain-verification.md"
      provides: "EMAIL-08 partial/deferred state"
      contains: "glitchstudios.io"
  key_links:
    - from: ".planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md"
      to: ".planning/phases/48-launch-blocker-proof-pass/48-VERIFICATION.md"
      via: "EMAIL-01 through EMAIL-07 final-status evidence"
      pattern: "EMAIL-01"
    - from: ".planning/phases/48-launch-blocker-proof-pass/artifacts/email/single-domain-testing-scope.md"
      to: ".planning/phases/48-launch-blocker-proof-pass/artifacts/email/resend-domain-verification.md"
      via: "user-approved single-domain test scope"
      pattern: "glitchtech.io"
---

<objective>
Collect real transactional email evidence for the verified `glitchtech.io` Resend sender.

Purpose: Close EMAIL-01 through EMAIL-07 where proof exists while preserving the user's decision to defer paid/multi-domain `glitchstudios.io` Resend and full DMARC proof.
Output: Updated email scope and smoke artifacts with evidence-backed passed rows or explicit blocked/deferred reasons.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-launch-blocker-proof-pass/48-VERIFICATION.md
@.planning/phases/48-launch-blocker-proof-pass/48-HUMAN-PROOF-GUIDE.md
@.planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md
@.planning/phases/48-launch-blocker-proof-pass/artifacts/email/resend-domain-verification.md
@.planning/phases/48-launch-blocker-proof-pass/artifacts/email/single-domain-testing-scope.md
@.planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-smoke-matrix.md
@.planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/mobile-checkout-proof.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Reconfirm the single-domain email proof contract</name>
  <files>.planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md, .planning/phases/48-launch-blocker-proof-pass/artifacts/email/resend-domain-verification.md, .planning/phases/48-launch-blocker-proof-pass/artifacts/email/single-domain-testing-scope.md</files>
  <read_first>
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/email/resend-domain-verification.md
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/email/single-domain-testing-scope.md
    - .planning/phases/48-launch-blocker-proof-pass/48-HUMAN-PROOF-GUIDE.md
    - .planning/phases/48-launch-blocker-proof-pass/48-VERIFICATION.md
  </read_first>
  <action>
    Update the email artifacts with an explicit `## 48-17 Evidence Contract` section before collecting proof.

    In `single-domain-testing-scope.md`, include these exact fields:
    - `tested_at: <ISO timestamp or blocked: not yet tested>`
    - `sender_domain: glitchtech.io`
    - `allowed_sender: Glitch Studios <noreply@glitchtech.io>`
    - `glitchstudios_io_resend_status: deferred_user_cost_decision`
    - `email08_final_behavior: deferred_not_passed`
    - `secret_safety: no API keys, OAuth secrets, passwords, tokens, full cookies, or real card numbers in artifacts`

    In `resend-domain-verification.md`, keep `glitchtech.io` as the verified/current testing domain only if dashboard evidence exists. Keep `glitchstudios.io` as deferred with the exact reason: `user does not want to pay for more than one Resend domain right now`.

    In `email-smoke-matrix.md`, add or preserve the evidence field contract:
    - `flow`
    - `tested_at`
    - `trigger_url_or_action`
    - `recipient_inbox_alias`
    - `resend_event_id_or_screenshot`
    - `inbox_screenshot`
    - `content_assertions`
    - `link_result`
    - `blocked_reason`
    - `status`

    Do not mark EMAIL-08 passed. Do not require adding or paying for `glitchstudios.io` in Resend.
  </action>
  <acceptance_criteria>
    - `rg -n 'sender_domain: glitchtech\\.io|allowed_sender: Glitch Studios <noreply@glitchtech\\.io>|glitchstudios_io_resend_status: deferred_user_cost_decision|email08_final_behavior: deferred_not_passed' .planning/phases/48-launch-blocker-proof-pass/artifacts/email/single-domain-testing-scope.md` returns all single-domain scope fields.
    - `rg -n 'user does not want to pay for more than one Resend domain right now|do not mark EMAIL-08 passed|DMARC|glitchstudios\\.io' .planning/phases/48-launch-blocker-proof-pass/artifacts/email/resend-domain-verification.md .planning/phases/48-launch-blocker-proof-pass/artifacts/email/single-domain-testing-scope.md` returns the explicit deferral.
    - `rg -n 'resend_event_id_or_screenshot|inbox_screenshot|link_result|blocked_reason|secret_safety' .planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md .planning/phases/48-launch-blocker-proof-pass/artifacts/email/single-domain-testing-scope.md` returns the evidence and safety fields.
    - `! rg -n '(RESEND_API_KEY=|GOOGLE_CLIENT_SECRET=|BETTER_AUTH_SECRET=|password:|full_cookie|sk_(live|test)_[A-Za-z0-9]|re_[A-Za-z0-9]{20,})' .planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md .planning/phases/48-launch-blocker-proof-pass/artifacts/email/resend-domain-verification.md .planning/phases/48-launch-blocker-proof-pass/artifacts/email/single-domain-testing-scope.md`
  </acceptance_criteria>
  <verify>
    <manual>Human confirms the email proof scope is limited to `glitchtech.io` and no second paid Resend domain is required.</manual>
    <automated>rg -n 'sender_domain: glitchtech\\.io|glitchstudios_io_resend_status: deferred_user_cost_decision|resend_event_id_or_screenshot|inbox_screenshot|link_result|blocked_reason' .planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md .planning/phases/48-launch-blocker-proof-pass/artifacts/email/single-domain-testing-scope.md</automated>
  </verify>
  <done>The email proof contract is explicit, secret-safe, and aligned with the single-domain user decision.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Collect account, contact, and newsletter evidence</name>
  <files>.planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md</files>
  <read_first>
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/email/single-domain-testing-scope.md
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-smoke-matrix.md
    - .planning/phases/48-launch-blocker-proof-pass/48-HUMAN-PROOF-GUIDE.md
  </read_first>
  <action>
    Trigger each flow from the live site or admin UI using only test recipients/inboxes controlled by the user. Update each row in `email-smoke-matrix.md` from `blocked` to `passed` only when all concrete fields below are filled. If any field is unavailable, keep `status: blocked` and fill `blocked_reason`.

    Required rows and fields:
    - `flow: password reset`; `trigger_url_or_action: https://glitchstudios.io/forgot-password or https://glitchtech.io/forgot-password`; `resend_event_id_or_screenshot:`; `inbox_screenshot:`; `content_assertions: reset email content and reset link present`; `link_result: reset link opened, password changed, login succeeded`; requirements `EMAIL-02, EMAIL-03, AUTH-29`.
    - `flow: account verification`; `trigger_url_or_action: register test customer`; `resend_event_id_or_screenshot:`; `inbox_screenshot:`; `content_assertions: verification email content and verification link present`; `link_result: verification link opened and account verified`; requirements `EMAIL-02`.
    - `flow: contact auto-reply`; `trigger_url_or_action: /contact submission`; `resend_event_id_or_screenshot:`; `inbox_screenshot:`; `content_assertions: submitter auto-reply body and brand sender present`; `link_result: not_applicable`; requirements `EMAIL-02`.
    - `flow: admin contact notification`; `trigger_url_or_action: same /contact submission`; `resend_event_id_or_screenshot:`; `inbox_screenshot:`; `content_assertions: submitted name, email, subject, message, and admin link if present`; `link_result:`; requirements `EMAIL-07`.
    - `flow: newsletter broadcast`; `trigger_url_or_action: /admin/newsletter/compose test send`; `resend_event_id_or_screenshot:`; `inbox_screenshot:`; `content_assertions: newsletter body rendered`; `link_result: unsubscribe link opens`; requirements `EMAIL-06`.
    - `flow: newsletter unsubscribe`; `trigger_url_or_action: unsubscribe token link`; `resend_event_id_or_screenshot: not_applicable unless a confirmation email is sent`; `inbox_screenshot:`; `content_assertions: subscriber inactive or confirmation state`; `link_result: subscriber inactive state proven`; requirements `EMAIL-06`.

    Secret safety: do not store passwords, verification/reset tokens, unsubscribe tokens, full cookies, API keys, OAuth secrets, or real card numbers. Use screenshot filenames or redacted IDs.
  </action>
  <acceptance_criteria>
    - `rg -n 'flow: password reset|flow: account verification|flow: contact auto-reply|flow: admin contact notification|flow: newsletter broadcast|flow: newsletter unsubscribe' .planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md` returns all six flow labels.
    - `rg -n 'tested_at:|trigger_url_or_action:|recipient_inbox_alias:|resend_event_id_or_screenshot:|inbox_screenshot:|content_assertions:|link_result:|blocked_reason:|status: (passed|blocked)' .planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md` returns concrete evidence fields.
    - `rg -n 'EMAIL-02|EMAIL-03|EMAIL-06|EMAIL-07|AUTH-29' .planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md` returns covered requirement IDs.
    - `! rg -n '(password:|token: [A-Za-z0-9_-]{12,}|Cookie:|RESEND_API_KEY=|GOOGLE_CLIENT_SECRET=|BETTER_AUTH_SECRET=|sk_(live|test)_[A-Za-z0-9])' .planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md`
  </acceptance_criteria>
  <verify>
    <manual>Human confirms every row marked `passed` has a real Resend event or screenshot, inbox evidence, and link/content result where applicable.</manual>
    <automated>rg -n 'flow: password reset|flow: account verification|flow: contact auto-reply|flow: admin contact notification|flow: newsletter broadcast|flow: newsletter unsubscribe|resend_event_id_or_screenshot:|inbox_screenshot:|link_result:|status: (passed|blocked)' .planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md</automated>
  </verify>
  <done>Account, contact, and newsletter email rows have evidence-backed passed statuses or exact blocked reasons.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Collect booking email and order-receipt status</name>
  <files>.planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md</files>
  <read_first>
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/email/single-domain-testing-scope.md
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/checkout/mobile-checkout-proof.md
    - .planning/phases/48-launch-blocker-proof-pass/48-HUMAN-PROOF-GUIDE.md
  </read_first>
  <action>
    Trigger booking email flows from the live site or admin UI and update `email-smoke-matrix.md` with the same field contract from Task 1.

    Required rows:
    - `flow: booking confirmation`; include `booking_id:`, `service:`, `date_time:`, `deposit_amount:`, `cancellation_policy_present: yes|no`, `resend_event_id_or_screenshot:`, `inbox_screenshot:`, `link_result:`.
    - `flow: booking modification`; include `booking_id:`, `old_date_time:`, `new_date_time:`, `cancellation_policy_present: yes|no`, `resend_event_id_or_screenshot:`, `inbox_screenshot:`, `link_result:`.
    - `flow: booking cancellation`; include `booking_id:`, `cancelled_date_time:`, `refund_or_deposit_language_present: yes|no`, `cancellation_policy_present: yes|no`, `resend_event_id_or_screenshot:`, `inbox_screenshot:`, `link_result:`.
    - `flow: order receipt`; include `stripe_payment_intent_or_session:`, `resend_event_id_or_screenshot:`, `inbox_screenshot:`, `content_assertions: items, license tier per beat, total, download links`, `link_result: download link opens or blocked: <reason>`.

    If the order receipt cannot be proven until Plan 48-20 performs physical iOS checkout, keep it `status: blocked` with `blocked_reason: awaiting 48-20 iOS checkout or a new Stripe test order receipt evidence`. This plan may pass booking rows while leaving order receipt blocked for 48-20.
  </action>
  <acceptance_criteria>
    - `rg -n 'flow: booking confirmation|flow: booking modification|flow: booking cancellation|flow: order receipt' .planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md` returns all four flow labels.
    - `rg -n 'booking_id:|service:|date_time:|deposit_amount:|old_date_time:|new_date_time:|cancelled_date_time:|refund_or_deposit_language_present:|stripe_payment_intent_or_session:|license tier|download links|EMAIL-04|EMAIL-05' .planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md` returns required booking/order fields.
    - `rg -n 'flow: order receipt|awaiting 48-20 iOS checkout|status: (passed|blocked)' .planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md` returns the order receipt final state.
    - `! rg -n '(sk_(live|test)_[A-Za-z0-9]|card number: [0-9]{12,19}|password:|Cookie:|RESEND_API_KEY=)' .planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md`
  </acceptance_criteria>
  <verify>
    <manual>Human confirms every booking/order email row marked `passed` has Resend event proof, inbox proof, required content proof, and link/download result where applicable.</manual>
    <automated>rg -n 'flow: booking confirmation|flow: booking modification|flow: booking cancellation|flow: order receipt|booking_id:|stripe_payment_intent_or_session:|resend_event_id_or_screenshot:|inbox_screenshot:|status: (passed|blocked)' .planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md</automated>
  </verify>
  <done>Booking and order receipt email rows have evidence-backed passed statuses or exact blocked reasons.</done>
</task>

</tasks>

<verification>
Run every automated verify command above. Then inspect `email-smoke-matrix.md` manually and confirm no row marked `passed` relies on code-only evidence, provider-only success, or missing inbox/link proof.
</verification>

<success_criteria>
EMAIL-01 through EMAIL-07 have current single-domain evidence-backed statuses. AUTH-29 can pass only if the password reset row includes delivered email, reset link, password reset, and login proof. EMAIL-08 remains deferred for the multi-domain/DMARC portion.
</success_criteria>

<output>
After completion, create `.planning/phases/48-launch-blocker-proof-pass/48-17-SUMMARY.md` with passed rows, blocked rows, secret-safety confirmation, and the EMAIL-08 deferral statement.
</output>
