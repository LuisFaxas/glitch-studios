---
phase: 48-launch-blocker-proof-pass
plan: 18
slug: google-oauth-production-auth-smoke
type: execute
wave: 6
depends_on: ["48-14", "48-17"]
files_modified:
  - .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/oauth-env-redirects.md
  - .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-smoke-matrix.md
  - .planning/phases/48-launch-blocker-proof-pass/48-18-SUMMARY.md
autonomous: false
gap_closure: true
requirements:
  - AUTH-20
  - AUTH-21
  - AUTH-22
  - AUTH-26
  - AUTH-29
  - AUTH-32
user_setup:
  - "Vercel Production env access for Google OAuth env presence"
  - "Google Cloud Console OAuth client access"
  - "Production-safe test account and inbox access for both brand hosts"
must_haves:
  truths:
    - "Google OAuth env presence is proven without exposing client secrets."
    - "Google Cloud Console callback redirects are configured exactly for both production brand hosts."
    - "Both-brand live auth smoke is recorded with browser evidence and does not repeat already completed AUTH-32 lint/code work."
  artifacts:
    - path: ".planning/phases/48-launch-blocker-proof-pass/artifacts/auth/oauth-env-redirects.md"
      provides: "Google OAuth env and redirect proof"
      contains: "GOOGLE_CLIENT_ID"
    - path: ".planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-smoke-matrix.md"
      provides: "Both-brand live auth smoke matrix"
      contains: "Google OAuth"
  key_links:
    - from: ".planning/phases/48-launch-blocker-proof-pass/artifacts/auth/oauth-env-redirects.md"
      to: ".planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-smoke-matrix.md"
      via: "Google env/redirects unlock browser login proof"
      pattern: "GOOGLE_CLIENT_ID"
    - from: ".planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md"
      to: ".planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-smoke-matrix.md"
      via: "forgot/reset and verify-email proof links"
      pattern: "password reset"
---

<objective>
Collect production Google OAuth configuration proof and both-brand live auth smoke evidence.

Purpose: Close AUTH-20, AUTH-21, AUTH-22, AUTH-26, AUTH-29, and the manual-smoke portion of AUTH-32 without rerunning or rewriting the AUTH-32 lint/code work that already passed.
Output: Updated OAuth env/redirect artifact and auth smoke matrix with evidence-backed passed rows or explicit blocked reasons.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-launch-blocker-proof-pass/48-VERIFICATION.md
@.planning/phases/48-launch-blocker-proof-pass/48-HUMAN-PROOF-GUIDE.md
@.planning/phases/48-launch-blocker-proof-pass/artifacts/auth/oauth-env-redirects.md
@.planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-smoke-matrix.md
@.planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-command-output.md
@.planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md
@src/lib/auth.ts
@src/lib/auth-providers.ts
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Record Google OAuth env and callback evidence</name>
  <files>.planning/phases/48-launch-blocker-proof-pass/artifacts/auth/oauth-env-redirects.md</files>
  <read_first>
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/oauth-env-redirects.md
    - .planning/phases/48-launch-blocker-proof-pass/48-HUMAN-PROOF-GUIDE.md
    - src/lib/auth.ts
    - src/lib/auth-providers.ts
  </read_first>
  <action>
    Human dashboard action is required. Configure or verify Vercel Production has `GOOGLE_CLIENT_ID` and `GOOGLE_CLIENT_SECRET`. Record only presence, never values.

    Configure or verify these exact Google Cloud Console authorized redirect URIs:
    - `https://glitchstudios.io/api/auth/callback/google`
    - `https://glitchtech.io/api/auth/callback/google`

    Update `oauth-env-redirects.md` with a `## 48-18 Google OAuth Evidence` section containing these exact fields:
    - `tested_at: <ISO timestamp>`
    - `vercel_env_presence_evidence: <screenshot filename or CLI output filename showing names only>`
    - `GOOGLE_CLIENT_ID: present|blocked: <reason>`
    - `GOOGLE_CLIENT_SECRET: present|blocked: <reason>`
    - `google_console_screenshot: <filename or blocked: <reason>>`
    - `redirect_uri_glitchstudios: configured|blocked: <reason>`
    - `redirect_uri_glitchtech: configured|blocked: <reason>`
    - `blocked_reason: <only if any field is blocked>`
    - `secret_safety: no API keys, OAuth secrets, passwords, tokens, full cookies, or real card numbers in artifacts`

    Leave Meta and GitHub rows as hidden/unconfigured unless the user intentionally configures them. Do not require Meta or GitHub for this launch proof.
  </action>
  <acceptance_criteria>
    - `rg -n '## 48-18 Google OAuth Evidence|tested_at:|vercel_env_presence_evidence:|GOOGLE_CLIENT_ID: (present|blocked:)|GOOGLE_CLIENT_SECRET: (present|blocked:)|google_console_screenshot:|redirect_uri_glitchstudios: (configured|blocked:)|redirect_uri_glitchtech: (configured|blocked:)' .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/oauth-env-redirects.md` returns all evidence fields.
    - `rg -n 'https://glitchstudios\\.io/api/auth/callback/google|https://glitchtech\\.io/api/auth/callback/google' .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/oauth-env-redirects.md` returns both callback URLs.
    - `! rg -n '(GOOGLE_CLIENT_SECRET=|GOOGLE_CLIENT_ID=[A-Za-z0-9_-]{10,}|client_secret|BETTER_AUTH_SECRET=|RESEND_API_KEY=|password:|Cookie:)' .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/oauth-env-redirects.md`
  </acceptance_criteria>
  <verify>
    <manual>Human confirms Vercel and Google Cloud Console proof is recorded without exposing secret values.</manual>
    <automated>rg -n 'GOOGLE_CLIENT_ID: (present|blocked:)|GOOGLE_CLIENT_SECRET: (present|blocked:)|redirect_uri_glitchstudios: (configured|blocked:)|redirect_uri_glitchtech: (configured|blocked:)|secret_safety:' .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/oauth-env-redirects.md</automated>
  </verify>
  <done>Google OAuth env and callback state is recorded with either configured proof or exact blocked reasons.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run both-brand live auth smoke and AUTH-32 manual proof</name>
  <files>.planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-smoke-matrix.md</files>
  <read_first>
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-smoke-matrix.md
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/oauth-env-redirects.md
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-command-output.md
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/email/email-smoke-matrix.md
  </read_first>
  <action>
    Use real browsers against `https://glitchstudios.io` and `https://glitchtech.io`. Update `auth-smoke-matrix.md` with a `## 48-18 Live Auth Smoke Evidence` section. For each brand host, record these exact fields:
    - `brand_host: glitchstudios.io|glitchtech.io`
    - `tested_at: <ISO timestamp>`
    - `device_browser: <browser and OS>`
    - `google_button_result: visible_and_clicked|blocked: <reason>`
    - `google_login_screenshot: <filename or blocked: <reason>>`
    - `callback_result: returned_to_same_brand_session|blocked: <reason>`
    - `session_result: session_exists|blocked: <reason>`
    - `social_row_result: google visible; meta/github hidden while unconfigured or configured if env added`
    - `email_password_login_result: passed|blocked: <reason>`
    - `register_customer_verification_result: passed|blocked: <reason and email-matrix link>`
    - `forgot_reset_result: passed|blocked: <reason and email-matrix link>`
    - `verify_email_result: passed|blocked: <reason and email-matrix link>`
    - `unverified_soft_gate_result: passed|blocked: <reason>`
    - `sign_out_unverified_result: passed|blocked: <reason>`
    - `auth32_manual_smoke_status: passed|blocked: <reason>`

    AUTH-32 command proof is already in `auth-command-output.md`; do not modify source code, do not repeat the lint/code cleanup, and do not mark AUTH-32 passed unless the existing command artifact still records exit 0 and the manual smoke fields above are passed. If manual smoke is deferred, unrun, or blocked for any reason, keep AUTH-32 blocked; EMAIL-08 is the only currently approved deferred launch proof.
  </action>
  <acceptance_criteria>
    - `rg -n '## 48-18 Live Auth Smoke Evidence|brand_host: glitchstudios\\.io|brand_host: glitchtech\\.io|device_browser:|google_button_result:|callback_result:|session_result:|auth32_manual_smoke_status: (passed|blocked:)' .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-smoke-matrix.md` returns both brand evidence blocks.
    - `rg -n 'unverified_soft_gate_result: (passed|blocked:)|sign_out_unverified_result: (passed|blocked:)|forgot_reset_result: (passed|blocked:)|verify_email_result: (passed|blocked:)|register_customer_verification_result: (passed|blocked:)' .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-smoke-matrix.md` returns AUTH-26/AUTH-29 rows.
    - `rg -n 'pnpm tsc --noEmit --pretty false|pnpm lint|exit status: 0' .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-command-output.md` returns existing AUTH-32 command proof.
    - `! rg -n '(password:|Cookie:|token: [A-Za-z0-9_-]{12,}|GOOGLE_CLIENT_SECRET=|BETTER_AUTH_SECRET=|RESEND_API_KEY=)' .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-smoke-matrix.md`
  </acceptance_criteria>
  <verify>
    <manual>Human confirms every `passed` live auth row was tested on the named brand host and browser.</manual>
    <automated>rg -n 'brand_host: glitchstudios\\.io|brand_host: glitchtech\\.io|google_button_result:|callback_result:|session_result:|unverified_soft_gate_result:|auth32_manual_smoke_status:' .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-smoke-matrix.md</automated>
  </verify>
  <done>Both-brand Google OAuth, auth surface, soft-gate, forgot/reset, verify-email, and AUTH-32 manual smoke statuses are recorded with evidence or blocked reasons.</done>
</task>

</tasks>

<verification>
Run both automated checks. Confirm no secrets were stored and no AUTH-32 source/lint cleanup files were modified by this plan.
</verification>

<success_criteria>
Google OAuth configuration and both-brand live auth smoke are evidence-backed where passed. AUTH-32 remains blocked unless both the existing command proof and the new manual auth smoke evidence pass.
</success_criteria>

<output>
After completion, create `.planning/phases/48-launch-blocker-proof-pass/48-18-SUMMARY.md` with OAuth config status, auth smoke status by brand, blocked rows, and secret-safety confirmation.
</output>
