---
phase: 48-launch-blocker-proof-pass
plan: 08
slug: auth32-react-compiler-ref-purity-static-fixes
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/MagicRings.jsx
  - src/components/ui/dither.jsx
  - src/components/tech/category-tile.tsx
  - src/components/home/hero-section.tsx
autonomous: true
gap_closure: true
requirements:
  - AUTH-32
must_haves:
  truths:
    - "Reported React compiler lint errors are fixed by changing code shape, not by disabling rules."
    - "Render-phase ref access, render-phase randomness, and dynamic component creation are removed from the reported files."
  artifacts:
    - path: "src/components/ui/dither.jsx"
      provides: "Stable shader uniforms without ref access during render"
      contains: "uniforms="
    - path: "src/components/tech/category-tile.tsx"
      provides: "Category icon rendering without component creation during render"
      contains: "renderCategoryIcon"
  key_links:
    - from: ".planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-command-output.md"
      to: "src/components/tech/category-tile.tsx"
      via: "react-hooks/static-components blocker"
      pattern: "static-components"
    - from: ".planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-command-output.md"
      to: "src/components/ui/dither.jsx"
      via: "react-hooks/refs blocker"
      pattern: "refs"
---

<objective>
Fix the reported React compiler lint blockers for render-phase ref access, render-phase randomness, and dynamic component creation.

Purpose: Keep React 19 compiler rules enabled and make the affected components conform to them.
Output: React compiler lint clean files for refs, purity, and static component reports.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-command-output.md
@src/components/MagicRings.jsx
@src/components/ui/dither.jsx
@src/components/tech/category-tile.tsx
@src/components/home/hero-section.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove render-phase ref access, randomness, and dynamic component creation</name>
  <files>src/components/MagicRings.jsx, src/components/ui/dither.jsx, src/components/tech/category-tile.tsx, src/components/home/hero-section.tsx</files>
  <read_first>
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-command-output.md
    - src/components/MagicRings.jsx
    - src/components/ui/dither.jsx
    - src/components/tech/category-tile.tsx
    - src/components/home/hero-section.tsx
  </read_first>
  <behavior>
    - `react-hooks/refs` no longer reports `MagicRings.jsx` or `dither.jsx`.
    - `react-hooks/static-components` no longer reports `category-tile.tsx`.
    - `react-hooks/purity` no longer reports `hero-section.tsx`.
  </behavior>
  <action>
    In `src/components/MagicRings.jsx`, initialize `propsRef` with the initial props object inside `useRef({ ... })`, then update `propsRef.current` inside a `useEffect` whose dependency list contains every prop currently written into the object. Do not assign `propsRef.current = ...` during render.

    In `src/components/ui/dither.jsx`, stop passing `waveUniformsRef.current` in JSX. Replace the uniforms ref with a stable object created by `useMemo` or lazy state, update all animation code to read and write that stable object directly, and pass `uniforms={waveUniforms}` to `<shaderMaterial>`.

    In `src/components/tech/category-tile.tsx`, stop storing `const Icon = iconFor(category.slug)` inside `CategoryTile`. Add a top-level `renderCategoryIcon(slug, className)` helper that uses a `switch` or direct map lookup to return the JSX icon node, and call that helper where the two `<Icon ... />` nodes currently render. Keep the exported `CATEGORY_ICON_MAP` and `getCategoryIcon` compatibility exports.

    In `src/components/home/hero-section.tsx`, remove `Math.random()` from render/useMemo. Replace the random color with a top-level constant named `HERO_DITHER_COLOR` using one of the existing palette tuples, and pass that constant to `<Dither waveColor={...}>`.

    Do not add eslint-disable comments.
  </action>
  <acceptance_criteria>
    - `rg -n 'waveUniformsRef\\.current|Math\\.random\\(|const Icon = iconFor' src/components/ui/dither.jsx src/components/tech/category-tile.tsx src/components/home/hero-section.tsx` returns no stale render-phase patterns.
    - `rg -n 'propsRef\\.current =|useEffect\\(|useMemo\\(|renderCategoryIcon|HERO_DITHER_COLOR|uniforms=\\{waveUniforms\\}' src/components/MagicRings.jsx src/components/ui/dither.jsx src/components/tech/category-tile.tsx src/components/home/hero-section.tsx` returns the new conforming patterns.
    - `pnpm exec eslint src/components/MagicRings.jsx src/components/ui/dither.jsx src/components/tech/category-tile.tsx src/components/home/hero-section.tsx` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec eslint src/components/MagicRings.jsx src/components/ui/dither.jsx src/components/tech/category-tile.tsx src/components/home/hero-section.tsx</automated>
  </verify>
  <done>Scoped ESLint passes for refs/static-components/purity files.</done>
</task>

</tasks>

<verification>
Run the scoped ESLint command for refs/static-components/purity files before writing the summary.
</verification>

<success_criteria>
AUTH-32 is no longer blocked by the reported React compiler lint errors in refs/static-components/purity files.
</success_criteria>

<output>
After completion, create `.planning/phases/48-launch-blocker-proof-pass/48-08-SUMMARY.md` with the fixed rule group and scoped ESLint output.
</output>
