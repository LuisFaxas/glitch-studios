---
phase: 48-launch-blocker-proof-pass
plan: 08
slug: auth32-react-compiler-lint-fixes
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/MagicRings.jsx
  - src/components/ui/dither.jsx
  - src/components/tech/category-tile.tsx
  - src/components/home/hero-section.tsx
  - src/components/admin/admin-availability-editor.tsx
  - src/components/admin/client-detail-sheet.tsx
  - src/components/admin/tech/benchmark-template-editor.tsx
  - src/components/admin/tech/product-form.tsx
  - src/components/admin/tech/spec-template-editor.tsx
  - src/components/beats/beat-search.tsx
  - src/components/beats/filter-bar.tsx
  - src/components/beats/license-modal.tsx
  - src/components/home/splash-overlay.tsx
  - src/components/portfolio/portfolio-carousel.tsx
  - src/components/services/service-grid.tsx
  - src/components/services/service-tabs.tsx
  - src/components/tiles/studios-cross-link-tile.tsx
  - src/components/tiles/tech-cross-link-tile.tsx
autonomous: true
gap_closure: true
requirements:
  - AUTH-32
must_haves:
  truths:
    - "Reported React compiler lint errors are fixed by changing code shape, not by disabling rules."
    - "Render-phase ref access, render-phase randomness, and dynamic component creation are removed from the reported files."
    - "Synchronous state updates in effect bodies are either moved to initial state, event handlers, async callbacks, or deferred callbacks with cleanup."
  artifacts:
    - path: "src/components/ui/dither.jsx"
      provides: "Stable shader uniforms without ref access during render"
      contains: "uniforms="
    - path: "src/components/tech/category-tile.tsx"
      provides: "Category icon rendering without component creation during render"
      contains: "renderCategoryIcon"
    - path: "src/components/beats/filter-bar.tsx"
      provides: "Filter sync without direct setState in effect bodies"
      contains: "setTimeout"
  key_links:
    - from: ".planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-command-output.md"
      to: "src/components/tech/category-tile.tsx"
      via: "react-hooks/static-components blocker"
      pattern: "static-components"
    - from: ".planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-command-output.md"
      to: "src/components/ui/dither.jsx"
      via: "react-hooks/refs blocker"
      pattern: "refs"
---

<objective>
Fix the reported React compiler lint blockers that prevent AUTH-32 from passing.

Purpose: Keep React 19 compiler rules enabled and make the affected components conform to them.
Output: React compiler lint clean files for refs, purity, static components, and set-state-in-effect reports.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-command-output.md
@src/components/MagicRings.jsx
@src/components/ui/dither.jsx
@src/components/tech/category-tile.tsx
@src/components/home/hero-section.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove render-phase ref access, randomness, and dynamic component creation</name>
  <files>src/components/MagicRings.jsx, src/components/ui/dither.jsx, src/components/tech/category-tile.tsx, src/components/home/hero-section.tsx</files>
  <read_first>
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-command-output.md
    - src/components/MagicRings.jsx
    - src/components/ui/dither.jsx
    - src/components/tech/category-tile.tsx
    - src/components/home/hero-section.tsx
  </read_first>
  <behavior>
    - `react-hooks/refs` no longer reports `MagicRings.jsx` or `dither.jsx`.
    - `react-hooks/static-components` no longer reports `category-tile.tsx`.
    - `react-hooks/purity` no longer reports `hero-section.tsx`.
  </behavior>
  <action>
    In `src/components/MagicRings.jsx`, initialize `propsRef` with the initial props object inside `useRef({ ... })`, then update `propsRef.current` inside a `useEffect` whose dependency list contains every prop currently written into the object. Do not assign `propsRef.current = ...` during render.

    In `src/components/ui/dither.jsx`, stop passing `waveUniformsRef.current` in JSX. Replace the uniforms ref with a stable object created by `useMemo` or lazy state, update all animation code to read and write that stable object directly, and pass `uniforms={waveUniforms}` to `<shaderMaterial>`.

    In `src/components/tech/category-tile.tsx`, stop storing `const Icon = iconFor(category.slug)` inside `CategoryTile`. Add a top-level `renderCategoryIcon(slug, className)` helper that uses a `switch` or direct map lookup to return the JSX icon node, and call that helper where the two `<Icon ... />` nodes currently render. Keep the exported `CATEGORY_ICON_MAP` and `getCategoryIcon` compatibility exports.

    In `src/components/home/hero-section.tsx`, remove `Math.random()` from render/useMemo. Replace the random color with a top-level constant named `HERO_DITHER_COLOR` using one of the existing palette tuples, and pass that constant to `<Dither waveColor={...}>`.

    Do not add eslint-disable comments.
  </action>
  <acceptance_criteria>
    - `rg -n 'propsRef\\.current =|waveUniformsRef\\.current|Math\\.random\\(|const Icon = iconFor' src/components/MagicRings.jsx src/components/ui/dither.jsx src/components/tech/category-tile.tsx src/components/home/hero-section.tsx` returns no matches.
    - `rg -n 'useEffect\\(|useMemo\\(|renderCategoryIcon|HERO_DITHER_COLOR|uniforms=\\{waveUniforms\\}' src/components/MagicRings.jsx src/components/ui/dither.jsx src/components/tech/category-tile.tsx src/components/home/hero-section.tsx` returns the new conforming patterns.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec eslint src/components/MagicRings.jsx src/components/ui/dither.jsx src/components/tech/category-tile.tsx src/components/home/hero-section.tsx</automated>
  </verify>
  <done>Scoped ESLint passes for refs/static-components/purity files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix async loader and query-sync set-state effects</name>
  <files>src/components/admin/admin-availability-editor.tsx, src/components/admin/client-detail-sheet.tsx, src/components/admin/tech/benchmark-template-editor.tsx, src/components/admin/tech/product-form.tsx, src/components/admin/tech/spec-template-editor.tsx, src/components/beats/beat-search.tsx, src/components/beats/filter-bar.tsx, src/components/beats/license-modal.tsx</files>
  <read_first>
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-command-output.md
    - src/components/admin/admin-availability-editor.tsx
    - src/components/admin/client-detail-sheet.tsx
    - src/components/admin/tech/benchmark-template-editor.tsx
    - src/components/admin/tech/product-form.tsx
    - src/components/admin/tech/spec-template-editor.tsx
    - src/components/beats/beat-search.tsx
    - src/components/beats/filter-bar.tsx
    - src/components/beats/license-modal.tsx
  </read_first>
  <behavior>
    - Async loader components still show loading while requests are in flight.
    - Beat search and filter URL/local state still sync after URL changes and clear actions.
    - License modal media-query state still reflects `matchMedia` initially and on change.
  </behavior>
  <action>
    For loader effects in `admin-availability-editor.tsx`, `client-detail-sheet.tsx`, `benchmark-template-editor.tsx`, `product-form.tsx`, and `spec-template-editor.tsx`, remove direct synchronous `setState(...)` calls from the effect body. Use one of these exact patterns per file:
    - If the state can be derived before the effect, set it in the `useState` initializer.
    - If the state must change after the effect starts, schedule the initial setter with `window.setTimeout(() => setX(...), 0)` and clear that timer in the effect cleanup alongside the existing cancellation flag.
    Keep existing async `.then/.catch/.finally` state updates because those occur in async callbacks, not directly in the effect body.

    In `beat-search.tsx` and `filter-bar.tsx`, keep debounced URL updates. For query-to-local sync effects, guard equality first and schedule local state updates with a zero-delay timer plus cleanup. For `filter-bar.tsx` BPM local-state sync, apply the same deferred guarded update to `setLocalBpm([bpmMin ?? 60, bpmMax ?? 200])`.

    In `license-modal.tsx`, initialize `matches` with a lazy state initializer using `typeof window !== "undefined" && window.matchMedia(query).matches`. Remove the direct `setMatches(mql.matches)` from the effect body; keep only the `change` event listener.

    Preserve public props, DOM output, and existing loading/empty-state copy.
  </action>
  <acceptance_criteria>
    - `rg -n 'setIsLoading\\(true\\)|setLoading\\(true\\)|setTemplate\\(null\\); return|setLocalValue\\(query \\?\\?|setLocalSearch\\(query \\?\\?|setLocalBpm\\(\\[bpmMin|setMatches\\(mql\\.matches\\)' src/components/admin/admin-availability-editor.tsx src/components/admin/client-detail-sheet.tsx src/components/admin/tech/benchmark-template-editor.tsx src/components/admin/tech/product-form.tsx src/components/admin/tech/spec-template-editor.tsx src/components/beats/beat-search.tsx src/components/beats/filter-bar.tsx src/components/beats/license-modal.tsx` returns no direct synchronous effect setters.
    - `rg -n 'setTimeout\\(|clearTimeout\\(|matchMedia\\(query\\)\\.matches' src/components/admin/admin-availability-editor.tsx src/components/admin/client-detail-sheet.tsx src/components/admin/tech/benchmark-template-editor.tsx src/components/admin/tech/product-form.tsx src/components/admin/tech/spec-template-editor.tsx src/components/beats/beat-search.tsx src/components/beats/filter-bar.tsx src/components/beats/license-modal.tsx` returns deferred or lazy-state patterns.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec eslint src/components/admin/admin-availability-editor.tsx src/components/admin/client-detail-sheet.tsx src/components/admin/tech/benchmark-template-editor.tsx src/components/admin/tech/product-form.tsx src/components/admin/tech/spec-template-editor.tsx src/components/beats/beat-search.tsx src/components/beats/filter-bar.tsx src/components/beats/license-modal.tsx</automated>
  </verify>
  <done>Scoped ESLint passes for async loader and query-sync files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fix navigation, deep-link, splash, and carousel set-state effects</name>
  <files>src/components/home/splash-overlay.tsx, src/components/portfolio/portfolio-carousel.tsx, src/components/services/service-grid.tsx, src/components/services/service-tabs.tsx, src/components/tiles/studios-cross-link-tile.tsx, src/components/tiles/tech-cross-link-tile.tsx</files>
  <read_first>
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-command-output.md
    - src/components/home/splash-overlay.tsx
    - src/components/portfolio/portfolio-carousel.tsx
    - src/components/services/service-grid.tsx
    - src/components/services/service-tabs.tsx
    - src/components/tiles/studios-cross-link-tile.tsx
    - src/components/tiles/tech-cross-link-tile.tsx
  </read_first>
  <behavior>
    - Splash mode `never` still starts in the completed state.
    - Portfolio carousel still initializes selected index, snap list, and filter reInit behavior.
    - Service hash deep links still select and expand the matching service after hydration.
    - Cross-brand tiles still use absolute URLs only on the matching production host.
  </behavior>
  <action>
    In `splash-overlay.tsx`, initialize `splashState` with `mode === "never" ? "done" : "pending"` and remove the direct `setSplashState("done")` branch from the effect body.

    In `portfolio-carousel.tsx`, defer the initial `onSelect()` and `setScrollSnaps(...)` calls with a zero-delay timer and cleanup. In the filter reset effect, defer `emblaApi.reInit()` and `setScrollSnaps(...)` with a zero-delay timer and cleanup. Keep Embla event subscriptions and unsubscriptions unchanged.

    In `service-grid.tsx` and `service-tabs.tsx`, keep hash deep-link behavior but schedule `setSelectedSlug`, `setExpandedSlug`, and `setActiveTab` inside a zero-delay timer with cleanup after confirming the hash matches an existing service slug.

    In `studios-cross-link-tile.tsx` and `tech-cross-link-tile.tsx`, keep the current default relative hrefs, but schedule the production-host `setHref(...)` correction with a zero-delay timer and cleanup. Do not change the same-tab behavior and do not add `target="_blank"`.

    Do not use eslint-disable comments.
  </action>
  <acceptance_criteria>
    - `rg -n 'mode === \"never\" \\? \"done\" : \"pending\"|setTimeout\\(|clearTimeout\\(' src/components/home/splash-overlay.tsx src/components/portfolio/portfolio-carousel.tsx src/components/services/service-grid.tsx src/components/services/service-tabs.tsx src/components/tiles/studios-cross-link-tile.tsx src/components/tiles/tech-cross-link-tile.tsx` returns lazy initialization and deferred setter patterns.
    - `rg -n 'target=\"_blank\"' src/components/tiles/studios-cross-link-tile.tsx src/components/tiles/tech-cross-link-tile.tsx` returns no matches.
    - `pnpm exec eslint src/components/home/splash-overlay.tsx src/components/portfolio/portfolio-carousel.tsx src/components/services/service-grid.tsx src/components/services/service-tabs.tsx src/components/tiles/studios-cross-link-tile.tsx src/components/tiles/tech-cross-link-tile.tsx` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec eslint src/components/home/splash-overlay.tsx src/components/portfolio/portfolio-carousel.tsx src/components/services/service-grid.tsx src/components/services/service-tabs.tsx src/components/tiles/studios-cross-link-tile.tsx src/components/tiles/tech-cross-link-tile.tsx</automated>
  </verify>
  <done>Scoped ESLint passes for splash, carousel, service, and cross-link files.</done>
</task>

</tasks>

<verification>
Run each scoped ESLint command. Then run `pnpm exec eslint` on all files in `files_modified` for this plan before writing the summary.
</verification>

<success_criteria>
AUTH-32 is no longer blocked by the reported React compiler lint errors in these files.
</success_criteria>

<output>
After completion, create `.planning/phases/48-launch-blocker-proof-pass/48-08-SUMMARY.md` with the fixed rule groups and scoped ESLint output.
</output>
