---
phase: 48-launch-blocker-proof-pass
plan: 07
slug: auth32-typescript-and-typed-rows
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/forensics-overlay-leak.spec.ts
  - tests/09-services-booking-mobile-audit.spec.ts
  - tests/mobile-audit.spec.ts
  - src/actions/admin-clients.ts
  - src/actions/admin-roles.ts
  - src/components/admin/client-list-table.tsx
  - src/app/admin/bookings/actions.ts
  - src/app/api/bookings/cancel/route.ts
  - src/app/api/bookings/reschedule/route.ts
autonomous: true
gap_closure: true
requirements:
  - AUTH-32
must_haves:
  truths:
    - "The repo no longer has the Phase 48 TypeScript blocker in `tests/forensics-overlay-leak.spec.ts`."
    - "`@typescript-eslint/no-explicit-any` errors in the reported admin/client/booking/test files are removed without suppressing the lint rule."
    - "Scoped ESLint passes for every file touched by this plan."
  artifacts:
    - path: "tests/forensics-overlay-leak.spec.ts"
      provides: "Typed overlay visibility diagnostic"
      contains: "instanceof HTMLElement"
    - path: "src/actions/admin-clients.ts"
      provides: "Typed raw SQL row handling for admin clients"
      contains: "Client"
    - path: "src/app/admin/bookings/actions.ts"
      provides: "Typed booking status filter"
      contains: "BookingStatus"
  key_links:
    - from: ".planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-command-output.md"
      to: "tests/forensics-overlay-leak.spec.ts"
      via: "TS2339 offsetParent blocker"
      pattern: "offsetParent"
    - from: ".planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-command-output.md"
      to: "src/actions/admin-clients.ts"
      via: "no-explicit-any lint blocker"
      pattern: "no-explicit-any"
---

<objective>
Remove the AUTH-32 TypeScript blocker and the reported no-explicit-any lint blockers in typed data/test surfaces.

Purpose: AUTH-32 cannot pass until `pnpm tsc --noEmit` and `pnpm lint` are unblocked.
Output: Typed source/test files with no `any` suppressions for the reported rows.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-launch-blocker-proof-pass/48-VERIFICATION.md
@.planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-command-output.md
@src/db/schema.ts
@src/types/booking.ts
@src/lib/booking/policy.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix the TypeScript overlay diagnostic and Playwright browser types</name>
  <files>tests/forensics-overlay-leak.spec.ts, tests/09-services-booking-mobile-audit.spec.ts, tests/mobile-audit.spec.ts</files>
  <read_first>
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-command-output.md
    - tests/forensics-overlay-leak.spec.ts
    - tests/09-services-booking-mobile-audit.spec.ts
    - tests/mobile-audit.spec.ts
  </read_first>
  <behavior>
    - `pnpm tsc --noEmit --pretty false` no longer reports `Property 'offsetParent' does not exist on type 'Element'`.
    - The two mobile audit helper functions use Playwright's `Browser` type instead of `browser: any`.
  </behavior>
  <action>
    In `tests/forensics-overlay-leak.spec.ts`, change the overlay `visible` diagnostic from direct `el.offsetParent !== null` access to an HTMLElement guard:
    `visible: el instanceof HTMLElement ? el.offsetParent !== null : false`.

    In `tests/09-services-booking-mobile-audit.spec.ts`, import `type Browser` from `@playwright/test` and change `mobileContext(browser: any)` to `mobileContext(browser: Browser)`.

    In `tests/mobile-audit.spec.ts`, import `type Browser` from `@playwright/test` and change `createMobileContext(browser: any, ...)` to `createMobileContext(browser: Browser, ...)`.

    Do not use `// @ts-ignore`, `// eslint-disable`, or `as any` for these fixes.
  </action>
  <acceptance_criteria>
    - `rg -n 'instanceof HTMLElement \\? el\\.offsetParent !== null : false' tests/forensics-overlay-leak.spec.ts` returns the guarded offsetParent access.
    - `rg -n 'type Browser' tests/09-services-booking-mobile-audit.spec.ts tests/mobile-audit.spec.ts` returns Browser imports in both files.
    - `rg -n 'browser: any|visible: el\\.offsetParent !== null' tests/forensics-overlay-leak.spec.ts tests/09-services-booking-mobile-audit.spec.ts tests/mobile-audit.spec.ts` returns no `browser: any` helper signatures or unguarded direct Element offsetParent diagnostic.
  </acceptance_criteria>
  <verify>
    <automated>pnpm tsc --noEmit --pretty false</automated>
  </verify>
  <done>TypeScript noEmit passes the previously reported overlay diagnostic file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace raw SQL `any[]` casts in admin client and role files</name>
  <files>src/actions/admin-clients.ts, src/actions/admin-roles.ts, src/components/admin/client-list-table.tsx</files>
  <read_first>
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-command-output.md
    - src/actions/admin-clients.ts
    - src/actions/admin-roles.ts
    - src/components/admin/client-list-table.tsx
    - src/db/schema.ts
    - src/lib/permissions.ts
  </read_first>
  <behavior>
    - All `as any[]` and `as any` casts reported by lint in these three files are gone.
    - Raw SQL rows are typed once at the query boundary using narrow local interfaces.
    - The client list type tab click keeps its existing runtime behavior.
  </behavior>
  <action>
    In `src/actions/admin-clients.ts`, add narrow local row interfaces for the raw SQL result shapes used by `registeredRows`, `guestOrderRows`, `guestBookingRows`, `orderRows`, `bookingRows`, `userRows`, `orderStats`, `bookingStats`, `purchaseQuery`, and `bookingQuery`. Cast each `db.execute(sql\`...\`)` result to the corresponding local row array type once, then remove every `as any[]`. Keep numeric coercion through `Number(...)` and keep date fallback behavior unchanged.

    In `src/actions/admin-roles.ts`, add local row interfaces for admin member rows, target user rows, and count rows. Replace `(rows as any[])`, `(targetUser as any[])`, and `(ownerCount as any[])` with those local row array types. Remove the unused `ne` import. In `assignRole`, replace `const session = await requirePermission("manage_roles")` with `await requirePermission("manage_roles")` because `session` is unused there.

    In `src/components/admin/client-list-table.tsx`, type `TYPE_TABS` so `tab.value` is already the same union accepted by `handleTypeChange`; remove `tab.value as any` at the click handler without changing visible labels or selected-state behavior.

    Do not silence `@typescript-eslint/no-explicit-any`; fix the types.
  </action>
  <acceptance_criteria>
    - `rg -n 'as any\\[\\]|as any|browser: any' src/actions/admin-clients.ts src/actions/admin-roles.ts src/components/admin/client-list-table.tsx` returns no matches.
    - `rg -n 'interface .*Row|type .*Row' src/actions/admin-clients.ts src/actions/admin-roles.ts` returns local row typing.
    - `rg -n 'tab\\.value as any|ne,' src/components/admin/client-list-table.tsx src/actions/admin-roles.ts` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec eslint src/actions/admin-clients.ts src/actions/admin-roles.ts src/components/admin/client-list-table.tsx</automated>
  </verify>
  <done>Scoped ESLint passes for admin client/role row typing without `any` casts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Type booking status and refund policy casts</name>
  <files>src/app/admin/bookings/actions.ts, src/app/api/bookings/cancel/route.ts, src/app/api/bookings/reschedule/route.ts</files>
  <read_first>
    - .planning/phases/48-launch-blocker-proof-pass/artifacts/auth/auth-command-output.md
    - src/app/admin/bookings/actions.ts
    - src/app/api/bookings/cancel/route.ts
    - src/app/api/bookings/reschedule/route.ts
    - src/types/booking.ts
    - src/lib/booking/policy.ts
    - src/db/schema.ts
  </read_first>
  <behavior>
    - Booking status filters and policy helpers use `BookingStatus` and `RefundPolicy` instead of `any`.
    - Invalid admin booking status filters are ignored rather than passed to Drizzle.
    - Existing cancellation/reschedule policy behavior is preserved.
  </behavior>
  <action>
    In `src/app/admin/bookings/actions.ts`, import `type BookingStatus` from `@/types/booking`. Add a local `BOOKING_STATUSES` constant containing exactly `pending`, `confirmed`, `cancelled`, `completed`, and `no_show`, plus an `isBookingStatus(value: string): value is BookingStatus` guard. Use the guard before pushing `eq(bookings.status, filters.status)` so no `as any` is needed.

    In `src/app/api/bookings/cancel/route.ts` and `src/app/api/bookings/reschedule/route.ts`, import `type BookingStatus` from `@/types/booking`. For cancel, also import `type RefundPolicy`. Replace `booking.status as any` with `booking.status as BookingStatus`. Replace `(config.refundPolicy as any) ?? "full"` with `(config.refundPolicy ?? "full") as RefundPolicy`.

    Do not change API response shapes, refund math, or policy window behavior.
  </action>
  <acceptance_criteria>
    - `rg -n 'as any|any\\]' src/app/admin/bookings/actions.ts src/app/api/bookings/cancel/route.ts src/app/api/bookings/reschedule/route.ts` returns no matches.
    - `rg -n 'BookingStatus|RefundPolicy|BOOKING_STATUSES|isBookingStatus' src/app/admin/bookings/actions.ts src/app/api/bookings/cancel/route.ts src/app/api/bookings/reschedule/route.ts` returns the expected typed guards/imports.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec eslint src/app/admin/bookings/actions.ts src/app/api/bookings/cancel/route.ts src/app/api/bookings/reschedule/route.ts</automated>
  </verify>
  <done>Scoped ESLint passes for the booking status/refund files without explicit any.</done>
</task>

</tasks>

<verification>
Run the three automated checks in task order. This plan is complete only when the TypeScript blocker is gone and scoped ESLint passes for every file listed here.
</verification>

<success_criteria>
AUTH-32 is no longer blocked by TypeScript or no-explicit-any errors in the reported typed-row/test files.
</success_criteria>

<output>
After completion, create `.planning/phases/48-launch-blocker-proof-pass/48-07-SUMMARY.md` with command results and a list of fixed AUTH-32 files.
</output>
