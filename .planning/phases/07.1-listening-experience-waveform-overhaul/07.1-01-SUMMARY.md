---
phase: 07.1-listening-experience-waveform-overhaul
plan: 01
subsystem: ui, database
tags: [canvas, waveform, audio-decode, jsonb, peaks, drizzle]

# Dependency graph
requires:
  - phase: 07-beats-catalog-overhaul
    provides: Beat catalog UI with BeatSummary type, PlayerBeat interface, audio player provider
provides:
  - waveformPeaks JSONB column on beats table
  - BeatSummary and PlayerBeat types with waveformPeaks field
  - getPublishedBeats action returning waveformPeaks
  - extractPeaks utility for R2 audio peak extraction
  - generateSyntheticPeaks for testing/seed data
  - Reusable canvas Waveform component with peaks, progress, interactive scrub
affects: [07.1-02, 07.1-03, beat-card, beat-row, player-bar, widget-now-playing]

# Tech tracking
tech-stack:
  added: [audio-decode]
  patterns: [canvas waveform rendering, JSONB peaks storage, synthetic peak generation]

key-files:
  created:
    - src/lib/peaks.ts
    - src/components/player/waveform.tsx
  modified:
    - src/db/schema.ts
    - src/types/beats.ts
    - src/actions/beats.ts
    - src/components/player/audio-player-provider.tsx
    - src/db/seed-beat.ts
    - src/components/beats/beat-card.tsx
    - src/components/beats/beat-row.tsx

key-decisions:
  - "audio-decode default import (v3.5 API), not named export"
  - "Inline synthetic peaks in seed script to avoid server-only import conflict"
  - "Canvas bars bottom-aligned with devicePixelRatio for retina"

patterns-established:
  - "Waveform component pattern: peaks/progress/interactive/onSeek props"
  - "Peak data flows from DB JSONB through action to component"

requirements-completed: [D-01, D-02, D-03, D-15]

# Metrics
duration: 5min
completed: 2026-03-31
---

# Phase 07.1 Plan 01: Waveform Data Foundation & Canvas Component Summary

**Waveform peaks JSONB column, peak extraction utility with audio-decode, and reusable canvas Waveform component with interactive scrubbing**

## Performance

- **Duration:** 5 min
- **Started:** 2026-03-31T04:55:29Z
- **Completed:** 2026-03-31T05:00:11Z
- **Tasks:** 2
- **Files modified:** 10

## Accomplishments
- Added waveformPeaks JSONB column to beats schema and pushed types through BeatSummary, PlayerBeat, getPublishedBeats action
- Created src/lib/peaks.ts with extractPeaks (R2 audio) and generateSyntheticPeaks (testing/seed)
- Built reusable canvas Waveform component with ResizeObserver, devicePixelRatio, touch scrubbing, and null-peaks fallback

## Task Commits

Each task was committed atomically:

1. **Task 1: Data layer -- schema, types, action, peak utility, seed** - `c2b1f44` (feat)
2. **Task 2: Reusable canvas Waveform component** - `0946f8b` (feat)

## Files Created/Modified
- `src/lib/peaks.ts` - Server-side peak extraction from R2 audio and synthetic peak generation
- `src/components/player/waveform.tsx` - Reusable canvas waveform renderer with interactive scrubbing
- `src/db/schema.ts` - Added waveformPeaks JSONB column to beats table
- `src/types/beats.ts` - Added waveformPeaks to BeatSummary interface
- `src/actions/beats.ts` - Returns waveformPeaks in getPublishedBeats mapping
- `src/components/player/audio-player-provider.tsx` - Added waveformPeaks to PlayerBeat interface
- `src/db/seed-beat.ts` - Generates 256 synthetic peaks for BREAK 5 seed data
- `src/components/beats/beat-card.tsx` - Passes waveformPeaks to player.play()
- `src/components/beats/beat-row.tsx` - Passes waveformPeaks to player.play()
- `package.json` / `pnpm-lock.yaml` - Added audio-decode dependency

## Decisions Made
- Used `audio-decode` default import (v3.5 changed from named to default export, uses `channelData` array instead of `getChannelData` method)
- Inlined synthetic peak generation in seed-beat.ts rather than importing from peaks.ts to avoid `server-only` dependency conflict in tsx script context
- Canvas waveform bars are bottom-aligned (y = displayHeight - barHeight) matching industry standard waveform display

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed audio-decode API mismatch**
- **Found during:** Task 1 (peak utility creation)
- **Issue:** Plan specified `import { decode } from "audio-decode"` and `audioBuffer.getChannelData(0)` but audio-decode v3.5 uses default export and `channelData` array
- **Fix:** Changed to `import decode from "audio-decode"` and `audioBuffer.channelData[0]`
- **Files modified:** src/lib/peaks.ts
- **Verification:** tsc --noEmit passes clean
- **Committed in:** c2b1f44

**2. [Rule 1 - Bug] Added waveformPeaks to play() calls in beat-card and beat-row**
- **Found during:** Task 1 (after adding waveformPeaks to PlayerBeat interface)
- **Issue:** beat-card.tsx and beat-row.tsx call play() without the new required waveformPeaks field, causing type errors
- **Fix:** Added `waveformPeaks: beat.waveformPeaks` to both play() call sites
- **Files modified:** src/components/beats/beat-card.tsx, src/components/beats/beat-row.tsx
- **Verification:** tsc --noEmit passes clean
- **Committed in:** c2b1f44

---

**Total deviations:** 2 auto-fixed (2 bugs)
**Impact on plan:** Both fixes necessary for TypeScript compilation. No scope creep.

## Issues Encountered
- drizzle-kit push failed due to a known bug in drizzle-kit 0.31.10 with check constraint parsing. The waveform_peaks column exists in the schema code and will be pushed on next successful migration or via manual SQL. This does not block downstream development.

## User Setup Required
None - no external service configuration required.

## Known Stubs
None - all data flows are wired end-to-end from schema through types and actions.

## Next Phase Readiness
- Plans 02 and 03 can now consume the Waveform component and waveformPeaks data
- Plan 02 (beat cards/rows) can import Waveform from src/components/player/waveform.tsx
- Plan 03 (player bar/widget) can access peaks via currentBeat.waveformPeaks from audio player context
- The drizzle-kit push issue should be resolved before seeding -- may need drizzle-kit upgrade or manual ALTER TABLE

---
*Phase: 07.1-listening-experience-waveform-overhaul*
*Completed: 2026-03-31*
