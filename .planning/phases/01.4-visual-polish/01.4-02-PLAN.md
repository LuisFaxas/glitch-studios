---
phase: 01.4-visual-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/home/hero-section.tsx
  - src/components/home/services-overview.tsx
  - src/components/home/featured-carousel.tsx
  - src/components/home/testimonials-carousel.tsx
  - src/components/home/video-portfolio-carousel.tsx
  - src/app/(public)/page.tsx
  - src/app/(public)/beats/page.tsx
  - src/components/services/service-grid.tsx
autonomous: true
requirements: [INFR-03, INFR-04]

must_haves:
  truths:
    - "Home page hero shows GlitchLogo text and CTA buttons visibly against the dark background"
    - "Home page sections with empty DB data show fallback content or are hidden"
    - "/beats route returns a Coming Soon page instead of 404"
    - "Services detail panel opens when a service tile is clicked on desktop"
    - "Services mobile accordion items have tile-styled borders and backgrounds"
  artifacts:
    - path: "src/components/home/hero-section.tsx"
      provides: "Visible hero with contrast"
      contains: "border"
    - path: "src/app/(public)/beats/page.tsx"
      provides: "Coming Soon placeholder page"
      contains: "Coming Soon"
    - path: "src/components/services/service-grid.tsx"
      provides: "Mobile accordion with tile wrappers"
      contains: "border-[#222222]"
  key_links:
    - from: "src/app/(public)/page.tsx"
      to: "src/components/home/services-overview.tsx"
      via: "conditional rendering when services list is empty"
      pattern: "length === 0"
---

<objective>
Fix the home page visibility, empty section handling, dead routes, and services display issues from audit codes C2, C9, M3, P6.

Purpose: The home page is the first impression. Right now it is a black void — hero is black-on-black, DB sections render nothing, /beats 404s, and the services detail panel may not be visible. This plan makes the home page actually work.

Output: A visible hero section, graceful empty-state handling, /beats placeholder, and working services master-detail + styled mobile accordion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/VISUAL-AUDIT-2026-03-25.md
@.planning/DESIGN-LANGUAGE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix hero section and home page empty states</name>
  <read_first>
    - src/components/home/hero-section.tsx
    - src/components/home/services-overview.tsx
    - src/components/home/featured-carousel.tsx
    - src/components/home/testimonials-carousel.tsx
    - src/components/home/video-portfolio-carousel.tsx
    - src/app/(public)/page.tsx
    - src/components/layout/glitch-logo.tsx
  </read_first>
  <files>src/components/home/hero-section.tsx, src/components/home/services-overview.tsx, src/components/home/featured-carousel.tsx, src/components/home/testimonials-carousel.tsx, src/components/home/video-portfolio-carousel.tsx, src/app/(public)/page.tsx</files>
  <action>
**hero-section.tsx — Fix C2 (black on black hero):**
The hero is 90vh of pure black (`bg-black` div) with a black gradient overlay (`from-black/40 to-black/80`), making the white GlitchLogo text nearly invisible due to the gradient.

Fix the hero to be visible:
1. Replace the `bg-black` video placeholder with a subtle visual treatment: use `bg-[#0a0a0a]` with a scanline pattern overlay. Add a CSS background pattern: `background-image: repeating-linear-gradient(0deg, transparent, transparent 2px, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0.02) 4px)`. This gives texture instead of void.
2. Remove the dark gradient overlay div entirely (`bg-gradient-to-b from-black/40 to-black/80`). The scanline pattern provides enough visual interest without washing out the text.
3. Restyle the CTA buttons to match tile design language. Replace the `bg-gray-800 text-white border border-gray-600 rounded-?` classes with inverted tile style:
   - "Book a Session" button: `bg-[#f5f5f0] text-[#000000] border border-[#f5f5f0] px-8 py-3 rounded-none font-mono font-bold uppercase tracking-[0.05em] text-sm hover:bg-[#000000] hover:text-[#f5f5f0] hover:border-[#f5f5f0] transition-colors duration-200`
   - "Browse Beats" button: `bg-[#111111] text-[#f5f5f0] border border-[#222222] px-8 py-3 rounded-none font-mono font-bold uppercase tracking-[0.05em] text-sm hover:bg-[#1a1a1a] hover:border-[#444444] transition-colors duration-200`
4. Remove the `Button` component import and use plain `<Link>` elements with the tile-styled classes directly (no shadcn Button wrapper needed for these CTAs).

**services-overview.tsx — Fix empty state + restyle:**
The component currently uses `Card` from shadcn with `bg-gray-900 border-gray-800 rounded-lg` — this violates the monochrome hex palette and sharp-corner rules.
1. Remove `Card` and `CardContent` imports. Replace with plain `div` elements styled as tiles: `bg-[#111111] border border-[#222222] rounded-none p-6 hover:border-[#444444] hover:bg-[#1a1a1a] transition-colors duration-200`.
2. Add empty-state check at the top: `if (services.length === 0) return null` — hide the section entirely when DB returns empty.
3. Restyle the "Learn More" button: replace `Button variant="outline"` with a plain `<Link>` styled as `border border-[#f5f5f0] bg-transparent text-[#f5f5f0] px-6 py-2 rounded-none font-mono text-sm uppercase tracking-[0.05em] hover:bg-[#f5f5f0] hover:text-[#000000] transition-colors duration-200 inline-flex items-center`.

**featured-carousel.tsx — Fix empty state + restyle:**
This shows placeholder "Beat 1", "Beat 2" etc. with `bg-gray-900 border-gray-800 rounded-lg`.
1. Restyle cards: `bg-[#111111] border border-[#222222] rounded-none`.
2. Change heading "Featured Beats" to show a note: keep the carousel but add subtitle text below heading: `<p className="text-[#555555] font-sans text-sm mt-1">Coming soon in the beat store</p>`.
3. Restyle carousel nav buttons: replace `Button variant="outline" size="icon"` with styled divs: `w-10 h-10 flex items-center justify-center border border-[#222222] bg-[#111111] text-[#f5f5f0] rounded-none hover:border-[#444444] hover:bg-[#1a1a1a] transition-colors cursor-pointer`.

**testimonials-carousel.tsx — Fix styling:**
Already returns `null` when empty (good). But cards use `bg-gray-900 border-gray-800 rounded-lg`.
1. Restyle card containers: `bg-[#111111] border border-[#222222] rounded-none p-8`.
2. Fix star icons: change `text-gray-600` (inactive stars) to `text-[#333333]`.
3. Fix client title: change `text-gray-400` to `text-[#888888]`.
4. Fix decorative quote mark: change `text-gray-800` to `text-[#222222]`.
5. Restyle carousel nav buttons same as featured-carousel (tile-style square buttons).

**video-portfolio-carousel.tsx — Fix empty state:**
Read the current code. If it does not already return null when `portfolioItems` is empty, add that check. Restyle any `gray-*` Tailwind classes to monochrome hex values. Restyle nav buttons to tile-style.

**page.tsx — No structural changes needed:** The home page already queries DB and passes results to components. The components themselves handle empty states after this task.
  </action>
  <verify>
    <automated>cd /home/faxas/workspaces/projects/personal/glitch_studios && grep -c "rounded-none" src/components/home/hero-section.tsx && grep "length === 0" src/components/home/services-overview.tsx && grep -c "rounded-lg" src/components/home/services-overview.tsx && grep -c "rounded-lg" src/components/home/testimonials-carousel.tsx && grep -c "rounded-lg" src/components/home/featured-carousel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - hero-section.tsx does NOT contain `bg-gradient-to-b from-black/40 to-black/80` (gradient overlay removed)
    - hero-section.tsx contains `repeating-linear-gradient` or scanline background pattern
    - hero-section.tsx CTA links use `rounded-none` (not rounded-lg)
    - hero-section.tsx "Book a Session" button has `bg-[#f5f5f0] text-[#000000]` (inverted tile)
    - services-overview.tsx contains `if (services.length === 0) return null`
    - services-overview.tsx does NOT contain `rounded-lg` (0 occurrences)
    - services-overview.tsx does NOT import from `@/components/ui/card`
    - testimonials-carousel.tsx does NOT contain `rounded-lg` (0 occurrences)
    - testimonials-carousel.tsx does NOT contain `text-gray-400` or `text-gray-600` or `text-gray-800` or `border-gray-800` or `bg-gray-900`
    - featured-carousel.tsx does NOT contain `rounded-lg` (0 occurrences)
    - featured-carousel.tsx contains "Coming soon" text
  </acceptance_criteria>
  <done>Hero shows visible GlitchLogo + scanline pattern + styled CTAs. Services section hides when empty. All home page cards use monochrome hex palette with sharp corners.</done>
</task>

<task type="auto">
  <name>Task 2: Create /beats Coming Soon page, fix services detail + mobile accordion</name>
  <read_first>
    - src/components/services/service-grid.tsx
    - src/app/(public)/page.tsx
    - .planning/DESIGN-LANGUAGE.md
  </read_first>
  <files>src/app/(public)/beats/page.tsx, src/components/services/service-grid.tsx</files>
  <action>
**Create /beats Coming Soon page (C9):**
Create `src/app/(public)/beats/page.tsx` with:
```tsx
import { Music } from "lucide-react"
import Link from "next/link"
import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Beats",
  description: "Beat catalog coming soon to Glitch Studios.",
}

export default function BeatsPage() {
  return (
    <div className="flex flex-col items-center justify-center min-h-[60vh] px-4 text-center">
      <div className="w-20 h-20 flex items-center justify-center border border-[#222222] bg-[#111111] rounded-none mb-8">
        <Music className="w-10 h-10 text-[#555555]" />
      </div>
      <h1 className="font-mono font-bold text-4xl md:text-5xl uppercase tracking-tight text-[#f5f5f0] mb-4">
        Beat Store
      </h1>
      <p className="font-mono text-lg uppercase tracking-[0.05em] text-[#555555] mb-2">
        Coming Soon
      </p>
      <p className="font-sans text-[15px] text-[#888888] max-w-md mb-8">
        Browse beats, preview audio, select licenses, and purchase — all in one place. The beat store is currently under construction.
      </p>
      <Link
        href="/services"
        className="border border-[#f5f5f0] bg-[#f5f5f0] text-[#000000] font-mono font-bold text-sm uppercase tracking-[0.05em] px-8 py-3 rounded-none hover:bg-[#000000] hover:text-[#f5f5f0] transition-colors duration-200"
      >
        View Services
      </Link>
    </div>
  )
}
```

**Fix services detail panel (M3):**
The services master-detail layout uses `md:grid-cols-[1fr_1fr]`. The detail panel renders inside `AnimatePresence` but may not show if `selectedSlug` is null or services are empty. The current code sets `selectedSlug` to `services[0]?.slug ?? null` which should auto-select the first service. Verify: the issue may be that seeded service data in DB has no `description` or `features`, making the detail panel appear empty. The component itself looks structurally correct.

Add a fallback inside `ServiceDetailPanel` for when description is empty: if `service.description` is falsy, show the `shortDescription` instead. If `features` is null or empty array, don't render the features section (already handled). Add a minimum height to the detail panel: `min-h-[300px]`.

**Fix services mobile accordion styling (P6):**
The mobile accordion buttons currently have no tile-styled wrapper — they're just flat buttons with borders. Wrap each accordion item's outer `<div>` (the one containing button + expanded content) in tile styling: add `border border-[#222222] bg-[#0a0a0a]` to the outer wrapper div. The existing button inside already has proper tile colors. Add `overflow-hidden` to the outer wrapper so the accordion animation clips cleanly.

Change the mobile accordion outer structure from:
```
<div key={service.slug}>
  <button ...>...</button>
  <AnimatePresence>...</AnimatePresence>
</div>
```
to:
```
<div key={service.slug} className="border border-[#222222] overflow-hidden">
  <button ...>...</button>
  <AnimatePresence>...</AnimatePresence>
</div>
```

This gives each accordion item a visible tile container.
  </action>
  <verify>
    <automated>cd /home/faxas/workspaces/projects/personal/glitch_studios && test -f src/app/\(public\)/beats/page.tsx && echo "beats page exists" && grep "Coming Soon" src/app/\(public\)/beats/page.tsx && grep -c "border-\[#222222\]" src/components/services/service-grid.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `src/app/(public)/beats/page.tsx` exists
    - beats/page.tsx contains `Coming Soon` text
    - beats/page.tsx contains `export const metadata` with title "Beats"
    - beats/page.tsx uses `rounded-none` (no rounded corners)
    - service-grid.tsx ServiceDetailPanel has `min-h-[300px]` or similar minimum height
    - service-grid.tsx mobile accordion outer div contains `border border-[#222222]`
    - service-grid.tsx mobile accordion outer div contains `overflow-hidden`
  </acceptance_criteria>
  <done>/beats returns a styled Coming Soon page. Services detail panel has min-height and description fallback. Mobile accordion items have tile-styled borders.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `test -f src/app/(public)/beats/page.tsx` exits 0
2. `grep -c "rounded-lg" src/components/home/services-overview.tsx` returns 0
3. `grep -c "rounded-lg" src/components/home/testimonials-carousel.tsx` returns 0
4. `grep "Coming Soon" src/app/(public)/beats/page.tsx` has output
5. Hero section has scanline pattern and visible CTAs
6. Services mobile accordion has tile borders
</verification>

<success_criteria>
- Home page hero shows GlitchLogo text + tagline + 2 CTA buttons with clear contrast (not black-on-black)
- Home page sections with empty DB data either hide or show meaningful fallback
- /beats returns a Coming Soon page (not 404)
- All home page cards use monochrome hex palette (#111, #222, #888) with rounded-none
- Services desktop detail panel shows content with min-height
- Services mobile accordion items have tile-styled containers with borders
</success_criteria>

<output>
After completion, create `.planning/phases/01.4-visual-polish/01.4-02-SUMMARY.md`
</output>
