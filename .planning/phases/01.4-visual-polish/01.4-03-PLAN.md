---
phase: 01.4-visual-polish
plan: 03
type: execute
wave: 2
depends_on: ["01.4-01", "01.4-02"]
files_modified:
  - src/components/layout/footer.tsx
  - src/components/forms/contact-form.tsx
  - src/components/forms/newsletter-form.tsx
  - src/app/(public)/contact/page.tsx
  - src/components/blog/post-card.tsx
  - src/components/artists/artist-card.tsx
  - src/components/portfolio/video-card.tsx
  - src/app/not-found.tsx
  - src/styles/globals.css
  - src/app/(public)/layout.tsx
autonomous: true
requirements: [INFR-03, INFR-04, INFR-05]

must_haves:
  truths:
    - "Footer uses monochrome hex palette with sharp corners and mono headings"
    - "Contact form inputs are flat (0px radius, #333 border, white focus border)"
    - "Contact form submit button is inverted tile (white bg, black text)"
    - "Contact form retains proper label-input associations for accessibility"
    - "Blog cards show a pattern/gradient placeholder when no cover image exists"
    - "Blog card, artist card, and video card hover glitch effects are visible (not invisible overlays)"
    - "All readable text uses #888 or lighter (not #555) for WCAG contrast compliance"
    - "404 Back to Home button has sharp corners (rounded-none)"
    - "Blog filter pills have sharp corners (rounded-none) -- verified via grep"
    - "Public layout main wrapper uses page-content class for consistent padding"
  artifacts:
    - path: "src/components/layout/footer.tsx"
      provides: "Monochrome restyled footer"
      contains: "bg-[#111111]"
    - path: "src/components/forms/contact-form.tsx"
      provides: "Flat-styled form inputs with accessible labels and inverted submit button"
      contains: "rounded-none"
    - path: "src/components/blog/post-card.tsx"
      provides: "Pattern placeholder for missing images, visible glitch hover"
      contains: "repeating-linear-gradient"
    - path: "src/components/artists/artist-card.tsx"
      provides: "Visible glitch hover effect (not empty overlay)"
      contains: "animate-glitch-hover"
    - path: "src/components/portfolio/video-card.tsx"
      provides: "Visible glitch hover effect (not empty overlay)"
      contains: "animate-glitch-hover"
    - path: "src/app/not-found.tsx"
      provides: "Sharp-cornered back button"
      contains: "rounded-none"
    - path: "src/app/(public)/layout.tsx"
      provides: "page-content class applied to main wrapper"
      contains: "page-content"
  key_links:
    - from: "src/components/layout/footer.tsx"
      to: "src/components/forms/newsletter-form.tsx"
      via: "newsletter form rendered inside footer"
      pattern: "NewsletterForm"
---

<objective>
Fix all remaining design consistency issues across footer, contact form, blog cards, artist cards, video cards, typography, and minor polish items from audit codes M2, M5, P1, P2, P5, N1-N4.

Purpose: After Plans 01 and 02 fix the sidebar and home page (the highest-impact areas), this plan sweeps through every remaining page component to ensure consistent Cyberpunk Metro design language. No more Tailwind default grays, no more rounded corners, no more inconsistent typography, no more invisible glitch effects on cards.

Output: Every visible component follows the monochrome hex palette, sharp corners, mono headings, flat tile aesthetic, and visible glitch hover effects.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/VISUAL-AUDIT-2026-03-25.md
@.planning/DESIGN-LANGUAGE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle footer, newsletter form, and social icons</name>
  <read_first>
    - src/components/layout/footer.tsx
    - src/components/forms/newsletter-form.tsx
    - .planning/DESIGN-LANGUAGE.md
  </read_first>
  <files>src/components/layout/footer.tsx, src/components/forms/newsletter-form.tsx</files>
  <action>
**footer.tsx -- Fix M2 (non-monochrome footer) and P5 (generic social icons) and P1 (typography):**

Complete restyle of the footer to match Cyberpunk Metro design language:

1. **Background and border:** Replace `bg-gray-900` with `bg-[#111111]`. Replace `border-gray-800` with `border-[#222222]`.

2. **Text colors:** Replace all `text-gray-400` with `text-[#888888]`. Replace `text-white` with `text-[#f5f5f0]`. IMPORTANT: Do NOT use `#555` for any readable text -- use `#888888` minimum for WCAG AA contrast against `#111` backgrounds (addresses review concern about #555 contrast).

3. **Typography (P1):** The "Stay in the loop" heading and any other footer headings should use mono font: `font-mono font-bold uppercase tracking-[0.05em] text-sm text-[#f5f5f0]`. Nav links should be: `font-mono text-xs uppercase tracking-[0.05em] text-[#888888] hover:text-[#f5f5f0] transition-colors`.

4. **Social icons (P5):** Replace the generic `Globe` and `ExternalLink` icons with text-labeled links since Lucide removed brand icons. Change the social links rendering from icon-only to icon + text label:
```tsx
const socialLinks = [
  { label: "Instagram", href: "https://instagram.com", icon: Camera },
  { label: "YouTube", href: "https://youtube.com", icon: Video },
  { label: "SoundCloud", href: "https://soundcloud.com", icon: Headphones },
]
```
Import `Camera`, `Video`, `Headphones` from `lucide-react` (same icons used in widget-social.tsx for consistency). Render each as:
```tsx
<a href={social.href} target="_blank" rel="noopener noreferrer"
   className="flex items-center gap-2 text-[#888888] hover:text-[#f5f5f0] transition-colors font-mono text-xs uppercase tracking-[0.05em]">
  <social.icon className="size-4" />
  <span>{social.label}</span>
</a>
```

5. **Bottom divider:** Replace `border-gray-800` with `border-[#222222]`. Replace `text-gray-400` on copyright with `text-[#888888]`.

6. **Copyright text:** Change to `font-mono text-[11px] uppercase tracking-[0.05em] text-[#888888]`. Note: use `#888888` not `#555555` for readability.

**newsletter-form.tsx -- Fix rounded "Join the List" button:**
The form uses shadcn `Button` and `Input` components which may have default rounded corners. Replace:
1. The `Input` component: replace with a plain `<input>` styled as: `flex-1 h-10 bg-[#111111] border border-[#333333] rounded-none px-3 text-sm text-[#f5f5f0] font-sans placeholder:text-[#555555] focus:border-[#f5f5f0] focus:outline-none transition-colors disabled:opacity-50`. Note: `#555555` is acceptable for placeholder text only (not expected to be read as body content).
2. The `Button` component: replace with a plain `<button>` styled as inverted tile: `bg-[#f5f5f0] text-[#000000] border border-[#f5f5f0] rounded-none px-6 h-10 font-mono font-bold text-sm uppercase tracking-[0.05em] hover:bg-[#000000] hover:text-[#f5f5f0] transition-colors disabled:opacity-50`.
3. Remove `Button` and `Input` imports from `@/components/ui/`.
  </action>
  <verify>
    <automated>cd /home/faxas/workspaces/projects/personal/glitch_studios && grep -c "gray-" src/components/layout/footer.tsx && grep -c "rounded-none" src/components/forms/newsletter-form.tsx && grep "Camera\|Video\|Headphones" src/components/layout/footer.tsx</automated>
  </verify>
  <acceptance_criteria>
    - footer.tsx contains 0 occurrences of `gray-` (all replaced with hex values)
    - footer.tsx contains `bg-[#111111]` for the footer background
    - footer.tsx contains `border-[#222222]` for borders
    - footer.tsx does NOT contain `text-[#555555]` for any readable text (only `#888888` or lighter)
    - footer.tsx imports Camera, Video, Headphones from lucide-react (not Globe, ExternalLink)
    - footer.tsx social links include `<span>{social.label}</span>` text labels
    - footer.tsx heading "Stay in the loop" has `font-mono` class
    - newsletter-form.tsx contains `rounded-none` on both input and button
    - newsletter-form.tsx does NOT import from `@/components/ui/button` or `@/components/ui/input`
    - newsletter-form.tsx submit button has `bg-[#f5f5f0] text-[#000000]` (inverted tile)
  </acceptance_criteria>
  <done>Footer is fully restyled to monochrome hex palette with mono typography, labeled social icons, WCAG-compliant text contrast, and sharp corners. Newsletter form has flat input and inverted tile button.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle contact form (keep accessibility), blog/artist/video card glitch fixes, 404, page padding</name>
  <read_first>
    - src/components/forms/contact-form.tsx
    - src/app/(public)/contact/page.tsx
    - src/components/blog/post-card.tsx
    - src/components/artists/artist-card.tsx
    - src/components/portfolio/video-card.tsx
    - src/components/blog/category-filter.tsx
    - src/app/not-found.tsx
    - src/styles/globals.css
    - src/app/(public)/layout.tsx
  </read_first>
  <files>src/components/forms/contact-form.tsx, src/app/(public)/contact/page.tsx, src/components/blog/post-card.tsx, src/components/artists/artist-card.tsx, src/components/portfolio/video-card.tsx, src/app/not-found.tsx, src/styles/globals.css, src/app/(public)/layout.tsx</files>
  <action>
**contact-form.tsx -- Fix P2 (contact form styling) -- KEEP form accessibility (addresses review concern #5):**
Restyle form elements to match tile design language. IMPORTANT: Do NOT blindly strip all shadcn/Base UI wrappers. The `Label` component from shadcn provides proper `htmlFor` association. Keep the `<Label>` import and component usage for accessibility. Only replace `Input`, `Button` with plain HTML equivalents that maintain the same `id`, `name`, `htmlFor` attributes.

1. **Keep Label imports:** Continue using `Label` from `@/components/ui/label` (it renders a `<label>` with proper htmlFor). Restyle: add `text-[#888888] font-mono text-xs uppercase tracking-[0.05em]` to each Label's className.

2. **Input fields:** Replace shadcn `Input` with plain `<input>` elements that keep the same `id` and `name` attributes. Style: `w-full h-10 bg-[#0a0a0a] border border-[#333333] rounded-none px-3 text-sm text-[#f5f5f0] font-sans placeholder:text-[#555555] focus:border-[#f5f5f0] focus:outline-none transition-colors disabled:opacity-50`. Remove `Input` import.

3. **Select dropdown:** Change `rounded-lg border-gray-800 bg-gray-800` to `rounded-none border-[#333333] bg-[#0a0a0a] text-[#f5f5f0] focus:border-[#f5f5f0]`. Remove `focus:ring-3 focus:ring-white/10` (no ring, just border change).

4. **Textarea:** Change `bg-gray-800 border-gray-800 rounded-lg` to `bg-[#0a0a0a] border border-[#333333] rounded-none focus:border-[#f5f5f0] focus:outline-none focus:ring-0`. Remove `focus:ring-3 focus:ring-white/10`. Change `placeholder:text-gray-600` to `placeholder:text-[#555555]`.

5. **Submit button:** Replace shadcn `Button` with a plain `<button>` styled as inverted tile: `w-full md:w-auto bg-[#f5f5f0] text-[#000000] border border-[#f5f5f0] rounded-none px-8 py-3 font-mono font-bold text-sm uppercase tracking-[0.05em] hover:bg-[#000000] hover:text-[#f5f5f0] transition-colors disabled:opacity-50 inline-flex items-center justify-center gap-2`. Remove `Button` import.

6. Remove `Button` and `Input` imports from `@/components/ui/`. Keep `Label` import.

**contact/page.tsx -- Fix form container:**
Change the form container from `bg-gray-900 border border-gray-800 rounded-lg p-8 md:p-12` to `bg-[#111111] border border-[#222222] rounded-none p-8 md:p-12`. Also change `text-gray-400` on the description paragraph to `text-[#888888]`.

**post-card.tsx -- Fix M5 (blog card images) + visible glitch hover:**
When `coverImageUrl` is falsy, the current fallback is `<div className="w-full h-full bg-[#111111]" />` -- a flat dark box indistinguishable from the card background.

Replace with a scanline pattern placeholder:
```tsx
<div
  className="w-full h-full bg-[#0a0a0a] flex items-center justify-center"
  style={{
    backgroundImage: "repeating-linear-gradient(0deg, transparent, transparent 3px, rgba(255,255,255,0.03) 3px, rgba(255,255,255,0.03) 4px)"
  }}
>
  <span className="font-mono text-[#333333] text-xs uppercase tracking-[0.1em]">No Image</span>
</div>
```

Also fix the glitch hover overlay in post-card.tsx -- same issue as Tile (C7): the overlay `<span>` has no visible content. Since blog cards are more complex (image + text), a simpler approach works: instead of duplicating content, add a semi-transparent overlay flash: change the glitch overlay from an empty span to: `<div className="pointer-events-none absolute inset-0 z-10 bg-[#f5f5f0]/5 animate-glitch-hover motion-reduce:hidden" aria-hidden="true" />`. The slight white flash combined with clip-path creates a visible glitch effect on the card. Add `motion-reduce:hidden` for reduced-motion support.

**artist-card.tsx -- Fix invisible glitch hover (addresses review concern about glitch pattern in other cards):**
The same invisible-overlay glitch pattern exists here. The current code at line 37-43:
```tsx
{isHovered && (
  <span className="pointer-events-none absolute inset-0 z-10 animate-glitch-hover" ... />
)}
```
Replace with a visible semi-transparent overlay flash, same approach as post-card:
```tsx
{isHovered && (
  <div className="pointer-events-none absolute inset-0 z-10 bg-[#f5f5f0]/5 animate-glitch-hover motion-reduce:hidden" aria-hidden="true" />
)}
```

**video-card.tsx -- Fix invisible glitch hover (addresses review concern about glitch pattern in other cards):**
Same fix. The current code at line 36-42:
```tsx
{isHovered && (
  <span className="pointer-events-none absolute inset-0 z-10 animate-glitch-hover" ... />
)}
```
Replace with:
```tsx
{isHovered && (
  <div className="pointer-events-none absolute inset-0 z-10 bg-[#f5f5f0]/5 animate-glitch-hover motion-reduce:hidden" aria-hidden="true" />
)}
```
Also note: the video-card outer div is missing `relative` positioning which would prevent the absolute overlay from working. Add `relative` to the outer div className if not present.

**not-found.tsx -- Fix N4 (rounded button on 404):**
Change the "Back to Home" link from `rounded-lg bg-gray-800 border-gray-600` to `rounded-none bg-[#f5f5f0] text-[#000000] border border-[#f5f5f0] font-mono font-bold text-sm uppercase tracking-[0.05em] hover:bg-[#000000] hover:text-[#f5f5f0] transition-colors`. Also change `text-gray-400` paragraph to `text-[#888888]`.

**category-filter.tsx -- Verify N2 (filter pill corners):**
The category filter already uses `rounded-none` (verified in codebase read). No code change needed. Verify as part of automated checks.

**globals.css -- Scrollbar styling and page content padding (N1):**
Add a custom scrollbar style for the sidebar (used by tile-nav.tsx which applies `sidebar-scroll` class to its `<aside>`):
```css
/* Sidebar scrollbar */
.sidebar-scroll::-webkit-scrollbar {
  width: 4px;
}
.sidebar-scroll::-webkit-scrollbar-track {
  background: transparent;
}
.sidebar-scroll::-webkit-scrollbar-thumb {
  background: #333;
  border-radius: 0;
}
.sidebar-scroll {
  scrollbar-width: thin;
  scrollbar-color: #333 transparent;
}
```

Add a utility class for consistent page content padding:
```css
/* Page content padding consistency (N1) */
.page-content {
  padding-left: var(--pad-page);
  padding-right: var(--pad-page);
}
```

**layout.tsx -- Apply page-content class (N1):**
In `src/app/(public)/layout.tsx`, add the `page-content` class to the `<main>` element. Change:
```tsx
<main className="flex-1 min-h-screen pb-16 md:pb-0">
```
to:
```tsx
<main className="page-content flex-1 min-h-screen pb-16 md:pb-0">
```
This ensures all public pages get consistent horizontal padding via the CSS custom property.
  </action>
  <verify>
    <automated>cd /home/faxas/workspaces/projects/personal/glitch_studios && grep -c "gray-" src/components/forms/contact-form.tsx && grep -c "rounded-none" src/components/forms/contact-form.tsx && grep "repeating-linear-gradient" src/components/blog/post-card.tsx && grep "rounded-none" src/app/not-found.tsx && grep "sidebar-scroll" src/styles/globals.css && grep "rounded-none" src/components/blog/category-filter.tsx && grep "page-content" src/app/\(public\)/layout.tsx && grep "Label" src/components/forms/contact-form.tsx && grep -c "motion-reduce" src/components/blog/post-card.tsx && grep -c "motion-reduce" src/components/artists/artist-card.tsx && grep -c "motion-reduce" src/components/portfolio/video-card.tsx && grep "bg-\[#f5f5f0\]/5" src/components/artists/artist-card.tsx && grep "bg-\[#f5f5f0\]/5" src/components/portfolio/video-card.tsx</automated>
  </verify>
  <acceptance_criteria>
    - contact-form.tsx contains 0 occurrences of `gray-` (all replaced with hex values)
    - contact-form.tsx contains multiple `rounded-none` occurrences (select, textarea, button)
    - contact-form.tsx submit button has `bg-[#f5f5f0] text-[#000000]` (inverted tile)
    - contact-form.tsx STILL imports `Label` from `@/components/ui/label` (accessibility preserved)
    - contact-form.tsx does NOT import from `@/components/ui/button` or `@/components/ui/input`
    - contact/page.tsx form container has `rounded-none` and `bg-[#111111]`
    - post-card.tsx contains `repeating-linear-gradient` in the no-image placeholder
    - post-card.tsx glitch overlay has `bg-[#f5f5f0]/5` and `motion-reduce:hidden`
    - artist-card.tsx glitch overlay has `bg-[#f5f5f0]/5` (visible, not empty span)
    - artist-card.tsx glitch overlay has `motion-reduce:hidden`
    - video-card.tsx glitch overlay has `bg-[#f5f5f0]/5` (visible, not empty span)
    - video-card.tsx glitch overlay has `motion-reduce:hidden`
    - video-card.tsx outer div has `relative` class
    - not-found.tsx "Back to Home" link has `rounded-none` (not `rounded-lg`)
    - not-found.tsx does NOT contain `gray-` classes (all hex)
    - globals.css contains `.sidebar-scroll` scrollbar styling
    - globals.css contains `scrollbar-width: thin`
    - globals.css contains `.page-content` utility class
    - category-filter.tsx contains `rounded-none` (verified, no change needed)
    - src/app/(public)/layout.tsx `<main>` element contains `page-content` class
  </acceptance_criteria>
  <done>Contact form is flat-styled with inverted submit button AND preserved Label accessibility. Blog, artist, and video cards all have visible glitch hover effects with reduced-motion support. 404 button has sharp corners. Category filter pills confirmed rounded-none. Page content padding applied. Sidebar scrollbar is styled. All readable text uses #888 minimum contrast.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -c "gray-" src/components/layout/footer.tsx` returns 0
2. `grep -c "gray-" src/components/forms/contact-form.tsx` returns 0
3. `grep -c "rounded-lg" src/app/not-found.tsx` returns 0
4. `grep "repeating-linear-gradient" src/components/blog/post-card.tsx` has output
5. `grep "sidebar-scroll" src/styles/globals.css` has output
6. `grep "bg-\[#f5f5f0\] text-\[#000000\]" src/components/forms/newsletter-form.tsx` has output
7. `grep "rounded-none" src/components/blog/category-filter.tsx` has output (verified unchanged)
8. `grep "page-content" src/app/\(public\)/layout.tsx` has output
9. `grep "Label" src/components/forms/contact-form.tsx` has output (Label import preserved)
10. `grep "bg-\[#f5f5f0\]/5" src/components/artists/artist-card.tsx` has output (visible glitch)
11. `grep "bg-\[#f5f5f0\]/5" src/components/portfolio/video-card.tsx` has output (visible glitch)
12. `grep -c "motion-reduce" src/components/blog/post-card.tsx` returns >= 1
13. `grep -c "motion-reduce" src/components/artists/artist-card.tsx` returns >= 1
14. `grep -c "motion-reduce" src/components/portfolio/video-card.tsx` returns >= 1
</verification>

<success_criteria>
- Footer uses #111111 bg, #222222 borders, #888888 text (not #555), mono headings, labeled social icons
- Newsletter form has flat input and inverted tile button (white bg, black text, sharp corners)
- Contact form inputs are flat (0px radius, #333 border, white focus border)
- Contact form submit is inverted tile (white bg, black text)
- Contact form STILL uses Label component for accessibility (not stripped)
- Blog cards show a scanline pattern placeholder when no cover image
- Blog card hover has a visible glitch flash effect with reduced-motion guard
- Artist card hover has a visible glitch flash effect (not invisible overlay)
- Video card hover has a visible glitch flash effect (not invisible overlay)
- All card glitch overlays have motion-reduce:hidden and aria-hidden
- 404 button has sharp corners and monochrome hex palette
- Blog category filter pills have rounded-none (verified)
- Public layout main wrapper has page-content class for consistent padding
- Sidebar has a thin styled scrollbar
- All Tailwind default gray-* classes eliminated from modified files
- All readable text uses #888888 minimum (WCAG AA contrast on dark backgrounds)
</success_criteria>

<output>
After completion, create `.planning/phases/01.4-visual-polish/01.4-03-SUMMARY.md`
</output>
