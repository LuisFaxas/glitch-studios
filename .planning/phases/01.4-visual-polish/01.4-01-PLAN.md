---
phase: 01.4-visual-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/tiles/tile.tsx
  - src/components/tiles/logo-tile.tsx
  - src/components/layout/tile-nav.tsx
  - src/components/layout/mobile-nav-overlay.tsx
  - src/components/layout/bottom-tab-bar.tsx
  - src/components/tiles/widget-social.tsx
  - src/components/layout/side-nav.tsx
  - src/styles/globals.css
autonomous: true
requirements: [INFR-03, INFR-04, INFR-05]

must_haves:
  truths:
    - "Logo tile shows white GLITCH wordmark on dark bg with visible warm glow"
    - "Sidebar nav tiles display icon and label side-by-side (horizontal, not stacked)"
    - "Widget tiles and auth tiles still use vertical (flex-col) layout — no regressions"
    - "Hovering any tile triggers visible glitch distortion on the tile content itself"
    - "Glitch duplication overlay is aria-hidden and triggers on focus-visible too"
    - "All sidebar content fits on 900px screen or a scroll-hint gradient is visible at the bottom"
    - "Sidebar has a collapsible icon-only compact mode (~64px) with a toggle"
    - "Mobile nav overlay shows logo, nav tiles, widgets, and auth when opened"
    - "Mobile nav overlay traps focus while open"
    - "Center grid icon in bottom tab bar shows active/inverted state on home page"
    - "Dead side-nav.tsx file is deleted"
  artifacts:
    - path: "src/components/tiles/tile.tsx"
      provides: "Layout prop for horizontal/vertical, glitch effect on content wrapper"
      contains: "layout"
    - path: "src/components/tiles/logo-tile.tsx"
      provides: "Inverted logo with explicit height and strong glow"
      contains: "invert"
    - path: "src/components/layout/tile-nav.tsx"
      provides: "Compact mode toggle, reduced padding, scroll hint, sidebar-scroll class"
      contains: "isCompact"
  key_links:
    - from: "src/components/tiles/tile.tsx"
      to: "src/styles/globals.css"
      via: "animate-glitch-hover class applied to content wrapper div"
      pattern: "animate-glitch-hover"
    - from: "src/components/layout/tile-nav.tsx"
      to: "src/components/tiles/tile.tsx"
      via: "layout='horizontal' and compact prop passed to Tile for nav rendering"
      pattern: "layout.*horizontal"
---

<objective>
Overhaul the sidebar, tile system, and mobile nav to fix audit issues C1, C3-C8, M1, M4, P3, P4 and address review concerns about shared component mutation, logo sizing, accessibility, and dead code.

Purpose: The sidebar is the visual anchor of the site. Right now the logo is invisible, tiles stack icons vertically wasting space, glitch hover is invisible, sign-in is hidden off-screen, there is no compact mode, the mobile overlay renders blank, dead code exists, and accessibility gaps are present. This plan fixes all of it.

Output: A functional sidebar with visible logo + glow, horizontal tile layout (via opt-in prop), working glitch hover with accessibility, compact mode, scroll hint, a mobile overlay with focus trapping, and dead code removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/VISUAL-AUDIT-2026-03-25.md
@.planning/DESIGN-LANGUAGE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Tile component — layout prop (not default mutation) and visible glitch hover</name>
  <read_first>
    - src/components/tiles/tile.tsx
    - src/styles/globals.css
    - .planning/DESIGN-LANGUAGE.md
  </read_first>
  <files>src/components/tiles/tile.tsx, src/styles/globals.css</files>
  <action>
Rewrite the Tile component to fix C3 (vertical stacking) and C7 (invisible glitch). Addresses review concern about shared Tile primitive mutation risk.

**Layout prop (C3) — KEEP flex-col as default, add opt-in horizontal layout:**
Do NOT change the default layout from flex-col. Instead, add a new optional prop `layout?: "horizontal" | "vertical"` that defaults to `"vertical"`. This prevents regressions in widgets, auth tiles, and other non-nav consumers.

In the TileProps interface, add:
```
layout?: "horizontal" | "vertical"
```

Also add `compact?: boolean` prop — when true, hide the label and sublabel, show only the icon, and reduce padding to `p-2`.

In the baseClasses, change the static `"flex flex-col items-start justify-start gap-2"` to a conditional:
```
layout === "horizontal"
  ? "flex flex-row items-center gap-3"
  : "flex flex-col items-start justify-start gap-2",
```

Keep `flex-col` for `size === "large"` tiles regardless of layout prop (override).

Nav tiles in tile-nav.tsx and mobile-nav-overlay.tsx will pass `layout="horizontal"` explicitly. Widgets and other consumers keep the default vertical layout untouched.

**Glitch hover fix (C7):** The current glitch overlay is an empty `<span>` with `animate-glitch-hover` — clip-path on nothing is invisible. Fix: wrap ALL visible tile content (icon + label + sublabel + children) in a `<div className="relative">` container. On hover OR focus-visible (addresses review accessibility concern), DUPLICATE that content container as an absolutely-positioned overlay with `animate-glitch-hover` applied to it. The overlay gets `absolute inset-0 overflow-hidden` and the duplicated content gets the same layout.

Concrete implementation for the glitch overlay:
```
{(isHovered || isFocused) && !isActive && (
  <div className="pointer-events-none absolute inset-0 overflow-hidden animate-glitch-hover motion-reduce:hidden" aria-hidden="true">
    <div className={contentLayoutClasses + " p-4"}>
      {icon && <span className="flex-shrink-0 [&>svg]:h-5 [&>svg]:w-5">{icon}</span>}
      {label && !compact && <span className="font-mono text-lg font-bold uppercase tracking-[0.05em]">{label}</span>}
    </div>
  </div>
)}
```

Key accessibility additions (addresses review concerns):
- Add `motion-reduce:hidden` to the glitch overlay so it respects `prefers-reduced-motion`
- Add `aria-hidden="true"` to the duplication overlay (already shown above)
- Track focus state: add `const [isFocused, setIsFocused] = useState(false)` and `onFocus`/`onBlur` handlers so glitch triggers on `focus-visible` too, not just hover

The scan line overlay stays as-is (it works fine as a separate sweeping line). Add `motion-reduce:hidden` to the scan line overlay too.

**Active state enhancement (P3):** The active tile already inverts correctly. Add a slightly stronger glow: change `shadow-[0_0_20px_rgba(255,255,255,0.08)]` to `shadow-[0_0_20px_rgba(255,255,255,0.12)]`.

**Also in globals.css:** No CSS changes needed for the glitch keyframes. However, verify that `motion-reduce:hidden` utility works with the existing `@media (prefers-reduced-motion: reduce)` rules. If Tailwind v4 doesn't provide `motion-reduce:` prefix natively, add a manual rule:
```css
@media (prefers-reduced-motion: reduce) {
  .motion-reduce\:hidden {
    display: none !important;
  }
}
```
Note: Tailwind v4 may already support this as `motion-reduce:hidden`. Check existing globals.css first — only add if missing.
  </action>
  <verify>
    <automated>cd /home/faxas/workspaces/projects/personal/glitch_studios && grep -c "layout" src/components/tiles/tile.tsx && grep -c "horizontal" src/components/tiles/tile.tsx && grep -c "compact" src/components/tiles/tile.tsx && grep -c "animate-glitch-hover" src/components/tiles/tile.tsx && grep -c "flex-col" src/components/tiles/tile.tsx && grep -c "aria-hidden" src/components/tiles/tile.tsx && grep -c "motion-reduce" src/components/tiles/tile.tsx && grep -c "isFocused\|onFocus\|onBlur" src/components/tiles/tile.tsx</automated>
  </verify>
  <acceptance_criteria>
    - tile.tsx contains `layout?: "horizontal" | "vertical"` in the TileProps interface
    - tile.tsx default layout is STILL `flex-col` (not changed to flex-row)
    - tile.tsx contains conditional `flex-row` only when `layout === "horizontal"`
    - tile.tsx contains `compact?: boolean` in the TileProps interface
    - tile.tsx contains at least 2 occurrences of `animate-glitch-hover` (the overlay div that duplicates content)
    - tile.tsx glitch overlay div contains `{icon` and `{label` (content duplication, not empty span)
    - tile.tsx glitch overlay has `aria-hidden="true"`
    - tile.tsx glitch overlay has `motion-reduce:hidden` or equivalent reduced-motion guard
    - tile.tsx has focus tracking (isFocused state, onFocus/onBlur handlers)
    - tile.tsx active state shadow contains `0.12` (stronger glow)
    - tile.tsx still has `flex-col` for `size === "large"` tiles
    - The old empty `<span className="pointer-events-none absolute inset-0 animate-glitch-hover">` is removed
  </acceptance_criteria>
  <done>Tile component has a layout prop (defaults to vertical, opt-in horizontal). Hovering or focusing a tile triggers visible clip-path distortion on duplicated content with reduced-motion guard. Compact mode hides labels. No regressions to widget/auth tiles.</done>
</task>

<task type="auto">
  <name>Task 2: Fix LogoTile (height + invert), TileNav, MobileNavOverlay (focus trap), BottomTabBar, delete dead side-nav.tsx</name>
  <read_first>
    - src/components/tiles/logo-tile.tsx
    - src/components/layout/tile-nav.tsx
    - src/components/layout/mobile-nav-overlay.tsx
    - src/components/layout/bottom-tab-bar.tsx
    - src/components/tiles/widget-social.tsx
    - src/components/layout/side-nav.tsx
    - src/components/tiles/tile.tsx (after Task 1 changes)
  </read_first>
  <files>src/components/tiles/logo-tile.tsx, src/components/layout/tile-nav.tsx, src/components/layout/mobile-nav-overlay.tsx, src/components/layout/bottom-tab-bar.tsx, src/components/tiles/widget-social.tsx, src/components/layout/side-nav.tsx</files>
  <action>
**DELETE side-nav.tsx — Address review concern #4 (dead code):**
Delete `src/components/layout/side-nav.tsx`. This is the old shadcn sidebar implementation that is no longer used — `tile-nav.tsx` is the active sidebar. Verify no imports reference it first:
```bash
grep -r "side-nav" src/ --include="*.tsx" --include="*.ts"
```
If any file imports from side-nav.tsx, update that import to use tile-nav.tsx instead. Then delete the file.

**logo-tile.tsx — Fix C1 (invisible logo) and C6 (weak glow):**
Addresses review concern #1: logo has no intrinsic height because the image is absolutely positioned inside the container.

FIRST fix the structural sizing issue — the container has no intrinsic height:
1. Add explicit `aspect-square` to the outer `motion.div` so the 2x2 grid tile has a defined height. The grid column width determines the width, and `aspect-square` ensures the height matches. Alternatively, add `min-h-[120px]` as a fallback.
2. Change the logo `<div>` from `absolute inset-0` to use `aspect-[3/1]` with `w-full` and remove `absolute inset-0`. This gives the logo image its own intrinsic dimensions. OR keep the absolute positioning but ensure the parent has `aspect-square`.

THEN fix visibility:
3. Add CSS `filter: invert(1)` to the logo `<div>` that has `backgroundImage: "url(/Untitled-2.png)"`. This inverts the black-on-white PNG to white-on-transparent on the dark bg.
4. Change `bg-cover` to `bg-contain` so the logo doesn't stretch.

Increase glow from `shadow-[0_0_15px_rgba(255,250,240,0.15)]` to a two-layer glow: `shadow-[0_0_25px_rgba(255,250,240,0.25),0_0_50px_rgba(255,250,240,0.08)]`.

Apply the same glitch-on-content fix as Tile: the hover overlay should duplicate the logo div with the animation, not animate an empty span. Add `aria-hidden="true"` and `motion-reduce:hidden` to the glitch overlay.

**tile-nav.tsx — Fix C4 (scroll/sign-in hidden), C5 (padding waste), C8 (no compact mode), M1 (social widget cut off):**

1. **Padding (C5):** Change outer `p-6` to `p-3`. This recovers 24px of space.

2. **Pass layout="horizontal" to nav Tiles:** All `<Tile>` components used for navigation in the sidebar should receive `layout="horizontal"`. This uses the new prop from Task 1 without mutating the Tile default. Widget tiles do NOT get this prop — they keep their default vertical layout.

3. **Compact mode (C8):** Add state `const [isCompact, setIsCompact] = useState(false)`. When compact, sidebar width shrinks from `w-[280px]` to `w-16` (64px). Add a toggle button at the top-right of the sidebar: a small square button with `PanelLeftClose` icon (from lucide) that toggles `isCompact`. When compact, pass `compact={true}` to all `<Tile>` components (which hides labels per Task 1). The LogoTile in compact mode should render as a small square (remove `col-span-2 row-span-2`, just show a 48px square with the inverted logo). Widgets section is hidden in compact mode (render null). Auth tile also gets `compact={true}`.

4. **Scroll and scrollbar (C4):** Add `className="sidebar-scroll"` to the `<aside>` element that wraps the sidebar content. This class is defined in globals.css (Plan 03 Task 2) and provides a thin 4px scrollbar with `#333` thumb on transparent track. Also add `overflow-y-auto` to the aside if not already present. After the flex-col sidebar content, if `isCompact` is false, add a sticky gradient div at the bottom: `<div className="pointer-events-none sticky bottom-0 h-8 bg-gradient-to-t from-black to-transparent" />`.

5. **Remove spacer div:** The current `<div className="flex-1" />` spacer pushes auth to the bottom but also pushes content off-screen. Remove it. Auth section should flow naturally after widgets with `mt-4`. If sidebar fits, great. If not, scrollbar + gradient hint handle it.

6. **Rename "About" to "Artists" (part of C9):** In the `navItems` array, change `{ label: "About", href: "/artists", ...}` to `{ label: "Artists", href: "/artists", ...}`.

7. **Social widget (M1):** Ensure `WidgetSocial` renders within the visible widget grid. No code change needed in widget-social.tsx itself — the padding reduction and spacer removal should fix the clipping.

**mobile-nav-overlay.tsx — Fix M4 (empty overlay) + focus trap (review concern #5):**
The overlay already renders LogoTile, Tiles, and Widgets. The reason it appears empty is:
- LogoTile is invisible (C1 fix in logo-tile.tsx handles this)
- The glitch entrance overlay `<motion.div className="pointer-events-none absolute inset-0 animate-glitch-hover" ...>` covers the entire screen and may not fade properly. Verify it has `z-[1]` and content area has `z-[2]` so content renders on top.
- Pass `layout="horizontal"` to all nav `<Tile>` components here too (consistent with sidebar).
- Rename "About" to "Artists" in the `navItems` array here too.
- Add `latestPostSlot` support: add an optional `latestPostSlot?: ReactNode` prop (same as TileNav), render it between WidgetStudioStatus and WidgetSocial in the widgets grid.

**Focus trap (addresses review accessibility concern):**
Add a simple focus trap to the mobile overlay. When `isOpen` is true:
1. On open, focus the close button automatically
2. Track first and last focusable elements in the overlay
3. On Tab at last element, wrap to first; on Shift+Tab at first, wrap to last
4. Implementation: add a `useEffect` that finds all `[tabindex], a, button, input, select, textarea` inside `overlayRef.current` and handles keydown Tab events. This is a lightweight trap — no external dependency needed.

Example:
```tsx
useEffect(() => {
  if (!isOpen || !overlayRef.current) return
  const overlay = overlayRef.current
  const focusable = overlay.querySelectorAll<HTMLElement>(
    'a[href], button:not([disabled]), input, select, textarea, [tabindex]:not([tabindex="-1"])'
  )
  if (focusable.length === 0) return
  const first = focusable[0]
  const last = focusable[focusable.length - 1]
  first.focus()

  function handleTab(e: KeyboardEvent) {
    if (e.key !== "Tab") return
    if (e.shiftKey && document.activeElement === first) {
      e.preventDefault()
      last.focus()
    } else if (!e.shiftKey && document.activeElement === last) {
      e.preventDefault()
      first.focus()
    }
  }
  overlay.addEventListener("keydown", handleTab)
  return () => overlay.removeEventListener("keydown", handleTab)
}, [isOpen])
```

**bottom-tab-bar.tsx — Fix P4 (no home active state):**
The center grid button should show active/inverted state when `pathname === "/"`. Change the center button's className logic:
```
const isHome = pathname === "/"
// In the center button:
className={clsx(
  "flex flex-1 items-center justify-center",
  "border-r border-[#222222]",
  "rounded-none transition-colors duration-200",
  "outline-none focus-visible:outline-1 focus-visible:outline-[#f5f5f0] focus-visible:outline-offset-[-2px]",
  "min-h-[48px]",
  isHome
    ? "bg-[#f5f5f0] text-[#000000]"
    : "bg-[#111111] text-[#f5f5f0] active:bg-[#0a0a0a]",
)}
```

**widget-social.tsx:** The small tiles currently use `size="small"` which includes `aspect-square` in Tile. In compact sidebar mode, the WidgetSocial is hidden (handled in tile-nav.tsx). No changes needed to the component itself.
  </action>
  <verify>
    <automated>cd /home/faxas/workspaces/projects/personal/glitch_studios && test ! -f src/components/layout/side-nav.tsx && echo "side-nav deleted" && grep -c "invert" src/components/tiles/logo-tile.tsx && grep -c "aspect-square\|min-h-" src/components/tiles/logo-tile.tsx && grep -c "isCompact" src/components/layout/tile-nav.tsx && grep "Artists" src/components/layout/tile-nav.tsx && grep "isHome" src/components/layout/bottom-tab-bar.tsx && grep "Artists" src/components/layout/mobile-nav-overlay.tsx && grep "sidebar-scroll" src/components/layout/tile-nav.tsx && grep -c "layout.*horizontal" src/components/layout/tile-nav.tsx && grep -c "handleTab\|focusable\|focus trap" src/components/layout/mobile-nav-overlay.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/layout/side-nav.tsx does NOT exist (deleted)
    - logo-tile.tsx has explicit sizing: `aspect-square` or `min-h-[120px]` on the container
    - logo-tile.tsx contains `filter: invert(1)` or `invert` in the logo div's style/className
    - logo-tile.tsx shadow contains `0_0_25px` (stronger glow, two-layer)
    - logo-tile.tsx glitch overlay has `aria-hidden="true"` and `motion-reduce:hidden`
    - tile-nav.tsx nav tiles pass `layout="horizontal"` to Tile components
    - tile-nav.tsx contains `isCompact` state variable
    - tile-nav.tsx outer aside uses `p-3` not `p-6`
    - tile-nav.tsx outer `<aside>` element has `className` containing `sidebar-scroll`
    - tile-nav.tsx navItems array has `label: "Artists"` not `label: "About"`
    - tile-nav.tsx does NOT contain `<div className="flex-1" />` spacer
    - tile-nav.tsx contains a gradient scroll hint div with `bg-gradient-to-t from-black`
    - tile-nav.tsx contains a toggle button for compact mode (PanelLeftClose or similar icon)
    - mobile-nav-overlay.tsx nav tiles pass `layout="horizontal"` to Tile components
    - mobile-nav-overlay.tsx navItems array has `label: "Artists"` not `label: "About"`
    - mobile-nav-overlay.tsx has a focus trap implementation (handleTab or similar)
    - mobile-nav-overlay.tsx glitch entrance overlay has z-index lower than content area
    - bottom-tab-bar.tsx contains `isHome` or `pathname === "/"` check for center button styling
    - bottom-tab-bar.tsx center button has conditional `bg-[#f5f5f0] text-[#000000]` for home active state
  </acceptance_criteria>
  <done>Dead side-nav.tsx deleted. Logo visible with intrinsic height, invert filter, and glow. Sidebar uses layout="horizontal" prop on nav tiles (not mutating Tile default). Sidebar has 12px padding, compact toggle, scroll hint. "About" renamed "Artists" everywhere. Mobile overlay shows visible content with focus trapping. Bottom tab center icon inverts on home page.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `test ! -f src/components/layout/side-nav.tsx` exits 0 (dead code deleted)
2. `grep -c "layout" src/components/tiles/tile.tsx` returns >= 3 (prop definition + usage)
3. `grep -c "flex-col" src/components/tiles/tile.tsx` returns >= 1 (default preserved)
4. `grep "invert" src/components/tiles/logo-tile.tsx` shows filter invert on logo
5. `grep "aspect-square\|min-h-" src/components/tiles/logo-tile.tsx` shows explicit sizing
6. `grep "isCompact" src/components/layout/tile-nav.tsx` shows compact mode state
7. `grep "Artists" src/components/layout/tile-nav.tsx` shows renamed nav item
8. `grep "isHome" src/components/layout/bottom-tab-bar.tsx` shows home active detection
9. `grep "Artists" src/components/layout/mobile-nav-overlay.tsx` shows renamed nav item
10. No occurrences of `label: "About"` in tile-nav.tsx or mobile-nav-overlay.tsx
11. `grep "sidebar-scroll" src/components/layout/tile-nav.tsx` shows class applied to aside
12. `grep "layout.*horizontal" src/components/layout/tile-nav.tsx` shows nav tiles using horizontal layout
13. `grep -c "handleTab\|focusable" src/components/layout/mobile-nav-overlay.tsx` returns >= 1 (focus trap)
14. `grep "motion-reduce" src/components/tiles/tile.tsx` shows reduced-motion guard
</verification>

<success_criteria>
- Dead side-nav.tsx is deleted from codebase
- Logo tile has explicit height (aspect-square or min-h) and shows the GLITCH wordmark (white on dark) with a two-layer warm glow
- All sidebar nav tiles use horizontal layout via `layout="horizontal"` prop — NOT by changing Tile default
- Widget tiles and auth tiles still use vertical layout (default unchanged)
- Hovering or focusing any tile visibly distorts tile content via clip-path animation
- Glitch duplication overlay is aria-hidden, triggers on focus-visible, and respects prefers-reduced-motion
- Sidebar padding is 12px; all content reachable via scroll with visible gradient hint
- Sidebar aside element has `sidebar-scroll` class for thin scrollbar styling
- Sidebar has working compact mode toggled by a button (icon-only, ~64px wide)
- Mobile nav overlay shows logo, nav tiles, widgets, and auth with focus trapping
- Bottom tab center icon inverts when on home page
- "About" renamed to "Artists" in sidebar and mobile overlay
</success_criteria>

<output>
After completion, create `.planning/phases/01.4-visual-polish/01.4-01-SUMMARY.md`
</output>
