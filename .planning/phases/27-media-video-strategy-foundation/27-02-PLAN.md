---
phase: 27-media-video-strategy-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
autonomous: true
requirements: [D-01, D-04]
must_haves:
  truths:
    - "next/image can fetch YouTube CDN thumbnails from i.ytimg.com without 502"
  artifacts:
    - path: "next.config.ts"
      provides: "i.ytimg.com remote pattern allowance"
      contains: "i.ytimg.com"
  key_links:
    - from: "<MediaEmbed> thumbnail"
      to: "next/image remote pattern allowlist"
      via: "i.ytimg.com hostname declaration"
      pattern: "hostname:.*i\\.ytimg\\.com"
---

<objective>
Add `i.ytimg.com` to `next.config.ts` `images.remotePatterns`. This is a tiny config change but a hard blocker for downstream plans: `<MediaEmbed>` will use `https://i.ytimg.com/vi/{id}/maxresdefault.jpg` (and `hqdefault.jpg` as fallback) and `next/image` will refuse the host until it is whitelisted.

Purpose: Unblock Plan 03 (`<MediaEmbed>`) and Plan 07 (public surfaces) — without this, every thumbnail returns 502 from `next/image`.

Output: `next.config.ts` lists `i.ytimg.com` as an allowed remote image host.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-media-video-strategy-foundation/27-RESEARCH.md
@next.config.ts

<interfaces>
Current `next.config.ts` `images.remotePatterns` allows: `img.youtube.com`, `*.r2.dev`, `*.r2.cloudflarestorage.com`, `placehold.co`.

Per RESEARCH Pitfall 2 (lines 953-966): `youtube-nocookie.com` thumbnail host is `i.ytimg.com`, NOT `img.youtube.com`. Both endpoints serve the same files, but YouTube CDN sometimes redirects, and `next/image` refuses redirected hosts not in the allowlist.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Whitelist i.ytimg.com in next.config.ts</name>
  <files>next.config.ts</files>
  <read_first>
    - next.config.ts (full file — understand current remotePatterns shape)
    - .planning/phases/27-media-video-strategy-foundation/27-RESEARCH.md § "Pitfall 2" (lines 953-966)
  </read_first>
  <action>
    Edit `next.config.ts`. In the `images.remotePatterns` array, add an entry for `i.ytimg.com` immediately after the `img.youtube.com` entry. Final `remotePatterns` array should be:

    ```typescript
    remotePatterns: [
      {
        protocol: "https",
        hostname: "img.youtube.com",
      },
      {
        protocol: "https",
        hostname: "i.ytimg.com",
      },
      {
        protocol: "https",
        hostname: "*.r2.dev",
      },
      {
        protocol: "https",
        hostname: "*.r2.cloudflarestorage.com",
      },
      {
        protocol: "https",
        hostname: "placehold.co",
      },
    ],
    ```

    Do not modify any other field in `next.config.ts`. Do not modify `allowedDevOrigins`. Do not add `headers()`, CSP, or Permissions-Policy (RESEARCH lines 1101-1113 confirms no CSP work needed).

    After editing, run `pnpm tsc --noEmit` to verify config still type-checks.
  </action>
  <verify>
    <automated>grep -c 'hostname: "i.ytimg.com"' next.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'hostname: "i.ytimg.com"' next.config.ts` returns 1
    - `grep -c 'hostname: "img.youtube.com"' next.config.ts` returns 1 (still present, not replaced)
    - `grep -c 'hostname: "\*.r2.dev"' next.config.ts` returns 1 (other entries preserved)
    - `pnpm tsc --noEmit` exits 0
    - No `headers()` block added (no CSP changes)
  </acceptance_criteria>
  <done>
    `next.config.ts` whitelists `i.ytimg.com` alongside `img.youtube.com`. Config still type-checks.
  </done>
</task>

</tasks>

<verification>
1. `grep "i.ytimg.com" next.config.ts` returns 1 line
2. `pnpm tsc --noEmit` passes
3. (Smoke — manual): on dev server, an `<Image src="https://i.ytimg.com/vi/dQw4w9WgXcQ/maxresdefault.jpg" />` would render without 502
</verification>

<success_criteria>
- `i.ytimg.com` is whitelisted in `next.config.ts`
- All previously whitelisted hosts remain
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/27-media-video-strategy-foundation/27-02-SUMMARY.md`
</output>
