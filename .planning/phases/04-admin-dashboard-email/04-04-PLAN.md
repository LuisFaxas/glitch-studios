---
phase: 04-admin-dashboard-email
plan: 04
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - src/app/admin/services/page.tsx
  - src/components/admin/service-page-form.tsx
  - src/actions/admin-services.ts
  - src/app/admin/team/page.tsx
  - src/app/admin/team/[id]/edit/page.tsx
  - src/app/admin/team/new/page.tsx
  - src/components/admin/team-member-form.tsx
  - src/app/admin/testimonials/page.tsx
  - src/components/admin/testimonial-form.tsx
  - src/actions/admin-content.ts
autonomous: true
requirements:
  - ADMN-03

must_haves:
  truths:
    - "Admin can create, edit, and delete service pages with rich text descriptions"
    - "Admin can manage team members with photos from media library"
    - "Admin can manage testimonials with service type and optional rating"
  artifacts:
    - path: "src/app/admin/services/page.tsx"
      provides: "Service page management with CRUD"
      contains: "ServicePageForm"
    - path: "src/app/admin/team/page.tsx"
      provides: "Team member list and management"
      contains: "TeamMemberForm"
    - path: "src/app/admin/testimonials/page.tsx"
      provides: "Testimonial management"
      contains: "TestimonialForm"
  key_links:
    - from: "src/components/admin/service-page-form.tsx"
      to: "src/components/admin/tiptap-editor.tsx"
      via: "TiptapEditor for full description"
      pattern: "TiptapEditor"
    - from: "src/components/admin/team-member-form.tsx"
      to: "src/components/admin/media-picker-dialog.tsx"
      via: "Photo selection from media library"
      pattern: "MediaPickerDialog"
---

<objective>
Service page, team member, and testimonial CRUD management pages.

Purpose: Completes content management (ADMN-03) for the three remaining entity types. All use the Tiptap editor and/or media picker from Plan 02.

Output: Admin pages for managing services, team members, and testimonials with full CRUD.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-admin-dashboard-email/04-CONTEXT.md
@.planning/phases/04-admin-dashboard-email/04-UI-SPEC.md

@src/db/schema.ts
@src/lib/permissions.ts
@src/components/admin/tiptap-editor.tsx
@src/components/admin/media-picker-dialog.tsx
@.planning/phases/04-admin-dashboard-email/04-01-SUMMARY.md
@.planning/phases/04-admin-dashboard-email/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Service page CRUD with Tiptap descriptions</name>
  <files>src/app/admin/services/page.tsx, src/components/admin/service-page-form.tsx, src/actions/admin-services.ts</files>
  <read_first>
    - src/db/schema.ts (services table with all columns)
    - src/app/admin/services/ (check if existing admin services page exists, may need to extend rather than create)
    - src/actions/admin-beats.ts (CRUD pattern reference)
    - .planning/phases/04-admin-dashboard-email/04-UI-SPEC.md (service page form spec)
  </read_first>
  <action>
NOTE: An admin services page may already exist from Phase 3 (booking config). Check `src/app/admin/services/` first. If it exists, extend it. If it's just booking config, add full service CRUD alongside.

**1. Create src/actions/admin-services.ts** ("use server") if not already existing:

- `getServices()`: query all services ordered by sortOrder, include booking config status
- `createService(data)`: requirePermission("manage_content"), insert into services table
  - Data: { name, slug (auto-gen), type (serviceTypeEnum), description (HTML from Tiptap), shortDescription, priceLabel, features (string[]), ctaText, sortOrder, isActive, heroImageUrl }
- `updateService(id, data)`: requirePermission("manage_content"), update services row
- `deleteService(id)`: requirePermission("manage_content"), delete from services (check for booking config dependency)
- `reorderServices(orderedIds: string[])`: requirePermission("manage_content"), update sortOrder for each

**2. Create src/components/admin/service-page-form.tsx** ("use client"):
- Fields per UI-SPEC:
  - Name: text input, required, min 2, placeholder "Service name"
  - Slug: text input, auto-gen from name, placeholder "service-slug"
  - Short Description: text input, required, max 160 chars, placeholder "Brief description for cards"
  - Full Description: TiptapEditor, required, min 100 chars, placeholder "Detailed service description..."
  - Type: native HTML select from serviceTypeEnum values
  - Base Price: text input (price label like "Starting at $200/hr"), placeholder "0.00"
  - Hero Image: MediaPickerDialog button (typeFilter="image")
  - Display Order: number input, default 0
  - Active: toggle switch
- Save button: "Save Changes" (primary CTA)
- Zod validation

**3. Update/create src/app/admin/services/page.tsx** (server, force-dynamic):
- If existing page has booking config only, wrap with service list + CRUD
- Table: Name, Type, Price, Order, Active (switch), Actions (Edit/Delete)
- "+ New Service" button
- Edit opens form in a new page or inline (follow existing pattern)
  </action>
  <verify>
    <automated>cd /home/faxas/workspaces/projects/personal/glitch_studios && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/actions/admin-services.ts contains `requirePermission("manage_content")`
    - src/actions/admin-services.ts contains `createService`
    - src/components/admin/service-page-form.tsx contains `TiptapEditor`
    - src/components/admin/service-page-form.tsx contains `MediaPickerDialog`
    - src/app/admin/services/page.tsx contains `force-dynamic`
  </acceptance_criteria>
  <done>Service pages are fully manageable from admin with rich text descriptions and hero images from media library</done>
</task>

<task type="auto">
  <name>Task 2: Team member and testimonial CRUD management</name>
  <files>src/app/admin/team/page.tsx, src/app/admin/team/new/page.tsx, src/app/admin/team/[id]/edit/page.tsx, src/components/admin/team-member-form.tsx, src/app/admin/testimonials/page.tsx, src/components/admin/testimonial-form.tsx, src/actions/admin-content.ts</files>
  <read_first>
    - src/db/schema.ts (teamMembers, testimonials tables)
    - src/components/admin/media-picker-dialog.tsx (for photo selection)
    - .planning/phases/04-admin-dashboard-email/04-UI-SPEC.md (team member form, testimonial form specs)
  </read_first>
  <action>
**1. Create src/actions/admin-content.ts** ("use server"):

Team member actions:
- `getTeamMembers()`: query teamMembers ordered by sortOrder
- `createTeamMember(data)`: requirePermission("manage_content")
  - Data: { name, slug (auto-gen), role, bio, photoUrl, socialLinks (JSON string with instagram/youtube/soundcloud), credits, sortOrder }
- `updateTeamMember(id, data)`: requirePermission("manage_content")
- `deleteTeamMember(id)`: requirePermission("manage_content")
- `reorderTeamMembers(orderedIds: string[])`: requirePermission("manage_content"), update sortOrder

Testimonial actions:
- `getTestimonials()`: query testimonials ordered by createdAt desc
- `createTestimonial(data)`: requirePermission("manage_content")
  - Data: { quote, clientName, serviceType, rating (1-5 nullable), photoUrl }
- `updateTestimonial(id, data)`: requirePermission("manage_content")
- `deleteTestimonial(id)`: requirePermission("manage_content")

**2. Create src/components/admin/team-member-form.tsx** ("use client"):
- Fields per UI-SPEC:
  - Name: text input, required, min 2, placeholder "Full name"
  - Role/Title: text input, required, min 2, placeholder "Producer, Engineer, etc."
  - Bio: textarea, required, min 20, max 1000, placeholder "Brief biography..."
  - Photo: MediaPickerDialog button (typeFilter="image"), shows preview
  - Instagram: text input, URL format, placeholder "https://instagram.com/..."
  - YouTube: text input, URL format, placeholder "https://youtube.com/..."
  - SoundCloud: text input, URL format, placeholder "https://soundcloud.com/..."
  - Display Order: number input, default 0
- Save: "Save Changes" CTA
- Zod validation

**3. Create src/components/admin/testimonial-form.tsx** ("use client"):
- Fields per UI-SPEC:
  - Client Name: text input, required, min 2, placeholder "Client name"
  - Quote: textarea, required, min 20, max 500, placeholder "Testimonial quote..."
  - Service Type: native HTML select from services list, required, placeholder "Select service"
  - Rating: number input, optional, 1-5 integer, placeholder "5"
  - Photo: MediaPickerDialog button (typeFilter="image"), optional
- Save: "Save Changes" CTA
- Zod validation

**4. Create src/app/admin/team/page.tsx** (server, force-dynamic):
- Page heading: "TEAM" with "+ Add Member" button (links to /admin/team/new)
- Table: Photo (48px), Name, Role, Order, Actions (Edit/Delete)
- Delete: AlertDialog confirmation

**5. Create src/app/admin/team/new/page.tsx** and src/app/admin/team/[id]/edit/page.tsx**:
- Standard create/edit pattern with TeamMemberForm

**6. Create src/app/admin/testimonials/page.tsx** (server, force-dynamic):
- Page heading: "TESTIMONIALS" with "+ Add Testimonial" button
- Table: Client Name, Quote (truncated), Service Type, Rating, Actions (Edit/Delete)
- Edit opens TestimonialForm in a Dialog or Sheet (simpler form, doesn't need full page)
- Delete: AlertDialog confirmation
  </action>
  <verify>
    <automated>cd /home/faxas/workspaces/projects/personal/glitch_studios && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/actions/admin-content.ts contains `createTeamMember`
    - src/actions/admin-content.ts contains `createTestimonial`
    - src/actions/admin-content.ts contains `requirePermission("manage_content")`
    - src/components/admin/team-member-form.tsx contains `MediaPickerDialog`
    - src/components/admin/testimonial-form.tsx contains `serviceType`
    - src/app/admin/team/page.tsx contains `force-dynamic`
    - src/app/admin/testimonials/page.tsx contains `force-dynamic`
    - src/app/admin/team/new/page.tsx exists
    - src/app/admin/team/[id]/edit/page.tsx exists
  </acceptance_criteria>
  <done>Team members and testimonials are fully manageable from admin with photos from media library, social links, service types, and ratings</done>
</task>

</tasks>

<verification>
- Service page CRUD works with Tiptap descriptions and hero images
- Team member CRUD works with photos from media library and social links
- Testimonial CRUD works with service type dropdown and optional rating
- All content management pages respect permission checks
</verification>

<success_criteria>
1. Services, team members, and testimonials have full admin CRUD
2. All forms use MediaPickerDialog for images
3. Service descriptions use Tiptap rich text editor
4. Permission enforcement on all content management actions
</success_criteria>

<output>
After completion, create `.planning/phases/04-admin-dashboard-email/04-04-SUMMARY.md`
</output>
