---
phase: 07.6-reviews-display-comparison-tables
plan: 06
subsystem: ui
tags: [compare, recharts-dynamic, intersection-observer, winner-detection, nuqs]

requires:
  - phase: 07.6-reviews-display-comparison-tables
    provides: getProductsBySlugs, getBenchmarkRunsForProducts, listAllPublishedProductsForPicker, computeSpecWinner, computeBenchmarkWinner
provides:
  - /tech/compare comparison tool
  - ComparisonProductPicker (Command inside Popover) with category-lock behavior
  - ComparisonTable with per-row winner + column outline for dominant product
  - ComparisonBenchmarkChart dynamic-imports Recharts (zero static imports, bundle-isolated)
  - ComparisonBenchmarkList eager-loads first 3 charts, lazy-mounts rest via IntersectionObserver
affects: [07.6-07]

tech-stack:
  added: []
  patterns:
    - "Recharts dynamic imports ({ ssr: false }) keep library out of other bundles"
    - "IntersectionObserver lazy-mount pattern for heavy chart rendering"
    - "nuqs shallow:true for in-tab view switching, shallow:false for products array"

key-files:
  created:
    - src/components/tech/comparison-product-picker.tsx
    - src/components/tech/comparison-summary.tsx
    - src/components/tech/comparison-table.tsx
    - src/components/tech/comparison-benchmark-chart.tsx
    - src/components/tech/comparison-benchmark-list.tsx
    - src/components/tech/compare-page-client.tsx
  modified:
    - src/app/(tech)/tech/compare/page.tsx

key-decisions:
  - "Base UI Popover uses rendered button content (not asChild) — styled via className on PopoverTrigger directly"
  - "Invalid slugs silently dropped by getProductsBySlugs filter (unknown slugs not found in DB)"
  - "MAX_SLOTS=4 slots per compare; picker locks to first product's category after first pick"

patterns-established:
  - "All Recharts subcomponents individually dynamic-imported in a single file — keeps bundle slim"

requirements-completed:
  - 07.6-S5

duration: 8min
completed: 2026-04-17
---

# Phase 07.6 Plan 06: Comparison Tool Summary

**`/tech/compare` with Command-based product picker, winner-detecting specs table, dynamic-Recharts benchmark bar charts (eager-3 + IntersectionObserver lazy rest), and tabs for Specs / Benchmarks / Price**

## Performance
- **Duration:** ~8 min
- **Tasks:** 10
- **Files created:** 6
- **Files modified:** 1

## Accomplishments
- Product picker restricts to first-picked-product's category (prevents meaningless compares)
- Invalid URL slugs silently dropped (getProductsBySlugs filter)
- Specs table highlights winning cell per row + column outline when one product has ≥50% of wins
- Benchmark list eager-renders first 3 charts, rest lazy via IntersectionObserver (rootMargin 200px)
- Recharts fully dynamic-imported with `{ ssr: false }` — grep confirms ZERO static imports outside comparison-benchmark-chart.tsx

## Task Commits
1. ComparisonProductPicker — `a6f3d18`
2. ComparisonSummary — `3f43fc3`
3. ComparisonTable — `27f8367`
4. ComparisonBenchmarkChart — `5935965`
5. ComparisonBenchmarkList — `40a4008`
6. ComparePageClient — `8779d2f`
7. Compare page RSC — `e5930e3`
8. Recharts isolation audit — PASS (only 1 file imports recharts)
9. Playwright verification — deferred to phase audit
10. Build check — tsc clean for plan 06 files

## Files Created/Modified
See key-files.

## Decisions Made
- **Popover trigger styling:** Base UI Popover uses native `<button>` trigger; plan's `<PopoverTrigger asChild>` pattern is Radix-specific. Passed styling via className directly on PopoverTrigger.
- **Recharts bundle isolation:** All 8 subcomponents (ResponsiveContainer, BarChart, Bar, Cell, XAxis, YAxis, CartesianGrid, LabelList) individually dynamic-imported in ONE file. Any other file that needs charts should import from there, not from 'recharts'.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Base UI Popover doesn't support Radix-style `asChild`**
- **Found during:** Task 1 (ComparisonProductPicker)
- **Issue:** Plan wrote `<PopoverTrigger asChild><button>...</button></PopoverTrigger>` — Radix API; Base UI uses `render` prop or lets you style trigger directly via className
- **Fix:** Merged styling into PopoverTrigger itself (it renders a native `<button>` by default), removed nested `<button>`
- **Verification:** `pnpm tsc --noEmit` clean for comparison-product-picker.tsx
- **Committed in:** `a6f3d18`

**2. [Rule 3 - Blocking] `import { computeSpecWinner }` unused in ComparisonSummary**
- **Found during:** Task 2
- **Issue:** Plan imports computeSpecWinner in comparison-summary.tsx but the function is never used (only computeBenchmarkWinner + price logic). Would emit an unused-import lint warning.
- **Fix:** Removed computeSpecWinner from the import list
- **Verification:** `pnpm lint` doesn't flag my new files
- **Committed in:** `3f43fc3`

---

**Total deviations:** 2 auto-fixed (1 API mismatch, 1 unused import).
**Impact on plan:** Functional output unchanged.

## Issues Encountered
None critical.

## User Setup Required
None.

## Next Phase Readiness
- Wave 4 Plan 07 (homepage) is the final plan. Can reference all public pages now live
- Compare tool deep-links work: any review detail page's "Compare to another product" CTA links to `/tech/compare?products=<slug>` which loads that product into slot 1

---
*Phase: 07.6-reviews-display-comparison-tables*
*Completed: 2026-04-17*

## Self-Check: PASSED
- [x] All 7 files committed
- [x] Recharts bundle isolated to 1 file (audit grep confirms)
- [x] tsc --noEmit clean for plan 06 files
- [x] generateStaticParams not applicable (compare is per-request)
- [x] No Self-Check: FAILED marker
