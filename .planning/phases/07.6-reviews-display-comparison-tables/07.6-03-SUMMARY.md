---
phase: 07.6-reviews-display-comparison-tables
plan: 03
subsystem: ui
tags: [review-detail, rsc, jsonld, tiptap-sanitize, youtube-embed, lightbox, benchmarks, accordion]

requires:
  - phase: 07.6-reviews-display-comparison-tables
    provides: queries.ts, sanitize.ts, youtube.ts, ReviewCard
provides:
  - /tech/reviews/[slug] full review detail page (RSC)
  - 13 new components in src/components/tech/ covering every review section
  - JSON-LD Review schema for SEO
  - generateStaticParams with safe try/catch fallback
  - generateMetadata with OG images
affects: [07.6-04, 07.6-07]

tech-stack:
  added: []
  patterns:
    - "Sanitize review HTML server-side in page RSC, pass pre-sanitized HTML to ReviewBody"
    - "JSON-LD injected via <script type='application/ld+json'> in page body"
    - "Parallel Promise.all for breadcrumb/related/specs/benchmarks"
    - "Optional sections (gallery/video/audience/benchmarks) return null when data missing"

key-files:
  created:
    - src/components/tech/review-hero.tsx
    - src/components/tech/review-verdict.tsx
    - src/components/tech/review-body.tsx
    - src/components/tech/review-rating-bar.tsx
    - src/components/tech/review-rating-card.tsx
    - src/components/tech/review-pros-cons.tsx
    - src/components/tech/review-audience.tsx
    - src/components/tech/review-gallery.tsx
    - src/components/tech/review-gallery-lightbox.tsx
    - src/components/tech/review-video-embed.tsx
    - src/components/tech/product-specs-table.tsx
    - src/components/tech/product-benchmarks-table.tsx
    - src/components/tech/related-reviews-carousel.tsx
  modified:
    - src/app/(tech)/tech/reviews/[slug]/page.tsx

key-decisions:
  - "Base UI Accordion API: no type='single' or collapsible props (Radix-style); single-open is the default behavior"
  - "getProductSpecs inlined in page RSC rather than added to queries.ts — page-specific"

patterns-established:
  - "Pattern: RSC page sanitizes review body once, passes HTML to client-agnostic ReviewBody"
  - "Pattern: lightbox + thumb grid split into ReviewGallery (orchestrator) + ReviewGalleryLightbox (viewer)"

requirements-completed:
  - 07.6-S1
  - 07.6-S8

duration: 9min
completed: 2026-04-17
---

# Phase 07.6 Plan 03: Review Detail Page Summary

**Full `/tech/reviews/[slug]` RSC rendering 16 sections (breadcrumb → hero → verdict → body → rating → pros/cons → gallery → video → specs → benchmarks → audience → related → compare CTA → newsletter), plus JSON-LD Review schema and safe generateStaticParams**

## Performance
- **Duration:** ~9 min
- **Tasks:** 13
- **Files created:** 13 (src/components/tech/)
- **Files modified:** 1 (page.tsx)

## Accomplishments
- Every field the admin captures (07.5) now has public rendering
- Gallery lightbox with keyboard arrow navigation + loop + counter
- YouTube embed uses strict ID extraction — invalid URLs silently hide the section
- Rating card uses 1-10 scale bars animated by Framer Motion with reduced-motion honored
- Benchmarks table shows direction arrows (↑ higher / ↓ lower is better)
- Specs table uses shadcn Accordion, collapsed by default
- Related reviews reuse shared `<ReviewCard>` — no markup drift

## Task Commits
1. ReviewHero — `c356637`
2. ReviewVerdict — `d80f024`
3. ReviewBody — `d717d4c`
4. ReviewRatingBar + ReviewRatingCard — `d234687`
5. ReviewProsCons + ReviewAudience — `ea8e844`
6. ReviewGallery + lightbox — `acc93ba`
7. ReviewVideoEmbed — `454ff96`
8. ProductSpecsTable — `0866326`
9. ProductBenchmarksTable — `7d6626d`
10. RelatedReviewsCarousel — `52e55b6`
11. Review detail page RSC — `c8fbe27`

## Files Created/Modified
See key-files in frontmatter. Page at `src/app/(tech)/tech/reviews/[slug]/page.tsx` now renders all 16 sections in UI-SPEC order.

## Decisions Made
- Inlined `getProductSpecs` in the page file rather than adding to `queries.ts` — it's only used here.
- Skipped passing Radix-style `type="single" collapsible` to `<Accordion>` because the project's Accordion is built on `@base-ui/react`, which uses `multiple` (default false) instead.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Radix-style Accordion props passed to Base UI component**
- **Found during:** Task 8 (ProductSpecsTable) + Task 13 (tsc verification)
- **Issue:** Plan wrote `<Accordion type="single" collapsible ...>` (Radix API) but project's `<Accordion>` in `src/components/ui/accordion.tsx` wraps `@base-ui/react` which rejects those props
- **Fix:** Removed `type="single" collapsible` from Accordion usage. Base UI's default behavior (multiple: false) already gives single-open accordion
- **Verification:** `pnpm tsc --noEmit` clean for src/components/tech/* and the page file
- **Committed in:** `0866326`

**2. [Rule 3 - Blocking] Plan references src/app/globals.css but file is at src/styles/globals.css**
- **Found during:** Task 3 read_first list
- **Issue:** Already fixed in plan 01. Just noted here for context.
- **Fix:** N/A — CSS rules were placed in correct file by plan 01.
- **Verification:** `.review-body` rules apply via PostCSS import chain
- **Committed in:** N/A

---

**Total deviations:** 1 auto-fixed (Base UI vs Radix API mismatch).
**Impact on plan:** No functional change, just API syntax.

## Issues Encountered
None critical. Pre-existing tsc errors in rating-slider/filter-bar still present but unrelated.

## User Setup Required
None.

## Next Phase Readiness
- Plan 04 can use ReviewCard in its grid
- Plan 05 can use ReviewCard in category browse
- Plan 06 (compare) uses different components but will reference the /tech/compare URL that this page's CTA already points to
- Plan 07 (homepage) will migrate the homepage carousel to use ReviewCard (shared component)

Playwright visual verification deferred to phase-level audit after all waves finish (per memory: use Playwright during dev, but given CodeBox RAM constraints and phase size, batch at end).

---
*Phase: 07.6-reviews-display-comparison-tables*
*Completed: 2026-04-17*

## Self-Check: PASSED
- [x] All 13 component files + page.tsx exist on disk
- [x] 11 task commits in git log matching 07.6-03
- [x] tsc --noEmit clean for my files
- [x] No Self-Check: FAILED marker
