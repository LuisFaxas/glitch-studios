---
phase: 07.6-reviews-display-comparison-tables
plan: 04
subsystem: ui
tags: [reviews-list, nuqs, cursor-pagination, shadcn-sheet, filter-bar]

requires:
  - phase: 07.6-reviews-display-comparison-tables
    provides: listPublishedReviews, listTopLevelCategoriesWithCounts, ReviewCard
provides:
  - /tech/reviews list page with filter bar (category pills + sort + search) syncing to URL via nuqs
  - Cursor pagination via encoded base64url search param
  - Mobile filter sheet with vertical pill stacks
  - Empty state copy for both no-reviews and filtered-no-results
affects: [07.6-07]

tech-stack:
  added: []
  patterns:
    - "Filter state in URL via nuqs with shallow:false so server re-renders on change"
    - "Debounced search via nuqs throttleMs:300"
    - "Cursor pagination as base64url-encoded JSON preserving other filters in href"

key-files:
  created:
    - src/components/tech/reviews-filter-bar.tsx
    - src/components/tech/reviews-mobile-filter-sheet.tsx
    - src/components/tech/reviews-load-more.tsx
    - src/components/tech/reviews-grid.tsx
  modified:
    - src/app/(tech)/tech/reviews/page.tsx

key-decisions:
  - "Mobile filter sheet renders as separate client component; hidden on desktop"
  - "Desktop sort Select + search Input hidden on mobile behind [Filter] button"
  - "Cursor applied only when sort=latest — filter changes that alter ordering invalidate the cursor naturally"

patterns-established:
  - "Pattern: Mobile filter sheet as child client component receiving nuqs state as controlled props"

requirements-completed:
  - 07.6-S2

duration: 6min
completed: 2026-04-17
---

# Phase 07.6 Plan 04: Reviews List Page Summary

**`/tech/reviews` with category pills + sort + debounced search all URL-synced via nuqs, cursor load-more, and a mobile filter Sheet that opens from the right**

## Performance
- **Duration:** ~6 min
- **Tasks:** 6
- **Files created:** 4
- **Files modified:** 1

## Accomplishments
- Desktop: horizontal filter bar (sort select + search input + pill row with fade mask)
- Mobile: stacked [Filter] button that opens a right-side Sheet with full sort/search/category controls
- Empty states differentiate "no reviews yet" vs "no reviews match filter" (with Clear filters CTA)
- Cursor load-more preserves other filter params via useSearchParams

## Task Commits
1. ReviewsFilterBar + ReviewsMobileFilterSheet — `55ad5f0`
2. ReviewsLoadMore — `c84b59d`
3. ReviewsGrid — `b9b4957`
4. Reviews list page RSC — `d4158da`
5. Playwright verification — deferred to phase audit
6. Build check — tsc clean for plan files (pre-existing project errors unrelated)

## Files Created/Modified
See key-files. Filter bar splits responsive concerns: desktop shows horizontal row, mobile shows single [Filter] button feeding a controlled Sheet.

## Decisions Made
- Plan's must_have "Mobile filter Sheet opens from right when [Filter] button pressed" required a component (`reviews-mobile-filter-sheet.tsx`) declared in files_modified but not covered by the task list. Added functional component wrapping the same 3 controls (sort buttons, search input, category pills) in a shadcn Sheet.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Plan declared mobile sheet file but had no task implementing it**
- **Found during:** Task 1 (reading plan's truth list)
- **Issue:** `files_modified` and must_haves reference `reviews-mobile-filter-sheet.tsx` but no `<task>` block creates it — tasks only covered 4 of 5 files
- **Fix:** Created a functional client component (`reviews-mobile-filter-sheet.tsx`) that takes the filter state/setters as controlled props from `ReviewsFilterBar`, wraps a shadcn `<Sheet>`, and presents sort/search/category in a vertical scrollable layout
- **Verification:** Mobile-only render path gated by `md:hidden`, full state reuse via controlled props
- **Committed in:** `55ad5f0`

**2. [Rule 3 - Blocking] `pnpm next lint` deprecated (same as prior plans)**
- **Found during:** Task 6
- **Fix:** Ran `pnpm tsc --noEmit` scoped check. No errors in my new files. Pre-existing project errors (rating-slider, filter-bar) unchanged.

---

**Total deviations:** 2 (1 missing critical, 1 tooling fallback).
**Impact on plan:** Mobile must_have now satisfied.

## Issues Encountered
None critical. nuqs `shallow:false` triggers full RSC re-render on every filter change — intentional per plan.

## User Setup Required
None.

## Next Phase Readiness
- Plan 05 can now also import nuqs NuqsAdapter if it needs filter state
- Plan 07 (homepage) can link to /tech/reviews?category=<slug> deep-links cleanly

---
*Phase: 07.6-reviews-display-comparison-tables*
*Completed: 2026-04-17*

## Self-Check: PASSED
- [x] All 4 new files + page update committed
- [x] Mobile filter sheet satisfies must_have D-06
- [x] tsc --noEmit clean for plan 04 files
- [x] No Self-Check: FAILED marker
