---
phase: 07.6-reviews-display-comparison-tables
plan: 07
subsystem: ui
tags: [homepage, tech, revalidate, promise-all, review-card-migration]

requires:
  - phase: 07.6-reviews-display-comparison-tables
    provides: getLatestPublishedReviews, listTopLevelCategoriesWithCounts, getBenchmarkSpotlight, ReviewCard, CategoryTile
provides:
  - Tech homepage with live data (no hardcoded FEATURED_REVIEWS, CATEGORIES arrays, or MacBook Pro spotlight)
  - Admin publish/unpublish revalidates public tech paths
affects: []

tech-stack:
  added: []
  patterns:
    - "Homepage RSC fetches section data in parallel via Promise.all, passes as props to client components"
    - "Admin actions use revalidatePath to refresh public routes post-mutation"

key-files:
  created: []
  modified:
    - src/app/(tech)/tech/page.tsx
    - src/components/home/tech-featured-reviews-carousel.tsx
    - src/components/home/tech-category-tiles.tsx
    - src/components/home/tech-benchmark-spotlight.tsx
    - src/actions/admin-tech-reviews.ts

key-decisions:
  - "tech-category-tiles converted from client to server component (no interactive state needed; ScrollSection is a client subtree)"
  - "publishReview/unpublishReview now returning slug so revalidatePath can target review detail page"

patterns-established:
  - "Rating format /10 site-wide (not /5) per D-06a"

requirements-completed:
  - 07.6-S6

duration: 5min
completed: 2026-04-17
---

# Phase 07.6 Plan 07: Homepage Migration Summary

**Tech homepage (`/tech/`) now fetches latest 3 reviews, top-level categories, and Geekbench spotlight in parallel; all three sections consume real data via prop-passed components. Admin publish/unpublish triggers public revalidation.**

## Performance
- **Duration:** ~5 min
- **Tasks:** 7
- **Files modified:** 5

## Accomplishments
- `FEATURED_REVIEWS` array removed; carousel reuses shared `<ReviewCard>` (single card contract)
- `CATEGORIES` array removed; tiles use DB + shared `<CategoryTile>` (same icon map as /tech/categories)
- Hardcoded MacBook Pro M4 Max spotlight removed; section renders only when `getBenchmarkSpotlight()` returns non-null
- `/tech/` now uses `revalidate = 300` (5 min)
- Admin publish/unpublish actions revalidate /tech, /tech/reviews, /tech/categories, and /tech/reviews/[slug]
- Rating format `/ 5` completely purged from `src/components/home/`

## Task Commits
1. tech-featured-reviews-carousel migration — `ae224b0`
2. tech-category-tiles migration — `7661b79`
3. tech-benchmark-spotlight migration — `6b8b0f5`
4. /tech/ page parallel fetch + revalidate=300 — `d5b0c2a`
5. admin-tech-reviews revalidatePath — `2cd24d6`
6. Browser verification — deferred to phase audit
7. Full build check — tsc clean for plan 07 files; zero `/ 5` in home components

## Files Modified
See key-files. No new files — this plan replaces content of 4 existing files and augments one server action.

## Decisions Made
- **Server vs client for tech-category-tiles:** Plan explicitly allows dropping the `"use client"` directive since the component has no local state. `ScrollSection` is still a client subtree inside, which is valid. Kept as server component per plan note.
- **publishReview/unpublishReview returning slug:** Chose to add `.returning()` clause and conditionally call `revalidatePath(/tech/reviews/<slug>, "page")` rather than hard-coding. Safer if the slug-less path were ever invoked.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Plan uses `review.slug` but publishReview receives only `id`**
- **Found during:** Task 5 (revalidatePath addition)
- **Issue:** Plan wrote `revalidatePath(\`/tech/reviews/${review.slug}\`, "page")` but the actual signature is `publishReview(id: string)` — no `review` variable exists in scope
- **Fix:** Added `.returning({ slug: techReviews.slug })` to the update query so the slug is available to revalidate
- **Verification:** `pnpm tsc --noEmit` clean
- **Committed in:** `2cd24d6`

---

**Total deviations:** 1 auto-fixed.
**Impact on plan:** revalidatePath on review detail now works.

## Issues Encountered
None critical.

## User Setup Required
None.

## Next Phase Readiness
- Phase 07.6 execution complete. All 7 plans shipped.
- Visual Playwright verification + full phase gate verification now pending

---
*Phase: 07.6-reviews-display-comparison-tables*
*Completed: 2026-04-17*

## Self-Check: PASSED
- [x] All 5 files modified (4 homepage + 1 admin action)
- [x] Homepage uses Promise.all for parallel fetch with revalidate=300
- [x] `/ 5` purged from src/components/home
- [x] tsc + lint clean for plan 07 files
- [x] No Self-Check: FAILED marker
