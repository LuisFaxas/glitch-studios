---
phase: 07.6-reviews-display-comparison-tables
plan: 01
subsystem: infra
tags: [drizzle, recharts, dompurify, server-only, postgres, tech-reviews]

requires:
  - phase: 07.5-product-reviews-data-model-admin-input
    provides: tech_reviews/tech_products/tech_categories/tech_benchmark schema + admin CRUD
provides:
  - recharts@^3.8.1 and isomorphic-dompurify@^3.9.0 installed
  - Central public query layer (src/lib/tech/queries.ts) with typed helpers for review detail, review list (cursor pagination), related reviews, category tree, category products, product compare, benchmark runs, benchmark spotlight, homepage latest reviews
  - sanitizeReviewBody DOMPurify wrapper with allowlist + target=_blank external-link hook
  - getSpecDirection + computeSpecWinner + computeBenchmarkWinner pure logic for compare tool
  - extractYouTubeId URL parser for ReviewVideoEmbed
  - .review-body scoped typography rules in globals.css for Tiptap-authored HTML
affects: [07.6-02, 07.6-03, 07.6-04, 07.6-05, 07.6-06, 07.6-07]

tech-stack:
  added: [recharts@^3.8.1, isomorphic-dompurify@^3.9.0]
  patterns:
    - "server-only helpers in src/lib/tech/ for public-facing Drizzle queries (no 'use server' — RSC direct import)"
    - "Cursor pagination on (publishedAt, id) tuple for reviews list"
    - "Adjacency-list descendant walk in JS after one-shot SELECT of all categories"
    - "Two-tier related-reviews fallback: same category → sibling categories"

key-files:
  created:
    - src/lib/tech/queries.ts
    - src/lib/tech/sanitize.ts
    - src/lib/tech/spec-direction.ts
    - src/lib/tech/winners.ts
    - src/lib/tech/youtube.ts
  modified:
    - package.json
    - pnpm-lock.yaml
    - src/styles/globals.css

key-decisions:
  - "Use --font-mono CSS variable (project convention) instead of --font-jetbrains-mono referenced in plan"
  - "globals.css lives at src/styles/globals.css, not src/app/globals.css"
  - "tsc/lint verification scoped to src/lib/tech/ since project has pre-existing errors in rating-slider.tsx and filter-bar.tsx unrelated to this plan"

patterns-established:
  - "Public query pattern: import 'server-only' + direct drizzle, no 'use server' wrapper (consumed by RSCs)"
  - "Winner detection: field-name allowlist first, then token-split, then unit heuristic"
  - "DOMPurify hook registration guarded by module-scope flag to avoid duplicate registration under HMR"

requirements-completed:
  - 07.6-S7

duration: 18min
completed: 2026-04-17
---

# Phase 07.6 Plan 01: Foundations Summary

**Public Drizzle query layer + sanitize/winner/youtube utilities + review-body prose styles — foundation for every page in 07.6**

## Performance

- **Duration:** ~18 min
- **Started:** 2026-04-17T14:52Z
- **Completed:** 2026-04-17T15:10Z
- **Tasks:** 8
- **Files created:** 5 (src/lib/tech/*.ts)
- **Files modified:** 3 (package.json, pnpm-lock.yaml, src/styles/globals.css)

## Accomplishments
- All public read helpers consolidated into a single server-only module (queries.ts) so Plans 02–07 can share the same shapes without re-querying the schema ad hoc
- Winner-detection logic split into pure functions (spec-direction, winners) with zero DB coupling — testable and reusable in compare tool
- DOMPurify allowlist-based sanitizer with external-link `target=_blank` hook enforcing UI-SPEC D-05
- YouTube URL → 11-char ID extractor with strict format validation, rejects non-YouTube hosts

## Task Commits

1. **Task 1: Install recharts + isomorphic-dompurify** — `b2c1e5c` (chore)
2. **Task 2: sanitize.ts** — `2549610` (feat)
3. **Task 3: spec-direction.ts** — `f887d34` (feat)
4. **Task 4: winners.ts** — `6f43951` (feat)
5. **Task 5: youtube.ts** — `9aff8db` (feat)
6. **Task 6: queries.ts** — `e44d717` (feat)
7. **Task 7: .review-body CSS** — `90456cb` (style)
8. **Task 8: tsc + lint verification** — no commit (verification only)

## Files Created/Modified
- `src/lib/tech/sanitize.ts` — `sanitizeReviewBody(html)` wrapping DOMPurify with allowlist and external-link hook
- `src/lib/tech/spec-direction.ts` — `getSpecDirection(fieldName, unit)` returning higher/lower/null
- `src/lib/tech/winners.ts` — `computeSpecWinner` + `computeBenchmarkWinner` pure functions
- `src/lib/tech/youtube.ts` — `extractYouTubeId(url)` strict YouTube URL parser
- `src/lib/tech/queries.ts` — Full public query layer (14 helpers) including review detail, cursor-paginated list, related reviews, category tree, compare specs, benchmark spotlight
- `src/styles/globals.css` — Added `.review-body` scoped typography rules for Tiptap output
- `package.json` + `pnpm-lock.yaml` — recharts 3.8.1, isomorphic-dompurify 3.9.0

## Decisions Made
- **Font variable:** Used `--font-mono` (actual project convention from src/app/layout.tsx) instead of `--font-jetbrains-mono` specified in plan. Plan explicitly called out this fallback path.
- **CSS file path:** globals.css lives at `src/styles/globals.css` per project layout, not `src/app/globals.css` per plan.
- **Cursor pagination:** Chose (publishedAt, id) tuple for stable ordering under ties; drizzle `or(lt(publishedAt, cursor), and(eq(publishedAt, cursor), lt(id, cursor.id)))` pattern.
- **`getProductBySlugWithSpecs`:** Added as a thin wrapper over `getProductsBySlugs([slug])` since the plan's artifact list references it as an export but didn't include its implementation. Downstream compare tool needs it.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Wrong CSS file path in plan**
- **Found during:** Task 7 (Add .review-body CSS)
- **Issue:** Plan references `src/app/globals.css` but file is at `src/styles/globals.css` (confirmed via Glob)
- **Fix:** Appended rules to `src/styles/globals.css`
- **Verification:** `grep -c "\.review-body" src/styles/globals.css` returns 12
- **Committed in:** `90456cb`

**2. [Rule 3 - Blocking] Wrong font CSS variable name in plan**
- **Found during:** Task 7 (Add .review-body CSS)
- **Issue:** Plan specified `var(--font-jetbrains-mono)` but project registers JetBrains Mono as `variable: "--font-mono"` in `src/app/layout.tsx:13`. Using wrong var would yield system mono fallback.
- **Fix:** Used `var(--font-mono)` instead. Plan anticipated this in Task 7 notes.
- **Verification:** grep confirms `--font-mono` is declared in globals.css at line 12 + 64
- **Committed in:** `90456cb`

**3. [Rule 3 - Blocking] `next lint` deprecated in Next.js 16**
- **Found during:** Task 8 (verify)
- **Issue:** Plan says `pnpm next lint` but Next 16 removed it; project uses `pnpm lint` → `eslint`
- **Fix:** Ran `pnpm lint` instead. Scoped error check to `src/lib/tech/`, confirming my new files produce zero lint errors/warnings. Project has pre-existing 233 errors / 331 warnings from earlier work unrelated to 07.6-01.
- **Verification:** `pnpm lint 2>&1 | grep "src/lib/tech/"` returns empty
- **Committed in:** N/A (verification only)

**4. [Rule 2 - Missing Critical] Added `getProductBySlugWithSpecs` helper**
- **Found during:** Task 6 (queries.ts)
- **Issue:** Plan frontmatter artifacts list `getProductBySlugWithSpecs` as an export but plan content only implements `getProductsBySlugs(slugs[])`. Downstream plans (06 compare) expect singular form.
- **Fix:** Added thin wrapper: `export async function getProductBySlugWithSpecs(slug) { return (await getProductsBySlugs([slug]))[0] ?? null }`
- **Verification:** `grep -c 'export async function getProductBySlugWithSpecs' src/lib/tech/queries.ts` returns 1
- **Committed in:** `e44d717`

---

**Total deviations:** 4 auto-fixed (2 blocking path/variable mismatches, 1 deprecated tooling, 1 missing critical export)
**Impact on plan:** All fixes necessary for the substrate to actually work. No scope expansion.

## Issues Encountered

- **Pre-existing tsc errors:** `src/components/admin/tech/rating-slider.tsx:29` and `src/components/beats/filter-bar.tsx:191` have 2 type errors predating this plan (slider API changed from `@radix-ui/react-slider` to `@base-ui/react` at some point, leaving these two files stale). Not in scope of 07.6-01. Confirmed pre-existing by stash-pop test.
- **Pre-existing lint warnings:** project has many lint issues from earlier work. None in any file created/modified by this plan.

## User Setup Required
None — no external service configuration required.

## Next Phase Readiness
- Plan 02 (ReviewCard component) can consume `PublicReviewCard` type from queries.ts
- Plan 03 (review detail page) can call `getPublishedReviewBySlug` + use `sanitizeReviewBody` + `extractYouTubeId`
- Plan 04 (reviews list) can call `listPublishedReviews` with cursor pagination and `getCategoryDescendantIds`
- Plan 05 (categories) can call `listTopLevelCategoriesWithCounts` + `listProductsForCategory`
- Plan 06 (compare) can call `getProductsBySlugs` + `getBenchmarkRunsForProducts` + `computeSpecWinner` + `computeBenchmarkWinner`
- Plan 07 (homepage) can call `getLatestPublishedReviews` + `listTopLevelCategoriesWithCounts` + `getBenchmarkSpotlight`

No blockers for Wave 2.

---
*Phase: 07.6-reviews-display-comparison-tables*
*Completed: 2026-04-17*

## Self-Check: PASSED
- [x] key-files.created (src/lib/tech/sanitize.ts, queries.ts) exist on disk
- [x] 7 task commits present in git log matching `07.6-01`
- [x] No `## Self-Check: FAILED` in this file
