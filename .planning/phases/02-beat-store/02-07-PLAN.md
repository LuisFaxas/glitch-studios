---
phase: 02-beat-store
plan: 07
type: execute
wave: 5
depends_on: ["02-01", "02-04", "02-06"]
files_modified:
  - src/app/(public)/checkout/page.tsx
  - src/app/(public)/checkout/success/page.tsx
  - src/app/api/checkout/route.ts
  - src/app/api/checkout/session-status/route.ts
  - src/app/api/webhooks/stripe/route.ts
  - src/lib/pdf-license.ts
  - src/lib/email/purchase-receipt.tsx
  - src/actions/orders.ts
  - src/actions/bundles.ts
  - src/components/beats/bundle-section.tsx
  - src/app/(public)/beats/page.tsx
autonomous: true
requirements:
  - BEAT-08
  - BEAT-09
  - BEAT-05
  - BEAT-10
  - MAIL-01
user_setup:
  - service: stripe
    why: "Payment processing for beat purchases"
    env_vars:
      - name: STRIPE_SECRET_KEY
        source: "Stripe Dashboard -> Developers -> API keys -> Secret key"
      - name: NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY
        source: "Stripe Dashboard -> Developers -> API keys -> Publishable key"
      - name: STRIPE_WEBHOOK_SECRET
        source: "Stripe Dashboard -> Developers -> Webhooks -> Signing secret (or stripe listen --forward-to output)"
    dashboard_config:
      - task: "Enable PayPal as payment method"
        location: "Stripe Dashboard -> Settings -> Payment methods -> PayPal -> Enable"
      - task: "Create webhook endpoint"
        location: "Stripe Dashboard -> Developers -> Webhooks -> Add endpoint, URL: https://your-domain/api/webhooks/stripe, Events: checkout.session.completed"
  - service: resend
    why: "Purchase receipt emails"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard -> API Keys -> Create API Key"

must_haves:
  truths:
    - "User can complete checkout via Stripe Embedded Checkout with card, Apple Pay, Google Pay, or PayPal"
    - "After successful payment, user sees order confirmation with download links"
    - "Download links use signed R2 URLs that expire after 24 hours"
    - "Auto-generated PDF license agreement is created per purchased item"
    - "Purchase receipt email is sent with download links"
    - "Guest users can checkout without an account"
    - "Bundle purchases apply the bundle discount percentage to the total"
    - "Public /beats page shows available bundles with discounted pricing"
  artifacts:
    - path: "src/app/(public)/checkout/page.tsx"
      provides: "Stripe Embedded Checkout page"
      contains: "EmbeddedCheckout"
    - path: "src/app/api/webhooks/stripe/route.ts"
      provides: "Stripe webhook handler for payment confirmation"
      contains: "checkout.session.completed"
    - path: "src/lib/pdf-license.ts"
      provides: "License PDF generation"
      exports: ["generateLicensePdf"]
    - path: "src/lib/email/purchase-receipt.tsx"
      provides: "React Email purchase receipt template"
      exports: ["PurchaseReceiptEmail"]
    - path: "src/actions/bundles.ts"
      provides: "Public bundle queries and discount calculation"
      exports: ["getPublishedBundles", "calculateBundleDiscount"]
    - path: "src/components/beats/bundle-section.tsx"
      provides: "Bundle display cards on /beats page"
      exports: ["BundleSection"]
  key_links:
    - from: "src/app/api/checkout/route.ts"
      to: "src/lib/stripe.ts"
      via: "Stripe Checkout Session creation"
      pattern: "stripe.checkout.sessions.create"
    - from: "src/app/api/checkout/route.ts"
      to: "src/actions/bundles.ts"
      via: "Bundle discount calculation during checkout"
      pattern: "calculateBundleDiscount"
    - from: "src/app/api/webhooks/stripe/route.ts"
      to: "src/lib/pdf-license.ts"
      via: "Generate license PDF after payment"
      pattern: "generateLicensePdf"
    - from: "src/app/api/webhooks/stripe/route.ts"
      to: "src/lib/email/purchase-receipt.tsx"
      via: "Send receipt email after payment"
      pattern: "PurchaseReceiptEmail"
    - from: "src/app/(public)/checkout/success/page.tsx"
      to: "src/lib/r2.ts"
      via: "Generate signed download URLs"
      pattern: "getDownloadUrl"
    - from: "src/components/beats/bundle-section.tsx"
      to: "src/components/cart/cart-provider.tsx"
      via: "useCart().addItem for each bundle beat"
      pattern: "useCart"
---

<objective>
Build the complete checkout and post-purchase flow: Stripe Embedded Checkout, webhook handler for payment confirmation, license PDF generation, purchase receipt email, download URL generation, order confirmation page, bundle discount logic, and public bundle display.

Purpose: This converts cart items into actual purchases with digital delivery — the core revenue flow. Bundle support enables discounted collections.
Output: Checkout page, webhook handler, PDF generation, email template, order confirmation with downloads, bundle display and discount logic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-beat-store/02-CONTEXT.md
@.planning/phases/02-beat-store/02-RESEARCH.md
@.planning/phases/02-beat-store/02-01-SUMMARY.md
@.planning/phases/02-beat-store/02-04-SUMMARY.md
@.planning/phases/02-beat-store/02-06-SUMMARY.md

<interfaces>
<!-- CartItem from src/types/beats.ts -->
```typescript
export interface CartItem {
  beatId: string; beatTitle: string; beatSlug: string;
  coverArtUrl: string | null; licenseTier: string;
  licenseTierDisplay: string; price: number;
}
```

<!-- Schema tables -->
```typescript
export const orders = pgTable("orders", { id, userId, guestEmail, stripeSessionId, stripePaymentIntentId, status, totalAmount, createdAt })
export const orderItems = pgTable("order_items", { id, orderId, beatId, licenseTier, price, licensePdfKey, downloadCount })
export const beats = pgTable("beats", { ... })
export const bundles = pgTable("bundles", { id, name, slug, description, discountPercent, coverArtKey, isActive, createdAt, updatedAt })
export const bundleBeats = pgTable("bundle_beats", { id, bundleId, beatId })
```

<!-- Stripe client -->
```typescript
import { stripe } from "@/lib/stripe"
```

<!-- R2 helpers -->
```typescript
export async function getUploadUrl(key: string, contentType: string, expiresIn?: number): Promise<string>
export async function getDownloadUrl(key: string, expiresIn?: number): Promise<string>
export function getPublicUrl(key: string): string
```

<!-- useCart -->
```typescript
export function useCart(): { items: CartItem[]; clearCart: () => void; total: number; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stripe Checkout Session API with bundle discount, Embedded Checkout page, and session status endpoint</name>
  <files>src/app/api/checkout/route.ts, src/app/api/checkout/session-status/route.ts, src/app/(public)/checkout/page.tsx, src/actions/bundles.ts</files>
  <read_first>
    - src/lib/stripe.ts (Stripe server client)
    - src/lib/stripe-client.ts (client-side Stripe loader)
    - src/types/beats.ts (CartItem)
    - src/components/cart/cart-provider.tsx (useCart for getting items)
    - src/db/schema.ts (bundles, bundleBeats tables)
  </read_first>
  <action>
1. Create `src/actions/bundles.ts` — "use server" module for public bundle queries:

```typescript
"use server"
import { db } from "@/lib/db"
import { bundles, bundleBeats, beats, beatPricing } from "@/db/schema"
import { eq, inArray, and } from "drizzle-orm"
import { getPublicUrl } from "@/lib/r2"

export async function getPublishedBundles() {
  // Get active bundles with their beats and lowest pricing
  const activeBundles = await db.select().from(bundles).where(eq(bundles.isActive, true))
  if (activeBundles.length === 0) return []

  const bundleIds = activeBundles.map(b => b.id)
  const junctions = await db.select().from(bundleBeats).where(inArray(bundleBeats.bundleId, bundleIds))

  const beatIds = [...new Set(junctions.map(j => j.beatId))]
  if (beatIds.length === 0) return activeBundles.map(b => ({ ...b, beats: [], originalTotal: 0, discountedTotal: 0 }))

  const beatRows = await db.select().from(beats).where(inArray(beats.id, beatIds))
  const pricing = await db.select().from(beatPricing).where(
    and(inArray(beatPricing.beatId, beatIds), eq(beatPricing.isActive, true))
  )

  return activeBundles.map(bundle => {
    const bundleBeatIds = junctions.filter(j => j.bundleId === bundle.id).map(j => j.beatId)
    const bundleBeatsData = beatRows.filter(b => bundleBeatIds.includes(b.id))

    // Calculate original total (sum of lowest active price per beat)
    const originalTotal = bundleBeatsData.reduce((sum, beat) => {
      const beatPrices = pricing.filter(p => p.beatId === beat.id).map(p => Number(p.price))
      const lowestPrice = beatPrices.length > 0 ? Math.min(...beatPrices) : 0
      return sum + lowestPrice
    }, 0)

    const discountedTotal = originalTotal * (1 - (bundle.discountPercent ?? 0) / 100)

    return {
      ...bundle,
      coverArtUrl: bundle.coverArtKey ? getPublicUrl(bundle.coverArtKey) : null,
      beats: bundleBeatsData.map(b => ({
        id: b.id,
        title: b.title,
        coverArtUrl: b.coverArtKey ? getPublicUrl(b.coverArtKey) : null,
      })),
      originalTotal,
      discountedTotal: Math.round(discountedTotal * 100) / 100,
    }
  })
}

export async function calculateBundleDiscount(beatIds: string[]): Promise<{
  bundleId: string | null
  bundleName: string | null
  discountPercent: number
} | null> {
  // Check if the cart items match any active bundle
  const activeBundles = await db.select().from(bundles).where(eq(bundles.isActive, true))
  if (activeBundles.length === 0) return null

  const bundleIdsAll = activeBundles.map(b => b.id)
  const junctions = await db.select().from(bundleBeats).where(inArray(bundleBeats.bundleId, bundleIdsAll))

  // Find a bundle where ALL its beats are in the cart
  for (const bundle of activeBundles) {
    const bundleBeatIds = junctions.filter(j => j.bundleId === bundle.id).map(j => j.beatId)
    const allBeatsInCart = bundleBeatIds.every(id => beatIds.includes(id))
    if (allBeatsInCart && bundleBeatIds.length > 0) {
      return {
        bundleId: bundle.id,
        bundleName: bundle.name,
        discountPercent: bundle.discountPercent ?? 0,
      }
    }
  }

  return null
}
```

2. Create `src/app/api/checkout/route.ts` — POST endpoint to create Stripe Checkout Session:

Per Research Pattern 4 and code example:
```typescript
import { stripe } from "@/lib/stripe"
import { NextResponse } from "next/server"
import { calculateBundleDiscount } from "@/actions/bundles"

export async function POST(request: Request) {
  const { items } = await request.json()
  // items: CartItem[]

  // Check for bundle discount (BEAT-10)
  const beatIds = items.map((i: any) => i.beatId)
  const bundleDiscount = await calculateBundleDiscount(beatIds)

  const line_items = items.map((item: any) => ({
    price_data: {
      currency: "usd",
      product_data: {
        name: `${item.beatTitle} - ${item.licenseTierDisplay}`,
        metadata: {
          beatId: item.beatId,
          licenseTier: item.licenseTier,
        },
      },
      unit_amount: Math.round(item.price * 100),
    },
    quantity: 1,
  }))

  // Apply bundle discount as a coupon if applicable
  const discounts = bundleDiscount && bundleDiscount.discountPercent > 0
    ? [{
        coupon: await getOrCreateBundleCoupon(bundleDiscount.discountPercent, bundleDiscount.bundleName ?? "Bundle"),
      }]
    : undefined

  const session = await stripe.checkout.sessions.create({
    ui_mode: "embedded",
    line_items,
    discounts,
    mode: "payment",
    return_url: `${process.env.NEXT_PUBLIC_SITE_URL}/checkout/success?session_id={CHECKOUT_SESSION_ID}`,
    metadata: {
      items: JSON.stringify(items.map((i: any) => ({
        beatId: i.beatId, beatTitle: i.beatTitle, licenseTier: i.licenseTier, price: i.price,
      }))),
      bundleId: bundleDiscount?.bundleId ?? "",
    },
  })

  return NextResponse.json({ clientSecret: session.client_secret })
}

// Helper: create or reuse a Stripe coupon for bundle discounts
async function getOrCreateBundleCoupon(percentOff: number, name: string): Promise<string> {
  const couponId = `bundle-${percentOff}pct`
  try {
    await stripe.coupons.retrieve(couponId)
    return couponId
  } catch {
    const coupon = await stripe.coupons.create({
      id: couponId,
      percent_off: percentOff,
      duration: "once",
      name: `${name} (${percentOff}% off)`,
    })
    return coupon.id
  }
}
```

3. Create `src/app/api/checkout/session-status/route.ts` — GET endpoint to check session:
```typescript
import { stripe } from "@/lib/stripe"
import { NextResponse } from "next/server"

export async function GET(request: Request) {
  const { searchParams } = new URL(request.url)
  const sessionId = searchParams.get("session_id")
  if (!sessionId) return NextResponse.json({ error: "Missing session_id" }, { status: 400 })

  const session = await stripe.checkout.sessions.retrieve(sessionId)
  return NextResponse.json({
    status: session.status,
    customerEmail: session.customer_details?.email,
    paymentStatus: session.payment_status,
  })
}
```

4. Create `src/app/(public)/checkout/page.tsx` — "use client" Stripe Embedded Checkout:

```typescript
"use client"
import { useCallback, useState } from "react"
import { loadStripe } from "@stripe/stripe-js"
import { EmbeddedCheckoutProvider, EmbeddedCheckout } from "@stripe/react-stripe-js"
import { useCart } from "@/components/cart/cart-provider"

const stripePromise = loadStripe(process.env.NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY!)

export default function CheckoutPage() {
  const { items, total } = useCart()

  const fetchClientSecret = useCallback(async () => {
    const response = await fetch("/api/checkout", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ items }),
    })
    const { clientSecret } = await response.json()
    return clientSecret
  }, [items])

  if (items.length === 0) {
    return (
      <div className="flex flex-col items-center justify-center min-h-[60vh] text-center px-4">
        <h1 className="font-mono font-bold text-[28px] uppercase tracking-[0.05em] mb-4">Checkout</h1>
        <p className="text-[15px] text-[#888] font-sans">Your cart is empty. Add some beats first.</p>
      </div>
    )
  }

  return (
    <div className="max-w-3xl mx-auto px-4 py-8">
      <h1 className="font-mono font-bold text-[28px] uppercase tracking-[0.05em] mb-8">Checkout</h1>
      <div id="checkout" className="bg-[#111] border border-[#222] rounded-none p-4">
        <EmbeddedCheckoutProvider stripe={stripePromise} options={{ fetchClientSecret }}>
          <EmbeddedCheckout />
        </EmbeddedCheckoutProvider>
      </div>
    </div>
  )
}
```

Note: The checkout page is "use client" because it uses the Stripe React components and cart context. The Stripe Embedded Checkout handles all payment UI including card, Apple Pay, Google Pay, and PayPal (if enabled in Stripe Dashboard).
  </action>
  <verify>
    <automated>cd /home/faxas/workspaces/projects/personal/glitch_studios && grep -c "checkout.sessions.create" src/app/api/checkout/route.ts && grep -c "EmbeddedCheckout" src/app/\(public\)/checkout/page.tsx && grep -c "calculateBundleDiscount" src/actions/bundles.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/app/api/checkout/route.ts contains `stripe.checkout.sessions.create`
    - src/app/api/checkout/route.ts contains `ui_mode: "embedded"`
    - src/app/api/checkout/route.ts contains `return_url`
    - src/app/api/checkout/route.ts contains `unit_amount`
    - src/app/api/checkout/route.ts contains `calculateBundleDiscount` (bundle discount logic)
    - src/app/api/checkout/route.ts contains `discounts` (Stripe coupon application)
    - src/app/api/checkout/session-status/route.ts contains `stripe.checkout.sessions.retrieve`
    - src/app/(public)/checkout/page.tsx contains `"use client"`
    - src/app/(public)/checkout/page.tsx contains `EmbeddedCheckoutProvider`
    - src/app/(public)/checkout/page.tsx contains `EmbeddedCheckout`
    - src/app/(public)/checkout/page.tsx contains `fetchClientSecret`
    - src/app/(public)/checkout/page.tsx contains `useCart`
    - src/actions/bundles.ts contains `export async function getPublishedBundles`
    - src/actions/bundles.ts contains `export async function calculateBundleDiscount`
    - src/actions/bundles.ts contains `discountPercent`
  </acceptance_criteria>
  <done>Stripe Embedded Checkout creates sessions from cart items with automatic bundle discount detection and renders the payment form on /checkout. Bundle queries and discount logic implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Webhook handler, PDF license generation, receipt email, order creation, success page, and bundle display</name>
  <files>src/app/api/webhooks/stripe/route.ts, src/lib/pdf-license.ts, src/lib/email/purchase-receipt.tsx, src/actions/orders.ts, src/app/(public)/checkout/success/page.tsx, src/components/beats/bundle-section.tsx, src/app/(public)/beats/page.tsx</files>
  <read_first>
    - src/lib/stripe.ts (stripe client)
    - src/lib/r2.ts (getUploadUrl, getDownloadUrl for PDF upload and file downloads)
    - src/db/schema.ts (orders, orderItems tables)
    - src/lib/db.ts (db client)
    - src/types/beats.ts (OrderWithItems, LICENSE_TIER_DISPLAY, DEFAULT_LICENSE_TIERS)
    - src/actions/bundles.ts (getPublishedBundles for bundle display)
    - src/app/(public)/beats/page.tsx (existing beats page to add BundleSection import)
  </read_first>
  <action>
1. Create `src/lib/pdf-license.ts` — license PDF generation:

Per Research code example:
```typescript
import { PDFDocument, StandardFonts, rgb } from "pdf-lib"

export async function generateLicensePdf(params: {
  buyerName: string
  buyerEmail: string
  beatTitle: string
  licenseTier: string
  usageRights: string[]
  orderId: string
  purchaseDate: Date
}): Promise<Uint8Array> {
  const doc = await PDFDocument.create()
  const page = doc.addPage([612, 792])
  const font = await doc.embedFont(StandardFonts.Helvetica)
  const boldFont = await doc.embedFont(StandardFonts.HelveticaBold)

  // Header
  page.drawText("GLITCH STUDIOS", { x: 50, y: 720, size: 24, font: boldFont, color: rgb(0, 0, 0) })
  page.drawText("Beat License Agreement", { x: 50, y: 690, size: 16, font })

  // License details
  let y = 650
  const fields = [
    ["Order ID:", params.orderId],
    ["Date:", params.purchaseDate.toLocaleDateString("en-US")],
    ["Beat:", params.beatTitle],
    ["License:", params.licenseTier],
    ["Licensee:", params.buyerName],
    ["Email:", params.buyerEmail],
  ]
  for (const [label, value] of fields) {
    page.drawText(label, { x: 50, y, size: 11, font: boldFont })
    page.drawText(value, { x: 150, y, size: 11, font })
    y -= 20
  }

  // Usage rights
  y -= 20
  page.drawText("Usage Rights:", { x: 50, y, size: 12, font: boldFont })
  y -= 18
  for (const right of params.usageRights) {
    page.drawText(`  - ${right}`, { x: 50, y, size: 10, font })
    y -= 16
  }

  // Footer
  y -= 30
  page.drawText("This license is non-transferable. For full terms, visit glitchstudios.com/terms.", { x: 50, y, size: 8, font })

  return doc.save()
}
```

2. Create `src/lib/email/purchase-receipt.tsx` — React Email template:

```tsx
import { Html, Head, Body, Container, Section, Heading, Text, Link, Hr } from "@react-email/components"

interface PurchaseReceiptProps {
  orderId: string
  buyerName: string
  items: { beatTitle: string; licenseTier: string; price: number; downloadUrl: string }[]
  total: number
}

export function PurchaseReceiptEmail({ orderId, buyerName, items, total }: PurchaseReceiptProps) {
  return (
    <Html>
      <Head />
      <Body style={{ backgroundColor: "#000", color: "#f5f5f0", fontFamily: "Inter, sans-serif" }}>
        <Container style={{ maxWidth: "600px", margin: "0 auto", padding: "24px" }}>
          <Heading style={{ fontFamily: "monospace", fontSize: "24px", fontWeight: "700" }}>
            GLITCH STUDIOS
          </Heading>
          <Text style={{ fontSize: "15px" }}>Thank you for your purchase, {buyerName}!</Text>
          <Text style={{ fontSize: "13px", color: "#888" }}>Order #{orderId}</Text>
          <Hr style={{ borderColor: "#222" }} />
          {items.map((item, i) => (
            <Section key={i} style={{ padding: "12px 0", borderBottom: "1px solid #222" }}>
              <Text style={{ fontSize: "15px", fontWeight: "700", fontFamily: "monospace" }}>{item.beatTitle}</Text>
              <Text style={{ fontSize: "11px", color: "#888" }}>{item.licenseTier} — ${item.price.toFixed(2)}</Text>
              <Link href={item.downloadUrl} style={{ color: "#f5f5f0", fontSize: "13px" }}>Download Files</Link>
            </Section>
          ))}
          <Hr style={{ borderColor: "#222" }} />
          <Text style={{ fontSize: "15px", fontWeight: "700", fontFamily: "monospace" }}>Total: ${total.toFixed(2)}</Text>
          <Text style={{ fontSize: "11px", color: "#888", marginTop: "24px" }}>
            Download links expire in 24 hours. Access your purchases anytime from your account dashboard.
          </Text>
        </Container>
      </Body>
    </Html>
  )
}
```

3. Create `src/actions/orders.ts` — "use server" order query functions:

```typescript
"use server"
import { db } from "@/lib/db"
import { orders, orderItems, beats } from "@/db/schema"
import { eq, desc } from "drizzle-orm"
import { getDownloadUrl } from "@/lib/r2"

export async function getOrderBySessionId(sessionId: string) {
  const [order] = await db.select().from(orders).where(eq(orders.stripeSessionId, sessionId))
  if (!order) return null

  const items = await db.select({
    id: orderItems.id, beatId: orderItems.beatId, licenseTier: orderItems.licenseTier,
    price: orderItems.price, licensePdfKey: orderItems.licensePdfKey,
    beatTitle: beats.title, beatSlug: beats.slug,
    coverArtKey: beats.coverArtKey, wavFileKey: beats.wavFileKey,
    stemsZipKey: beats.stemsZipKey, previewAudioKey: beats.previewAudioKey,
  }).from(orderItems)
    .innerJoin(beats, eq(orderItems.beatId, beats.id))
    .where(eq(orderItems.orderId, order.id))

  return { ...order, items }
}

export async function getOrderDownloadUrls(orderId: string, itemId: string) {
  // Generate signed URLs valid for 24 hours (86400 seconds) for all files associated with an order item
  const [item] = await db.select({
    licenseTier: orderItems.licenseTier,
    licensePdfKey: orderItems.licensePdfKey,
    wavFileKey: beats.wavFileKey,
    stemsZipKey: beats.stemsZipKey,
    previewAudioKey: beats.previewAudioKey,
  }).from(orderItems)
    .innerJoin(beats, eq(orderItems.beatId, beats.id))
    .where(eq(orderItems.id, itemId))

  if (!item) return null

  const urls: Record<string, string> = {}
  // Always include MP3 preview
  if (item.previewAudioKey) urls.mp3 = await getDownloadUrl(item.previewAudioKey, 86400)
  // WAV for wav_lease and above
  if (item.wavFileKey && ["wav_lease", "stems", "unlimited", "exclusive"].includes(item.licenseTier))
    urls.wav = await getDownloadUrl(item.wavFileKey, 86400)
  // Stems for stems and above
  if (item.stemsZipKey && ["stems", "unlimited", "exclusive"].includes(item.licenseTier))
    urls.stems = await getDownloadUrl(item.stemsZipKey, 86400)
  // License PDF
  if (item.licensePdfKey) urls.license = await getDownloadUrl(item.licensePdfKey, 86400)

  return urls
}
```

Note: All `getDownloadUrl` calls use `86400` (24 hours) to match the email copy ("Download links expire in 24 hours"). Re-downloads via `/dashboard/purchases` always generate fresh signed URLs.

4. Create `src/app/api/webhooks/stripe/route.ts` — Stripe webhook handler:

CRITICAL: Use `request.text()` NOT `request.json()` for raw body (per Research Pitfall 3).

```typescript
import Stripe from "stripe"
import { stripe } from "@/lib/stripe"
import { db } from "@/lib/db"
import { orders, orderItems, beats, beatPricing } from "@/db/schema"
import { eq } from "drizzle-orm"
import { generateLicensePdf } from "@/lib/pdf-license"
import { getUploadUrl, getDownloadUrl } from "@/lib/r2"
import { Resend } from "resend"
import { PurchaseReceiptEmail } from "@/lib/email/purchase-receipt"
import { DEFAULT_LICENSE_TIERS } from "@/types/beats"

const resend = new Resend(process.env.RESEND_API_KEY)

export async function POST(request: Request) {
  const body = await request.text()
  const signature = request.headers.get("stripe-signature")!

  let event: Stripe.Event
  try {
    event = stripe.webhooks.constructEvent(body, signature, process.env.STRIPE_WEBHOOK_SECRET!)
  } catch (err) {
    return new Response("Webhook signature verification failed", { status: 400 })
  }

  if (event.type === "checkout.session.completed") {
    const session = event.data.object as Stripe.Checkout.Session
    const customerEmail = session.customer_details?.email ?? ""
    const customerName = session.customer_details?.name ?? "Customer"
    const cartItems = JSON.parse(session.metadata?.items ?? "[]")

    // 1. Create order
    const [order] = await db.insert(orders).values({
      stripeSessionId: session.id,
      stripePaymentIntentId: session.payment_intent as string,
      guestEmail: customerEmail,
      status: "completed",
      totalAmount: (session.amount_total! / 100).toFixed(2),
    }).returning()

    // 2. Create order items + generate PDFs
    for (const item of cartItems) {
      const tierDef = DEFAULT_LICENSE_TIERS.find(t => t.tier === item.licenseTier)

      // Generate license PDF
      const pdfBytes = await generateLicensePdf({
        buyerName: customerName,
        buyerEmail: customerEmail,
        beatTitle: item.beatTitle ?? "Beat",
        licenseTier: tierDef?.displayName ?? item.licenseTier,
        usageRights: tierDef?.usageRights ?? [],
        orderId: order.id,
        purchaseDate: new Date(),
      })

      // Upload PDF to R2
      const pdfKey = `licenses/${order.id}/${item.beatId}-${item.licenseTier}.pdf`
      const uploadUrl = await getUploadUrl(pdfKey, "application/pdf")
      await fetch(uploadUrl, { method: "PUT", body: pdfBytes, headers: { "Content-Type": "application/pdf" } })

      // Insert order item
      await db.insert(orderItems).values({
        orderId: order.id,
        beatId: item.beatId,
        licenseTier: item.licenseTier,
        price: item.price.toFixed(2),
        licensePdfKey: pdfKey,
      })

      // Mark exclusive as sold
      if (item.licenseTier === "exclusive") {
        await db.update(beats).set({ status: "sold_exclusive" }).where(eq(beats.id, item.beatId))
      }
    }

    // 3. Send receipt email (if RESEND_API_KEY is set)
    if (process.env.RESEND_API_KEY) {
      // Inline query for order items with beat titles (avoid importing "use server" actions in API route)
      const dbOrderItems = await db.select({
        beatTitle: beats.title,
        licenseTier: orderItems.licenseTier,
        price: orderItems.price,
      }).from(orderItems)
        .innerJoin(beats, eq(orderItems.beatId, beats.id))
        .where(eq(orderItems.orderId, order.id))

      // Build email items — compute tierDef per item (not referencing loop-scoped variable)
      const emailItems = dbOrderItems.map((item) => {
        const itemTierDef = DEFAULT_LICENSE_TIERS.find(t => t.tier === item.licenseTier)
        return {
          beatTitle: item.beatTitle,
          licenseTier: itemTierDef?.displayName ?? item.licenseTier,
          price: Number(item.price),
          downloadUrl: `${process.env.NEXT_PUBLIC_SITE_URL}/checkout/success?session_id=${session.id}`,
        }
      })

      await resend.emails.send({
        from: "Glitch Studios <noreply@glitchstudios.com>",
        to: customerEmail,
        subject: `Your Glitch Studios Order #${order.id.slice(0, 8)}`,
        react: PurchaseReceiptEmail({
          orderId: order.id.slice(0, 8),
          buyerName: customerName,
          items: emailItems,
          total: Number(order.totalAmount),
        }),
      })
    }
  }

  return new Response(JSON.stringify({ received: true }), { status: 200 })
}
```

Note: The email section no longer references `tierDef` from the cart item loop. Instead, it queries `orderItems` from the database and computes `itemTierDef` per item using `DEFAULT_LICENSE_TIERS.find(t => t.tier === item.licenseTier)` within the `.map()` callback.

5. Create `src/app/(public)/checkout/success/page.tsx` — order confirmation + downloads:

```typescript
"use client"
import { useSearchParams } from "next/navigation"
import { useEffect, useState, useCallback } from "react"
import { useCart } from "@/components/cart/cart-provider"
import { getOrderBySessionId, getOrderDownloadUrls } from "@/actions/orders"
import Link from "next/link"

type OrderData = Awaited<ReturnType<typeof getOrderBySessionId>>
type DownloadUrls = Record<string, string>

export default function CheckoutSuccessPage() {
  const searchParams = useSearchParams()
  const sessionId = searchParams.get("session_id")
  const { clearCart } = useCart()

  const [status, setStatus] = useState<"loading" | "confirmed" | "error">("loading")
  const [order, setOrder] = useState<OrderData>(null)
  const [downloadUrls, setDownloadUrls] = useState<Record<string, DownloadUrls>>({})
  const [cartCleared, setCartCleared] = useState(false)

  // Poll session status until payment is confirmed, then fetch order
  useEffect(() => {
    if (!sessionId) {
      setStatus("error")
      return
    }

    let cancelled = false
    let pollCount = 0
    const maxPolls = 30 // 30 attempts * 2s = 60s max

    async function pollStatus() {
      try {
        const res = await fetch(`/api/checkout/session-status?session_id=${sessionId}`)
        const data = await res.json()

        if (cancelled) return

        if (data.status === "complete" && data.paymentStatus === "paid") {
          // Payment confirmed — fetch order details
          const orderData = await getOrderBySessionId(sessionId!)
          if (orderData) {
            setOrder(orderData)
            setStatus("confirmed")

            // Generate download URLs for each item
            const urls: Record<string, DownloadUrls> = {}
            for (const item of orderData.items) {
              const itemUrls = await getOrderDownloadUrls(orderData.id, item.id)
              if (itemUrls) urls[item.id] = itemUrls
            }
            setDownloadUrls(urls)
          } else {
            // Order not yet created by webhook — retry
            pollCount++
            if (pollCount < maxPolls) {
              setTimeout(pollStatus, 2000)
            } else {
              setStatus("error")
            }
          }
        } else if (data.status === "expired") {
          setStatus("error")
        } else {
          // Still processing — poll again
          pollCount++
          if (pollCount < maxPolls) {
            setTimeout(pollStatus, 2000)
          } else {
            setStatus("error")
          }
        }
      } catch {
        if (!cancelled) setStatus("error")
      }
    }

    pollStatus()
    return () => { cancelled = true }
  }, [sessionId])

  // Clear cart once on confirmation (separate effect to avoid re-clearing)
  useEffect(() => {
    if (status === "confirmed" && !cartCleared) {
      clearCart()
      setCartCleared(true)
    }
  }, [status, cartCleared, clearCart])

  // --- Loading state ---
  if (status === "loading") {
    return (
      <div className="flex flex-col items-center justify-center min-h-[60vh] text-center px-4">
        <h1 className="font-mono font-bold text-[28px] uppercase tracking-[0.05em] mb-4">Processing Payment</h1>
        <p className="text-[15px] text-[#888] font-sans">Confirming your purchase...</p>
        <div className="mt-6 w-8 h-8 border-2 border-[#333] border-t-[#f5f5f0] animate-spin" />
      </div>
    )
  }

  // --- Error state ---
  if (status === "error" || !order) {
    return (
      <div className="flex flex-col items-center justify-center min-h-[60vh] text-center px-4">
        <h1 className="font-mono font-bold text-[28px] uppercase tracking-[0.05em] mb-4">Payment Issue</h1>
        <p className="text-[15px] text-[#888] font-sans max-w-md">
          Payment didn't go through. Please try again or use a different payment method.
        </p>
        <Link
          href="/beats"
          className="mt-6 bg-[#111] border border-[#222] px-6 py-3 font-mono text-[13px] uppercase tracking-[0.05em] hover:bg-[#1a1a1a] transition-colors"
        >
          Back to Beats
        </Link>
      </div>
    )
  }

  // --- Confirmed state ---
  return (
    <div className="max-w-2xl mx-auto px-4 py-8">
      <h1 className="font-mono font-bold text-[28px] uppercase tracking-[0.05em] mb-2">Order Confirmed</h1>
      <p className="text-[13px] text-[#888] font-mono mb-8">Order #{order.id.slice(0, 8)}</p>

      <div className="space-y-4">
        {order.items.map((item) => {
          const urls = downloadUrls[item.id] ?? {}
          return (
            <div key={item.id} className="bg-[#111] border border-[#222] p-4">
              <div className="flex items-start justify-between mb-3">
                <div>
                  <h2 className="font-mono font-bold text-[15px] uppercase">{item.beatTitle}</h2>
                  <p className="text-[11px] text-[#888] font-sans mt-1">{item.licenseTier} — ${Number(item.price).toFixed(2)}</p>
                </div>
              </div>
              <div className="flex flex-wrap gap-2">
                {urls.mp3 && (
                  <a href={urls.mp3} download className="bg-[#111] border border-[#222] px-4 py-2 font-mono text-[11px] uppercase hover:bg-[#1a1a1a] transition-colors">
                    MP3
                  </a>
                )}
                {urls.wav && (
                  <a href={urls.wav} download className="bg-[#111] border border-[#222] px-4 py-2 font-mono text-[11px] uppercase hover:bg-[#1a1a1a] transition-colors">
                    WAV
                  </a>
                )}
                {urls.stems && (
                  <a href={urls.stems} download className="bg-[#111] border border-[#222] px-4 py-2 font-mono text-[11px] uppercase hover:bg-[#1a1a1a] transition-colors">
                    Stems
                  </a>
                )}
                {urls.license && (
                  <a href={urls.license} download className="bg-[#111] border border-[#222] px-4 py-2 font-mono text-[11px] uppercase hover:bg-[#1a1a1a] transition-colors">
                    License PDF
                  </a>
                )}
              </div>
            </div>
          )
        })}
      </div>

      <div className="mt-8 text-[11px] text-[#888] font-sans">
        <p>Download links expire in 24 hours. Access your purchases anytime from your account dashboard.</p>
      </div>

      {/* Guest account CTA (per D-16) */}
      {order.guestEmail && !order.userId && (
        <div className="mt-8 bg-[#111] border border-[#222] p-4">
          <p className="text-[15px] font-sans mb-3">
            Create an account to access your downloads anytime and track future purchases.
          </p>
          <Link
            href="/register"
            className="inline-block bg-[#f5f5f0] text-[#000] px-6 py-3 font-mono text-[13px] uppercase tracking-[0.05em] hover:bg-[#e0e0d8] transition-colors"
          >
            Create Account
          </Link>
        </div>
      )}

      {/* Download error fallback note */}
      <p className="mt-4 text-[11px] text-[#555] font-sans">
        Download failed? Your files are safe — try again from your purchase history.
      </p>
    </div>
  )
}
```

6. Create `src/components/beats/bundle-section.tsx` — "use client" bundle display for /beats page (BEAT-10):

Props: `{ bundles: Awaited<ReturnType<typeof getPublishedBundles>> }`

Per Cyberpunk Metro design language:
- Section heading: "BUNDLES" — 28px JetBrains Mono 700 uppercase, with subtitle "Save with curated collections" in 15px Inter 400 `#888`
- Each bundle as a card: `bg-[#111] border border-[#222] rounded-none p-4`
  - Bundle name: 15px JetBrains Mono 700 uppercase
  - Beat count: "X beats" in 11px Inter 400 `#888`
  - Price display: original price struck through (`line-through text-[#555]`), discounted price in 15px JetBrains Mono 700, discount badge (`bg-[#333] text-[#f5f5f0] px-2 py-0.5 text-[11px] font-mono uppercase`, e.g., "20% OFF")
  - "Add Bundle to Cart" button: inverted tile style. On click, adds all beats in the bundle to cart with their lowest-tier pricing. Shows toast "Added {bundleName} ({N} beats) to cart".
  - Beat thumbnails: row of small cover art images (32x32) for each beat in the bundle
- If no bundles are active, do not render this section at all
- Uses `useCart()` from `@/components/cart/cart-provider` to add bundle beats to cart

7. Update `src/app/(public)/beats/page.tsx` — add BundleSection import and rendering:
- Import `getPublishedBundles` from `@/actions/bundles`
- Import `BundleSection` from `@/components/beats/bundle-section`
- Call `const bundles = await getPublishedBundles()` in the server component
- Render `{bundles.length > 0 && <BundleSection bundles={bundles} />}` above the filters and beat list
  </action>
  <verify>
    <automated>cd /home/faxas/workspaces/projects/personal/glitch_studios && grep -c "constructEvent" src/app/api/webhooks/stripe/route.ts && grep -c "generateLicensePdf" src/lib/pdf-license.ts && grep -c "PurchaseReceiptEmail" src/lib/email/purchase-receipt.tsx && grep -c "BundleSection" src/components/beats/bundle-section.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/app/api/webhooks/stripe/route.ts contains `request.text()` (NOT request.json)
    - src/app/api/webhooks/stripe/route.ts contains `stripe.webhooks.constructEvent`
    - src/app/api/webhooks/stripe/route.ts contains `checkout.session.completed`
    - src/app/api/webhooks/stripe/route.ts contains `generateLicensePdf`
    - src/app/api/webhooks/stripe/route.ts contains `resend.emails.send`
    - src/app/api/webhooks/stripe/route.ts contains `sold_exclusive`
    - src/app/api/webhooks/stripe/route.ts email section computes `itemTierDef` per item (NOT referencing loop-scoped `tierDef`)
    - src/lib/pdf-license.ts contains `PDFDocument.create`
    - src/lib/pdf-license.ts contains `GLITCH STUDIOS`
    - src/lib/pdf-license.ts contains `export async function generateLicensePdf`
    - src/lib/email/purchase-receipt.tsx contains `export function PurchaseReceiptEmail`
    - src/lib/email/purchase-receipt.tsx contains `Download Files`
    - src/actions/orders.ts contains `export async function getOrderBySessionId`
    - src/actions/orders.ts contains `export async function getOrderDownloadUrls`
    - src/actions/orders.ts contains `getDownloadUrl` with `86400` (24-hour expiry, matching email copy)
    - src/app/(public)/checkout/success/page.tsx contains `useSearchParams` for session_id extraction
    - src/app/(public)/checkout/success/page.tsx contains `/api/checkout/session-status` polling
    - src/app/(public)/checkout/success/page.tsx contains `getOrderBySessionId`
    - src/app/(public)/checkout/success/page.tsx contains `getOrderDownloadUrls`
    - src/app/(public)/checkout/success/page.tsx contains `clearCart`
    - src/app/(public)/checkout/success/page.tsx contains download link rendering (MP3, WAV, Stems, License PDF)
    - src/app/(public)/checkout/success/page.tsx contains guest account CTA with link to /register
    - src/app/(public)/checkout/success/page.tsx contains `Order Confirmed`
    - src/components/beats/bundle-section.tsx contains `BundleSection`
    - src/components/beats/bundle-section.tsx contains `discountedTotal` or `discount`
    - src/components/beats/bundle-section.tsx contains `Add Bundle`
    - src/components/beats/bundle-section.tsx contains `useCart`
    - src/app/(public)/beats/page.tsx contains `BundleSection` import
    - src/app/(public)/beats/page.tsx contains `getPublishedBundles` call
  </acceptance_criteria>
  <done>Complete checkout flow: Stripe webhook creates order records, generates license PDFs (uploaded to R2), sends receipt email via Resend, success page polls for confirmation then shows download links with 24-hour signed URLs. Exclusive licenses marked as sold. Bundle section displays active bundles with discounted pricing on the /beats page and adds all bundle beats to cart via useCart(). Bundle discount applied at checkout via Stripe coupons.</done>
</task>

</tasks>

<verification>
- `pnpm tsc --noEmit` passes
- Webhook handler reads raw body (not JSON)
- Webhook email section computes tierDef per item independently (no scoped variable leak)
- License PDF includes all required fields
- Receipt email renders with React Email
- Download URLs are signed with 24-hour expiry (86400s) matching email copy
- Exclusive license marks beat as sold_exclusive
- Success page polls session status, fetches order, renders download buttons, clears cart
- Guest users see account creation CTA
- Bundle discount applied when cart matches a bundle
- Bundle section renders on /beats page with discount display
- beats/page.tsx imports and renders BundleSection
</verification>

<success_criteria>
A user can complete checkout via Stripe Embedded Checkout. Upon payment confirmation, the webhook creates order records, generates license PDFs, uploads them to R2, and sends a receipt email. The success page polls for payment confirmation, then shows download links with 24-hour signed URLs. Guest users can checkout without an account and are offered account creation afterward. Bundle discounts are automatically detected and applied at checkout. Active bundles are displayed on the /beats page with discounted pricing and "Add Bundle to Cart" functionality.
</success_criteria>

<output>
After completion, create `.planning/phases/02-beat-store/02-07-SUMMARY.md`
</output>
