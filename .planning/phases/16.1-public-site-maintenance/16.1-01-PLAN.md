---
phase: 16.1-public-site-maintenance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/tiles/tech-cross-link-tile.tsx
  - src/components/tiles/studios-cross-link-tile.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Clicking the GlitchTech cross-link tile from a Studios route transitions the CURRENT tab to the tech origin (no new tab)"
    - "Clicking the Studios cross-link tile from a tech route transitions the CURRENT tab to the studios origin (no new tab)"
    - "The middleware host-based rewrite (src/middleware.ts) continues to route correctly after the change — visiting glitchtech.io root still lands on the tech route group"
    - "A code comment in each tile file documents the audio-stop limitation and explains WHY same-tab is intentional (so future engineers don't re-add target=_blank)"
  artifacts:
    - path: "src/components/tiles/tech-cross-link-tile.tsx"
      provides: "Same-tab Studios→Tech cross-brand nav tile"
      contains: "no target=\"_blank\""
    - path: "src/components/tiles/studios-cross-link-tile.tsx"
      provides: "Same-tab Tech→Studios cross-brand nav tile"
      contains: "no target=\"_blank\""
  key_links:
    - from: "src/components/tiles/tech-cross-link-tile.tsx"
      to: "https://glitchtech.io (or equivalent tech origin URL)"
      via: "<a href> or <Link href> with no target attribute"
      pattern: "href=.*(glitchtech|tech)"
    - from: "src/components/tiles/studios-cross-link-tile.tsx"
      to: "https://glitchstudios.io (or equivalent studios origin URL)"
      via: "<a href> or <Link href> with no target attribute"
      pattern: "href=.*(glitchstudios|studios)"
---

<objective>
Remove the cross-origin `target="_blank"` branch from both sub-brand cross-link tiles so Studios ↔ GlitchTech navigation stays in the same tab (per D-01). Document the audio-stop limitation in code comments so future engineers don't "fix" it by re-adding new-tab (per D-02).

Purpose: SPA-like continuity — clicking the brand switcher feels like a route change, not a browser tab spawn. Accepts the known trade-off that `<audio>` elements can't persist across origins; revisit when brands consolidate origins.

Output: Two tile components updated; Playwright spec confirming same-tab behavior; in-code documentation of the known limitation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/16.1-public-site-maintenance/16.1-CONTEXT.md

@src/components/tiles/tech-cross-link-tile.tsx
@src/components/tiles/studios-cross-link-tile.tsx
@src/middleware.ts

# Memory rule — informs why audio stops across origins
# Reference: feedback_playwright_verification.md (Playwright is the verification standard)
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remove target="_blank" from tech-cross-link-tile.tsx (D-01/D-02)</name>
  <files>src/components/tiles/tech-cross-link-tile.tsx</files>
  <read_first>
    - src/components/tiles/tech-cross-link-tile.tsx (entire file — see what conditional/prop guards the target attribute)
    - src/middleware.ts (confirm host-based rewrite path still handles cross-origin nav)
    - src/components/tiles/studios-cross-link-tile.tsx (mirror reference, should have same pattern)
  </read_first>
  <behavior>
    - After change: clicking this tile fires same-tab navigation to the tech origin.
    - After change: NO `target="_blank"` string appears in the file.
    - After change: a comment block above the anchor/Link element explains: (1) same-tab is intentional per D-01, (2) cross-origin nav causes `<audio>` elements to stop mid-song because browsers cannot persist audio across origins, (3) do not re-add target="_blank" without revisiting the deferred "unified-origin" decision.
  </behavior>
  <action>
    1. Open `src/components/tiles/tech-cross-link-tile.tsx`.
    2. Locate the anchor / Link element that handles cross-origin nav. There may be a conditional branch that sets `target="_blank"` based on origin or a prop — remove it entirely so the element NEVER opens a new tab.
    3. Remove any `rel="noopener noreferrer"` that was only present because of the new-tab branch (external-link `rel` is not needed for same-tab nav). If the element is a plain `<a>` going cross-origin, keep `rel="noopener"` as a safety measure but drop `noreferrer`.
    4. Add a comment block directly above the anchor/Link element, verbatim:
       ```
       // D-01/D-02 (Phase 16.1): Cross-brand navigation MUST stay in the same tab.
       // Known limitation: `<audio>` elements cannot persist across origins,
       // so any currently-playing beat will stop mid-song when this link is clicked.
       // Do NOT re-introduce target="_blank" — revisit only if brands unify origins
       // (tracked as "audio continuity across brands" in deferred ideas).
       ```
    5. Save. Do not change the visual/styling or the href target.
  </action>
  <verify>
    <automated>grep -c 'target="_blank"' src/components/tiles/tech-cross-link-tile.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'target="_blank"' src/components/tiles/tech-cross-link-tile.tsx` returns exit code 1 (string absent).
    - `grep -q 'D-01/D-02' src/components/tiles/tech-cross-link-tile.tsx` returns exit code 0 (comment present).
    - `pnpm tsc --noEmit` passes.
  </acceptance_criteria>
  <done>File no longer opens a new tab; comment block documents the trade-off; typecheck clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Mirror the fix into studios-cross-link-tile.tsx (D-01/D-02)</name>
  <files>src/components/tiles/studios-cross-link-tile.tsx</files>
  <read_first>
    - src/components/tiles/studios-cross-link-tile.tsx (entire file)
    - src/components/tiles/tech-cross-link-tile.tsx (the version just edited in Task 1 — use it as the reference pattern)
  </read_first>
  <behavior>
    - After change: clicking this tile fires same-tab navigation to the studios origin.
    - No `target="_blank"` string remains.
    - Same comment block present (adapted for direction — "tech → studios" instead of "studios → tech" if wording required).
  </behavior>
  <action>
    1. Open `src/components/tiles/studios-cross-link-tile.tsx`.
    2. Repeat Task 1 steps — remove `target="_blank"` and its associated `rel` noise, keeping `rel="noopener"` if a plain `<a>` is used cross-origin.
    3. Paste the same `D-01/D-02 (Phase 16.1)` comment block above the anchor/Link.
    4. Save.
  </action>
  <verify>
    <automated>grep -c 'target="_blank"' src/components/tiles/studios-cross-link-tile.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'target="_blank"' src/components/tiles/studios-cross-link-tile.tsx` returns exit code 1.
    - `grep -q 'D-01/D-02' src/components/tiles/studios-cross-link-tile.tsx` returns exit code 0.
    - `pnpm tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Mirror file matches Task 1 behavior; typecheck clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Playwright regression — same-tab cross-link nav</name>
  <files>tests/16.1-cross-link-nav.spec.ts</files>
  <read_first>
    - tests/ directory (list existing spec files to match naming + bootstrap convention)
    - src/middleware.ts (confirm how host rewrite decides which brand loads; test may need to set Host headers or use dev URL strategy)
  </read_first>
  <behavior>
    - Test 1: Load the homepage. Click the tech cross-link tile. Assert `context.pages().length === 1` (no new tab opened). Assert the URL changed to the tech origin or tech route group (depends on dev environment — use whatever the middleware serves).
    - Test 2: Load a tech page. Click the studios cross-link tile. Same assertions in reverse.
    - Test 3 (optional, documented skip): attempt to assert audio-stop behavior on cross-origin nav. Marked `.skip()` with a comment pointing at D-01/D-02 ("audio persistence across origins is a known limitation, deferred per 16.1 CONTEXT"). This is the documentation-as-test pattern.
  </behavior>
  <action>
    1. Create `tests/16.1-cross-link-nav.spec.ts` following the project's existing Playwright spec conventions (look at `tests/07.5-*.spec.ts` or any similar spec for the bootstrap shape — baseURL, contexts, etc.).
    2. Implement Test 1 and Test 2 as described in `<behavior>`. Use `page.locator()` with a stable selector (data-testid or accessible name) — prefer adding a `data-testid` to each cross-link tile if one isn't already present (add in Task 1/Task 2 if missing; skip if already there).
    3. Implement Test 3 as `test.skip(...)` with an explanatory comment.
    4. Run `pnpm playwright test tests/16.1-cross-link-nav.spec.ts`.
  </action>
  <verify>
    <automated>pnpm playwright test tests/16.1-cross-link-nav.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - Test 1 passes: after click, `context.pages().length === 1`.
    - Test 2 passes: same assertion in reverse direction.
    - Test 3 is skipped with a comment referencing D-01/D-02.
    - Spec file committed under `tests/`.
  </acceptance_criteria>
  <done>Playwright spec green; regression coverage locked in for future work.</done>
</task>

</tasks>

<verification>
- `grep -r 'target="_blank"' src/components/tiles/` shows NO matches for cross-link tiles (other tiles unaffected).
- `pnpm tsc --noEmit` passes.
- `pnpm playwright test tests/16.1-cross-link-nav.spec.ts` passes.
- Manual smoke: on dev server, click GlitchTech cross-link while a beat is playing — tab transitions, audio stops, no new tab opens.
</verification>

<success_criteria>
Plan complete when:
- [ ] Both cross-link tile files have `target="_blank"` removed
- [ ] Both files contain the D-01/D-02 comment block
- [ ] `tests/16.1-cross-link-nav.spec.ts` exists and passes
- [ ] `pnpm tsc --noEmit` passes
- [ ] Changes committed
</success_criteria>

<output>
After completion, create `.planning/phases/16.1-public-site-maintenance/16.1-01-SUMMARY.md`
</output>
