---
phase: 16.1-public-site-maintenance
plan: 05
status: complete
wave: 3
---

## Plan 16.1-05 — Responsive Breakpoint Audit + Fixes

### Coverage
- **27 routes × 8 viewports = 216 snapshots** committed to `snapshots/`
- **3 hard D-16 assertions** (logo-within-viewport at 1280 / 1366 / 1440) all pass
- Viewports covered (D-15): `375×667 / 393×852 / 768×1024 / 1024×768 / 1280×800 / 1366×768 / 1440×900 / 1920×1080`
- Total test count: 219 (216 snapshot captures + 3 logo-fit assertions)
- Full run: 17.6 minutes, serial, workers=1, all passed

### Changes
| File | Change |
|------|--------|
| `src/components/home/hero-section.tsx` | Added `data-testid="glitch-logo"` + `min-w-0` to hero wordmark wrapper (regression anchor + flex-width guard) |
| `tests/16.1-breakpoints.spec.ts` | NEW — 27×8 breakpoint spec + 3 D-16 logo assertions; imports `ROUTES` and `BREAKPOINT_VIEWPORTS` from the shared inventory module |
| `.planning/phases/16.1-public-site-maintenance/snapshots/` | NEW — 216 PNGs in 27 route subdirectories; visual regression baseline |
| `.planning/phases/16.1-public-site-maintenance/16.1-AUDIT-SWEEP.md` | Appended "Breakpoint Findings (Plan 05)" section with D-16 result + cross-route scan + no-inline-fix rationale |

### Findings
- **D-16 homepage logo clipping at 13"**: **NOT REPRODUCED** on current layout. Logo renders at max-width 600px centered within ~1216px content area (sidebar collapsed on homepage). `body.scrollWidth === viewport.width` at every 13" width. Plausibly resolved by earlier Plan 02 splash/hero changes.
- **Zero inline fixes applied**. No layout overflow, no broken tiles, no truncated text at any viewport × route.
- **Empty-below-fold on `/` and `/tech`**: expected per memory `project_placeholder_first_build` (DB unseeded for beats/reviews/portfolio). Content gap tracked in backlog 999.7; NOT a layout bug.
- **Blog + About paired tiles** on GlitchTech sidebar verified visually at 1024/1280/1366/1440/1920 — Plan 03's grid fix holds.

### Verification
- `pnpm tsc --noEmit` — PASS
- `pnpm playwright test tests/16.1-breakpoints.spec.ts --project=desktop` — 219 passed (17.6m)
- `grep 'data-testid="glitch-logo"' src/components/home/hero-section.tsx` — present
- `find snapshots/ -name "*.png" | wc -l` — 216

### Success criteria
- [x] Homepage glitch logo clean at 1280 / 1366 / 1440 (13" priority)
- [x] Hero wordmark wrapper has `data-testid="glitch-logo"`
- [x] Breakpoint spec covers every public route × 8 viewports via imported `ROUTES`
- [x] Snapshot baseline committed to `.planning/phases/16.1-public-site-maintenance/snapshots/`
- [x] Breakpoint findings logged in AUDIT-SWEEP.md
- [x] No architecture-level issues requiring promotion (all "empty" content states already tracked in 999.7/999.8)
- [x] Changes committed

### Notes for downstream (Phase 17+ design review)
- The 216 snapshots are now the starting visual baseline for v3.0 redesign work. Any post-16.1 change that alters layout at any of the 8 viewports will be caught by re-running `pnpm playwright test tests/16.1-breakpoints.spec.ts`.
- When real content lands (backlog 999.7 MBP review seed), re-run the sweep to refresh the below-fold snapshots — dynamic-slug routes will render meaningfully instead of empty.
- The `min-w-0` on the hero wordmark wrapper stops the logo's 4.5:1 aspect ratio from stretching flex parents at future breakpoint additions — a pre-emptive guard, not a fix for a reproduced bug.
