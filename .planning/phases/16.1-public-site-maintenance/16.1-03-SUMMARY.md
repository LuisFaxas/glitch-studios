---
phase: 16.1-public-site-maintenance
plan: 03
status: complete
wave: 1
commits:
  - 3ed79ec  # socials (shipped during the Plan 02 cart fixup — single source of truth)
  - 4b5e6b9  # Blog nav + benchmarks stub + /tech/blog stub
---

## Plan 16.1-03 — Wiring (blog link, socials, benchmarks)

### Changes

| Decision | File(s) | Change |
|----------|---------|--------|
| D-09 | `src/components/layout/tech-nav-config.ts` | Add Blog entry (BookOpen icon, small desktop tile, col-span-3 mobile) between Benchmarks and About — matches `public-nav-config.ts` Blog conventions |
| D-09 | `src/app/(tech)/tech/blog/page.tsx` | New stub page so the Blog nav tile does not 404. Honest empty-state copy + links to reviews and methodology |
| D-10 | `src/app/(tech)/tech/benchmarks/page.tsx` | Replace "benchmark visualizations land in Phase 07.6" with "No benchmarks published yet... data lands as new reviews are scored" + methodology link |
| D-11/D-12 | `src/components/icons/social-icons.tsx`, `widget-social.tsx`, `widget-social-tech.tsx`, `footer.tsx`, `mobile-nav-overlay.tsx`, `nav-config-types.ts` | Unified social config (shipped in commit `3ed79ec` alongside the cart fix — both work was interleaved). IG `@glitchtech.io`, TikTok `@glitchtech.io`, YT `@glitchtech_io`, X as muted placeholder (no anchor). SoundCloud removed. |

### Verification
- `pnpm tsc --noEmit` — PASS
- `pnpm playwright test tests/16.1-wiring.spec.ts` — 8 passed, 2 skipped (mobile skips for desktop-only sidebar tiles)
- `curl /tech/blog` — HTTP 200 (was 404)
- `curl /tech/benchmarks` — HTTP 200 (no more "phase 07.6" string anywhere in tech codebase — `grep -rn "phase 7.6\|phase 07.6" src/app/(tech)` returns 0)
- `grep -c 'glitchstudios' src/components/layout/ src/components/tiles/ src/components/icons/` — 0 wrong social handles remaining

### Success criteria
- [x] GlitchTech sidebar has a Blog entry pointing at `/tech/blog`
- [x] Both brand footers + sidebars + mobile overlay render the same 3 active socials (IG/TikTok/YT, all `@glitchtech.io` variants) with `target=_blank rel=noopener noreferrer`
- [x] X renders as a muted `<span>` placeholder across all 4 render sites (footer + 2 sidebar widgets + mobile overlay) — no navigation
- [x] `phase 7.6` string absent from `/tech/*` codebase
- [x] `/tech/blog` returns HTTP 200 with an honest empty state, not a 404
- [x] tsc + 8 new Playwright assertions pass
- [x] Changes committed (`4b5e6b9` + earlier `3ed79ec`)

### Deviations from plan
- Plan 03 listed 3 tasks; the social work was bundled into commit `3ed79ec` together with the cart height fix because both were touching overlapping files (nav/footer widgets) and the user surfaced social handle bugs while reviewing the cart.
- The plan did NOT list creating `/tech/blog/page.tsx`. Adding a Blog nav tile that 404s would violate the phase goal ("everything works"), so I created a minimal stub page with the same empty-state pattern as the benchmarks fix. Small scope addition, approved by execution logic.

### Notes for downstream plans
- The audit sweep (Plan 04) should confirm the Blog tile + benchmarks page + socials look correct on all viewports.
- When the tech editorial pipeline actually ships (likely Phase 17+), `/tech/blog/page.tsx` becomes the real listing page — replace the stub, don't discard the route.
