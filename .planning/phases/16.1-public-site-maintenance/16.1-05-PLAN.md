---
phase: 16.1-public-site-maintenance
plan: 05
type: execute
wave: 3
depends_on: ["16.1-01", "16.1-02", "16.1-03", "16.1-04"]
files_modified:
  - .planning/phases/16.1-public-site-maintenance/snapshots/
  - tests/16.1-breakpoints.spec.ts
  - src/components/layout/glitch-logo.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Homepage glitch logo no longer clips at any viewport in the 1280–1440 range (13-inch laptop priority per D-16)"
    - "Every public route renders without layout breakage across the 8-viewport matrix: 375 / 393 / 768 / 1024 / 1280 / 1366 / 1440 / 1920 (D-15)"
    - "Playwright visual-regression baselines are committed covering routes × 8 viewports (D-17)"
    - "Any breakage found during the sweep has been fixed inline (CSS / Tailwind / component adjustment) or documented as accepted + promoted to 999.x"
    - "The glitch logo component carries `data-testid=\"glitch-logo\"` on its outermost element so the breakpoint spec's bounding-box assertions have a stable selector"
  artifacts:
    - path: ".planning/phases/16.1-public-site-maintenance/snapshots/"
      provides: "Per-route × per-viewport visual regression baseline"
    - path: "tests/16.1-breakpoints.spec.ts"
      provides: "Playwright breakpoint sweep spec"
      min_lines: 40
  key_links:
    - from: "tests/16.1-breakpoints.spec.ts"
      to: ".planning/phases/16.1-public-site-maintenance/snapshots/"
      via: "page.screenshot({ path: ... })"
      pattern: "snapshots/"
    - from: "tests/16.1-breakpoints.spec.ts"
      to: "tests/16.1-route-inventory.ts"
      via: "import { ROUTES } from './16.1-route-inventory'"
      pattern: "from.*16\\.1-route-inventory"
---

<objective>
Fix the homepage glitch logo clipping on 13" laptop viewports first (D-16), then run the full breakpoint sweep across 8 viewports × every public route (D-15), commit visual-regression baselines (D-17), and fix any additional layout breakage encountered inline.

Purpose: The homepage on a 13" screen was described as "total mess" — this is the highest-visibility regression before v3.0 design review. Without breakpoint coverage, every future redesign adds more untested viewports.

Output: Clean homepage at all 8 viewports (priority: 1280/1366/1440), breakpoint spec covering all public routes, committed snapshot baseline, inline fixes for breakage found during the sweep.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16.1-public-site-maintenance/16.1-CONTEXT.md
@.planning/phases/16.1-public-site-maintenance/16.1-AUDIT-SWEEP.md
@.planning/phases/16.1-public-site-maintenance/16.1-04-SUMMARY.md

# Route inventory lives in tests/16.1-route-inventory.ts (produced by Plan 04) — import it, do NOT re-derive.
# Memory rules
# - feedback_playwright_verification.md
# - feedback_design_quality.md (site rated 4/10 — expect to find layout issues)
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix homepage glitch logo clipping at 1280–1440 (D-16)</name>
  <files>
    - src/components/home/hero.tsx (or whichever file renders the homepage hero logo — confirm via grep)
    - src/components/layout/glitch-logo.tsx (the glitch logo component)
    - src/app/globals.css or the Tailwind config if a media-query rule needs adjusting
  </files>
  <read_first>
    - Run `grep -rln "glitch-logo\|GlitchLogo\|hero" src/components/home/ src/app/\(public\)/page.tsx`
    - src/components/layout/glitch-logo.tsx (understand the logo sizing — likely uses fixed pixel widths or `vw` units)
    - .planning/phases/16.1-public-site-maintenance/audit-screenshots/home-1280.png (the baseline from Plan 04 showing the clipping — if available)
    - src/app/(public)/page.tsx (homepage — understand hero layout wrapper constraints)
  </read_first>
  <behavior>
    - Load `/` at 1280×800, 1366×768, 1440×900. At each viewport: the glitch logo fully renders WITHIN the viewport bounds — no horizontal overflow, no clipping of the logo edges, no scrollbar-triggered shift.
    - Responsive behavior across the 375–1920 range remains smooth (no new mid-breakpoint regressions introduced).
    - The glitch logo component carries a stable `data-testid="glitch-logo"` attribute on its outermost element.
  </behavior>
  <action>
    1. **Locate the glitch logo component and ensure it has `data-testid="glitch-logo"`.** Run:
       ```
       grep -rn 'glitch.logo\|GlitchLogo' src/components/ src/app/ --include='*.tsx' | head
       ```
       Identify the component file (likely `src/components/layout/glitch-logo.tsx`, but confirm via grep — if multiple components exist, pick the one used by the homepage hero). If the outermost element does NOT already have `data-testid="glitch-logo"`, add that attribute now. Record the confirmed file path and add it to `files_modified` in the plan frontmatter if it isn't already there.
    2. Open the homepage hero + glitch logo files identified in `<read_first>`.
    3. Diagnose the clipping. Likely causes:
       (a) Fixed logo width (e.g., `w-[1200px]`) larger than 1280 minus sidebar width → logo overflows container
       (b) `vw`-based sizing without a max-width cap → scales too large on mid-size laptops
       (c) Sidebar claims ~240px at desktop so effective hero width is ~1040px at 1280 viewport — if logo is sized for full-bleed desktop (1920px), it clips here
       (d) Missing breakpoint in Tailwind for `lg:` / `xl:` that should scale down the logo between 1280–1440
    4. Apply the fix. Preferred approaches in order:
       - Use `clamp()` in CSS for fluid sizing with a sensible max-width cap.
       - Add a Tailwind `xl:` modifier that constrains logo width between 1280 and 1536: e.g., `w-full max-w-[900px] xl:max-w-[1100px] 2xl:max-w-[1400px]`.
       - If the hero uses a grid/flex layout, add `min-w-0` to the logo's wrapper to prevent overflow from forcing the container wider.
    5. Test the fix at 1280, 1366, 1440 manually in Playwright headed mode or by viewport-resizing the dev server.
    6. Add comment: `// D-16 (Phase 16.1): logo sizing fixed for 13" laptop range (1280–1440). Use clamp/xl: breakpoints, not fixed widths.`
    7. Run `pnpm tsc --noEmit` and `pnpm lint`.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'data-testid="glitch-logo"' src/components/` returns exit code 0 (pattern found — testid exists on the logo component, invariant established by the first action step).
    - Playwright: `await page.setViewportSize({ width: 1280, height: 800 }); await page.goto('/'); const bbox = await page.locator('[data-testid="glitch-logo"]').boundingBox(); expect(bbox.x + bbox.width).toBeLessThanOrEqual(1280);` passes.
    - Same assertion at 1366×768 and 1440×900.
    - No horizontal scrollbar on `/` at any of 1280/1366/1440.
    - `pnpm tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Homepage logo renders clean at all 13"-range viewports; testid in place for regression coverage.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build breakpoint sweep spec + capture baselines (D-15, D-17)</name>
  <files>
    - tests/16.1-breakpoints.spec.ts
    - .planning/phases/16.1-public-site-maintenance/snapshots/
  </files>
  <read_first>
    - tests/16.1-route-inventory.ts (authoritative ROUTES array — import this, do NOT re-derive)
    - tests/16.1-audit-sweep.spec.ts (from Plan 04 — reference for viewport-loop pattern)
    - .planning/phases/16.1-public-site-maintenance/16.1-AUDIT-SWEEP.md (context — NOT for re-deriving routes; routes come from the shared module)
    - Any existing Playwright config (`playwright.config.ts`) to align baseURL + project conventions
  </read_first>
  <behavior>
    - The spec iterates every route in the shared `ROUTES` array × 8 viewports (375, 393, 768, 1024, 1280, 1366, 1440, 1920).
    - At each cell, it takes a full-page screenshot and writes to `snapshots/{route-slug}/{viewport}.png`.
    - On subsequent runs, the spec uses `expect(page).toHaveScreenshot()` to diff against the committed baseline. First run establishes the baseline.
    - No CTA clicking in this spec — pure visual-regression coverage. (Plan 04 already did CTA smoke.)
  </behavior>
  <action>
    1. Create `tests/16.1-breakpoints.spec.ts`.
    2. Define the canonical viewport tuples:
       ```typescript
       const VIEWPORTS = [
         { width: 375, height: 667 },   // iPhone SE / small phone
         { width: 393, height: 852 },   // modern phone (iPhone 15 class)
         { width: 768, height: 1024 },  // tablet portrait
         { width: 1024, height: 768 },  // tablet landscape
         { width: 1280, height: 800 },  // 13" laptop (D-16 priority)
         { width: 1366, height: 768 },  // 13" laptop HD
         { width: 1440, height: 900 },  // 13" laptop retina / small desktop
         { width: 1920, height: 1080 }, // desktop
       ];
       ```
       Use exactly these tuples — do NOT substitute a constant height like 800.
    3. **Import `ROUTES` from `tests/16.1-route-inventory.ts`. Iterate over it directly — do NOT redefine inline.**
       ```typescript
       import { ROUTES } from './16.1-route-inventory';
       ```
    4. For each route × viewport, use `test.describe` grouping by route:
       ```
       for (const route of ROUTES) {
         test.describe(`Route: ${route.url}`, () => {
           for (const vp of VIEWPORTS) {
             test(`at ${vp.width}×${vp.height}`, async ({ page }) => {
               await page.setViewportSize(vp);
               await page.goto(route.url);
               await page.waitForLoadState('networkidle');
               await expect(page).toHaveScreenshot(`${route.slug}/${vp.width}.png`, {
                 fullPage: true,
                 maxDiffPixelRatio: 0.02,
               });
             });
           }
         });
       }
       ```
    5. Configure `playwright.config.ts` (or the spec's `test.use`) to write snapshots to `.planning/phases/16.1-public-site-maintenance/snapshots/` instead of the default Playwright snapshot dir. Use `testConfig.expect.toHaveScreenshot.pathTemplate` or equivalent — confirm the exact API in the installed Playwright version (`pnpm playwright --version`).
    6. **Pre-flight resource check BEFORE running the spec.** Run `free -h` to check available RAM. If less than 4GB is available (or if `pm2 status` shows a build running on any project), split `ROUTES` into batches of 5 and run the spec with `--grep` filters sequentially instead of one full run. CodeBox has 19GB total shared across all projects per CLAUDE.md — a routes × 8 viewports sweep is heavy.
    7. Run the spec with `--update-snapshots` on first pass to establish baseline:
       `pnpm playwright test tests/16.1-breakpoints.spec.ts --update-snapshots --reporter=line`
       Expect this to take significant time — if >10 min, subdivide via step 6's batching strategy.
    8. Commit the spec + the full `snapshots/` directory.
    9. Re-run the spec WITHOUT `--update-snapshots` and confirm it passes (validates the baseline was captured correctly):
       `pnpm playwright test tests/16.1-breakpoints.spec.ts --reporter=line`
  </action>
  <verify>
    <automated>ls .planning/phases/16.1-public-site-maintenance/snapshots/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `tests/16.1-breakpoints.spec.ts` exists and imports `ROUTES` from `./16.1-route-inventory` (no inline routes array).
    - Viewport array contains exactly the 8 canonical tuples: 375×667, 393×852, 768×1024, 1024×768, 1280×800, 1366×768, 1440×900, 1920×1080.
    - `.planning/phases/16.1-public-site-maintenance/snapshots/` contains subdirectories per route, each with 8 PNGs (one per viewport).
    - Re-running `pnpm playwright test tests/16.1-breakpoints.spec.ts` passes without `--update-snapshots` (baseline is stable).
    - `pnpm tsc --noEmit` passes (spec files included).
  </acceptance_criteria>
  <done>Full breakpoint baseline committed; spec is re-runnable as a regression guard; ROUTES imported from shared module.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fix inline breakage discovered during the sweep</name>
  <files>
    - (variable — whatever component/page files the sweep reveals as broken)
    - .planning/phases/16.1-public-site-maintenance/16.1-AUDIT-SWEEP.md (append breakpoint findings section)
  </files>
  <read_first>
    - .planning/phases/16.1-public-site-maintenance/snapshots/ (the baselines captured in Task 2 — review them visually to spot issues)
    - 16.1-AUDIT-SWEEP.md (existing triage — don't duplicate findings already listed)
  </read_first>
  <behavior>
    - Every route × viewport snapshot has been visually inspected (by Claude scanning the PNGs or running the dev server at each viewport).
    - Issues found fall into two groups:
      (a) **Fixable inline** — Tailwind class tweak, `min-w-0`, breakpoint addition, `overflow-x-hidden`, text truncation. Handle here.
      (b) **Architecture-level** — requires new components or layout rework. Promote to 999.x with a note in AUDIT-SWEEP.md.
    - Any inline fix is followed by a re-run of Task 2's spec with `--update-snapshots` for the affected routes to refresh the baseline.
  </behavior>
  <action>
    1. Review the snapshots in `snapshots/`. Scan for:
       - Horizontal overflow (content past viewport edge)
       - Text overlapping or truncating mid-word
       - Images not scaling (fixed-size hitting small viewport)
       - Navigation breaking at a specific breakpoint
       - Footer collapsing weirdly at tablet widths
    2. For each fixable issue:
       - Apply the Tailwind / CSS fix at the responsible component file.
       - Add comment referencing D-15 and the viewport that exposed the issue.
       - Re-run `pnpm playwright test tests/16.1-breakpoints.spec.ts tests/<route> --update-snapshots` to refresh the affected snapshots.
       - Append a "## Breakpoint Findings" section to AUDIT-SWEEP.md with a bullet describing the fix:
         ```markdown
         ## Breakpoint Findings (from Plan 05)
         - [/beats, 768px] Filter sidebar overlapped grid — fixed by adding `md:flex-col lg:flex-row` to the wrapper.
         - [/about, 1024px] Team card grid had 5 cards in a 4-col with awkward orphan — fixed by switching to `grid-cols-2 lg:grid-cols-3`.
         - [/tech/reviews, 375px] BPR tier badges wrapped text — fixed with `whitespace-nowrap`.
         ```
    3. For architecture-level issues (not fixable inline): add a bullet under `## Triage Summary → Promote-to-999.x` in AUDIT-SWEEP.md and add a matching line to `ROADMAP.md §Backlog`.
    4. Commit all inline fixes + updated baselines + updated AUDIT-SWEEP.md.
    5. Final run: `pnpm playwright test tests/16.1-breakpoints.spec.ts --reporter=line` WITHOUT `--update-snapshots` — must pass. This is the regression guard.
  </action>
  <verify>
    <automated>pnpm playwright test tests/16.1-breakpoints.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm playwright test tests/16.1-breakpoints.spec.ts` passes without `--update-snapshots` (after all fixes + baseline refresh).
    - `## Breakpoint Findings` section exists in AUDIT-SWEEP.md with entries for every inline fix applied (or a "None — all clean" note).
    - Any promoted-to-999.x items are reflected in ROADMAP.md.
    - `pnpm tsc --noEmit` and `pnpm lint` pass.
  </acceptance_criteria>
  <done>All routes render clean across 8 viewports; spec passes against committed baseline; findings documented.</done>
</task>

</tasks>

<verification>
- `pnpm playwright test tests/16.1-breakpoints.spec.ts` passes without `--update-snapshots`.
- Homepage at 1280×800 shows full glitch logo, no clipping.
- `.planning/phases/16.1-public-site-maintenance/snapshots/` contains full baseline.
- `pnpm tsc --noEmit` and `pnpm lint` pass.
- AUDIT-SWEEP.md has a Breakpoint Findings section.
- Glitch logo carries `data-testid="glitch-logo"` on its outermost element.
</verification>

<success_criteria>
Plan complete when:
- [ ] Homepage glitch logo clean at 1280/1366/1440 (13" priority)
- [ ] Glitch logo component has `data-testid="glitch-logo"`
- [ ] Breakpoint spec covers every public route × 8 viewports via imported `ROUTES`
- [ ] Snapshot baseline committed to `.planning/phases/16.1-public-site-maintenance/snapshots/`
- [ ] Spec passes without `--update-snapshots` on a clean run
- [ ] Breakpoint findings logged in AUDIT-SWEEP.md
- [ ] Any architecture-level issues promoted to 999.x
- [ ] Changes committed
</success_criteria>

<output>
After completion, create `.planning/phases/16.1-public-site-maintenance/16.1-05-SUMMARY.md`
</output>
