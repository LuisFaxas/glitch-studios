---
phase: 16.1-public-site-maintenance
plan: 01
status: complete
wave: 1
commit: ed6bc63
---

## Plan 16.1-01 — Sub-brand SPA navigation (D-01/D-02)

### Changes
- `src/components/tiles/tech-cross-link-tile.tsx` — removed conditional `target="_blank"` + associated `rel="noopener noreferrer"` spread; removed now-unused `crossOrigin` state; added `data-testid="tech-cross-link-tile"`; added D-01/D-02 comment block.
- `src/components/tiles/studios-cross-link-tile.tsx` — mirrored the same change (data-testid `studios-cross-link-tile`).
- `tests/16.1-cross-link-nav.spec.ts` — new Playwright spec. Two desktop assertions (`context.pages().length` unchanged after click, target attr absent), plus a `test.skip` for the deferred audio-continuity work.

### Verification
- `pnpm tsc --noEmit` — PASS (exit 0).
- `pnpm playwright test tests/16.1-cross-link-nav.spec.ts` — 2 passed, 4 skipped (2 mobile skips since cross-link tiles only render at `md` breakpoint; 2 deferred audio-continuity skips).
- grep audit: no JSX `target="_blank"` attribute remains in either tile file — the only string matches are inside the "Do NOT re-introduce" comment (intentional).

### Success criteria
- [x] Both cross-link tile files have JSX `target="_blank"` removed
- [x] Both files contain the D-01/D-02 comment block
- [x] `tests/16.1-cross-link-nav.spec.ts` exists and passes
- [x] `pnpm tsc --noEmit` passes
- [x] Changes committed (`ed6bc63`)

### Known limitation (documented, not a bug)
Cross-origin nav (e.g., `glitchstudios.io` → `https://glitchtech.io/`) stops any playing `<audio>` element mid-song. Accepted trade-off per D-01/D-02. Audio continuity across brands is deferred to a future unified-origin phase.

### Notes for downstream plans
- `data-testid="tech-cross-link-tile"` and `data-testid="studios-cross-link-tile"` are now stable Playwright anchors — Plans 04/05 can reference them.
- Cross-link tiles only render in the EXPANDED sidebar state (the collapsed-state branch in `tile-nav.tsx` does not mount the cross-link slot). This is why the regression spec navigates to `/beats` (expanded by default) rather than `/` (homepage defaults to collapsed).
