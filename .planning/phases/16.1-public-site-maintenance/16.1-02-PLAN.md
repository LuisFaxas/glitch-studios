---
phase: 16.1-public-site-maintenance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/home/splash-overlay.tsx
  - src/lib/get-splash-mode.ts
  - src/components/layout/tile-nav.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Splash overlay renders on first visit to each brand within a session — Studios splash fires on first Studios route, GlitchTech splash fires on first GlitchTech route (D-03, D-04)"
    - "Splash does NOT re-fire within the same session for the same brand after the initial show (sessionStorage key prevents repeat)"
    - "Root cause of intermittent splash non-render is identified and fixed BEFORE per-brand keys are added (D-05)"
    - "Collapsed sidebar cart renders as an interactive <Link href='/cart'> covering the full tile bounding box (not a bare icon in a <div>) — D-07"
    - "Collapsed sidebar sign-in already uses <Link> but matches the cart's new styling; both elements receive the hover-glitch effect used site-wide — D-08"
    - "All four cart/sign-in instances (collapsed cart, collapsed sign-in, expanded cart, expanded sign-in) carry the confirmed hover-glitch utility class so the site-wide hover-glitch rule from `feedback_glitch_headers.md` applies uniformly per D-08"
    - "GlitchTech /tech hero mirrors the Studios hero heartbeat + motion timing with 'tech'-substituted copy — D-06"
  artifacts:
    - path: "src/components/home/splash-overlay.tsx"
      provides: "Per-brand session-scoped splash with diagnosed render gate"
      contains: "sessionStorage"
    - path: "src/components/layout/tile-nav.tsx"
      provides: "Collapsed cart + sign-in as interactive elements with hover-glitch"
      contains: "<CartIcon"
  key_links:
    - from: "src/components/home/splash-overlay.tsx"
      to: "sessionStorage per-brand key"
      via: "useEffect reading brand from props/context"
      pattern: "glitch_(studios|tech)_splash_seen"
    - from: "src/components/layout/tile-nav.tsx:~100-103"
      to: "<CartIcon /> component (src/components/cart/cart-icon.tsx)"
      via: "<Link href='/cart'> wrapper matching expanded-state pattern (lines 180-183)"
      pattern: "CartIcon"
---

<objective>
Restore visual parity across the site by (1) fixing the splash overlay's intermittent non-render via root-cause investigation, then re-keying storage to per-brand per-session (D-03/D-04/D-05); (2) replacing the bare-icon collapsed cart with an interactive `<Link>`+`<CartIcon>` pattern matching the expanded state and applying hover-glitch to both cart and sign-in (D-07/D-08); (3) bringing the `/tech` hero to parity with the Studios hero heartbeat/motion timing with "tech"-substituted copy (D-06).

Purpose: The site's brand identity hinges on these three visual moments — the intro splash, consistent hover-glitch buttons, and the heartbeat hero. Each is currently broken or missing on one brand.

Output: Splash fires reliably once-per-brand-per-session; sidebar cart/sign-in buttons feel like real buttons; `/tech` hero matches `/` hero.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16.1-public-site-maintenance/16.1-CONTEXT.md
@.planning/phases/06.1-homepage-flair

@src/components/home/splash-overlay.tsx
@src/lib/get-splash-mode.ts
@src/actions/settings/set-splash-mode.ts
@src/components/layout/tile-nav.tsx
@src/components/cart/cart-icon.tsx
@src/app/(public)/layout.tsx
@src/app/(tech)/layout.tsx

# Memory rules
# - feedback_glitch_headers.md (hover-only RGB-split glitch for every header/button)
# - feedback_sidebar_design.md (no compact mode, no constant animations)
# - feedback_playwright_verification.md (Playwright verifies visual output)
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Diagnose & fix splash overlay render gate (D-05)</name>
  <files>src/components/home/splash-overlay.tsx, src/lib/get-splash-mode.ts</files>
  <read_first>
    - src/components/home/splash-overlay.tsx (current render conditions — see what guards the render: mode check, sessionStorage, hydration mismatch, hasMounted, etc.)
    - src/lib/get-splash-mode.ts (current splash mode resolution — admin flag default? always-on? once-per-session?)
    - src/actions/settings/set-splash-mode.ts (confirm admin-level flag values; verify it's not stuck disabled in prod)
    - src/app/(public)/layout.tsx (where overlay mounts on Studios side)
    - src/app/(tech)/layout.tsx (confirm overlay also mounts on tech side, or if missing this is part of the bug)
    - .planning/phases/06.1-homepage-flair/*-SUMMARY.md (original splash implementation decisions from Phase 06.1)
  </read_first>
  <behavior>
    - After fix: splash consistently renders on first page load when the `splash_mode` admin flag allows it.
    - After fix: the single current-session storage key (before per-brand migration in Task 2) reliably prevents re-firing within a session.
    - Root cause documented inline in the component file (comment block explaining what was broken + D-05 reference).
  </behavior>
  <action>
    1. Inspect the current `splash-overlay.tsx` render path. Likely causes per CONTEXT D-05:
       (a) `splash_mode` admin flag reads as `disabled` in prod DB — confirm by checking `get-splash-mode.ts` default and the DB value
       (b) hydration-mismatch suppression (e.g., `useEffect` setting `hasMounted` too late so SSR renders null and client never flips it)
       (c) overlay mounted only in `(public)/layout.tsx` and missing from `(tech)/layout.tsx` — meaning tech splash never had a chance to render
       (d) sessionStorage key already set from a previous session that never cleared (if using localStorage by mistake)
    2. Identify which cause applies (or multiple). Fix the root cause — do NOT jump straight to per-brand keying yet (Task 2 does that).
    3. If the fix reveals the overlay is not mounted on `(tech)/layout.tsx`, add it there so tech routes can show their splash.
    4. Add a comment block above the render gate describing the fix:
       ```
       // D-05 (Phase 16.1): Fixed render gate.
       // Root cause was: <describe>
       // Do not remove the <specific check>, it prevents <regression>.
       ```
    5. Run `pnpm tsc --noEmit` and manually smoke-test by clearing sessionStorage and reloading `/` and `/tech`.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'D-05' src/components/home/splash-overlay.tsx` returns exit code 0 (pattern found — comment present).
    - Manually: with sessionStorage cleared, `pnpm dev` then loading `/` shows the splash; loading `/tech` shows the splash (if Task 1 added the tech-side mount).
    - `pnpm tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Splash reliably renders on first visit under allowed splash_mode; root cause documented inline.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Per-brand sessionStorage keys (D-03, D-04)</name>
  <files>src/components/home/splash-overlay.tsx</files>
  <read_first>
    - src/components/home/splash-overlay.tsx (as it stands after Task 1)
    - Any brand-context hook or helper already in the codebase that tells a client component which brand it's on (grep `useBrand`, `brand`, or route-group detection via usePathname). If none exists, use `usePathname()` + prefix check.
  </read_first>
  <behavior>
    - Storage key `glitch_studios_splash_seen` is used on Studios routes (anything NOT under `/tech`).
    - Storage key `glitch_tech_splash_seen` is used on GlitchTech routes (pathname starts with `/tech`).
    - Splash fires once per brand per session. Visiting `/` then `/tech` shows splash twice (once per brand).
    - Both keys use `sessionStorage` (not localStorage) so keys clear on tab close.
  </behavior>
  <action>
    1. Inside `splash-overlay.tsx`, determine the current brand at render time. Preferred: pass a `brand: "studios" | "tech"` prop from the consuming layout (`(public)/layout.tsx` → `brand="studios"`, `(tech)/layout.tsx` → `brand="tech"`). Fallback: use `usePathname()` and check `pathname.startsWith('/tech')`.
    2. Derive the storage key: `const storageKey = brand === 'tech' ? 'glitch_tech_splash_seen' : 'glitch_studios_splash_seen'`.
    3. Replace the single key currently used (likely a string literal) with `storageKey`.
    4. If adding a `brand` prop: update the two layouts to pass it. Example:
       - `src/app/(public)/layout.tsx`: `<SplashOverlay brand="studios" />`
       - `src/app/(tech)/layout.tsx`: `<SplashOverlay brand="tech" />`
    5. Add inline comment: `// D-03/D-04 (Phase 16.1): per-brand sessionStorage keys so each brand fires once per session.`
    6. Run `pnpm tsc --noEmit`.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && grep -E "glitch_(studios|tech)_splash_seen" src/components/home/splash-overlay.tsx | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Grep count ≥ 2 (both keys referenced) OR a single ternary expression producing them (verify by reading the file).
    - `grep -q 'D-03/D-04' src/components/home/splash-overlay.tsx` returns exit code 0 (pattern found — comment present).
    - Manual: load `/`, see splash. Navigate to `/tech`, see splash again. Reload `/tech`, NO splash (already seen this session). Reload `/`, NO splash.
  </acceptance_criteria>
  <done>Per-brand splash keying in place; both keys wired through the layout mount points.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fix collapsed-state cart + sign-in in tile-nav.tsx (D-07, D-08)</name>
  <files>src/components/layout/tile-nav.tsx</files>
  <read_first>
    - src/components/layout/tile-nav.tsx (entire file — line numbers in CONTEXT.md are approximate; read the whole file to locate the collapsed-state bug)
    - src/components/cart/cart-icon.tsx (understand what CartIcon renders)
    - Tailwind config or global css for the hover-glitch utility class (confirm its exact name — the class may be named `hover-glitch`, `glitch-hover`, `glitch-on-hover`, a CSS-module class, or a Tailwind plugin utility). The FIRST action step below confirms the literal class name; all subsequent references use `CONFIRMED_GLITCH_CLASS` as a placeholder for that literal string.
  </read_first>
  <behavior>
    - Collapsed-state cart: clickable Link wrapping CartIcon, full-box click target, border + bg matches adjacent auth button, the confirmed hover-glitch utility class applied.
    - Collapsed-state sign-in: already a Link, but ensure the confirmed hover-glitch class is applied to both the signed-in and signed-out branches.
    - Expanded-state cart + sign-in: also receive the confirmed hover-glitch class so both states are visually consistent site-wide (per D-08 + memory rule `feedback_glitch_headers.md`).
  </behavior>
  <action>
    1. **Confirm the hover-glitch utility class name FIRST.** Before any edits, grep the codebase for the existing hover-glitch utility class. Run:
       ```
       grep -rE '(hover[-_]?glitch|glitch[-_]?hover)' src/ --include='*.tsx' --include='*.ts' --include='*.css' | head -20
       ```
       Also check the Tailwind config and `src/app/globals.css`. Record the exact class name as `CONFIRMED_GLITCH_CLASS` (e.g., `hover-glitch`, `glitch-on-hover`, `hover:glitch`, a CSS-module export, or a custom Tailwind plugin utility). All subsequent action steps and acceptance criteria in this task MUST substitute `CONFIRMED_GLITCH_CLASS` for the literal string `hover-glitch`. If no such utility exists yet, create one in `src/app/globals.css` following the existing glitch/hover pattern in the codebase — consult `feedback_glitch_headers.md` for the required RGB-split hover-only behavior — and use that class name as `CONFIRMED_GLITCH_CLASS`.
    2. Open `src/components/layout/tile-nav.tsx`. Locate the collapsed-state cart block (approximately ~lines 100-103 per CONTEXT.md; read the whole file since line numbers are approximate). The target markup looks like:
       ```
       <div className="mt-2 flex items-center justify-center border border-[#222] bg-[#111] text-[#f5f5f0] p-3 w-full">
         <ShoppingCart className="h-5 w-5" />
       </div>
       ```
    3. Replace with a `<Link>` that wraps `<CartIcon />` (matching the expanded-state pattern, approximately ~lines 180-183). Keep the same Tailwind border/bg classes for visual consistency. Add `CONFIRMED_GLITCH_CLASS` (the literal class name found in step 1) plus a hover transition so it matches the other buttons. Example replacement (substitute `CONFIRMED_GLITCH_CLASS` with the literal):
       ```
       <Link
         href="/cart"
         aria-label="Cart"
         className="CONFIRMED_GLITCH_CLASS mt-2 flex items-center justify-center p-3 border border-[#222] bg-[#111] hover:bg-[#1a1a1a] text-[#f5f5f0] w-full transition-colors duration-200"
       >
         <CartIcon />
       </Link>
       ```
       Verify the correct href (`/cart` is the likely target — grep existing `href="/cart"` for confirmation).
    4. Import `CartIcon` from `@/components/cart/cart-icon` at the top of the file if not already imported. Remove the now-unused `ShoppingCart` import from lucide-react if no other usage remains (grep within the file first).
    5. Locate the collapsed-state sign-in block (approximately ~lines 105-130). Both the signed-in `<Link>` (to /admin or /dashboard) and signed-out `<Link>` (to /login) should receive `CONFIRMED_GLITCH_CLASS` — add to the existing `className` strings.
    6. Locate the expanded-state cart + sign-in blocks (approximately ~lines 180-220). Add `CONFIRMED_GLITCH_CLASS` to each outermost interactive element so all four instances (collapsed cart, collapsed sign-in, expanded cart, expanded sign-in) are consistent.
    7. Add comment above the collapsed cart:
       ```
       {/* D-07/D-08 (Phase 16.1): full-box Link + CartIcon matching expanded state, with site-wide hover-glitch per feedback_glitch_headers.md */}
       ```
    8. Run `pnpm tsc --noEmit` and `pnpm lint`.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && grep -c 'CartIcon' src/components/layout/tile-nav.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '<div className="[^"]*"[^>]*>\s*<ShoppingCart' src/components/layout/tile-nav.tsx` returns no match (the bare-icon-in-div pattern is gone).
    - `grep -c 'CartIcon' src/components/layout/tile-nav.tsx` returns a count ≥ 2 (collapsed + expanded both use it).
    - `grep -c '<CONFIRMED_GLITCH_CLASS>' src/components/layout/tile-nav.tsx` returns a count ≥ 4 — one each for: collapsed cart, collapsed sign-in, expanded cart, expanded sign-in. (Substitute the literal class name found in the first action step for `<CONFIRMED_GLITCH_CLASS>`.)
    - `grep -q 'D-07/D-08' src/components/layout/tile-nav.tsx` returns exit code 0 (pattern found — comment present).
    - Playwright: on collapsed sidebar, click anywhere within the cart tile bounding box (not just the center icon) → navigates to `/cart`.
  </acceptance_criteria>
  <done>All four cart/sign-in instances are full-box interactive with hover-glitch; typecheck + lint pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: GlitchTech /tech hero parity with Studios hero (D-06)</name>
  <files>src/app/(tech)/page.tsx or src/app/(tech)/tech/page.tsx (TBD by grep), src/components/tech/hero-tech.tsx (new or existing — TBD)</files>
  <read_first>
    - Find Studios hero: `grep -l heartbeat src/ -r` and `grep -l "hero" src/components/home/ src/app/\(public\)/`. Likely candidate: `src/components/home/hero.tsx` or `src/app/(public)/page.tsx`.
    - Read the full Studios hero component (layout markup, motion timing, heartbeat SVG/CSS)
    - Find the current tech hero: `ls src/app/(tech)/`, `grep -l "hero\|Hero" src/app/\(tech\)/ -r`, `grep -l "hero" src/components/tech/ -r`
    - Read current tech hero (if it exists) to understand what copy/structure to preserve or replace
    - src/components/home/hero.tsx (or whichever file Studios hero lives in — source of truth for motion timing)
  </read_first>
  <behavior>
    - /tech hero uses the SAME JSX layout structure as Studios hero: identical heartbeat animation, motion timing (delays, durations), layout blocks (headline, subheadline, CTA, scroll indicator).
    - Copy is substituted minimally: "studios" → "tech", "production" → "hardware reviews" (or equivalent — keep it honest to the tech brand value), CTAs point at tech destinations (e.g., "Browse Reviews" → `/tech/reviews`, "Read Methodology" → `/tech/methodology`).
    - No NEW animations, no reimagined layout. Parity is the goal. Any glitch/animation keyframes on headings MUST be hover-gated per `feedback_glitch_headers.md` — no auto-running glitch on headings.
  </behavior>
  <action>
    1. **Locate Studios hero** — run `grep -rn "heartbeat" src/` and `grep -rn "Hero\|hero" src/components/home/ src/app/\(public\)/page.tsx 2>/dev/null`. Identify the exact file(s) + component name. Read the full hero source.
    2. **Locate tech hero** — `ls src/app/\(tech\)/` and `grep -rn "Hero\|hero" src/app/\(tech\)/ src/components/tech/ 2>/dev/null`. Determine: does a tech hero component exist? Is it imported on the tech homepage? What's it currently rendering?
    3. **Create or refactor tech hero component**:
       - If a `src/components/tech/hero-tech.tsx` (or similar) already exists: refactor it to mirror the Studios hero JSX structure exactly, with only copy/destination substitutions.
       - If it doesn't exist: create it by copying the Studios hero file to a tech variant, then apply substitutions.
       - If the Studios hero is a single file used by both brands via a `brand` prop pattern: extend that pattern — pass `brand="tech"` and make copy decisions prop-driven.
    4. **Copy substitutions** (Claude's discretion on exact wording, but honest to the brand):
       - "Glitch Studios" → "GlitchTech"
       - Studio-service language → hardware-review language ("book a session" → "browse reviews", "our sound" → "our methodology")
       - CTA hrefs: `/book` → `/tech/methodology`, `/beats` → `/tech/reviews`, etc.
       - Respect memory rule `project_brand_name.md`: use "GlitchTech" (not "GlitchTek") in any new copy.
    5. **Wire into tech homepage**: update `src/app/(tech)/page.tsx` (or whichever file renders the /tech homepage) to render the new hero in the same position the Studios homepage renders its hero.
    6. **Preserve heartbeat animation**: do NOT rewrite the heartbeat SVG/CSS. Copy the exact same markup + timing values from Studios.
    7. Add comment in tech hero file: `// D-06 (Phase 16.1): parity with Studios hero — heartbeat + motion timing identical, copy substituted for tech brand.`
    8. Run `pnpm tsc --noEmit` and `pnpm lint`.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - Playwright screenshot of `/tech` hero at 1280×800 and 375×812 — heartbeat animation present, layout visually matches Studios `/` hero.
    - `grep -q 'D-06' src/components/tech/` (or wherever hero file lives) returns exit code 0 (pattern found — comment present).
    - `grep -ri "GlitchTek" src/components/tech/hero*.tsx src/app/\(tech\)/page.tsx 2>/dev/null` returns no matches (brand name correctly spelled).
    - `grep -E 'animation.*glitch|glitch.*animation' <tech-hero-file>` — any match MUST be gated by `:hover` (i.e., occurs inside a `:hover` selector / Tailwind `hover:` modifier / hover-scoped CSS block). No auto-running keyframes on headings, per memory rule `feedback_glitch_headers.md`.
    - `pnpm tsc --noEmit` passes.
    - `pnpm lint` passes.
  </acceptance_criteria>
  <done>Tech hero renders identical heartbeat + motion timing to Studios hero with tech-adapted copy; typecheck + lint clean.</done>
</task>

</tasks>

<verification>
- `pnpm tsc --noEmit` passes.
- `pnpm lint` passes.
- Manual Playwright / dev-server smoke:
  - Clear sessionStorage → load `/` → Studios splash fires → reload `/` → no splash → navigate to `/tech` → Tech splash fires → reload `/tech` → no splash.
  - Collapsed sidebar cart tile is fully clickable, hover triggers glitch; sign-in same behavior.
  - `/tech` hero visually matches `/` hero heartbeat animation.
- `grep -r 'target="_blank"' src/components/layout/tile-nav.tsx` shows NO unintended new-tab additions.
</verification>

<success_criteria>
Plan complete when:
- [ ] Splash renders reliably on first visit to each brand per session
- [ ] Per-brand sessionStorage keys (`glitch_studios_splash_seen`, `glitch_tech_splash_seen`) are in use
- [ ] Collapsed sidebar cart is an interactive `<Link><CartIcon /></Link>` matching expanded state
- [ ] All four cart + sign-in instances (collapsed + expanded × cart + sign-in) have the confirmed hover-glitch utility class
- [ ] `/tech` hero mirrors Studios hero heartbeat + motion with tech copy
- [ ] `pnpm tsc --noEmit` and `pnpm lint` pass
- [ ] Changes committed
</success_criteria>

<output>
After completion, create `.planning/phases/16.1-public-site-maintenance/16.1-02-SUMMARY.md`
</output>
