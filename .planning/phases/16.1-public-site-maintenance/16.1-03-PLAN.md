---
phase: 16.1-public-site-maintenance
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/tech-nav-config.ts
  - src/components/layout/footer.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "GlitchTech sidebar has a Blog entry pointing at /tech/blog, using the BookOpen icon, matching the Studios public nav convention (D-09)"
    - "Both Studios and GlitchTech footers render the same three ACTIVE social icons: IG @glitchtech.io, TikTok @glitchtech.io, YouTube @glitchtech_io — each opens in a new tab with target=_blank rel=noopener noreferrer (D-11, D-12)"
    - "Both footers render the same three COMING-SOON placeholders: Facebook, X (Twitter), Threads — rendered in a muted/disabled state consistent across brands (D-11)"
    - "The string 'phase 7.6' no longer appears anywhere in the public-facing /tech codebase; replaced with an honest empty-state or methodology-linked stub (D-10)"
  artifacts:
    - path: "src/components/layout/tech-nav-config.ts"
      provides: "Tech sidebar nav with Blog entry"
      contains: "/tech/blog"
    - path: "src/components/layout/footer.tsx"
      provides: "Unified social handles across both brands with coming-soon placeholders"
      contains: "glitchtech.io"
  key_links:
    - from: "src/components/layout/tech-nav-config.ts"
      to: "/tech/blog route"
      via: "nav config entry with BookOpen icon"
      pattern: "/tech/blog"
    - from: "src/components/layout/footer.tsx"
      to: "instagram.com/glitchtech.io, tiktok.com/@glitchtech.io, youtube.com/@glitchtech_io"
      via: "anchor tags with target=_blank rel=noopener noreferrer"
      pattern: "target=\"_blank\""
---

<objective>
Wire up three pieces of chronically broken or stale content on the public site: (1) the missing Blog entry in the GlitchTech sidebar (D-09); (2) unified social icons with real handles + coming-soon placeholders in both brand footers (D-11, D-12); (3) replace the stale "phase 7.6" stub on `/tech/benchmarks` with an honest empty-state or methodology-linked placeholder (D-10).

Purpose: Every public route should feel like it was written by a real team for real readers. Stale placeholders and broken wiring erode credibility before launch.

Output: Tech sidebar links to /tech/blog; both footers show consistent social icons; benchmarks page no longer promises phases that don't exist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16.1-public-site-maintenance/16.1-CONTEXT.md

@src/components/layout/tech-nav-config.ts
@src/components/layout/public-nav-config.ts
@src/components/layout/footer.tsx

# Memory rules
# - project_brand_name.md (use "GlitchTech", not "GlitchTek" — fix as touched, no bulk rename)
# - feedback_glitch_headers.md (hover-glitch pattern already established — don't overlook for social icons if the style calls for it; Claude's discretion on whether icons in the footer follow this)
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Blog entry to tech-nav-config.ts (D-09)</name>
  <files>src/components/layout/tech-nav-config.ts</files>
  <read_first>
    - src/components/layout/tech-nav-config.ts (entire file — see existing nav entry shape)
    - src/components/layout/public-nav-config.ts (reference — Studios Blog entry pattern; copy its icon/href/label convention)
    - src/components/layout/nav-config-types.ts (nav entry type definition — confirm required fields)
  </read_first>
  <behavior>
    - `tech-nav-config.ts` exports a nav array that now includes a Blog entry with:
      - label: "Blog"
      - href: "/tech/blog"
      - icon: BookOpen (from lucide-react, matching Studios convention)
    - Entry appears at the same relative position Studios has its Blog entry in `public-nav-config.ts` (typically near Services/Content — match the visual order).
  </behavior>
  <action>
    1. Open `src/components/layout/public-nav-config.ts`. Locate the existing Blog entry. Note its icon import (`BookOpen` from `lucide-react`), label, and position in the array.
    2. Open `src/components/layout/tech-nav-config.ts`.
    3. Add the `BookOpen` import from `lucide-react` if not already imported.
    4. Add a Blog nav entry to the tech nav array:
       ```
       {
         label: "Blog",
         href: "/tech/blog",
         icon: BookOpen,
       }
       ```
       Match whatever shape the existing tech nav entries use (grep the file for other entries to confirm — may include `description`, `external`, `section` etc.).
    5. Position the entry to mirror Studios' Blog placement (if Studios has Blog between two sections, place Tech Blog in the equivalent gap).
    6. Add a comment: `// D-09 (Phase 16.1): Blog entry added to match Studios nav convention; /tech/blog route arrives in Phase 20.`
    7. Run `pnpm tsc --noEmit`.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && grep -c '/tech/blog' src/components/layout/tech-nav-config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '/tech/blog' src/components/layout/tech-nav-config.ts` returns exit code 0 (pattern found — entry present).
    - `grep -q 'BookOpen' src/components/layout/tech-nav-config.ts` returns exit code 0 (pattern found — icon imported).
    - `grep -q 'D-09' src/components/layout/tech-nav-config.ts` returns exit code 0 (pattern found — comment present).
    - `pnpm tsc --noEmit` passes.
    - Manual: sidebar on any /tech route shows Blog entry; clicking it navigates to /tech/blog (will 404 until Phase 20 — acceptable for now).
  </acceptance_criteria>
  <done>Blog entry wired; typecheck passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Unified social handles with coming-soon placeholders in footer(s) (D-11, D-12)</name>
  <files>src/components/layout/footer.tsx</files>
  <read_first>
    - src/components/layout/footer.tsx (entire file — understand current social icon markup, current handles, whether there's a single footer rendering both brands or two variants)
    - grep `tech` / `glitch` in src/components/layout/ for any separate tech-footer component file — `grep -rln "footer" src/components/ src/app/\(tech\)/` to find tech-specific footers
    - Confirm brand detection in footer: does it use a prop, a hook, or route-group separation? Choose the pattern the file already uses.
  </read_first>
  <behavior>
    - Both Studios and GlitchTech footers (whether one file with a brand discriminator or two files) render the SAME three active social links and the SAME three coming-soon placeholders.
    - ACTIVE (all open in new tab):
      - Instagram: `https://instagram.com/glitchtech.io` — label `@glitchtech.io`
      - TikTok: `https://tiktok.com/@glitchtech.io` — label `@glitchtech.io`
      - YouTube: `https://youtube.com/@glitchtech_io` — label `@glitchtech_io`
    - COMING-SOON (rendered but non-interactive):
      - Facebook, X (Twitter), Threads — muted icon, cursor-default, aria-label="Coming soon", no `<a href>` wrapper (or a button with `disabled` attribute — Claude's discretion on `<span>` vs `<button disabled>` — just ensure it's not clickable, is visually de-emphasized, and behaves consistently across both brands).
    - All active anchors include `target="_blank" rel="noopener noreferrer"`.
    - Visual treatment for coming-soon: use `opacity-40` or `text-[#555]` (matches the muted-grey tone already in the codebase for inactive elements — confirm which pattern the codebase prefers by reading surrounding Tailwind classes).
  </behavior>
  <action>
    1. Inspect `src/components/layout/footer.tsx`. Determine:
       - Is this a single footer with a `brand` prop/detection, or is there a separate `tech-footer.tsx` variant?
       - If separate: both files need the SAME social markup (either extract a shared `<SocialIcons />` component or duplicate with a `// D-11 sync point` comment reminding future edits to touch both).
    2. Refactor (Claude's discretion — either approach is fine):
       - **Option A (preferred if footer already accepts a brand prop):** write a single `<SocialIcons />` inline block inside footer.tsx that renders the 6 icons (3 active, 3 coming-soon) identically for both brands.
       - **Option B (if two separate footer files exist):** extract a new shared `src/components/layout/social-icons.tsx` component and import it from both footers. Add this new file to `files_modified` mentally — update the plan's `files_modified` frontmatter to include it.
    3. **Handle suffix carefully:** TikTok handle is `@glitchtech.io` (with `.io` suffix); IG uses the same `@glitchtech.io`; YouTube uses `@glitchtech_io` (underscore, no `.io`). Do not mix suffixes. For each ACTIVE icon, render an `<a>`:
       ```
       <a
         href="https://instagram.com/glitchtech.io"
         target="_blank"
         rel="noopener noreferrer"
         aria-label="Instagram — @glitchtech.io"
         className="hover-glitch <matching existing icon classes>"
       >
         <Instagram className="h-5 w-5" />
       </a>
       ```
       Use lucide-react icons: `Instagram`, and whatever the project uses for TikTok + YouTube (lucide has `Youtube`; TikTok may require a custom SVG or a brand icon pack — grep `TikTok\|tiktok` in the codebase first; if no existing icon, use a simple SVG or the Music2 lucide icon as a placeholder with an aria-label clarifying).
    4. For each COMING-SOON icon, render a non-interactive element:
       ```
       <span
         aria-label="Facebook — coming soon"
         className="opacity-40 cursor-default <matching icon layout classes>"
         title="Coming soon"
       >
         <Facebook className="h-5 w-5" />
       </span>
       ```
       Consistent treatment across all three coming-soon icons.
    5. Delete any old social handles that don't match the new list (e.g., old Studios Twitter handles, broken links).
    6. If the Studios footer previously had a different set of handles locked in: these are overwritten per D-11 (unified presence across brands).
    7. Add comment: `// D-11/D-12 (Phase 16.1): unified social presence across both brands; coming-soon placeholders render muted and non-interactive. When FB/X/Threads go live, flip to <a> elements matching the active pattern.`
    8. Run `pnpm tsc --noEmit` and `pnpm lint`.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && grep -c 'glitchtech' src/components/layout/footer.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'instagram.com/glitchtech.io' src/components/layout/footer.tsx` returns exit code 0 (pattern found — IG handle present; or same in the shared social-icons file).
    - `grep -q 'tiktok.com/@glitchtech.io' src/components/layout/footer.tsx` returns exit code 0 (pattern found — TikTok handle with `.io` suffix present; or shared file).
    - `grep -q 'youtube.com/@glitchtech_io' src/components/layout/footer.tsx` returns exit code 0 (pattern found — YouTube handle with underscore-io suffix present; or shared file).
    - `grep -c 'target="_blank"' src/components/layout/footer.tsx` returns a count ≥ 3 (one per active social icon).
    - `grep -c 'coming soon\|Coming soon' src/components/layout/footer.tsx` returns a count ≥ 3 (or the shared file).
    - Playwright: load `/` footer and `/tech` footer — both show 6 social icons, 3 active (hover + click opens new tab), 3 muted/coming-soon (no click navigation).
    - `pnpm tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Unified social presence wired, coming-soon treatment consistent, typecheck + lint clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Remove "phase 7.6" stale copy from /tech/benchmarks (D-10)</name>
  <files>src/app/(tech)/benchmarks/page.tsx OR src/app/(tech)/tech/benchmarks/page.tsx (TBD by grep)</files>
  <read_first>
    - Run `grep -rn "phase 7.6" src/app/\(tech\)/` to find the exact file + line
    - Run `grep -rn "phase 7.6" src/` broader to catch any secondary occurrences (components, metadata, etc.)
    - Read the full page file once located — understand the surrounding copy so the replacement fits naturally
    - Check if a /tech/methodology page exists yet (`ls src/app/\(tech\)/`) — Phase 17 builds it; if it doesn't exist, don't link to it (or link anyway, since the 404 is temporary and honest — Claude's discretion)
  </read_first>
  <behavior>
    - The string `phase 7.6` (case-insensitive) no longer appears anywhere under `src/app/(tech)/`.
    - The replaced copy is honest: describes what the page WILL show when benchmarks are published, doesn't reference internal phase numbers, doesn't promise specific dates.
    - Tone matches the rest of the tech brand voice (technical, understated).
  </behavior>
  <action>
    1. Run `grep -rn "phase 7.6" src/` to locate ALL occurrences. For each occurrence:
    2. Replace with an honest empty-state. Suggested copy (Claude's discretion on exact wording — the following is a template):
       ```
       Benchmark data populates as reviews are published.
       See the methodology for how GlitchTech scores hardware.
       ```
       Or, if the page should link to methodology:
       ```
       No benchmarks published yet — data arrives with the first flagship review.
       Read the methodology to understand how results are measured.
       ```
    3. If the page has a visual empty-state component, use it. If not, wrap the text in a `<section>` with reasonable Tailwind (`text-[#888] text-center py-16`) so it doesn't look like raw unstyled placeholder text.
    4. Optional: add a link to `/tech/methodology` (will 404 until Phase 17 — that's fine, honest placeholder over false promise).
    5. Add comment: `{/* D-10 (Phase 16.1): honest empty state replacing "phase 7.6" stub. Revisit copy when benchmarks go live. */}`
    6. Confirm no OTHER stale phase references in the file (e.g., "phase 15", "phase 16" hardcoded into public copy). Grep broadly and fix as touched.
    7. Run `pnpm tsc --noEmit` and `pnpm lint`.
  </action>
  <verify>
    <automated>grep -ri "phase 7.6" src/app/ src/components/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ri "phase 7.6" src/app/ src/components/` returns exit code 1 (pattern absent — stale copy removed).
    - `grep -q 'D-10' <the benchmarks page file>` returns exit code 0 (pattern found — comment present).
    - `pnpm tsc --noEmit` passes.
    - Playwright: load `/tech/benchmarks` (or whatever the exact route is) → sees the new honest copy, not the stale phase reference.
  </acceptance_criteria>
  <done>Stale copy purged; new empty-state renders honestly; typecheck passes.</done>
</task>

</tasks>

<verification>
- `pnpm tsc --noEmit` and `pnpm lint` pass.
- `grep -ri "phase 7.6" src/` returns no matches.
- `grep -q '/tech/blog' src/components/layout/tech-nav-config.ts` passes.
- Footer on both `/` and `/tech` renders identical social icons (3 active + 3 coming-soon).
</verification>

<success_criteria>
Plan complete when:
- [ ] Blog entry in tech-nav-config.ts pointing at /tech/blog with BookOpen icon
- [ ] Footer has unified social handles with IG/TikTok/YouTube active + FB/X/Threads coming-soon
- [ ] All active socials open in new tab with `target="_blank" rel="noopener noreferrer"`
- [ ] String "phase 7.6" absent from entire /tech codebase
- [ ] Typecheck + lint pass
- [ ] Changes committed
</success_criteria>

<output>
After completion, create `.planning/phases/16.1-public-site-maintenance/16.1-03-SUMMARY.md`
</output>
