---
phase: 16.1-public-site-maintenance
plan: 04
status: complete
wave: 2
commits:
  - 71c4744  # in-phase fixes (queries.ts leftJoin, next.config placehold.co, widget href)
  - <sweep-artifacts>  # route-inventory + sweep spec + AUDIT-SWEEP.md + 81 screenshots + backlog entries
---

## Plan 16.1-04 — Public-Site Audit Sweep + Triage

### Coverage
- 27 public routes (Studios + Tech) × 3 viewports (mobile 375, laptop 1280, desktop 1920) = **81 page loads**
- 81 full-page screenshots committed to `audit-screenshots/` as visual regression baseline
- Serial Playwright run, single worker, soft-fail on HTTP errors, hard-fail on page crash

### Findings (5 categories, triaged per D-14)

| ID | Severity | Description | Triage |
|----|----------|-------------|--------|
| F-1 | blocker | `/tech/reviews/[slug]` 404s on the only linked slug | Fix-in-phase (code resilience) + Promote to **999.7** (seed data) |
| F-2 | blocker | `/tech/categories/laptops` crashed HTTP 500 on `placehold.co` host | Fix-in-phase (next.config allowlist) |
| F-3 | info | Lone-nav-tile heuristic false-positives after HMR drift | Accepted; documented method note |
| F-4 | info | Hydration-mismatch warnings on 8 tech routes (first pass only) | Accepted; reappeared zero times after pm2 restart |
| F-5 | info | `_next/image` 400 Bad Request for placeholder URL query strings | Accepted; clears when real imagery ships (Promote to **999.8**) |

### Changes

| File | Change |
|------|--------|
| `src/lib/tech/queries.ts` | `getPublishedReviewBySlug` switched `techCategories` + `user` innerJoin → leftJoin; `PublicReviewDetail.product.categoryId/Name/Slug` now `string \| null` |
| `src/app/(tech)/tech/reviews/[slug]/page.tsx` | Skip `getCategoryBreadcrumb` + `getRelatedReviews` when `review.product.categoryId` is null (both require a concrete id) |
| `src/components/tiles/widget-latest-review.tsx` | href `/tech/reviews/macbook-pro-m4` → `/tech/reviews` (placeholder-first pattern; no dead-click) |
| `next.config.ts` | Added `placehold.co` to `images.remotePatterns` with note tying it to memory `project_placeholder_first_build` |
| `.planning/ROADMAP.md` | Inserted Phase **999.7** (seed representative tech catalog) + Phase **999.8** (replace placeholder imagery) |
| `tests/16.1-route-inventory.ts` | NEW — canonical ROUTES array + SWEEP_VIEWPORTS + BREAKPOINT_VIEWPORTS (Plan 05 imports from here) |
| `tests/16.1-audit-sweep.spec.ts` | NEW — 27×3 Playwright spec; stale-copy probe, lone-nav-tile detector, console error capture, full-page screenshots |
| `.planning/phases/16.1-public-site-maintenance/16.1-AUDIT-SWEEP.md` | NEW — triaged findings report with method notes |
| `.planning/phases/16.1-public-site-maintenance/audit-findings.json` | NEW — machine-readable findings log |
| `.planning/phases/16.1-public-site-maintenance/audit-screenshots/` | NEW — 81 PNGs |

### Verification
- `pnpm tsc --noEmit` — PASS
- `pnpm playwright test tests/16.1-audit-sweep.spec.ts --project=desktop` — 81 passed (1 soft-logged 404 on unseeded `/tech/reviews/macbook-pro-m4`)
- 5 Plan 01/02/03 regression specs still green (22 total Playwright cases: 16 pass, 6 expected mobile/deferred skips)
- `curl http://localhost:3004/tech/categories/laptops` — HTTP 200 (was HTTP 500)
- All other 26 routes return HTTP 200 or 307→/login (auth-gated dashboard/checkout)

### Success criteria
- [x] Every public route in `src/app/(public)` + `src/app/(tech)` listed in the inventory with kind, layout group, and auth-gating annotation
- [x] Middleware cross-checked — no virtual-only routes beyond the inventory
- [x] Medium-depth sweep at 3 viewports × 27 routes, screenshots committed
- [x] Findings triaged: every blocker is either closed in-phase or has an owning backlog entry; no `Fix-in-phase` bucket left without an owner
- [x] Stale-copy regression probe (`phase 7.6`, `GlitchTek`) passes on every route
- [x] `tests/16.1-route-inventory.ts` exported for Plan 05 consumption

### Notes for downstream plans
- Plan 05 (breakpoint audit) consumes `ROUTES` + `BREAKPOINT_VIEWPORTS` from the shared inventory file — do not re-derive.
- The `_next/image` 400 warnings on `placehold.co` URLs are cosmetic; they'll disappear when 999.8 (real imagery) ships.
- The backlog entries 999.7 + 999.8 should be promoted before launch so the MacBook Pro review detail can be reviewed against real data.
- When the dev server is running, re-run the sweep after a `pm2 restart glitch_studios` to avoid HMR-drift false positives.
