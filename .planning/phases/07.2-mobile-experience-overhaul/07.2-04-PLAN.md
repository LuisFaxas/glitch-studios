---
phase: 07.2-mobile-experience-overhaul
plan: 04
type: execute
wave: 3
depends_on:
  - 07.2-02
  - 07.2-03
files_modified:
  - tests/mobile-audit.spec.ts
autonomous: false
requirements:
  - D-04
  - D-06
  - D-09
  - D-11

must_haves:
  truths:
    - "Post-fix screenshots at 375px, 390px, 430px exist for every public page"
    - "User has reviewed screenshots and approved the mobile experience"
    - "No horizontal overflow on any page at any test viewport"
  artifacts:
    - path: ".planning/phases/07.2-mobile-experience-overhaul/screenshots/"
      provides: "Post-fix verification screenshots for all pages at all viewports"
  key_links:
    - from: "tests/mobile-audit.spec.ts"
      to: "All modified components"
      via: "Playwright screenshots capture rendered output of all changes from Plans 02 and 03"
      pattern: "screenshot"
---

<objective>
Re-run the Playwright audit to capture the post-fix state of all mobile pages, review screenshots for any remaining issues found during the audit, fix them, and get user approval.

Purpose: Per D-11 -- Playwright verification after all structural fixes. This is the quality gate for the phase.
Output: Post-fix screenshots + any additional page-specific fixes + user approval.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07.2-mobile-experience-overhaul/07.2-CONTEXT.md
@.planning/phases/07.2-mobile-experience-overhaul/07.2-UI-SPEC.md
@.planning/phases/07.2-mobile-experience-overhaul/07.2-02-SUMMARY.md (structural changes made)
@.planning/phases/07.2-mobile-experience-overhaul/07.2-03-SUMMARY.md (hero/splash fixes made)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run post-fix audit and fix any remaining mobile issues</name>
  <files>tests/mobile-audit.spec.ts</files>
  <read_first>
    - tests/mobile-audit.spec.ts (audit spec from Plan 01)
    - .planning/phases/07.2-mobile-experience-overhaul/07.2-02-SUMMARY.md (what structural changes were made)
    - .planning/phases/07.2-mobile-experience-overhaul/07.2-03-SUMMARY.md (what hero/splash changes were made)
  </read_first>
  <action>
1. Update `tests/mobile-audit.spec.ts` to add a second describe block for post-fix screenshots. Change the screenshot filename pattern for the new run to `{page.name}-{vp.name}-postfix.png` to avoid overwriting the pre-fix screenshots. Alternatively, just re-run the existing spec (overwriting is fine since we have the pre-fix summary notes).

2. Run the audit: `npx playwright test tests/mobile-audit.spec.ts --project=desktop`

3. Read every post-fix screenshot at 375px viewport (the tightest constraint). For each page, check against the UI-SPEC Page-by-Page Mobile Audit Checklist:
   - No horizontal overflow (page width never exceeds viewport)
   - No overlapping elements
   - Touch targets at least 48x48px
   - Text readable (body at 15px, nothing below 11px)
   - Tab bar clearance (content not behind tab bar)
   - Player bar clearance (if applicable)
   - Images responsive
   - Carousels swipeable (Embla -- structural check only, not interaction)

4. For any issues found, make targeted CSS/layout fixes. Common fixes:
   - Horizontal overflow: add `overflow-x-hidden` to offending container or constrain width with `max-w-full`
   - Text overflow: add `truncate` or `break-words`
   - Image overflow: add `max-w-full` and `h-auto`
   - Spacing: adjust padding/margin for mobile breakpoint

5. After fixes, re-run the audit to capture final state.

The goal is to fix everything in a single pass. Do NOT create elaborate refactors -- these are CSS tweaks. If a page needs more than 3 files changed, note it for a future phase.
  </action>
  <verify>
    <automated>cd /home/faxas/workspaces/projects/personal/glitch_studios && npx playwright test tests/mobile-audit.spec.ts --project=desktop 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All Playwright tests pass (30 tests, 10 pages x 3 viewports)
    - Post-fix screenshots exist in `.planning/phases/07.2-mobile-experience-overhaul/screenshots/`
    - Any additional CSS fixes are documented in the summary
  </acceptance_criteria>
  <done>Post-fix screenshots captured for all pages at all viewports. Any remaining mobile issues have been fixed with targeted CSS changes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User verification of complete mobile experience</name>
  <files>none</files>
  <action>
Present the user with the complete mobile experience for review. All structural changes (tab bar, dynamic padding, hero, overlay) and page-by-page fixes are complete. The user must verify on a real device or emulator.

What was built:
- Tab bar restructured: [Beats, Services, Portfolio, Blog, Menu, Cart] with menu trigger on the right (D-01)
- Dynamic bottom padding: content area adjusts when player bar is active (D-07)
- Hero section: 70vh on mobile (vs 90vh desktop), no sidebar offset on mobile (D-04/D-05)
- Overlay: logo tile spans full width (D-03)
- All pages audited and fixed at 375px, 390px, 430px viewports (D-09/D-11)

How to verify:
1. Open https://glitch-studios.codebox.local on your phone (or use Chrome DevTools mobile emulation at 375px)
2. Verify tab bar: 6 icons in order -- Beats, Services, Portfolio, Blog, grid/menu icon, cart icon. Menu is NOT in the center.
3. Tap the menu (grid) icon -- overlay opens with full-width Glitch logo at top, navigation tiles, widgets, Sign In
4. Close overlay, navigate to /beats, play a beat
5. Verify player bar appears ABOVE the tab bar (stacked, both visible)
6. Scroll down on any page -- content should not be hidden behind the tab bar or player bar
7. Go to home page -- hero should be shorter than desktop, CTAs visible without scrolling
8. Check these pages for no horizontal overflow or broken layout: /services, /portfolio, /artists, /blog, /contact, /login

Screenshots are in `.planning/phases/07.2-mobile-experience-overhaul/screenshots/` if you want to review them in the file system.

Resume signal: Type "approved" or describe issues to fix.
  </action>
  <verify>User types "approved"</verify>
  <done>User has verified and approved the mobile experience across all pages and viewports.</done>
</task>

</tasks>

<verification>
- All 30 Playwright audit tests pass
- User approval of mobile experience
- No horizontal overflow on any page at 375px
</verification>

<success_criteria>
User has verified and approved the mobile experience on a real device or emulator. All pages render correctly at mobile viewports with no layout breakage.
</success_criteria>

<output>
After completion, create `.planning/phases/07.2-mobile-experience-overhaul/07.2-04-SUMMARY.md`
</output>
