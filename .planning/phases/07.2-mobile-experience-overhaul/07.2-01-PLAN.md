---
phase: 07.2-mobile-experience-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/mobile-audit.spec.ts
autonomous: true
requirements:
  - D-06
  - D-09
  - D-10
  - D-11

must_haves:
  truths:
    - "Every public page has been screenshotted at 375px, 390px, and 430px viewports"
    - "Screenshots exist in the phase screenshots directory for inspection"
  artifacts:
    - path: "tests/mobile-audit.spec.ts"
      provides: "Parameterized Playwright audit spec that screenshots all 11 public pages at 3 mobile viewports"
      min_lines: 40
    - path: ".planning/phases/07.2-mobile-experience-overhaul/screenshots/"
      provides: "PNG screenshots of every page at every viewport"
  key_links:
    - from: "tests/mobile-audit.spec.ts"
      to: "playwright.config.ts"
      via: "Uses @playwright/test imports and base URL from config"
      pattern: "import.*@playwright/test"
---

<objective>
Capture the current mobile state of every public page via Playwright screenshots at 3 viewports (375px, 390px, 430px). This is the audit-first step -- before any fixes are made, the AI must see what is broken.

Purpose: Per D-06, D-09, D-11 -- full Playwright audit required before making changes. Screenshots become the reference for identifying all mobile issues.
Output: `tests/mobile-audit.spec.ts` + ~33 PNG screenshots in `.planning/phases/07.2-mobile-experience-overhaul/screenshots/`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.2-mobile-experience-overhaul/07.2-CONTEXT.md
@.planning/phases/07.2-mobile-experience-overhaul/07.2-RESEARCH.md
@.planning/phases/07.2-mobile-experience-overhaul/07.2-UI-SPEC.md

<interfaces>
<!-- Existing Playwright patterns from tests/homepage-flair.spec.ts -->
From tests/homepage-flair.spec.ts:
```typescript
import { test, expect } from "@playwright/test"
import path from "path"

const screenshotDir = path.resolve(
  __dirname,
  "../.planning/phases/06.1-homepage-flair/screenshots"
)
// Splash skip pattern:
await page.addInitScript(() => {
  sessionStorage.setItem("glitch-splash-seen", "true")
})
await page.goto("/", { waitUntil: "networkidle" })
await page.waitForTimeout(1000)
await page.screenshot({ path: path.join(screenshotDir, `homepage-${projectName}-full.png`), fullPage: true })
```

From playwright.config.ts:
```typescript
baseURL: "http://localhost:3004",
projects: [
  { name: "desktop", use: { viewport: { width: 1440, height: 900 } } },
  { name: "mobile", use: { viewport: { width: 375, height: 812 } } },
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create multi-viewport mobile audit Playwright spec</name>
  <files>tests/mobile-audit.spec.ts</files>
  <read_first>
    - tests/homepage-flair.spec.ts (existing Playwright pattern -- splash skip, screenshot dir, waitForTimeout)
    - playwright.config.ts (base URL, project structure)
  </read_first>
  <action>
Create `tests/mobile-audit.spec.ts` that parameterizes across 3 viewports and 11 pages.

Viewports (per D-10 / UI-SPEC):
- `{ name: "375", width: 375, height: 812 }` (iPhone SE / 13 mini)
- `{ name: "390", width: 390, height: 844 }` (iPhone 14/15/16)
- `{ name: "430", width: 430, height: 932 }` (iPhone Pro Max)

Pages (per D-09):
- `{ name: "home", path: "/" }`
- `{ name: "beats", path: "/beats" }`
- `{ name: "beat-detail", path: "/beats/midnight-drive" }`
- `{ name: "services", path: "/services" }`
- `{ name: "portfolio", path: "/portfolio" }`
- `{ name: "artists", path: "/artists" }`
- `{ name: "blog", path: "/blog" }`
- `{ name: "contact", path: "/contact" }`
- `{ name: "login", path: "/login" }`
- `{ name: "signup", path: "/signup" }`

Screenshot output directory: `path.resolve(__dirname, "../.planning/phases/07.2-mobile-experience-overhaul/screenshots")`

File naming: `{page.name}-{vp.name}-default.png`

Structure:
```typescript
import { test } from "@playwright/test"
import path from "path"

const screenshotDir = path.resolve(
  __dirname,
  "../.planning/phases/07.2-mobile-experience-overhaul/screenshots"
)

const viewports = [
  { name: "375", width: 375, height: 812 },
  { name: "390", width: 390, height: 844 },
  { name: "430", width: 430, height: 932 },
]

const pages = [
  { name: "home", path: "/" },
  { name: "beats", path: "/beats" },
  { name: "beat-detail", path: "/beats/midnight-drive" },
  { name: "services", path: "/services" },
  { name: "portfolio", path: "/portfolio" },
  { name: "artists", path: "/artists" },
  { name: "blog", path: "/blog" },
  { name: "contact", path: "/contact" },
  { name: "login", path: "/login" },
  { name: "signup", path: "/signup" },
]

test.describe("Mobile audit - pre-fix screenshots", () => {
  for (const vp of viewports) {
    for (const pg of pages) {
      test(`${pg.name} at ${vp.name}px`, async ({ browser }) => {
        const ctx = await browser.newContext({
          viewport: { width: vp.width, height: vp.height },
        })
        const page = await ctx.newPage()
        await page.addInitScript(() => {
          sessionStorage.setItem("glitch-splash-seen", "true")
        })
        await page.goto(pg.path, { waitUntil: "networkidle" })
        await page.waitForTimeout(500)
        await page.screenshot({
          path: path.join(screenshotDir, `${pg.name}-${vp.name}-default.png`),
          fullPage: true,
        })
        await ctx.close()
      })
    }
  }
})
```

Note: The blog-post detail page is omitted because no known slug exists in the seed data without first querying the database. If `/beats/midnight-drive` 404s, the screenshot will still capture the 404 page (which is useful audit data). The spec should NOT fail on 404s -- it captures whatever renders.
  </action>
  <verify>
    <automated>cd /home/faxas/workspaces/projects/personal/glitch_studios && npx playwright test tests/mobile-audit.spec.ts --project=desktop 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/mobile-audit.spec.ts exists and contains `test.describe("Mobile audit`
    - tests/mobile-audit.spec.ts contains `viewports` array with entries for widths 375, 390, 430
    - tests/mobile-audit.spec.ts contains `pages` array with at least 10 entries
    - tests/mobile-audit.spec.ts contains `sessionStorage.setItem("glitch-splash-seen", "true")`
    - tests/mobile-audit.spec.ts contains `fullPage: true`
    - Screenshot directory `.planning/phases/07.2-mobile-experience-overhaul/screenshots/` contains PNG files after test run
    - All 30 tests pass (10 pages x 3 viewports)
  </acceptance_criteria>
  <done>30 screenshots exist in the phase screenshots directory covering all 10 pages at 3 viewports. The audit captures the pre-fix mobile state.</done>
</task>

</tasks>

<verification>
- `ls .planning/phases/07.2-mobile-experience-overhaul/screenshots/*.png | wc -l` returns >= 30
- `npx playwright test tests/mobile-audit.spec.ts --project=desktop` passes all tests
</verification>

<success_criteria>
Every public page has been captured at 375px, 390px, and 430px. The screenshots are available for the executor of Plan 02 to inspect and identify issues.
</success_criteria>

<output>
After completion, create `.planning/phases/07.2-mobile-experience-overhaul/07.2-01-SUMMARY.md`
</output>
