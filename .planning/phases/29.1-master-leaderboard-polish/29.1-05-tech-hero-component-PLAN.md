---
phase: 29.1-master-leaderboard-polish
plan: 05
type: execute
wave: 5
depends_on: ["29.1-04"]
files_modified:
  - src/components/tech/tech-hero.tsx
  - src/app/(tech)/tech/rankings/page.tsx
  - src/app/(tech)/tech/rankings/[slug]/page.tsx
  - src/app/(tech)/tech/categories/[slug]/page.tsx
  - tests/29.1-05-hero-sections.spec.ts
autonomous: false
requirements: [D-08, D-09, D-10, D-11]
must_haves:
  truths:
    - "TechHero component is exported from src/components/tech/tech-hero.tsx with props eyebrow, title, subhead, ctaLabel, ctaHref, ctaOpensInNewTab, tone."
    - "/tech/rankings hub renders TechHero with eyebrow='RANKINGS', title='Rankings', subhead per D-09, CTA 'Read methodology' → /tech/about#methodology, tone='cyan'."
    - "/tech/rankings/[slug] renders TechHero with eyebrow='RANKINGS', title='{Category} Rankings', CTA 'Read methodology' → /tech/about#methodology, tone='cyan'."
    - "/tech/categories/[slug] renders TechHero with eyebrow='CATEGORY', title='{Category}', CTA 'View Rankings' → /tech/rankings/[slug], tone='amber'."
    - "Hero h1 wraps in <GlitchHeading> — hover-only RGB-split, no auto-running animation."
  artifacts:
    - path: "src/components/tech/tech-hero.tsx"
      provides: "Reusable TechHero component derived from beats single-slide visual rhythm"
      exports: ["TechHero"]
    - path: "src/app/(tech)/tech/rankings/page.tsx"
      provides: "Hub page using TechHero (replaces the placeholder h1)"
    - path: "src/app/(tech)/tech/rankings/[slug]/page.tsx"
      provides: "Canonical leaderboard page using TechHero"
    - path: "src/app/(tech)/tech/categories/[slug]/page.tsx"
      provides: "Category page using TechHero with amber tone + 'View Rankings' CTA"
  key_links:
    - from: "src/app/(tech)/tech/rankings/page.tsx"
      to: "src/components/tech/tech-hero.tsx"
      via: "import { TechHero } + render"
      pattern: "<TechHero"
    - from: "src/app/(tech)/tech/rankings/[slug]/page.tsx"
      to: "src/components/tech/tech-hero.tsx"
      via: "import { TechHero } + render"
      pattern: "<TechHero"
    - from: "src/app/(tech)/tech/categories/[slug]/page.tsx"
      to: "src/components/tech/tech-hero.tsx"
      via: "import { TechHero } + render"
      pattern: "<TechHero"
---

<objective>
Extract a reusable `TechHero` component from the beats hero's single-slide visual rhythm (full-bleed dark layered-gradient bg, mono uppercase h1, sans subhead, single bordered CTA). Mount it on three surfaces: rankings hub, canonical leaderboard, and category detail page. Per RESEARCH §RQ-2, do NOT replicate the beats 3-slide Embla carousel — these surfaces are navigational, not promotional.

Purpose: Promote rankings + category pages to a unified editorial hero treatment. Hover-only RGB-split glitch on every h1 per memory.

Output: New `tech-hero.tsx` component. Three pages mount it (replacing placeholder h1s left by Plan 03 + the bare category-page h1). Playwright spec asserts h1, subhead, CTA href per surface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29.1-master-leaderboard-polish/29.1-CONTEXT.md
@.planning/phases/29.1-master-leaderboard-polish/29.1-RESEARCH.md

<interfaces>
<!-- Existing GlitchHeading component used for hover-only RGB-split h1s -->
From src/components/ui/glitch-heading.tsx (existing — verified by use across the site):
```tsx
export function GlitchHeading(props: { text: string; children: React.ReactNode; className?: string }): JSX.Element
```

<!-- Beats hero pattern source — read for the layered-gradient values + chrome -->
From src/components/beats/beats-hero-carousel.tsx:
- 3-slide Embla; each slide uses inline style={{ background: layered linear+radial gradients }}
- Slide chrome: mono h2, sans subhead, bordered CTA
- We replicate the SINGLE-SLIDE rhythm only — no carousel.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/components/tech/tech-hero.tsx</name>
  <files>src/components/tech/tech-hero.tsx</files>
  <read_first>
    - src/components/beats/beats-hero-carousel.tsx (the source pattern — copy its single-slide chrome verbatim, drop the carousel)
    - src/components/ui/glitch-heading.tsx (confirm GlitchHeading import path + props)
    - .planning/phases/29.1-master-leaderboard-polish/29.1-RESEARCH.md (§RQ-2 — verbatim TechHero JSX block)
  </read_first>
  <action>
    Create `src/components/tech/tech-hero.tsx` with the EXACT contents below (sourced verbatim from RESEARCH.md §RQ-2):

    ```tsx
    // src/components/tech/tech-hero.tsx
    // Phase 29.1 D-08 — derived from the beats-store hero (single slide rhythm).
    // Hover-only RGB-split glitch on h1 (memory: feedback_glitch_headers.md).
    // No auto-running animations.
    import Link from "next/link"
    import { GlitchHeading } from "@/components/ui/glitch-heading"

    export interface TechHeroProps {
      eyebrow?: string
      title: string
      subhead: string
      ctaLabel: string
      ctaHref: string
      ctaOpensInNewTab?: boolean
      tone?: "cyan" | "amber"
    }

    const GRADIENTS: Record<NonNullable<TechHeroProps["tone"]>, string> = {
      cyan: `
        radial-gradient(ellipse at 70% 50%, rgba(56,189,248,0.08) 0%, transparent 60%),
        repeating-linear-gradient(0deg, transparent, transparent 3px, rgba(56,189,248,0.03) 3px, rgba(56,189,248,0.03) 4px),
        repeating-linear-gradient(90deg, transparent, transparent 8px, rgba(56,189,248,0.02) 8px, rgba(56,189,248,0.02) 9px),
        linear-gradient(to right, #0a0a0a 0%, #0a0e14 50%, #0a0a0a 100%)
      `,
      amber: `
        radial-gradient(ellipse at 70% 50%, rgba(245,158,11,0.08) 0%, transparent 60%),
        repeating-linear-gradient(0deg, transparent, transparent 3px, rgba(245,158,11,0.03) 3px, rgba(245,158,11,0.03) 4px),
        repeating-linear-gradient(90deg, transparent, transparent 8px, rgba(245,158,11,0.02) 8px, rgba(245,158,11,0.02) 9px),
        linear-gradient(to right, #0a0a0a 0%, #140d0a 50%, #0a0a0a 100%)
      `,
    }

    export function TechHero({
      eyebrow,
      title,
      subhead,
      ctaLabel,
      ctaHref,
      ctaOpensInNewTab = false,
      tone = "cyan",
    }: TechHeroProps) {
      return (
        <div data-tech-hero data-tone={tone}>
          {eyebrow && (
            <span className="mb-2 block font-mono text-[11px] uppercase tracking-[0.15em] text-[#555]">
              {eyebrow}
            </span>
          )}
          <div
            className="relative h-[280px] w-full overflow-hidden"
            style={{ background: GRADIENTS[tone] }}
          >
            <div className="flex h-full flex-col items-center justify-center text-center px-6 md:px-10">
              <h1 className="font-mono text-[24px] font-bold uppercase tracking-[0.05em] text-[#f5f5f0] md:text-[40px]">
                <GlitchHeading text={title}>{title}</GlitchHeading>
              </h1>
              <p className="mt-3 max-w-2xl font-sans text-[13px] text-[#888] md:text-[15px]">
                {subhead}
              </p>
              <Link
                href={ctaHref}
                {...(ctaOpensInNewTab
                  ? { target: "_blank", rel: "noopener noreferrer" }
                  : {})}
                className="mt-5 w-fit border border-[#f5f5f0] px-6 py-2 font-mono text-[12px] uppercase tracking-[0.05em] text-[#f5f5f0] transition-colors hover:bg-[#f5f5f0] hover:text-[#000]"
              >
                {ctaLabel}
              </Link>
            </div>
          </div>
        </div>
      )
    }
    ```

    Notes:
    - The `data-tech-hero` and `data-tone` attributes are non-visual hooks for Playwright stability per RESEARCH §RQ-4.
    - No motion/animation imports, no `useEffect`. Static, server-renderable.
    - Brand: GlitchTech (no string literal here, but used elsewhere in file headers — keep consistent).
  </action>
  <verify>
    <automated>test -f src/components/tech/tech-hero.tsx &amp;&amp; grep -q "export function TechHero" src/components/tech/tech-hero.tsx &amp;&amp; grep -q "GlitchHeading" src/components/tech/tech-hero.tsx &amp;&amp; grep -q "data-tech-hero" src/components/tech/tech-hero.tsx &amp;&amp; pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/components/tech/tech-hero.tsx` exits 0
    - `grep -q "export function TechHero" src/components/tech/tech-hero.tsx` exits 0
    - `grep -q "GlitchHeading" src/components/tech/tech-hero.tsx` exits 0
    - `grep -q "data-tech-hero" src/components/tech/tech-hero.tsx` exits 0
    - `grep -q "h-\[280px\]" src/components/tech/tech-hero.tsx` exits 0
    - `! grep -q "useEffect\|useState\|framer-motion" src/components/tech/tech-hero.tsx` (no auto-running animations / state)
    - `pnpm tsc --noEmit` exits 0
    - `pnpm lint` exits 0
  </acceptance_criteria>
  <done>TechHero exists, exports the named component, no auto-running animation logic, h1 wraps in GlitchHeading.</done>
</task>

<task type="auto">
  <name>Task 2: Mount TechHero on /tech/rankings hub page</name>
  <files>src/app/(tech)/tech/rankings/page.tsx</files>
  <read_first>
    - src/app/(tech)/tech/rankings/page.tsx (created in Plan 03 — has placeholder comment + bare h1)
    - src/components/tech/tech-hero.tsx (just created)
    - .planning/phases/29.1-master-leaderboard-polish/29.1-CONTEXT.md (D-09 — exact copy)
  </read_first>
  <action>
    In `src/app/(tech)/tech/rankings/page.tsx`:

    1. Add the import: `import { TechHero } from "@/components/tech/tech-hero"`
    2. Remove (or replace) the placeholder comment block AND the bare `<h1>...<GlitchHeading>...</h1>` block AND the immediately-following `<p>...</p>` subhead from Plan 03.
    3. Insert the TechHero component above the `<section>` containing the category-tile grid:

       ```tsx
       <TechHero
         eyebrow="RANKINGS"
         title="Rankings"
         subhead="Every laptop, phone, and PC we've reviewed — ranked side-by-side by GlitchMark, BPR, and benchmark."
         ctaLabel="Read methodology"
         ctaHref="/tech/about#methodology"
         tone="cyan"
       />
       ```

    4. Remove the standalone `import { GlitchHeading } from "@/components/ui/glitch-heading"` line if it's no longer used after the h1 is replaced (the TechHero internally imports GlitchHeading).
  </action>
  <verify>
    <automated>grep -q "TechHero" "src/app/(tech)/tech/rankings/page.tsx" &amp;&amp; grep -q 'eyebrow="RANKINGS"' "src/app/(tech)/tech/rankings/page.tsx" &amp;&amp; grep -q 'tone="cyan"' "src/app/(tech)/tech/rankings/page.tsx" &amp;&amp; grep -q '/tech/about#methodology' "src/app/(tech)/tech/rankings/page.tsx" &amp;&amp; pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "TechHero" "src/app/(tech)/tech/rankings/page.tsx"` exits 0
    - `grep -q 'eyebrow="RANKINGS"' "src/app/(tech)/tech/rankings/page.tsx"` exits 0
    - `grep -q 'tone="cyan"' "src/app/(tech)/tech/rankings/page.tsx"` exits 0
    - `grep -q '/tech/about#methodology' "src/app/(tech)/tech/rankings/page.tsx"` exits 0
    - `grep -q "Read methodology" "src/app/(tech)/tech/rankings/page.tsx"` exits 0
    - `pnpm tsc --noEmit` exits 0
    - `pnpm lint` exits 0
  </acceptance_criteria>
  <done>Visiting `/tech/rankings` shows the TechHero with cyan tone, eyebrow RANKINGS, h1 "Rankings", and CTA "Read methodology" → `/tech/about#methodology`.</done>
</task>

<task type="auto">
  <name>Task 3: Mount TechHero on /tech/rankings/[slug]</name>
  <files>src/app/(tech)/tech/rankings/[slug]/page.tsx</files>
  <read_first>
    - src/app/(tech)/tech/rankings/[slug]/page.tsx (created in Plan 03 — has placeholder comment + GlitchHeading h1)
    - src/components/tech/tech-hero.tsx
    - .planning/phases/29.1-master-leaderboard-polish/29.1-CONTEXT.md (D-09 — exact copy)
  </read_first>
  <action>
    In `src/app/(tech)/tech/rankings/[slug]/page.tsx`:

    1. Add import: `import { TechHero } from "@/components/tech/tech-hero"`
    2. Remove the placeholder comment + bare h1 left by Plan 03.
    3. Insert at the top of the page body (above the breadcrumb / leaderboard table):

       ```tsx
       <TechHero
         eyebrow="RANKINGS"
         title={`${category.name} Rankings`}
         subhead={`Compare every ${category.name.toLowerCase()} we've reviewed — sortable by GlitchMark, BPR, and key benchmarks.`}
         ctaLabel="Read methodology"
         ctaHref="/tech/about#methodology"
         tone="cyan"
       />
       ```

    4. Remove now-unused GlitchHeading import if it's no longer used elsewhere in the file.
  </action>
  <verify>
    <automated>grep -q "TechHero" "src/app/(tech)/tech/rankings/[slug]/page.tsx" &amp;&amp; grep -q 'tone="cyan"' "src/app/(tech)/tech/rankings/[slug]/page.tsx" &amp;&amp; grep -q "category.name.*Rankings" "src/app/(tech)/tech/rankings/[slug]/page.tsx" &amp;&amp; grep -q '/tech/about#methodology' "src/app/(tech)/tech/rankings/[slug]/page.tsx" &amp;&amp; pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "TechHero" "src/app/(tech)/tech/rankings/[slug]/page.tsx"` exits 0
    - `grep -q 'tone="cyan"' "src/app/(tech)/tech/rankings/[slug]/page.tsx"` exits 0
    - `grep -q "category.name" "src/app/(tech)/tech/rankings/[slug]/page.tsx"` exits 0 (title is interpolated)
    - `grep -q '/tech/about#methodology' "src/app/(tech)/tech/rankings/[slug]/page.tsx"` exits 0
    - `pnpm tsc --noEmit` exits 0
    - `pnpm lint` exits 0
  </acceptance_criteria>
  <done>Visiting `/tech/rankings/laptops` shows TechHero with title "Laptops Rankings" + cyan tone + Read-methodology CTA.</done>
</task>

<task type="auto">
  <name>Task 4: Mount TechHero on /tech/categories/[slug] with amber tone + View Rankings CTA</name>
  <files>src/app/(tech)/tech/categories/[slug]/page.tsx</files>
  <read_first>
    - src/app/(tech)/tech/categories/[slug]/page.tsx (after Plan 03 it has the updated CTA href; this plan replaces the chrome with TechHero)
    - src/components/tech/tech-hero.tsx
    - .planning/phases/29.1-master-leaderboard-polish/29.1-CONTEXT.md (D-09 — exact copy + tone="amber")
    - .planning/phases/29.1-master-leaderboard-polish/29.1-RESEARCH.md (§RQ-2 — amber tone for category)
  </read_first>
  <action>
    In `src/app/(tech)/tech/categories/[slug]/page.tsx`:

    1. Add import: `import { TechHero } from "@/components/tech/tech-hero"` (if not already present)
    2. Locate the existing page chrome (the bare h1 "{category.name}" + the original "View Rankings" CTA you updated in Plan 03). Replace BOTH with a single `<TechHero>`:

       ```tsx
       <TechHero
         eyebrow="CATEGORY"
         title={category.name}
         subhead={`Browse our ${category.name.toLowerCase()} reviews.`}
         ctaLabel="View Rankings"
         ctaHref={`/tech/rankings/${slug}`}
         tone="amber"
       />
       ```

    3. The page may also have downstream chrome (e.g., a child-category list, product cards). Leave those untouched — they are out of scope for 29.1 (Phase 31 will reframe the category page editorially).

    4. The `/tech/rankings/${slug}` href ensures clicking the CTA goes direct (no redirect hop), confirming Plan 03's CTA-update intent in the new format.
  </action>
  <verify>
    <automated>grep -q "TechHero" "src/app/(tech)/tech/categories/[slug]/page.tsx" &amp;&amp; grep -q 'tone="amber"' "src/app/(tech)/tech/categories/[slug]/page.tsx" &amp;&amp; grep -q "View Rankings" "src/app/(tech)/tech/categories/[slug]/page.tsx" &amp;&amp; grep -q "/tech/rankings/" "src/app/(tech)/tech/categories/[slug]/page.tsx" &amp;&amp; pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "TechHero" "src/app/(tech)/tech/categories/[slug]/page.tsx"` exits 0
    - `grep -q 'tone="amber"' "src/app/(tech)/tech/categories/[slug]/page.tsx"` exits 0
    - `grep -q '"View Rankings"' "src/app/(tech)/tech/categories/[slug]/page.tsx"` exits 0
    - `grep -q "/tech/rankings/" "src/app/(tech)/tech/categories/[slug]/page.tsx"` exits 0
    - `pnpm tsc --noEmit` exits 0
    - `pnpm lint` exits 0
  </acceptance_criteria>
  <done>Visiting `/tech/categories/laptops` shows TechHero (amber tone, eyebrow CATEGORY, h1 "Laptops", CTA "View Rankings" → `/tech/rankings/laptops`).</done>
</task>

<task type="auto">
  <name>Task 5: Playwright spec — heroes per surface (h1, subhead, CTA href, no auto-animation)</name>
  <files>tests/29.1-05-hero-sections.spec.ts</files>
  <read_first>
    - .planning/phases/29.1-master-leaderboard-polish/29.1-RESEARCH.md (§Verification Strategy plan-5 row)
    - tests/29.1-03-rankings-routes.spec.ts (match describe + setTimeout pattern)
  </read_first>
  <action>
    Create `tests/29.1-05-hero-sections.spec.ts`:

    ```ts
    import { test, expect } from "@playwright/test"

    test.describe("29.1-05 — TechHero per surface (D-08/D-09/D-10/D-11)", () => {
      test.setTimeout(30_000)

      test("/tech/rankings hub hero — RANKINGS / Rankings / Read methodology / cyan", async ({ page }) => {
        await page.goto("/tech/rankings")
        const hero = page.locator('[data-tech-hero]').first()
        await expect(hero).toBeVisible({ timeout: 5_000 })
        await expect(hero).toHaveAttribute("data-tone", "cyan")
        await expect(hero).toContainText("RANKINGS")
        await expect(
          page.getByRole("heading", { level: 1, name: /^Rankings$/i }),
        ).toBeVisible()
        const cta = hero.locator('a[href="/tech/about#methodology"]')
        await expect(cta).toBeVisible()
        await expect(cta).toContainText(/Read methodology/i)
      })

      test("/tech/rankings/laptops leaderboard hero — RANKINGS / Laptops Rankings / Read methodology / cyan", async ({ page }) => {
        await page.goto("/tech/rankings/laptops")
        const hero = page.locator('[data-tech-hero]').first()
        await expect(hero).toBeVisible({ timeout: 5_000 })
        await expect(hero).toHaveAttribute("data-tone", "cyan")
        await expect(
          page.getByRole("heading", { level: 1, name: /Laptops Rankings/i }),
        ).toBeVisible()
        const cta = hero.locator('a[href="/tech/about#methodology"]')
        await expect(cta).toBeVisible()
      })

      test("/tech/categories/laptops hero — CATEGORY / Laptops / View Rankings / amber", async ({ page }) => {
        await page.goto("/tech/categories/laptops")
        const hero = page.locator('[data-tech-hero]').first()
        await expect(hero).toBeVisible({ timeout: 5_000 })
        await expect(hero).toHaveAttribute("data-tone", "amber")
        await expect(hero).toContainText("CATEGORY")
        await expect(
          page.getByRole("heading", { level: 1, name: /^Laptops$/i }),
        ).toBeVisible()
        const cta = hero.locator('a[href="/tech/rankings/laptops"]')
        await expect(cta).toBeVisible()
        await expect(cta).toContainText(/View Rankings/i)
      })

      test("hero h1 has NO auto-running animation (hover-only glitch rule)", async ({ page }) => {
        await page.goto("/tech/rankings")
        const h1 = page.getByRole("heading", { level: 1, name: /^Rankings$/i })
        await expect(h1).toBeVisible()
        // Sample the computed animation-name twice with a delay; if anything
        // is auto-running, the value would differ between samples or be non-"none".
        const sample1 = await h1.evaluate((el) => getComputedStyle(el).animationName)
        await page.waitForTimeout(500)
        const sample2 = await h1.evaluate((el) => getComputedStyle(el).animationName)
        expect(sample1).toBe(sample2)
        // GlitchHeading uses CSS transitions on hover — the base h1 element
        // should not carry an active animation-name. "none" is the contract.
        // (If the implementation wraps in a span with animations, this check
        // applies to the outer h1; adjust if needed by reading GlitchHeading.)
        expect(sample1).toBe("none")
      })
    })
    ```

    If the `animation-name === "none"` assertion fails because GlitchHeading uses a different animation primitive (e.g., transitions only — which is correct per the memory), the assertion already passes (transitions don't appear in `animation-name`). If it fails because the implementation actually does run an idle animation, that's a memory violation that needs fixing in `GlitchHeading` itself — log the finding in the SUMMARY and either weaken the assertion to "no `infinite` keyword" or fix GlitchHeading. Default expectation: the spec passes as written.
  </action>
  <verify>
    <automated>test -f tests/29.1-05-hero-sections.spec.ts &amp;&amp; pnpm exec playwright test tests/29.1-05-hero-sections.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/29.1-05-hero-sections.spec.ts` exits 0
    - Dev server is running on `http://localhost:3004`
    - `pnpm exec playwright test tests/29.1-05-hero-sections.spec.ts` exits 0
    - `pnpm tsc --noEmit` exits 0
    - `pnpm lint` exits 0
  </acceptance_criteria>
  <done>All four spec assertions pass: hub hero, leaderboard hero, category hero, no-auto-animation rule.</done>
</task>

</tasks>

<verification>
- TechHero exists. Three pages mount it. Each surface uses the right tone, eyebrow, CTA href.
- Playwright asserts h1 + CTA href for each of the 3 surfaces, plus the no-auto-animation rule.
</verification>

<success_criteria>
Three rankings/category surfaces show a unified hero treatment derived from the beats single-slide rhythm. h1 uses GlitchHeading. No auto-running animations.
</success_criteria>

<output>
After completion, create `.planning/phases/29.1-master-leaderboard-polish/29.1-05-SUMMARY.md`.
</output>
