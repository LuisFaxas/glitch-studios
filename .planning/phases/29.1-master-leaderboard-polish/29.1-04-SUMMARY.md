---
phase: 29.1-master-leaderboard-polish
plan: 04
status: complete
---

## What was built

GlitchTech sidebar reordered to D-01 spec. Rankings discoverable from
desktop tile-nav (position 2) and mobile overlay menu (first item).
About reachable from both surfaces (last item). Bottom-tab capacity
preserved (4 items unchanged).

Stable Playwright hooks: `data-mobile-menu-trigger` on the bottom-tab
menu open button; `data-mobile-overlay` on the overlay dialog panel.

## Key files

- modified: src/components/layout/tech-nav-config.ts (+Rankings, +About;
  techMobileMenuItems reordered with Rankings first, About last)
- modified: src/components/layout/bottom-tab-bar.tsx (data-mobile-menu-trigger)
- modified: src/components/layout/mobile-nav-overlay.tsx (data-mobile-overlay)
- created: tests/29.1-04-nav-order.spec.ts (2 cases × desktop+mobile = 4)

## Verification

- `pnpm tsc --noEmit` → clean
- `pnpm exec playwright test tests/29.1-04-nav-order.spec.ts` → 4/4 pass
- Manual curl confirmed aria-labels render in D-01 order.

## Notes

- Spec uses `aria-label` to enumerate sidebar links (works in both
  collapsed and expanded sidebar states; collapsed mode hides text labels
  but keeps aria-label set unconditionally).
- Trophy icon for Rankings, Info icon for About — both already in
  lucide-react and used elsewhere in the codebase.
