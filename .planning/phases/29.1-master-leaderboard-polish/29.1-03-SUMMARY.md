---
phase: 29.1-master-leaderboard-polish
plan: 03
status: complete
---

## What was built

Canonical Rankings URL structure live: `/tech/rankings` (hub) and
`/tech/rankings/[slug]` (per-category leaderboard). Legacy
`/tech/categories/[slug]/rankings` is now a permanent redirect to the new
canonical. CTA on `/tech/categories/[slug]` goes direct (no hop).

## Key files

- created: src/app/(tech)/tech/rankings/page.tsx (hub)
- created: src/app/(tech)/tech/rankings/[slug]/page.tsx (canonical leaderboard)
- modified: src/app/(tech)/tech/categories/[slug]/rankings/page.tsx (now a permanentRedirect)
- modified: src/app/(tech)/tech/categories/[slug]/page.tsx (View Rankings CTA goes direct)
- modified: src/lib/tech/queries.ts (added getRankingsHubCategories)
- created: tests/29.1-03-rankings-routes.spec.ts (4 cases, desktop)

## Verification

- `pnpm tsc --noEmit` → clean
- `pnpm exec playwright test tests/29.1-03-rankings-routes.spec.ts --project=desktop` → 4/4 pass
- Manual curl: hub 200, canonical 200, legacy redirects to canonical

## Notes

- Hub tile presence is conditional on having ≥1 published review per
  level-1 category (D-05). Dev DB has 7 products but 0 published reviews,
  so the hub renders the "More categories coming soon" fallback only.
  In production, Laptops will tile.
- Canonical page uses `max-w-[1600px]` per RESEARCH §RQ-3 to give plan 06
  filter bar + plan 07 wide tables breathing room.
- Plan 05 hero placeholder comment is left at the top of both new pages so
  plan 05 can drop in the new TechHero component without re-finding the
  insertion point.