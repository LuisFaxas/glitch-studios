---
phase: 29.1-master-leaderboard-polish
plan: 03
type: execute
wave: 3
depends_on: ["29.1-02"]
files_modified:
  - src/app/(tech)/tech/rankings/page.tsx
  - src/app/(tech)/tech/rankings/[slug]/page.tsx
  - src/app/(tech)/tech/categories/[slug]/rankings/page.tsx
  - src/app/(tech)/tech/categories/[slug]/page.tsx
  - src/lib/tech/queries.ts
  - tests/29.1-03-rankings-routes.spec.ts
autonomous: false
requirements: [D-04, D-05, D-06, D-07]
must_haves:
  truths:
    - "/tech/rankings hub page renders one tile per level=1 category that has at least one published review (today: Laptops only) plus a 'More categories coming soon' message."
    - "/tech/rankings/laptops renders the same leaderboard data as the Phase 29 /tech/categories/laptops/rankings page did."
    - "/tech/categories/laptops/rankings issues a 301/308 redirect to /tech/rankings/laptops."
    - "The 'View Rankings' CTA on /tech/categories/[slug]/page.tsx points at /tech/rankings/[slug]."
  artifacts:
    - path: "src/app/(tech)/tech/rankings/page.tsx"
      provides: "Hub page listing categories that have rankings"
    - path: "src/app/(tech)/tech/rankings/[slug]/page.tsx"
      provides: "Canonical leaderboard route — clones Phase 29 leaderboard contents"
    - path: "src/app/(tech)/tech/categories/[slug]/rankings/page.tsx"
      provides: "Permanent redirect from legacy URL to new canonical"
    - path: "src/lib/tech/queries.ts"
      provides: "getRankingsHubCategories() helper added"
      exports: ["getRankingsHubCategories"]
    - path: "tests/29.1-03-rankings-routes.spec.ts"
      provides: "Spec asserting hub renders, canonical renders, legacy redirects, CTA href correct"
  key_links:
    - from: "src/app/(tech)/tech/categories/[slug]/page.tsx"
      to: "/tech/rankings/[slug]"
      via: "View Rankings CTA Link href"
      pattern: "/tech/rankings/"
    - from: "src/app/(tech)/tech/categories/[slug]/rankings/page.tsx"
      to: "/tech/rankings/[slug]"
      via: "permanentRedirect call"
      pattern: "permanentRedirect.*tech/rankings/"
---

<objective>
Ship the new canonical rankings URL structure: `/tech/rankings` (hub) and `/tech/rankings/[slug]` (per-category leaderboard). Redirect the legacy `/tech/categories/[slug]/rankings` permanently to the new canonical URL. Update the `/tech/categories/[slug]` "View Rankings" CTA to point at the new canonical.

Purpose: Position Rankings as a top-level discovery surface. Preserve external links via 308 redirect.

Output: Two new routes (hub + canonical), one redirect file replacing the old route, one CTA href update, one new query helper, Playwright spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29.1-master-leaderboard-polish/29.1-CONTEXT.md
@.planning/phases/29.1-master-leaderboard-polish/29.1-RESEARCH.md

<interfaces>
From src/lib/tech/leaderboard.ts:
```ts
export async function getLeaderboardRows(categorySlug: string): Promise<LeaderboardRow[]>
export async function getLeaderboardBenchmarkColumns(categorySlug: string): Promise<BenchmarkColumn[]>
```

From src/lib/tech/queries.ts (existing):
```ts
export async function getCategoryBySlug(slug: string): Promise<TechCategory | null>
export async function getCategoryBreadcrumb(slug: string): Promise<TechCategory[]>
```

From src/components/tech/leaderboard-table.tsx:
```ts
export function LeaderboardTable(props: { rows: LeaderboardRow[]; benchmarkColumns: BenchmarkColumn[]; ... }): JSX.Element
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create canonical /tech/rankings/[slug]/page.tsx by porting Phase 29 leaderboard body</name>
  <files>src/app/(tech)/tech/rankings/[slug]/page.tsx</files>
  <read_first>
    - src/app/(tech)/tech/categories/[slug]/rankings/page.tsx (full file — this is the source we're cloning)
    - src/lib/tech/leaderboard.ts (confirm getLeaderboardRows + getLeaderboardBenchmarkColumns signatures)
    - src/lib/tech/queries.ts (confirm getCategoryBySlug, getCategoryBreadcrumb signatures)
    - src/components/tech/leaderboard-table.tsx (confirm props signature)
    - .planning/phases/29.1-master-leaderboard-polish/29.1-RESEARCH.md (§RQ-3)
  </read_first>
  <action>
    Create directory `src/app/(tech)/tech/rankings/[slug]/`. Inside, create `page.tsx` containing a near-verbatim copy of the body of `src/app/(tech)/tech/categories/[slug]/rankings/page.tsx`, with these specific changes:

    1. Page wrapper class: change `mx-auto max-w-[1400px] px-4 py-8 md:py-12` to `mx-auto max-w-[1600px] px-4 py-8 md:py-12` per RESEARCH §RQ-3.

    2. Above the leaderboard, leave a clearly marked HTML comment placeholder for the hero:
       ```tsx
       {/* Phase 29.1 Plan 05 will replace this h1 with <TechHero eyebrow="RANKINGS" title={`${category.name} Rankings`} subhead={`Compare every ${category.name.toLowerCase()} we've reviewed — sortable by GlitchMark, BPR, and key benchmarks.`} ctaLabel="Read methodology" ctaHref="/tech/about#methodology" tone="cyan" /> */}
       <h1 className="font-mono text-[44px] font-bold uppercase leading-none md:text-[64px]">
         <GlitchHeading text={`${category.name.toUpperCase()} RANKINGS`}>{`${category.name.toUpperCase()} RANKINGS`}</GlitchHeading>
       </h1>
       ```
       (If the source page already has an h1, replace it with the wrapped GlitchHeading version above — the bare h1 in Phase 29 needs the GlitchHeading wrap regardless because of the site-wide hover-only-glitch rule.)

    3. Imports: keep all imports from the source intact. ADD `import { GlitchHeading } from "@/components/ui/glitch-heading"` if not already present.

    4. Metadata: ensure `export async function generateMetadata({ params })` returns `{ title: \`${category.name} Rankings — GlitchTech\`, description: ... }`. Brand spelling MUST be "GlitchTech" (NOT GlitchTek).

    5. Param shape: Next 16 uses `{ params: Promise<{ slug: string }> }`. Match the source file's pattern exactly.

    6. `notFound()` from `next/navigation` when `getCategoryBySlug(slug)` returns null. Match source.

    7. Do NOT modify Phase 29's `LeaderboardTable` component or any other shared component — Plans 06/07/08/09 land those edits.
  </action>
  <verify>
    <automated>test -f "src/app/(tech)/tech/rankings/[slug]/page.tsx" &amp;&amp; grep -q "getLeaderboardRows" "src/app/(tech)/tech/rankings/[slug]/page.tsx" &amp;&amp; grep -q "max-w-\[1600px\]" "src/app/(tech)/tech/rankings/[slug]/page.tsx" &amp;&amp; grep -q "GlitchHeading" "src/app/(tech)/tech/rankings/[slug]/page.tsx" &amp;&amp; ! grep -q "GlitchTek" "src/app/(tech)/tech/rankings/[slug]/page.tsx" &amp;&amp; pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `test -f "src/app/(tech)/tech/rankings/[slug]/page.tsx"` exits 0
    - `grep -q "getLeaderboardRows" "src/app/(tech)/tech/rankings/[slug]/page.tsx"` exits 0
    - `grep -q "max-w-\[1600px\]" "src/app/(tech)/tech/rankings/[slug]/page.tsx"` exits 0
    - `grep -q "GlitchHeading" "src/app/(tech)/tech/rankings/[slug]/page.tsx"` exits 0
    - `! grep -q "GlitchTek" "src/app/(tech)/tech/rankings/[slug]/page.tsx"` (brand spelling)
    - `pnpm tsc --noEmit` exits 0
    - `pnpm lint` exits 0
  </acceptance_criteria>
  <done>Visiting `/tech/rankings/laptops` renders the same leaderboard rows as Phase 29's `/tech/categories/laptops/rankings`.</done>
</task>

<task type="auto">
  <name>Task 2: Add getRankingsHubCategories helper to queries.ts</name>
  <files>src/lib/tech/queries.ts</files>
  <read_first>
    - src/lib/tech/queries.ts (full file — find existing list-categories helpers, db import, eq/and operators, schema imports)
    - src/db/schema.ts (confirm techCategories.level column + tech_reviews.published column + categoryId FK)
  </read_first>
  <action>
    Append (or insert near other `list*Categories` helpers) the following export to `src/lib/tech/queries.ts`:

    ```ts
    /**
     * Phase 29.1 D-05 — list level-1 tech categories that have at least one
     * PUBLISHED tech_review. Used by the /tech/rankings hub page to render
     * one tile per category. Today this returns ["Laptops"]; future categories
     * auto-tile once they get their first published review.
     */
    export async function getRankingsHubCategories() {
      // Inspect existing query patterns in this file and choose the matching idiom.
      // A correct Drizzle implementation:
      //   const rows = await db
      //     .select()
      //     .from(techCategories)
      //     .where(
      //       and(
      //         eq(techCategories.level, 1),
      //         exists(
      //           db
      //             .select({ one: sql`1` })
      //             .from(techReviews)
      //             .where(
      //               and(
      //                 eq(techReviews.categoryId, techCategories.id),
      //                 eq(techReviews.published, true),
      //               ),
      //             ),
      //         ),
      //       ),
      //     )
      //     .orderBy(techCategories.sortOrder, techCategories.name)
      //   return rows
      //
      // Wrap with `unstable_cache` if other helpers in this file are wrapped — match the convention.
    }
    ```

    Implementation rules:
    - Use the SAME `db` import, the SAME schema imports, and the SAME drizzle helpers (`and`, `eq`, `exists`, `sql`) already used in this file.
    - If `unstable_cache` wraps other read helpers in this file, wrap this one with `tags: ["leaderboard"]` (Phase 29 tag) so it invalidates when reviews publish/unpublish.
    - Return type can be inferred from the query result (Drizzle's typed result shape is fine — no manual type annotation required).
    - If a function with this exact behavior already exists under a different name (e.g., `listLevel1CategoriesWithReviews`), simply re-export an alias:
      ```ts
      export { listLevel1CategoriesWithReviews as getRankingsHubCategories } from "..."
      ```
  </action>
  <verify>
    <automated>grep -q "export.*getRankingsHubCategories" src/lib/tech/queries.ts &amp;&amp; pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export.*getRankingsHubCategories" src/lib/tech/queries.ts` exits 0
    - `pnpm tsc --noEmit` exits 0
    - `pnpm lint` exits 0
  </acceptance_criteria>
  <done>Helper exported. Calling it returns the level=1 categories that have at least one published review.</done>
</task>

<task type="auto">
  <name>Task 3: Create /tech/rankings/page.tsx hub</name>
  <files>src/app/(tech)/tech/rankings/page.tsx</files>
  <read_first>
    - src/components/tech/category-tile.tsx (confirm export name + props)
    - src/lib/tech/queries.ts (confirm getRankingsHubCategories was just added)
    - .planning/phases/29.1-master-leaderboard-polish/29.1-CONTEXT.md (D-05)
  </read_first>
  <action>
    Create `src/app/(tech)/tech/rankings/page.tsx` with:

    ```tsx
    import type { Metadata } from "next"
    import Link from "next/link"
    import { GlitchHeading } from "@/components/ui/glitch-heading"
    import { CategoryTile } from "@/components/tech/category-tile"
    import { getRankingsHubCategories } from "@/lib/tech/queries"

    export const dynamic = "force-static"
    export const revalidate = 3600

    export const metadata: Metadata = {
      title: "Rankings — GlitchTech",
      description:
        "Every laptop, phone, and PC we've reviewed — ranked side-by-side by GlitchMark, BPR, and benchmark.",
    }

    export default async function TechRankingsHubPage() {
      const categories = await getRankingsHubCategories()

      return (
        <main className="min-h-screen bg-black text-[#f5f5f0]">
          <div className="mx-auto max-w-[1600px] px-4 py-8 md:py-12">
            {/* Phase 29.1 Plan 05 will replace this h1 with <TechHero eyebrow="RANKINGS" title="Rankings" subhead="Every laptop, phone, and PC we've reviewed — ranked side-by-side by GlitchMark, BPR, and benchmark." ctaLabel="Read methodology" ctaHref="/tech/about#methodology" tone="cyan" /> */}
            <h1 className="font-mono text-[44px] font-bold uppercase leading-none md:text-[64px]">
              <GlitchHeading text="RANKINGS">RANKINGS</GlitchHeading>
            </h1>
            <p className="mt-4 max-w-2xl font-sans text-sm text-[#888] md:text-base">
              Every laptop, phone, and PC we&apos;ve reviewed — ranked side-by-side by GlitchMark, BPR, and benchmark.
            </p>

            <section className="mt-10 grid grid-cols-1 gap-4 md:grid-cols-2 lg:grid-cols-3">
              {categories.map((cat) => (
                <Link key={cat.slug} href={`/tech/rankings/${cat.slug}`} className="block">
                  <CategoryTile category={cat} />
                </Link>
              ))}
            </section>

            <p className="mt-8 font-mono text-[11px] uppercase tracking-[0.1em] text-[#888]">
              More categories coming soon.
            </p>
          </div>
        </main>
      )
    }
    ```

    If `CategoryTile`'s prop name is something other than `category` (read the file to confirm), pass the correctly-named prop. If `CategoryTile` does not accept a Link wrap (e.g., it renders its own internal `<Link>`), drop the outer `<Link>` and pass the href via prop. Inspect the component before writing.

    Brand: GlitchTech. h1 wrapped in `<GlitchHeading>` per memory.
  </action>
  <verify>
    <automated>test -f "src/app/(tech)/tech/rankings/page.tsx" &amp;&amp; grep -q "getRankingsHubCategories" "src/app/(tech)/tech/rankings/page.tsx" &amp;&amp; grep -q "More categories coming soon" "src/app/(tech)/tech/rankings/page.tsx" &amp;&amp; grep -q "GlitchHeading" "src/app/(tech)/tech/rankings/page.tsx" &amp;&amp; ! grep -q "GlitchTek" "src/app/(tech)/tech/rankings/page.tsx" &amp;&amp; pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `test -f "src/app/(tech)/tech/rankings/page.tsx"` exits 0
    - `grep -q "getRankingsHubCategories" "src/app/(tech)/tech/rankings/page.tsx"` exits 0
    - `grep -q "More categories coming soon" "src/app/(tech)/tech/rankings/page.tsx"` exits 0
    - `grep -q "GlitchHeading" "src/app/(tech)/tech/rankings/page.tsx"` exits 0
    - `! grep -q "GlitchTek" "src/app/(tech)/tech/rankings/page.tsx"`
    - `pnpm tsc --noEmit` exits 0
    - `pnpm lint` exits 0
  </acceptance_criteria>
  <done>Hub page renders. Visiting `/tech/rankings` lists at least one Laptops tile + the "More categories coming soon" line.</done>
</task>

<task type="auto">
  <name>Task 4: Convert legacy /tech/categories/[slug]/rankings/page.tsx into a permanentRedirect</name>
  <files>src/app/(tech)/tech/categories/[slug]/rankings/page.tsx</files>
  <read_first>
    - src/app/(tech)/tech/categories/[slug]/rankings/page.tsx (current Phase 29 page — entirely replaced)
    - .planning/phases/29.1-master-leaderboard-polish/29.1-RESEARCH.md (§RQ-1 — verbatim redirect snippet)
  </read_first>
  <action>
    Replace the entire contents of `src/app/(tech)/tech/categories/[slug]/rankings/page.tsx` with:

    ```tsx
    // Phase 29.1 — replaces the original Phase 29 implementation with a permanent redirect.
    // New canonical URL is /tech/rankings/[slug] (D-04/D-06).
    import { permanentRedirect } from "next/navigation"

    interface PageProps {
      params: Promise<{ slug: string }>
    }

    export default async function LegacyRankingsRedirect({ params }: PageProps) {
      const { slug } = await params
      permanentRedirect(`/tech/rankings/${slug}`)
    }
    ```
  </action>
  <verify>
    <automated>grep -q "permanentRedirect" "src/app/(tech)/tech/categories/[slug]/rankings/page.tsx" &amp;&amp; grep -q "tech/rankings" "src/app/(tech)/tech/categories/[slug]/rankings/page.tsx" &amp;&amp; ! grep -q "LeaderboardTable\|getLeaderboardRows" "src/app/(tech)/tech/categories/[slug]/rankings/page.tsx" &amp;&amp; pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "permanentRedirect" "src/app/(tech)/tech/categories/[slug]/rankings/page.tsx"` exits 0
    - `grep -q 'permanentRedirect.*\`/tech/rankings/' "src/app/(tech)/tech/categories/[slug]/rankings/page.tsx"` matches (redirect target uses `/tech/rankings/${slug}`)
    - `! grep -q "LeaderboardTable\|getLeaderboardRows" "src/app/(tech)/tech/categories/[slug]/rankings/page.tsx"` (the file is no longer rendering the leaderboard)
    - File is under 25 lines
    - `pnpm tsc --noEmit` exits 0
    - `pnpm lint` exits 0
  </acceptance_criteria>
  <done>Visiting `/tech/categories/laptops/rankings` issues a 301/308 to `/tech/rankings/laptops`.</done>
</task>

<task type="auto">
  <name>Task 5: Update /tech/categories/[slug]/page.tsx "View Rankings" CTA href</name>
  <files>src/app/(tech)/tech/categories/[slug]/page.tsx</files>
  <read_first>
    - src/app/(tech)/tech/categories/[slug]/page.tsx (full file — find the "View Rankings" CTA)
    - .planning/phases/29.1-master-leaderboard-polish/29.1-CONTEXT.md (D-07)
  </read_first>
  <action>
    Locate every Link/anchor in `src/app/(tech)/tech/categories/[slug]/page.tsx` whose text matches "View Rankings" (or whose href interpolates the rankings path). Update the href so it points at the new canonical:

    Replace any href of the form:
       `\`/tech/categories/${slug}/rankings\`` (or `"/tech/categories/${slug}/rankings"`)
    with:
       `\`/tech/rankings/${slug}\``

    Verify there are no remaining `/tech/categories/.../rankings` href patterns in this file.

    Do NOT add the hero/TechHero here — Plan 05 lands the hero on this page. This task ONLY changes the CTA href.
  </action>
  <verify>
    <automated>grep -q "/tech/rankings/" "src/app/(tech)/tech/categories/[slug]/page.tsx" &amp;&amp; ! grep -q "/tech/categories/.*/rankings" "src/app/(tech)/tech/categories/[slug]/page.tsx" &amp;&amp; pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "/tech/rankings/" "src/app/(tech)/tech/categories/[slug]/page.tsx"` exits 0
    - `! grep -q "/tech/categories/.*/rankings" "src/app/(tech)/tech/categories/[slug]/page.tsx"` (no remaining references to old path)
    - `pnpm tsc --noEmit` exits 0
    - `pnpm lint` exits 0
  </acceptance_criteria>
  <done>Click on "View Rankings" from `/tech/categories/laptops` lands on `/tech/rankings/laptops` directly (no redirect hop).</done>
</task>

<task type="auto">
  <name>Task 6: Playwright spec — hub renders, canonical renders, legacy redirects, CTA correct</name>
  <files>tests/29.1-03-rankings-routes.spec.ts</files>
  <read_first>
    - .planning/phases/29.1-master-leaderboard-polish/29.1-RESEARCH.md (§Verification Strategy plan-3 row)
    - tests/29.1-01-tech-nav-active-state.spec.ts (match describe + setTimeout pattern)
  </read_first>
  <action>
    Create `tests/29.1-03-rankings-routes.spec.ts`:

    ```ts
    import { test, expect } from "@playwright/test"

    test.describe("29.1-03 — Rankings routes + redirect (D-04/D-05/D-06/D-07)", () => {
      test.setTimeout(30_000)

      test("/tech/rankings hub renders Laptops tile + 'More categories coming soon'", async ({ page }) => {
        await page.goto("/tech/rankings")
        await expect(
          page.getByRole("heading", { level: 1, name: /^RANKINGS$/i }),
        ).toBeVisible({ timeout: 5_000 })
        await expect(page.getByText(/More categories coming soon/i)).toBeVisible()
        // At least one tile linking to /tech/rankings/laptops should exist.
        await expect(
          page.locator('a[href="/tech/rankings/laptops"]').first(),
        ).toBeVisible()
      })

      test("/tech/rankings/laptops renders the leaderboard table (or its empty state)", async ({ page }) => {
        await page.goto("/tech/rankings/laptops")
        await expect(
          page.getByRole("heading", { level: 1, name: /LAPTOPS RANKINGS/i }),
        ).toBeVisible({ timeout: 5_000 })
        // Phase 29 leaderboard either renders a <table> or the LeaderboardEmptyState.
        // Either is acceptable for this gate.
        const tableOrEmpty = page.locator('table, [data-empty-state]')
        await expect(tableOrEmpty.first()).toBeVisible({ timeout: 5_000 })
      })

      test("/tech/categories/laptops/rankings issues a permanent redirect to /tech/rankings/laptops", async ({ page }) => {
        const response = await page.goto("/tech/categories/laptops/rankings", {
          waitUntil: "domcontentloaded",
        })
        expect([301, 308]).toContain(response?.status() ?? 0)
        await expect(page).toHaveURL(/\/tech\/rankings\/laptops/)
      })

      test("'View Rankings' CTA on /tech/categories/laptops points at /tech/rankings/laptops", async ({ page }) => {
        await page.goto("/tech/categories/laptops")
        const cta = page.locator('a[href="/tech/rankings/laptops"]').first()
        await expect(cta).toBeVisible({ timeout: 5_000 })
        // No redirect hop on click.
        await Promise.all([
          page.waitForURL(/\/tech\/rankings\/laptops$/, { timeout: 5_000 }),
          cta.click(),
        ])
      })
    })
    ```

    Note: the tests assume `laptops` is the slug for the seeded Laptops category (Phase 29 placeholder data).
  </action>
  <verify>
    <automated>test -f tests/29.1-03-rankings-routes.spec.ts &amp;&amp; pnpm exec playwright test tests/29.1-03-rankings-routes.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/29.1-03-rankings-routes.spec.ts` exits 0
    - Dev server is running on `http://localhost:3004`
    - `pnpm exec playwright test tests/29.1-03-rankings-routes.spec.ts` exits 0
    - `pnpm tsc --noEmit` exits 0
    - `pnpm lint` exits 0
  </acceptance_criteria>
  <done>Hub renders, canonical renders, legacy redirects, CTA goes direct.</done>
</task>

</tasks>

<verification>
- All 4 routes behave as designed: hub (200), canonical (200), legacy (308), CTA (no hop).
- `grep -rn "/tech/categories/.*/rankings" src/app/` returns only the redirect file itself.
- Playwright spec passes on desktop + mobile.
</verification>

<success_criteria>
The new URL structure is live. `/tech/rankings/[slug]` is canonical. The legacy URL preserves external traffic via 308. The category page CTA goes direct.
</success_criteria>

<output>
After completion, create `.planning/phases/29.1-master-leaderboard-polish/29.1-03-SUMMARY.md`.
</output>
