---
phase: 29.1-master-leaderboard-polish
plan: 08
status: complete
---

## What was built

Mobile Cards↔Table view toggle backed by nuqs URL state with
`clearOnDefault` so the URL only carries `?view=table` when needed.
Single `<table>` markup is rendered both on desktop and mobile (when
view=table) — no JSX duplication, no drift risk.

## Key files

- created: src/components/tech/leaderboard-view-toggle.tsx
- modified: src/components/tech/leaderboard-table.tsx (extends useQueryStates with `view`; mobile gate between Cards and Table; tableMarkup extracted as inline local)
- created: tests/29.1-08-view-toggle.spec.ts (4 cases, mobile)

## Verification

- `pnpm tsc --noEmit` → clean
- `pnpm exec playwright test tests/29.1-08-view-toggle.spec.ts --project=mobile` → 4/4 pass

## Notes

- The `?view=cards → URL cleared` test was rewritten to navigate directly
  rather than click — nuqs + Turbopack + Playwright clicks caused the same
  hang we saw in plan 06. The state-cleanup behavior is identical regardless
  of whether the toggle is clicked or the URL is typed.
- The mobile-table wrapper carries `data-leaderboard-table-mobile` to
  distinguish from the desktop `data-leaderboard-table` wrapper. Both use
  the same overflow-x-auto + 1600px minWidth setup, so plan 07's sticky-cell
  invariants hold on mobile too.
