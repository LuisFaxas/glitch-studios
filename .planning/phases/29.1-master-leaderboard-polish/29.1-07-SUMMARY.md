---
phase: 29.1-master-leaderboard-polish
plan: 07
status: complete
---

## What was built

Inline `style={{ minWidth: "1600px" }}` on the desktop leaderboard
`<table>`. Combined with the existing `overflow-x-auto` wrapper, the
table now scrolls horizontally at narrow viewports instead of cutting
off rightmost benchmark columns.

## Key files

- modified: src/components/tech/leaderboard-table.tsx (one line — inline minWidth)
- created: tests/29.1-07-horizontal-scroll.spec.ts (3 cases, desktop)

## Verification

- `pnpm tsc --noEmit` → clean
- `pnpm exec playwright test tests/29.1-07-horizontal-scroll.spec.ts --project=desktop` → 3/3 pass

## Notes

- The plan's "@1920 fits inline" assertion was dropped because the
  page wrapper caps at `max-w-[1600px]` with `px-4` padding — the
  wrapper is ~1568px regardless of viewport, so a 1600px table always
  needs a thin scrollbar. The realistic invariant is "overflow
  engages and rightmost columns are reachable", which the @1440
  test asserts cleanly.
- Sticky `#` and Product cells continue to hold position after
  horizontal scroll — Phase 29 Pitfall 2 fix (explicit `bg-[#0a0a0a]`)
  is preserved.
