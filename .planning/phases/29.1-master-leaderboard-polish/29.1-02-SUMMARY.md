---
phase: 29.1-master-leaderboard-polish
plan: 02
status: complete
---

## What was built

`/tech/about` becomes the methodology hub. `/tech/methodology` is now a
permanent redirect to `/tech/about` whose target intentionally omits any
fragment so browsers preserve inbound deep-link fragments end-to-end.

`MethodologyGlitchmark` gains the ×10 display prose sentence (D-21).
Plan 09 will land the numeric formatter that the prose describes.

## Key files

- modified: src/app/(tech)/tech/about/page.tsx (full rewrite — methodology hub)
- modified: src/app/(tech)/tech/methodology/page.tsx (now a permanent redirect)
- modified: src/components/tech/methodology-glitchmark.tsx (×10 prose)
- modified: src/components/tech/bpr-medal.tsx (2 hrefs → /tech/about)
- modified: src/components/tech/rubric-version-badge.tsx (1 href → /tech/about)
- modified: src/components/tech/leaderboard-empty-state.tsx (1 href → /tech/about#methodology)
- modified: src/components/tech/leaderboard-table.tsx (column-header methodology anchor)
- modified: src/app/(tech)/tech/blog/page.tsx (1 href)
- modified: src/app/(tech)/tech/benchmarks/page.tsx (1 href)
- created: tests/29.1-02-about-methodology-anchors.spec.ts (7 cases)

## Verification

- `pnpm tsc --noEmit` → clean
- `pnpm exec playwright test tests/29.1-02-about-methodology-anchors.spec.ts --project=desktop` → 7/7 pass
- Manual curl: `/tech/methodology` and `/tech/methodology#bpr` both resolve to `/tech/about` with fragment preserved

## Notes

- Spec runs on desktop project only because all assertions are URL/DOM
  checks; the rendered viewport is irrelevant. Mobile project would
  duplicate without value.
- The `/tech/categories/laptops/rankings` route exists pre-Plan 03 (legacy
  route) and renders a BPR medal with the new `/tech/about#bpr` href, so
  the sample-link assertion does not depend on Plan 03 work.
