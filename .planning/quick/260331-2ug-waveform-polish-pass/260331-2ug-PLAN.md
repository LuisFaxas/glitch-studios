---
phase: quick
plan: 260331-2ug
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/tiles/widget-now-playing.tsx
  - src/components/player/player-bar.tsx
  - src/components/beats/beat-row.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Sidebar widget waveform fills the entire tile area as a background element"
    - "Player bar desktop waveform renders visible colored bars with gradient (not black)"
    - "List view row waveform is clickable/scrubbable to seek audio"
    - "List view rows show beat duration next to the waveform"
  artifacts:
    - path: "src/components/tiles/widget-now-playing.tsx"
      provides: "Full-bleed waveform filling entire widget"
    - path: "src/components/player/player-bar.tsx"
      provides: "Working WaveSurfer gradient rendering"
    - path: "src/components/beats/beat-row.tsx"
      provides: "Interactive waveform with duration display"
  key_links:
    - from: "src/components/beats/beat-row.tsx"
      to: "audio-player-provider"
      via: "useAudioPlayer seek() called on waveform onSeek"
      pattern: "seek\\(.*duration\\)"
---

<objective>
Fix four waveform issues identified in live review: (1) sidebar widget waveform too small, (2) player bar waveform showing as black bar, (3) list view waveform not interactive, (4) list rows missing duration display.

Purpose: Polish the listening experience across all waveform surfaces.
Output: All three waveform components fixed and visually correct.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/player/waveform.tsx
@src/components/player/player-bar.tsx
@src/components/beats/beat-row.tsx
@src/components/tiles/widget-now-playing.tsx
@src/components/player/audio-player-provider.tsx
@src/components/tiles/tile.tsx
</context>

<interfaces>
<!-- From audio-player-provider.tsx -->
```typescript
interface AudioPlayerContextValue {
  currentBeat: PlayerBeat | null
  isPlaying: boolean
  currentTime: number
  duration: number
  seek: (time: number) => void
  // ... other fields
}
```

<!-- From waveform.tsx -->
```typescript
interface WaveformProps {
  peaks: number[] | null
  progress: number        // 0-1
  height: number          // canvas px height
  interactive?: boolean   // enables click/drag scrub
  onSeek?: (progress: number) => void // 0-1
  mirrored?: boolean
  gradient?: boolean
  gradientColors?: { from: string; to: string }
  // ... other fields
}
```

<!-- From tile.tsx -->
```typescript
// Tile with size="wide" applies: "px-4 py-5" padding by default
// !p-0 override is already used on the active widget
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix sidebar widget — full-bleed waveform background</name>
  <files>src/components/tiles/widget-now-playing.tsx</files>
  <action>
    The waveform must fill the ENTIRE tile as a background element, not sit in a padded wrapper.

    In the active-beat return block of WidgetNowPlaying:
    1. Restructure so the waveform is positioned absolutely to fill the entire tile. Use a wrapper div with `absolute inset-0` containing the Waveform component.
    2. Remove the `<div className="px-3 pb-3">` wrapper around the Waveform. The waveform should have NO padding — it spans edge to edge, top to bottom.
    3. Change Waveform height from `height={40}` to a dynamic approach: use `className="absolute inset-0"` on the waveform container and set `height` to the full tile height. Since the Waveform component uses a fixed `height` prop for its container style, instead use CSS to make the waveform container fill the tile: set `style={{ height: '100%', width: '100%' }}` by passing `className="absolute inset-0 z-0"` on a wrapper div and making the Waveform `height={999}` (it uses clientHeight internally via ResizeObserver, so the actual rendered height will match the container).
    4. Actually, simpler approach: wrap the Waveform in a div with `className="absolute inset-0 z-0"` and set the Waveform `height` to a large value like 200 — the canvas style is `height: 100%` which will fill the absolute container, and the ResizeObserver + `canvas.clientHeight` will pick up the real height.
    5. Place the track info content (cover art, title, artist, play button, time) in a `relative z-10` container so it floats above the waveform background.
    6. Keep mirrored, gradient, interactive, and onSeek props as they are.
    7. Ensure the empty/no-track state still works (no absolute positioning needed there).
  </action>
  <verify>
    <automated>cd /home/faxas/workspaces/projects/personal/glitch_studios && npx next lint --file src/components/tiles/widget-now-playing.tsx</automated>
  </verify>
  <done>Sidebar widget waveform fills the entire tile as a semi-transparent background. Track info overlays on top. Waveform is still interactive/scrubbable.</done>
</task>

<task type="auto">
  <name>Task 2: Fix player bar WaveSurfer gradient + list row interactivity and duration</name>
  <files>src/components/player/player-bar.tsx, src/components/beats/beat-row.tsx</files>
  <action>
    **Player bar fix (player-bar.tsx):**
    The WaveSurfer desktop waveform shows as a black bar because gradients created from an offscreen canvas context do not transfer to WaveSurfer's internal canvas. WaveSurfer.create() accepts CSS color strings, arrays, or its own gradient format — NOT CanvasGradient objects from a different canvas.

    Fix the gradient approach on lines 54-62:
    1. Remove the offscreen canvas gradient creation entirely (delete the `const offscreen = ...`, `progressGrad`, `waveGrad` lines).
    2. Pass gradient colors directly as arrays to WaveSurfer — WaveSurfer v7 accepts `waveColor` and `progressColor` as either a string or a CanvasGradient. Since cross-canvas gradients don't work, use WaveSurfer's built-in gradient support instead.
    3. WaveSurfer v7 `waveColor` and `progressColor` DO accept CSS strings only (not gradient objects from other canvases). The correct approach: pass plain color strings for now, OR create the gradient AFTER WaveSurfer renders by hooking into the `ready` event and accessing WaveSurfer's own canvas context. Simplest fix: just use flat colors that look good — `waveColor: "#444444"` and `progressColor: "#f5f5f0"`. This ensures visibility immediately.
    4. For a premium gradient look without the offscreen canvas hack: WaveSurfer v7's `waveColor` accepts a `CanvasGradient` but it MUST be created from the WaveSurfer's own canvas. Add a `ws.on('ready', () => { ... })` handler that accesses `ws.getWrapper().querySelector('canvas')`, creates gradients from THAT canvas's context, then calls `ws.setOptions({ waveColor: grad, progressColor: pGrad })`.
    5. Add visual affordance that the waveform is scrubbable: the Hover plugin is already configured (good), but also add `cursorColor: "#f5f5f0"` and `cursorWidth: 1` (already there — verify they're working with the fix).

    **Beat row fix (beat-row.tsx):**
    1. Change `interactive={false}` to `interactive={true}` (or just `interactive`) on the Waveform in the beat row (line 117).
    2. Add `onSeek` handler: when the row's waveform is clicked/scrubbed, it should either (a) start playing the beat if not already playing, then seek, or (b) if it's the current beat, just seek. Create a `handleRowSeek` function:
       - If `isCurrentBeat`: call `seek(p * duration)` (seek within current playback)
       - If NOT the current beat: call `play(beatPlayerData)` first, then after a short delay or in the next tick, the seek won't work reliably on a fresh load. Instead, just start playback — the click position will show as progress once the beat starts. This is acceptable UX.
    3. Add `cursor-pointer` to the waveform's className when interactive.
    4. Add duration display: After the waveform div (the `<div className="hidden min-w-0 flex-1 items-center md:flex">`), add a time span showing the beat duration. Use `formatTime` helper (copy from player-bar.tsx or extract). For duration: if it's the currently playing beat, use `duration` from useAudioPlayer. Otherwise, use `beat.duration` if available in the BeatSummary type. If BeatSummary doesn't have duration, show `beat.bpm` context is already there — check if there's a duration field. If not, only show duration for the currently active beat, and show "--:--" for others (since we can't know duration without loading the audio).
    5. The time display should be: `<span className="hidden md:block shrink-0 font-mono text-[11px] text-[#888] min-w-[36px] text-right">{timeDisplay}</span>` placed between the waveform and the BPM/Key badges.
    6. Important: the waveform click for seek must NOT trigger `onToggleExpand` — the waveform onSeek handler already calls `e.stopPropagation()` internally via the Waveform component's pointer handlers, so this should work. Verify by checking that `handlePointerDown` in waveform.tsx calls `e.stopPropagation()` (it does on line 206).
  </action>
  <verify>
    <automated>cd /home/faxas/workspaces/projects/personal/glitch_studios && npx next lint --file src/components/player/player-bar.tsx --file src/components/beats/beat-row.tsx</automated>
  </verify>
  <done>Player bar waveform renders visible gradient-colored bars (not black). List view waveforms are clickable to seek. List rows show duration for the active beat. Hover plugin provides scrub affordance on player bar.</done>
</task>

</tasks>

<verification>
1. Load the beats page — list view rows should show waveforms that respond to click (cursor: pointer)
2. Click a beat's waveform in list view — should start playback and seek to clicked position
3. Player bar should show colored waveform bars with gradient, not a black bar
4. Hover over player bar waveform — should show hover line indicator (scrub affordance)
5. Sidebar widget waveform should fill the entire tile as a background, with track info overlaid
6. Active beat in list view should show duration timestamp next to waveform
</verification>

<success_criteria>
- All four waveform surfaces render correctly with visible bars
- Player bar WaveSurfer gradient renders (no black bar)
- List view waveforms are interactive (click to seek/play)
- Sidebar widget waveform fills the full tile area
- Duration displayed on list rows
</success_criteria>

<output>
After completion, create `.planning/quick/260331-2ug-waveform-polish-pass/260331-2ug-SUMMARY.md`
</output>
