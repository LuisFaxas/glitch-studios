---
phase: 260504-eq6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/home/hero-section.tsx
  - src/components/home/studios-hero-section.tsx
  - src/components/home/tech-hero-section.tsx
  - src/components/home/scroll-arrow.tsx
autonomous: true
requirements:
  - eq6-A-hero-rebalance
  - eq6-B-wordmark-bump
  - eq6-C-scroll-arrow-functional
  - eq6-D-mobile-desktop-verify
tags:
  - hero
  - mobile
  - scroll-arrow
  - tailwind

must_haves:
  truths:
    - "On mobile (390x844), the GLITCH logo reads visually larger than the CTA cluster on / and /tech."
    - "The under-logo wordmark (STUDIOS / TECH) is visibly larger than before on mobile (22px vs 18px) without breaking desktop."
    - "Mobile vertical rhythm is tightened: subtitle→logo gap and logo→CTA gap feel balanced (no 150px dead-air gap)."
    - "Clicking the scroll arrow on / scrolls past the hero into the next section."
    - "Clicking the scroll arrow on /tech scrolls past the hero into the next section."
    - "Scroll arrow exposes accessible name ('Scroll to next section') and uses cursor-pointer."
    - "Reduced-motion users get instant scroll (behavior: 'auto') instead of smooth scroll."
    - "Desktop (md+) breakpoints remain visually identical to before — D-16 ladder untouched."
    - "Dither shader, splash overlay, sidebar, and below-the-fold sections are unchanged."
  artifacts:
    - path: "src/components/home/hero-section.tsx"
      provides: "Rebalanced mobile hero geometry (logo cap 320px, top-[42%], bottom-20, tighter CTAs)"
      contains: "max-w-[320px]"
    - path: "src/components/home/studios-hero-section.tsx"
      provides: "Bumped STUDIOS wordmark on mobile (text-[22px], tracking-[0.4em])"
      contains: "text-[22px]"
    - path: "src/components/home/tech-hero-section.tsx"
      provides: "Bumped TECH wordmark + shrunk inline secondary CTAs on mobile"
      contains: "text-[22px]"
    - path: "src/components/home/scroll-arrow.tsx"
      provides: "Functional <button> with onClick smooth-scroll, aria-label, hover state"
      contains: "type=\"button\""
  key_links:
    - from: "src/components/home/scroll-arrow.tsx"
      to: "closest <section> ancestor"
      via: "e.currentTarget.closest('section') + window.scrollTo"
      pattern: "closest\\(['\"]section['\"]\\)"
    - from: "src/components/home/tech-hero-section.tsx"
      to: "src/components/home/hero-section.tsx"
      via: "secondaryCtas prop (Compare/Benchmarks Link components)"
      pattern: "secondaryCtas="
    - from: "src/components/home/studios-hero-section.tsx"
      to: "src/components/home/hero-section.tsx"
      via: "wordmark prop (heartbeat ECG + STUDIOS span)"
      pattern: "wordmark="
---

<objective>
Rebalance the shared hero (used by both `/` Studios and `/tech` Tech) on mobile so the GLITCH logo dominates the CTA cluster, the under-logo wordmark text is legible against the 84px logo, the dead-air gap between logo and CTAs collapses, and the decorative scroll arrow becomes a real, accessible button that smooth-scrolls past the hero.

Purpose: User confirmed the "Balanced" option from a 3-tier mobile-rebalance proposal after measuring buttons (~26K px²) physically larger than the logo (~20K px²) at 390x844. Desktop must stay byte-identical (D-16 ladder is locked).

Output: Edited `hero-section.tsx`, `studios-hero-section.tsx`, `tech-hero-section.tsx`, `scroll-arrow.tsx`, plus before/after Playwright screenshots in `.playwright-mcp/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from the four files above. -->

From src/components/home/hero-section.tsx:
```typescript
interface HeroSectionProps {
  title?: string
  subtitle?: string
  ctaText?: string
  ctaLink?: string
  backgroundMediaUrl?: string
  wordmark?: React.ReactNode       // <-- studios + tech inject heartbeat+wordmark here
  secondaryCtas?: React.ReactNode  // <-- tech overrides default Beats/Portfolio with Compare/Benchmarks
  tagline?: string
}
```

Existing mobile geometry (current state, BEFORE edits):
- Center stack absolute wrapper: `top-[38%] -translate-y-1/2`
- Subtitle `<p>`: `text-sm md:text-4xl ... mb-6 md:mb-8`
- Logo container: `w-[60vw] max-w-[280px] md:w-[70vw] md:max-w-[400px] lg:w-[60vw] lg:max-w-[460px] xl:max-w-[520px] 2xl:max-w-[600px]`  ← D-16 LADDER, only mobile cap changes
- CTA absolute wrapper: `bottom-28 md:bottom-32`
- CTA grid: `grid grid-cols-2 md:flex ... w-full max-w-[320px] md:max-w-none md:w-auto`
- Primary CTA: `col-span-2 ... px-5 py-3 md:px-10 md:py-3 ... text-xs md:text-sm`
- Default secondary CTAs (Beats / Portfolio in `default` branch): `... px-4 py-3 md:px-6 md:py-3 ... text-xs md:text-sm`

From src/components/home/scroll-arrow.tsx:
```typescript
// Currently a decorative <div>. Must become <button type="button">.
// Keep `useReducedMotion` and the existing `motion.div` bounce (y: [0, 10, 0]).
// Keep SVG path/dimensions identical.
```

From src/components/home/tech-hero-section.tsx:
```typescript
// secondaryCtas inline:
//   <Link className="... px-4 py-3 md:px-6 md:py-3 ... text-xs md:text-sm">Compare</Link>
//   <Link className="... px-4 py-3 md:px-6 md:py-3 ... text-xs md:text-sm">Benchmarks</Link>
// MUST receive same shrink as default branch — see Task 1B.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rebalance hero-section.tsx mobile geometry (per spec section A)</name>
  <files>src/components/home/hero-section.tsx</files>
  <behavior>
    After this task, the file should contain:
    - Center stack wrapper class includes `top-[42%]` (NOT `top-[38%]`).
    - Subtitle `<p>` class includes `mb-3 md:mb-8` (NOT `mb-6 md:mb-8`).
    - Logo container class includes `max-w-[320px] md:max-w-[400px]` (mobile cap bumped, md+ unchanged). The full ladder is preserved: `w-[60vw] max-w-[320px] md:w-[70vw] md:max-w-[400px] lg:w-[60vw] lg:max-w-[460px] xl:max-w-[520px] 2xl:max-w-[600px]`.
    - CTA absolute wrapper class includes `bottom-20 md:bottom-32` (NOT `bottom-28 md:bottom-32`).
    - CTA grid wrapper class includes `max-w-[260px] md:max-w-none` (NOT `max-w-[320px] md:max-w-none`).
    - Primary CTA `<GlitchLink>` class: `px-4 py-2.5 md:px-10 md:py-3 ... text-[11px] md:text-sm`.
    - Default branch secondary CTAs (Beats + Portfolio `<GlitchLink>`s): `px-3 py-2 md:px-6 md:py-3 ... text-[10px] md:text-sm`.
    - The D-16 13" laptop comment block (lines ~113–126) must remain valid — md/lg/xl/2xl values unchanged. Update the literal `mobile: 280px cap` line in the comment to `mobile: 320px cap` so the doc reflects reality.
    - No other changes to the file (Dither, splash, scrim, sidebar collapsed translate, tagline, ScrollArrow placement, bottom lip — all untouched).
  </behavior>
  <action>
Open `src/components/home/hero-section.tsx` and apply these targeted Edit calls. Use Edit tool, not Write — we only want diffs, not a rewrite.

1. Center stack wrapper (line ~106):
   - Find: `"absolute inset-x-0 top-[38%] -translate-y-1/2 z-10 flex flex-col items-center justify-center pointer-events-none"`
   - Replace: `"absolute inset-x-0 top-[42%] -translate-y-1/2 z-10 flex flex-col items-center justify-center pointer-events-none"`

2. Subtitle margin (line ~110):
   - Find: `mb-6 md:mb-8 px-4 text-center`
   - Replace: `mb-3 md:mb-8 px-4 text-center`

3. Logo container (line ~129) — bump mobile cap only:
   - Find: `className="w-[60vw] max-w-[280px] md:w-[70vw] md:max-w-[400px] lg:w-[60vw] lg:max-w-[460px] xl:max-w-[520px] 2xl:max-w-[600px] min-w-0 pointer-events-auto flex justify-center"`
   - Replace: `className="w-[60vw] max-w-[320px] md:w-[70vw] md:max-w-[400px] lg:w-[60vw] lg:max-w-[460px] xl:max-w-[520px] 2xl:max-w-[600px] min-w-0 pointer-events-auto flex justify-center"`

4. D-16 comment line (inside the `/* … */` block, ~line 120) — keep doc in sync:
   - Find: `              mobile: 280px cap`
   - Replace: `              mobile: 320px cap`

5. CTA absolute wrapper (line ~145):
   - Find: `"absolute z-10 inset-x-0 bottom-28 md:bottom-32 flex flex-col items-center gap-4 px-6 text-center pointer-events-none"`
   - Replace: `"absolute z-10 inset-x-0 bottom-20 md:bottom-32 flex flex-col items-center gap-4 px-6 text-center pointer-events-none"`

6. CTA grid wrapper (line ~149):
   - Find: `<div className="grid grid-cols-2 md:flex md:items-center md:justify-center gap-2 md:gap-3 w-full max-w-[320px] md:max-w-none md:w-auto pointer-events-auto">`
   - Replace: `<div className="grid grid-cols-2 md:flex md:items-center md:justify-center gap-2 md:gap-3 w-full max-w-[260px] md:max-w-none md:w-auto pointer-events-auto">`

7. Primary CTA (line ~153):
   - Find: `className="col-span-2 bg-[#f5f5f0] text-[#000] border border-[#f5f5f0] px-5 py-3 md:px-10 md:py-3 rounded-none font-mono font-bold uppercase tracking-[0.05em] text-xs md:text-sm hover:bg-[#000] hover:text-[#f5f5f0] hover:border-[#f5f5f0] transition-colors duration-200 text-center"`
   - Replace: `className="col-span-2 bg-[#f5f5f0] text-[#000] border border-[#f5f5f0] px-4 py-2.5 md:px-10 md:py-3 rounded-none font-mono font-bold uppercase tracking-[0.05em] text-[11px] md:text-sm hover:bg-[#000] hover:text-[#f5f5f0] hover:border-[#f5f5f0] transition-colors duration-200 text-center"`

8. Default secondary CTAs — Beats (line ~162):
   - Find: `className="bg-[#111] text-[#f5f5f0] border border-[#444] px-4 py-3 md:px-6 md:py-3 rounded-none font-mono font-bold uppercase tracking-[0.05em] text-xs md:text-sm hover:bg-[#1a1a1a] hover:border-[#666] transition-colors duration-200 text-center"`
     (this string occurs twice — Beats and Portfolio. Use replace_all=true OR run two scoped Edits using surrounding context like `href="/beats"` and `href="/portfolio"`.)
   - Replace: `className="bg-[#111] text-[#f5f5f0] border border-[#444] px-3 py-2 md:px-6 md:py-3 rounded-none font-mono font-bold uppercase tracking-[0.05em] text-[10px] md:text-sm hover:bg-[#1a1a1a] hover:border-[#666] transition-colors duration-200 text-center"`

   IMPORTANT: This same className string ALSO appears in `tech-hero-section.tsx` (Compare/Benchmarks). DO NOT replace_all across the workspace — scope to this file only. Use the Edit tool with `file_path` set to `src/components/home/hero-section.tsx` and `replace_all: true` (only safe because we are inside one file and want both occurrences in `default` branch updated).

Self-check after edits: re-Read the file and grep for `top-[42%]`, `mb-3 md:mb-8`, `max-w-[320px]`, `bottom-20 md:bottom-32`, `max-w-[260px] md:max-w-none`, `px-4 py-2.5`, `text-[11px]`, `px-3 py-2`, `text-[10px]`. All 9 markers must be present. The strings `top-[38%]`, `bottom-28 md:bottom-32`, and the OLD `max-w-[280px] md:max-w-[400px]` substring must NOT be present.
  </action>
  <verify>
    <automated>cd /Users/faxas/FAXAS_HQ/Projects/Coding_Projects/PERSONAL/glitch_studios &amp;&amp; node -e "const f=require('fs').readFileSync('src/components/home/hero-section.tsx','utf8'); const must=['top-[42%]','mb-3 md:mb-8','max-w-[320px] md:w-[70vw] md:max-w-[400px]','bottom-20 md:bottom-32','max-w-[260px] md:max-w-none','px-4 py-2.5 md:px-10 md:py-3','text-[11px] md:text-sm','px-3 py-2 md:px-6 md:py-3','text-[10px] md:text-sm','mobile: 320px cap']; const forbid=['top-[38%]','bottom-28 md:bottom-32','max-w-[280px] md:w-[70vw]','max-w-[320px] md:max-w-none md:w-auto pointer-events-auto','px-5 py-3 md:px-10 md:py-3','px-4 py-3 md:px-6 md:py-3']; const m=must.filter(s=>!f.includes(s)); const b=forbid.filter(s=>f.includes(s)); if(m.length||b.length){console.error('MISSING:',m,'\nFORBIDDEN PRESENT:',b);process.exit(1)} console.log('hero-section.tsx OK')"</automated>
  </verify>
  <done>All 10 required substrings present; all 6 forbidden substrings absent. D-16 ladder intact for md/lg/xl/2xl. No unrelated edits.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Bump under-logo wordmark on studios + tech hero, shrink tech inline secondary CTAs (per spec section B)</name>
  <files>src/components/home/studios-hero-section.tsx, src/components/home/tech-hero-section.tsx</files>
  <behavior>
    studios-hero-section.tsx:
    - The `<span>` containing "STUDIOS" wordmark uses `text-[22px] md:text-3xl` (NOT `text-lg md:text-3xl`) and `tracking-[0.4em] md:tracking-[0.5em]` (NOT `tracking-[0.5em]` only).

    tech-hero-section.tsx:
    - The `<span>` containing "TECH" wordmark uses the SAME swap: `text-[22px] md:text-3xl`, `tracking-[0.4em] md:tracking-[0.5em]`.
    - Compare `<Link>` and Benchmarks `<Link>` className shrink: `px-3 py-2 md:px-6 md:py-3 ... text-[10px] md:text-sm` (was `px-4 py-3 md:px-6 md:py-3 ... text-xs md:text-sm`).

    Everything else (TechPulseLine usage, beam-glitch layers, glitchTextLayer copies, aria-label, brand text content, gap-3 md:gap-4 wrappers) stays identical.
  </behavior>
  <action>
File 1: `src/components/home/studios-hero-section.tsx`

Edit the STUDIOS wordmark span (line ~52):
- Find: `className={\`${styles.glitchTextWrapper} font-mono text-lg md:text-3xl font-bold uppercase tracking-[0.5em] text-[#f5f5f0]\`}`
- Replace: `className={\`${styles.glitchTextWrapper} font-mono text-[22px] md:text-3xl font-bold uppercase tracking-[0.4em] md:tracking-[0.5em] text-[#f5f5f0]\`}`

File 2: `src/components/home/tech-hero-section.tsx`

Edit 2a — TECH wordmark span (line ~33):
- Find: `className={\`${styles.glitchTextWrapper} font-mono text-lg md:text-3xl font-bold uppercase tracking-[0.5em] text-[#f5f5f0]\`}`
- Replace: `className={\`${styles.glitchTextWrapper} font-mono text-[22px] md:text-3xl font-bold uppercase tracking-[0.4em] md:tracking-[0.5em] text-[#f5f5f0]\`}`

Edit 2b — Compare + Benchmarks Link className (lines ~58 and ~64; identical strings, use `replace_all: true` SCOPED to this file only):
- Find: `className="bg-[#111] text-[#f5f5f0] border border-[#444] px-4 py-3 md:px-6 md:py-3 rounded-none font-mono font-bold uppercase tracking-[0.05em] text-xs md:text-sm hover:bg-[#1a1a1a] hover:border-[#666] transition-colors duration-200 text-center"`
- Replace: `className="bg-[#111] text-[#f5f5f0] border border-[#444] px-3 py-2 md:px-6 md:py-3 rounded-none font-mono font-bold uppercase tracking-[0.05em] text-[10px] md:text-sm hover:bg-[#1a1a1a] hover:border-[#666] transition-colors duration-200 text-center"`

Self-check: re-Read both files. Confirm `text-lg md:text-3xl` no longer appears in either, `tracking-[0.5em] text-[#f5f5f0]` (without the new `md:tracking-[0.5em]` form) is gone, and tech file no longer has `px-4 py-3 md:px-6 md:py-3`.
  </action>
  <verify>
    <automated>cd /Users/faxas/FAXAS_HQ/Projects/Coding_Projects/PERSONAL/glitch_studios &amp;&amp; node -e "const fs=require('fs');const s=fs.readFileSync('src/components/home/studios-hero-section.tsx','utf8');const t=fs.readFileSync('src/components/home/tech-hero-section.tsx','utf8');const checks=[[s,'studios','text-[22px] md:text-3xl',true],[s,'studios','tracking-[0.4em] md:tracking-[0.5em]',true],[s,'studios','text-lg md:text-3xl',false],[t,'tech','text-[22px] md:text-3xl',true],[t,'tech','tracking-[0.4em] md:tracking-[0.5em]',true],[t,'tech','text-lg md:text-3xl',false],[t,'tech','px-3 py-2 md:px-6 md:py-3',true],[t,'tech','text-[10px] md:text-sm',true],[t,'tech','px-4 py-3 md:px-6 md:py-3',false],[t,'tech','text-xs md:text-sm',false]];let bad=[];for(const [src,name,sub,want] of checks){const has=src.includes(sub);if(has!==want)bad.push(\`\${name}: \${want?'MISSING':'FORBIDDEN PRESENT'} -> \${sub}\`)}if(bad.length){console.error(bad.join('\n'));process.exit(1)}console.log('wordmark + tech CTA shrink OK')"</automated>
  </verify>
  <done>Both wordmark spans use `text-[22px] md:text-3xl` + `tracking-[0.4em] md:tracking-[0.5em]`. Tech inline secondary CTAs use shrunk mobile padding/font. No `text-lg md:text-3xl` or old `px-4 py-3 md:px-6 md:py-3` remain in these two files.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Convert ScrollArrow to functional accessible button (per spec section C)</name>
  <files>src/components/home/scroll-arrow.tsx</files>
  <behavior>
    The component:
    - Renders a `<button type="button">` as the OUTER element (replacing the outer `<div className="flex flex-col items-center gap-3">`).
    - Has `aria-label="Scroll to next section"`.
    - Has `cursor-pointer` and a hover state (e.g. `text-[#f5f5f0] hover:text-white` applied to the SVG, or scale-up on hover via `hover:scale-110 transition-transform`).
    - Resets default button styling: `bg-transparent border-0 p-0` (or equivalent — must NOT show a default browser button border/background).
    - On click: locates closest `<section>` ancestor via `e.currentTarget.closest("section")` and calls `window.scrollTo({ top: section.offsetTop + section.offsetHeight, behavior: shouldReduceMotion ? "auto" : "smooth" })`. Guards against null section (no-op if not found).
    - `useReducedMotion()` hook usage and the `motion.div` bounce animation (y: [0, 10, 0], duration 2, repeat Infinity, easeInOut) remain UNTOUCHED.
    - SVG path `M4 4 L20 16 L36 4`, dimensions `width="40" height="24" viewBox="0 0 40 20"`, strokeWidth `2.5`, strokeLinecap `square` are all identical.
  </behavior>
  <action>
Replace the entire body of `src/components/home/scroll-arrow.tsx` with the version below. This is a complete rewrite of a 26-line file — Edit-by-substring is brittle here, so use the Write tool.

```tsx
"use client"

import { motion, useReducedMotion } from "motion/react"

export function ScrollArrow() {
  const shouldReduceMotion = useReducedMotion()

  const handleClick = (e: React.MouseEvent<HTMLButtonElement>) => {
    const section = e.currentTarget.closest("section")
    if (!section) return
    window.scrollTo({
      top: section.offsetTop + section.offsetHeight,
      behavior: shouldReduceMotion ? "auto" : "smooth",
    })
  }

  return (
    <button
      type="button"
      onClick={handleClick}
      aria-label="Scroll to next section"
      className="flex flex-col items-center gap-3 cursor-pointer bg-transparent border-0 p-0 text-[#f5f5f0] hover:text-white transition-colors"
    >
      {/* Single bold arrow */}
      <motion.div
        animate={shouldReduceMotion ? undefined : { y: [0, 10, 0] }}
        transition={
          shouldReduceMotion
            ? undefined
            : { duration: 2, repeat: Infinity, ease: "easeInOut" }
        }
      >
        <svg width="40" height="24" viewBox="0 0 40 20" className="text-current">
          <path d="M4 4 L20 16 L36 4" stroke="currentColor" strokeWidth="2.5" fill="none" strokeLinecap="square" />
        </svg>
      </motion.div>
    </button>
  )
}
```

Notes for the executor:
- The SVG class changes from `text-[#f5f5f0]` (literal) to `text-current` so it inherits the button's hover state. This preserves the painted color (#f5f5f0 default, white on hover) without breaking the existing render.
- DO NOT add new framer-motion APIs, new dependencies, or new hooks beyond what's shown.
- DO NOT change SVG geometry or strokeWidth.
- Read the file back after writing and confirm the diff is exactly what's shown (no extra blank lines, no removed `"use client"` directive).
  </action>
  <verify>
    <automated>cd /Users/faxas/FAXAS_HQ/Projects/Coding_Projects/PERSONAL/glitch_studios &amp;&amp; node -e "const f=require('fs').readFileSync('src/components/home/scroll-arrow.tsx','utf8');const must=['type=\"button\"','aria-label=\"Scroll to next section\"','cursor-pointer','bg-transparent','border-0','closest(\"section\")','behavior: shouldReduceMotion ? \"auto\" : \"smooth\"','M4 4 L20 16 L36 4','useReducedMotion','y: [0, 10, 0]'];const m=must.filter(s=>!f.includes(s));if(m.length){console.error('MISSING:',m);process.exit(1)}console.log('scroll-arrow.tsx OK')" &amp;&amp; npx --yes tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -E "scroll-arrow|hero-section|studios-hero|tech-hero" || echo "No tsc errors in edited files"</automated>
  </verify>
  <done>scroll-arrow.tsx renders a `<button type="button">` with aria-label, cursor-pointer, default-button-style reset, onClick handler that scrolls past closest section respecting reduced-motion. Bounce animation + SVG geometry preserved. `tsc --noEmit` shows no errors for the edited files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Mobile + desktop visual + scroll verification on / and /tech (per spec section D)</name>
  <what-built>
    Three code edits landed: (1) hero-section.tsx mobile geometry rebalance, (2) studios+tech wordmark bump + tech inline CTA shrink, (3) ScrollArrow converted to functional button. Now verify the visual outcome and the click-to-scroll behavior on both routes at both viewports.
  </what-built>
  <how-to-verify>
1. Start dev server (if not already running):
   ```
   cd /Users/faxas/FAXAS_HQ/Projects/Coding_Projects/PERSONAL/glitch_studios
   npm run dev
   ```
   Wait for "Ready" / port confirmation. Note the URL (Caddy: `https://glitch-studios.localhost` OR `http://localhost:3000`).

2. Use Playwright MCP (or `npx playwright` headed if MCP unavailable). For EACH of the four combinations below, capture a "before-scroll" screenshot of the hero, then click the ScrollArrow, then capture an "after-scroll" screenshot. Save ALL screenshots to `.playwright-mcp/` (NEVER the repo root — see CLAUDE.md CodeBox constraints).

   Combinations:
   - `/` at viewport 390x844 (mobile)
   - `/` at viewport 1440x900 (desktop)
   - `/tech` at viewport 390x844 (mobile)
   - `/tech` at viewport 1440x900 (desktop)

   Naming convention for screenshots:
   - `.playwright-mcp/eq6-home-mobile-before.png`
   - `.playwright-mcp/eq6-home-mobile-after.png`
   - `.playwright-mcp/eq6-home-desktop-before.png`
   - `.playwright-mcp/eq6-home-desktop-after.png`
   - `.playwright-mcp/eq6-tech-mobile-before.png`
   - `.playwright-mcp/eq6-tech-mobile-after.png`
   - `.playwright-mcp/eq6-tech-desktop-before.png`
   - `.playwright-mcp/eq6-tech-desktop-after.png`

3. For mobile screenshots, visually confirm:
   - GLITCH logo reads visually larger than the CTA cluster (logo width ~320px cap, no longer dwarfed).
   - Subtitle sits closer to the logo than before (mb-3 instead of mb-6).
   - "STUDIOS" / "TECH" wordmark under the logo is clearly larger (22px vs prior 18px), letter-spacing slightly tighter (0.4em vs 0.5em).
   - CTA cluster sits noticeably higher in the viewport (bottom-20 vs bottom-28) and is narrower (max-w-[260px]).
   - Primary "Book a Session" / "Explore Reviews" button is visibly smaller in font + padding than before.
   - Secondary CTAs (Beats/Portfolio on /, Compare/Benchmarks on /tech) are visibly smaller — both routes match each other.

4. For desktop screenshots, confirm NO visual change versus the prior production state (D-16 ladder intact). The center stack `top-[42%]` does shift things by 4% of viewport height — that's expected and is the only intended desktop delta.

5. For each route, click the ScrollArrow once and confirm the page scrolls past the hero section into the next section (e.g., the "Featured Work" / "Latest Reviews" / etc. section below). Do NOT just check that scrollY changed — verify the next section is now in view.

6. Spot-check: dither shader still renders (background animation visible), splash overlay still appears on first load, sidebar still toggles correctly, and the section just below the hero is unchanged.

7. Reduced-motion check (manual, optional but recommended): toggle macOS "Reduce motion" (System Settings → Accessibility → Display → Reduce motion) and reload `/`. Click the arrow — confirm scroll is instant (no smooth animation) and arrow bounce stops.
  </how-to-verify>
  <resume-signal>Type "approved" once all 8 screenshots are captured and the four scroll-arrow clicks behave correctly. If any screenshot reveals a visual regression (broken md+ layout, dither shader gone, splash gone, sidebar broken), describe the issue and the executor will revise.</resume-signal>
</task>

</tasks>

<verification>
- All four edited files pass `tsc --noEmit` cleanly.
- Mobile screenshots at 390x844 show: bigger logo, smaller CTAs, larger STUDIOS/TECH wordmark, tighter vertical rhythm.
- Desktop screenshots at 1440x900 show: no change vs prior baseline (D-16 ladder honored, only the `top-[42%]` shift is the intentional cross-viewport delta).
- ScrollArrow click on `/` scrolls past the hero into the next section.
- ScrollArrow click on `/tech` scrolls past the hero into the next section.
- Dither shader, splash overlay, sidebar, and below-hero sections unchanged.
- All 8 verification screenshots saved under `.playwright-mcp/`, none committed to repo root.
</verification>

<success_criteria>
- [ ] hero-section.tsx mobile geometry updated per A.1–A.7; D-16 ladder preserved for md/lg/xl/2xl.
- [ ] studios-hero-section.tsx STUDIOS wordmark bumped to text-[22px] / tracking-[0.4em] on mobile.
- [ ] tech-hero-section.tsx TECH wordmark bumped AND inline Compare/Benchmarks CTAs shrunk to match default block.
- [ ] scroll-arrow.tsx is a `<button type="button">` with aria-label, cursor-pointer, default-button-style reset, working onClick that respects reduced-motion.
- [ ] Playwright run on / and /tech at 390x844 + 1440x900 produces 8 screenshots in `.playwright-mcp/`.
- [ ] Click on arrow scrolls past hero on both routes.
- [ ] User confirms (`approved`) the visual rebalance matches the "Balanced" tier.
</success_criteria>

<output>
After completion, create `.planning/quick/260504-eq6-rebalance-home-tech-hero-sections-mobile/260504-eq6-SUMMARY.md` recording: files edited (4), screenshots captured (8 paths), any deviations from spec, and a one-line confirmation that the D-16 ladder + dither/splash/sidebar are untouched.
</output>
