---
phase: quick-260331-5ob
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/beats/beats-hero-carousel.tsx
  - src/app/(public)/beats/page.tsx
autonomous: true
requirements: [QUICK-5ob]
must_haves:
  truths:
    - "Beats page shows a full-width auto-looping carousel above the catalog"
    - "Carousel has 3 slides: bundle promo, licensing pitch, studio booking CTA"
    - "Bundle data is integrated into slide 1 (no separate BundleSection)"
    - "Carousel pauses on hover and has dot indicators"
  artifacts:
    - path: "src/components/beats/beats-hero-carousel.tsx"
      provides: "Hero carousel component with 3 slides and Embla autoplay"
    - path: "src/app/(public)/beats/page.tsx"
      provides: "Updated page using carousel instead of BundleSection"
  key_links:
    - from: "src/app/(public)/beats/page.tsx"
      to: "src/components/beats/beats-hero-carousel.tsx"
      via: "import and render with bundles prop"
      pattern: "BeatsHeroCarousel"
---

<objective>
Replace the dead zone above the beats catalog (plain "BEATS" heading + sparse BundleSection) with a full-width auto-looping Embla hero carousel. Three slides: bundle promo with add-to-cart, licensing pitch, and studio booking CTA. ~200px height. Removes the separate BundleSection component from the page.

Purpose: Turn wasted space into a compelling storefront banner that promotes bundles, licensing, and studio services.
Output: New `BeatsHeroCarousel` component, updated beats page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/app/(public)/beats/page.tsx
@src/components/beats/bundle-section.tsx
@src/components/home/video-portfolio-carousel.tsx

<interfaces>
From src/components/beats/bundle-section.tsx:
```typescript
type BundleData = Awaited<ReturnType<typeof getPublishedBundles>>
// BundleData[number] has: id, title, description, beats[], originalTotal, discountedTotal, discountPercent
```

From src/components/cart/cart-provider.tsx:
```typescript
const { addItem } = useCart()
// addItem({ beatId, beatTitle, beatSlug, coverArtUrl, licenseTier, licenseTierDisplay, price })
```

Embla setup pattern (from video-portfolio-carousel.tsx):
```typescript
import useEmblaCarousel from "embla-carousel-react"
const [emblaRef, emblaApi] = useEmblaCarousel({ loop: true })
```

Autoplay plugin already installed: `embla-carousel-autoplay`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BeatsHeroCarousel component</name>
  <files>src/components/beats/beats-hero-carousel.tsx</files>
  <action>
Create a "use client" component `BeatsHeroCarousel` that accepts `bundles: BundleData` prop.

**Embla setup:**
- Import `useEmblaCarousel` and `Autoplay` from `embla-carousel-autoplay`
- Configure: `{ loop: true, align: "start" }` with `Autoplay({ delay: 5000, stopOnInteraction: false, stopOnMouseEnter: true })`
- Track `selectedIndex` via `emblaApi.on("select", ...)` for dot indicators

**Container:**
- Full-width, ~200px height (`h-[200px]`), `overflow-hidden`, `relative`
- No side padding — edge-to-edge within the page layout

**Slide 1 — Bundle Promo:**
- If bundles exist, show first bundle: title, beat count, discount badge (e.g. "20% OFF"), original price struck through, discounted price
- "Add Bundle to Cart" button — reuse the `handleAddBundle` logic from BundleSection (import `useCart`, add each beat as an item, toast success)
- If no bundles, show a generic "Beat Packs Coming Soon" placeholder
- Background: subtle gradient `bg-gradient-to-r from-[#0a0a0a] to-[#151515]`

**Slide 2 — Licensing Pitch:**
- Heading: "License Beats from $29"
- Subtext: "Instant delivery. Commercial rights. All genres."
- "Browse Catalog" button — scrolls down to the catalog section (use `document.getElementById("beat-catalog")?.scrollIntoView({ behavior: "smooth" })`)
- Background: gradient with slight purple/blue tint, e.g. `from-[#0a0a0a] via-[#0d0a14] to-[#0a0a0a]`

**Slide 3 — Studio Booking CTA:**
- Heading: "Book a Session at Glitch"
- Subtext: "Recording. Mixing. Mastering. Video production."
- "Book Now" button — links to `/booking` via Next.js `Link`
- Background: gradient with slight warm tint, e.g. `from-[#0a0a0a] via-[#140d0a] to-[#0a0a0a]`

**Slide styling (all slides):**
- Each slide: `flex-shrink-0 w-full h-full` (one slide visible at a time)
- Content: centered vertically, left-aligned with `px-6 md:px-10`
- Headings: `font-mono text-[24px] md:text-[28px] font-bold uppercase tracking-[0.05em] text-[#f5f5f0]`
- Subtext: `font-sans text-[13px] md:text-[15px] text-[#888]`
- Buttons: `border border-[#f5f5f0] px-6 py-2 font-mono text-[12px] uppercase tracking-[0.05em] text-[#f5f5f0] hover:bg-[#f5f5f0] hover:text-[#000] transition-colors`

**Dot indicators:**
- Row of 3 dots at bottom center, `absolute bottom-3`
- Active dot: `bg-[#f5f5f0] w-2 h-2`, inactive: `bg-[#444] w-2 h-2`
- Clickable — `emblaApi.scrollTo(index)` on click

**Small "BEATS" label above carousel:**
- Add a thin `font-mono text-[11px] uppercase tracking-[0.15em] text-[#555] mb-2` label reading "BEATS" above the carousel div (inside the component)
  </action>
  <verify>
    <automated>cd /home/faxas/workspaces/projects/personal/glitch_studios && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>BeatsHeroCarousel renders 3 slides with Embla autoplay, dot indicators, pause-on-hover, and bundle add-to-cart functionality</done>
</task>

<task type="auto">
  <name>Task 2: Wire carousel into beats page and remove BundleSection</name>
  <files>src/app/(public)/beats/page.tsx</files>
  <action>
Update the beats page:

1. Replace `import { BundleSection }` with `import { BeatsHeroCarousel }` from `@/components/beats/beats-hero-carousel`
2. Remove the `h1` "BEATS" heading (now handled inside the carousel component as a thin label)
3. Remove `{bundles.length > 0 && <BundleSection bundles={bundles} />}`
4. Add `<BeatsHeroCarousel bundles={bundles} />` before the `BeatCatalog`
5. Add `id="beat-catalog"` to the div wrapping `BeatCatalog` so slide 2's "Browse Catalog" scroll-to works
6. Keep the `getPublishedBundles()` call in the server component — bundles data still fetched server-side and passed as props

Final JSX structure:
```tsx
<div className="flex flex-col gap-6">
  <BeatsHeroCarousel bundles={bundles} />
  <div id="beat-catalog">
    <BeatCatalog beats={beats} filterOptions={filterOptions} hasActiveFilters={hasActiveFilters} />
  </div>
</div>
```

Do NOT delete `bundle-section.tsx` — it may be used elsewhere or referenced. Just remove the import from this page.
  </action>
  <verify>
    <automated>cd /home/faxas/workspaces/projects/personal/glitch_studios && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Beats page renders hero carousel above catalog, no separate BundleSection visible, bundles integrated into carousel slide 1</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- Visit beats page: carousel visible above catalog, auto-loops through 3 slides
- Slide 1 shows bundle promo with add-to-cart (if bundles exist)
- Slide 2 shows licensing pitch, "Browse Catalog" scrolls to catalog
- Slide 3 shows studio booking CTA, "Book Now" links to /booking
- Hover pauses autoplay, dot indicators work
- No separate "BUNDLES" section visible
</verification>

<success_criteria>
- Hero carousel replaces the dead zone and BundleSection on the beats page
- 3 slides auto-loop with 5s delay, pause on hover
- Bundle add-to-cart works from slide 1
- Page structure is cleaner: carousel -> catalog (no gap of empty space)
</success_criteria>

<output>
After completion, create `.planning/quick/260331-5ob-beats-hero-carousel/260331-5ob-SUMMARY.md`
</output>
